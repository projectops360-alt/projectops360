-- ============================================================================
-- ProjectOps360° — Isabella App Screens corpus (GENERATED — do not edit)
-- Generated by docs/app-knowledge/scripts/generate-app-screens-seed.mjs
-- Idempotent + version-aware: unchanged packages are untouched; changed ones
-- get a new current version and re-pending chunks. Requires pgcrypto (digest).
-- ============================================================================
WITH data(slug, title_en, body_en, title_es, body_es, source_ref) AS (
  VALUES
    ('screen-admin-backfill', 'Admin Backfill Console screen', 'The Backfill Administration Console at `/admin/backfill`, described in its own header as the single approved surface for executing Historical Backfill into the Project Event Graph. There is no navigation link — you type the URL. Access is gated server-side by `canRunBackfill`: org `owner`/`admin`, or an email in the `BACKFILL_ADMIN_EMAILS` allowlist; everyone else gets `notFound()` before any query runs. The server then reads up to 500 rows from `projects` with the admin client. `BackfillConsole` offers a Scope select (single project or entire organization), a required Reason field (max 500 chars, recorded in the audit event), a "Dry run (preview)" button and an "Execute" button that stays disabled until a dry run has completed for that exact scope and a reason is typed; Execute also raises a `confirm()` dialog. Both call `runBackfillAction`, which re-checks authorization, validates with zod and loops `backfillProject` over the targets. That engine reads `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`, `risks`, `decisions`, `documents` and `drawing_files` and writes derived events, each carrying provenance (source table, record, field, inference method). Results show per-project events created/duplicate/failed, a replay-readiness score, confidence distribution, and a "Download report" JSON button. Related: screen-admin-living-graph-observability, screen-living-graph.
Source: src/app/[locale]/(app)/admin/backfill/{page,backfill-console,actions}, src/lib/events/{backfill,backfill-access,backfill-reports}.ts.
Verify: as an org owner/admin, open /admin/backfill, run a dry run on one project, type a reason, then Execute.', 'Pantalla Consola de Backfill (admin)', 'La Consola de Administración de Backfill en `/admin/backfill`, descrita en su propio encabezado como la única superficie aprobada para ejecutar el Backfill Histórico hacia el Project Event Graph. No hay enlace en la navegación: se escribe la URL. El acceso se controla en el servidor con `canRunBackfill`: `owner`/`admin` de la organización, o un correo de la lista `BACKFILL_ADMIN_EMAILS`; el resto recibe `notFound()` antes de cualquier consulta. Luego el servidor lee hasta 500 filas de `projects` con el cliente admin. `BackfillConsole` ofrece un selector de alcance (un proyecto o toda la organización), un campo Razón obligatorio (máximo 500 caracteres, queda en el evento de auditoría), un botón "Dry run (preview)" y un botón "Execute" que permanece deshabilitado hasta que se haya hecho un dry run de ese mismo alcance y se escriba una razón; Execute además abre un diálogo `confirm()`. Ambos llaman a `runBackfillAction`, que revalida la autorización, valida con zod y recorre `backfillProject`. Ese motor lee `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`, `risks`, `decisions`, `documents` y `drawing_files` y escribe eventos derivados, cada uno con su procedencia (tabla, registro, campo, método de inferencia). Los resultados muestran por proyecto los eventos creados/duplicados/fallidos, un puntaje de preparación para replay, la distribución de confianza y un botón "Download report" en JSON. Relacionadas: screen-admin-living-graph-observability, screen-living-graph.
Fuente: src/app/[locale]/(app)/admin/backfill/{page,backfill-console,actions}, src/lib/events/{backfill,backfill-access,backfill-reports}.ts.
Verifica: como owner/admin, abre /admin/backfill, ejecuta un dry run sobre un proyecto, escribe una razón y pulsa Execute.', '/admin/backfill — generated from: src/app/[locale]/(app)/admin/backfill/page.tsx, src/app/[locale]/(app)/admin/backfill/backfill-console.tsx, src/app/[locale]/(app)/admin/backfill/actions.ts, src/lib/events/backfill-access.ts, src/lib/events/backfill.ts, src/lib/events/backfill-reports.ts'),
    ('screen-admin-console', 'Admin Console screen', 'The platform-wide administration surface at `/admin`, cross-tenant by design. The gate is strict and server-side: `requirePlatformAdmin` authorizes an email only if `admin_authorized_users` holds an active row for it, or it matches the hardcoded emergency owner in `ADMIN_CONSOLE_ALLOWED_EMAILS` (`efrain.pradas@gmail.com`). This list is deliberately independent of `PRODUCT_BRAIN_ALLOWED_EMAILS`. Denial is `notFound()`, logged as `admin_access_denied`, and the gate runs *before* any business query, so a rejected user causes zero reads.

Six tabs render after the gate: Overview, Companies, Users & Projects, Project Tasks, Plans and Admin Access. Data is loaded with the service-role client from `organizations`, `profiles`, `projects`, `roadmap_tasks`, `milestones`, `subscriptions`, `plans` and `admin_authorized_users`, plus the `SECURITY DEFINER` RPCs `admin_list_company_users`, `admin_get_user_emails` and `admin_rename_organization`. The Users & Projects and Project Tasks tabs add text search and company/user/project/status filters. Writes are few and each re-checks the gate: `renameOrgAdminAction` renames any organization through the RPC, and `grantSystemAdminAction` / `revokeSystemAdminAction` insert or deactivate rows in `admin_authorized_users`; all three record to `audit_logs`. The Plans tab is read-only and links out to `/organization/plans` to actually edit. Related: screen-admin-backfill, screen-org-plans, screen-org-members.
Source: src/app/[locale]/(app)/admin/{page,actions}.ts(x), src/components/admin-console/admin-console.tsx, src/lib/admin-console/{access.server,queries}.ts.
Verify: signed in as the platform owner, open /admin and switch to the "Admin Access" tab; any other account gets a 404.', 'Pantalla Consola de Administración', 'La superficie de administración a nivel de plataforma en `/admin`, transversal a todos los inquilinos por diseño. El control es estricto y del lado del servidor: `requirePlatformAdmin` autoriza un correo solo si `admin_authorized_users` tiene una fila activa para él, o si coincide con el propietario de emergencia fijo en `ADMIN_CONSOLE_ALLOWED_EMAILS` (`efrain.pradas@gmail.com`). Esa lista es deliberadamente independiente de `PRODUCT_BRAIN_ALLOWED_EMAILS`. La negación es `notFound()`, se registra como `admin_access_denied` y el control corre *antes* de cualquier consulta de negocio, de modo que un usuario rechazado no provoca ni una sola lectura.

Tras el control se muestran seis pestañas: Resumen, Compañías, Usuarios y proyectos, Tareas por proyecto, Planes y Acceso de administradores. Los datos se cargan con el cliente de rol de servicio desde `organizations`, `profiles`, `projects`, `roadmap_tasks`, `milestones`, `subscriptions`, `plans` y `admin_authorized_users`, además de los RPC `SECURITY DEFINER` `admin_list_company_users`, `admin_get_user_emails` y `admin_rename_organization`. Las pestañas de usuarios y de tareas añaden búsqueda por texto y filtros de compañía, usuario, proyecto y estado. Las escrituras son pocas y cada una revalida el control: `renameOrgAdminAction` renombra cualquier organización mediante el RPC, y `grantSystemAdminAction` / `revokeSystemAdminAction` insertan o desactivan filas en `admin_authorized_users`; las tres quedan asentadas en `audit_logs`. La pestaña de Planes es de solo lectura y enlaza a `/organization/plans` para editarlos. Relacionadas: screen-admin-backfill, screen-org-plans, screen-org-members.
Fuente: src/app/[locale]/(app)/admin/{page,actions}.ts(x), src/components/admin-console/admin-console.tsx, src/lib/admin-console/{access.server,queries}.ts.
Verifica: con la sesión del propietario de la plataforma, abre /admin y cambia a la pestaña "Acceso de administradores"; cualquier otra cuenta recibe un 404.', '/admin — generated from: src/app/[locale]/(app)/admin/page.tsx, src/app/[locale]/(app)/admin/actions.ts, src/components/admin-console/admin-console.tsx, src/lib/admin-console/access.server.ts, src/lib/admin-console/queries.ts, src/lib/admin-console/audit.ts'),
    ('screen-admin-living-graph-observability', 'Living Graph Observability screen', 'An internal admin diagnostics panel at `/admin/living-graph-observability` for the Living Graph Realtime Engine (LGRE). Access uses the same strict server-side email allowlist as the Product Brain cockpit (`isProductBrainAllowedEmail`); unauthorized users get a 404 and no diagnostics data is loaded. The code is explicit about safety: only safe aggregates reach the client — infra health booleans plus an org-scoped project list (id and display title, up to 50) — never payloads, ledger rows, or secrets. The server computes environment health via `getLivingGraphEnvironmentHealth` (which reads `project_event_log`); failures degrade to null. The client panel owns exactly one scoped realtime subscription (via `useLiveGraphSync`) and reports session counters it genuinely instruments: subscription count, active channels, reconnects, stale and fresh-recovery counts, permission losses, notices, errors, and warnings. Metrics it does not track (delta sync, recalculation, rendering — which live in-memory per graph consumer) are honestly rendered as "not instrumented", never fake zeros. Controls include a project selector, a refresh button (`router.refresh()`), and connection-state indicators. Copy comes from the `livingGraphObservability` translation namespace. Related screens: the project Living Graph (`/projects/{id}/execution-map/living-graph`) and the Backfill Console that feeds the event graph.

Source: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx.
Verify: with an allowlisted email, open /admin/living-graph-observability and select a project to watch realtime counters.', 'Pantalla Observabilidad del Living Graph', 'Un panel interno de diagnóstico para administradores en `/admin/living-graph-observability`, dedicado al motor de tiempo real del Living Graph (LGRE). El acceso usa la misma lista blanca estricta de correos del cockpit de Product Brain (`isProductBrainAllowedEmail`), aplicada en el servidor; los no autorizados reciben un 404 y no se carga ningún dato de diagnóstico. El código es explícito en seguridad: al cliente solo llegan agregados seguros — booleanos de salud de infraestructura y una lista de proyectos de la organización (id y título, hasta 50) — nunca payloads, filas del ledger ni secretos. El servidor calcula la salud del entorno con `getLivingGraphEnvironmentHealth` (que lee `project_event_log`); los fallos degradan a null. El panel cliente posee exactamente una suscripción realtime acotada (vía `useLiveGraphSync`) y reporta los contadores de sesión que realmente instrumenta: suscripciones, canales activos, reconexiones, conteos de datos obsoletos y recuperación, pérdidas de permiso, avisos, errores y advertencias. Las métricas que no rastrea (delta sync, recálculo, render) se muestran honestamente como "no instrumentadas", nunca ceros falsos. Los controles incluyen selector de proyecto, botón de refrescar e indicadores de conexión. Pantallas relacionadas: el Living Graph del proyecto y la Consola de Backfill que alimenta el grafo de eventos.

Fuente: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx.
Verifica: con un correo en la lista blanca, abre /admin/living-graph-observability y elige un proyecto para ver los contadores en tiempo real.', '/admin/living-graph-observability — generated from: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx, src/lib/living-graph/observability/environment-health.server.ts'),
    ('screen-ai-operator', 'AI Operator screen', 'A lightweight hub at `/ai-operator` that groups the app''s AI-powered intake modules. After confirming the session with `getOrgContext()`, the page shows a centered Bot icon with a title and description pulled from the next-intl `placeholders.aiOperator` namespace — the "placeholders" namespace is an honest signal that this surface is still an early hub rather than a full module. Below the header are exactly two navigation cards. The first, "Project Import Intelligence", links to `/import` and describes uploading Excel, CSV, JSON, Word, or PDF files to turn them into a full project (bilingual copy is inlined in the component). The second links to the global Drawing Intelligence screen at `/drawing-intelligence`, with its title and description coming from the `drawingIntelligence.aiOperatorCard` translations. Both cards are simple `Link`s with hover affordances and arrow icons. The screen reads no Supabase tables beyond the auth/org check, writes nothing, and calls no server actions — it is purely a launcher. There is no chat or agent execution on this page despite the name. Related screens: Import (`/import`), global Drawing Intelligence (`/drawing-intelligence`), and the Home dashboard whose "AI Operator Briefing" section is a separate, data-driven feature.

Source: src/app/[locale]/(app)/ai-operator/page.tsx.
Verify: open /ai-operator from the app; the two cards navigate to Import and Drawing Intelligence.', 'Pantalla Operador IA', 'Un hub ligero en `/ai-operator` que agrupa los módulos de ingesta con IA de la aplicación. Tras confirmar la sesión con `getOrgContext()`, la página muestra un icono de robot centrado con título y descripción tomados del namespace `placeholders.aiOperator` de next-intl — que el namespace se llame "placeholders" es una señal honesta de que esta superficie sigue siendo un hub inicial y no un módulo completo. Bajo el encabezado hay exactamente dos tarjetas de navegación. La primera, "Importación Inteligente de Proyectos", enlaza a `/import` y describe subir archivos Excel, CSV, JSON, Word o PDF para convertirlos en un proyecto completo (texto bilingüe en el propio componente). La segunda enlaza a la pantalla global de Drawing Intelligence en `/drawing-intelligence`, con título y descripción de las traducciones `drawingIntelligence.aiOperatorCard`. Ambas tarjetas son simples `Link` con efectos al pasar el cursor. La pantalla no lee tablas de Supabase más allá de la verificación de sesión, no escribe nada y no llama server actions — es puramente un lanzador. A pesar del nombre, aquí no hay chat ni ejecución de agentes. Pantallas relacionadas: Importación (`/import`), Drawing Intelligence global (`/drawing-intelligence`) y el dashboard de inicio, cuyo "Resumen del Operador IA" es una funcionalidad aparte basada en datos.

Fuente: src/app/[locale]/(app)/ai-operator/page.tsx.
Verifica: abre /ai-operator en la aplicación; las dos tarjetas navegan a Importación y Drawing Intelligence.', '/ai-operator — generated from: src/app/[locale]/(app)/ai-operator/page.tsx'),
    ('screen-change-password', 'Change password screen', 'A standalone password screen at `/change-password` that deliberately lives outside the `(app)` route group (no app shell), so the forced-change gate in the app layout can redirect here without a loop. It requires a session: the page calls `supabase.auth.getUser()` and redirects to `/login` if there is none. It then reads two things — `user_metadata.must_change_password` (the `forced` flow, set when an admin created the login with a temporary password) and a `mode` from the query string: `?recovery=1` arrives from the reset email sent by `requestPasswordResetAction`, `?invite=1` from `inviteMemberAction`. The heading and copy change accordingly ("Create your password" for an invite, otherwise "Set a new password"). The centered card asks for a new password and its confirmation, both **minimum 12 characters**, and a "Save and continue" button. Submitting calls `changeOwnPasswordAction`, which uses the user''s own session — never the service role — to run `supabase.auth.updateUser`, setting the password and clearing `must_change_password`. Errors cover weak password, reusing the same password, mismatch, expired session and a generic failure; strings here are hardcoded EN/ES pairs rather than next-intl keys. In recovery or invite mode it shows a "Password updated" confirmation with a Continue button; otherwise it navigates straight to `/`. Related: screen-login, screen-org-members, screen-team.
Source: change-password/page.tsx, change-password-form.tsx, change-password/actions.ts.
Verify: sign in with an account flagged `must_change_password` (created from Members with a temp password) — you land on /change-password.', 'Pantalla Cambio de contraseña', 'Una pantalla independiente en `/change-password` que vive a propósito fuera del grupo `(app)` (sin shell de la aplicación), para que la puerta de cambio forzado del layout pueda redirigir aquí sin bucles. Exige sesión: la página llama `supabase.auth.getUser()` y redirige a `/login` si no la hay. Luego lee dos cosas: `user_metadata.must_change_password` (el flujo `forced`, activo cuando un administrador creó el acceso con clave temporal) y un `mode` en la cadena de consulta: `?recovery=1` llega desde el correo de recuperación que envía `requestPasswordResetAction` y `?invite=1` desde `inviteMemberAction`. El título y el texto cambian según el caso ("Crea tu contraseña" en una invitación; si no, "Establece una nueva contraseña"). La tarjeta centrada pide la nueva contraseña y su confirmación, ambas de **mínimo 12 caracteres**, con el botón "Guardar y continuar". Al enviar se llama `changeOwnPasswordAction`, que usa la sesión del propio usuario —nunca el service role— para ejecutar `supabase.auth.updateUser`, fijar la contraseña y limpiar `must_change_password`. Los errores cubren contraseña débil, reutilizar la misma, no coincidencia, sesión expirada y fallo genérico; estos textos son pares EN/ES escritos en el componente, no claves de next-intl. En modo recuperación o invitación aparece una confirmación "Contraseña actualizada" con botón Continuar; en el resto de casos navega directo a `/`. Relacionadas: screen-login, screen-org-members, screen-team.
Fuente: change-password/page.tsx, change-password-form.tsx, change-password/actions.ts.
Verifica: inicia sesión con una cuenta marcada `must_change_password` (creada desde Miembros con clave temporal) y comprueba que llegas a /change-password.', '/change-password — generated from: src/app/[locale]/change-password/page.tsx, src/app/[locale]/change-password/change-password-form.tsx, src/app/[locale]/change-password/actions.ts, src/app/[locale]/(app)/layout.tsx, src/app/[locale]/(auth)/actions.ts'),
    ('screen-drawing-intelligence-global', 'Drawing Intelligence (global) screen', 'The org-level entry point for the Drawing/BIM Intelligence module at `/drawing-intelligence`, reached from the navigation or from the AI Operator hub. It is a project selector, not the analysis surface itself. The server page (forced dynamic) queries two Supabase tables in parallel, both scoped to the organization and excluding soft-deleted rows: `projects` (id, slug, localized title) and `drawing_files` (id, project_id) to count drawings per project — the code notes that `drawing_files` may not exist until its migration is applied, in which case counts honestly degrade to zero. The header shows the module title and subtitle from the `drawingIntelligence` translations plus a highlighted badge (`notStorageNote`) clarifying this module is not a file-storage feature. If the org has projects, a card grid lists each project with its drawing count ("N drawings" or "no drawings yet"), and each card links to that project''s own Drawing Intelligence tab at `/projects/{id}/drawing-intelligence`, where uploads and analysis actually happen. With no projects, a dashed empty state shows the `global.noProjects` message. Nothing is written from this screen. Related screens: per-project Drawing Intelligence, AI Operator, Projects list.

Source: src/app/[locale]/(app)/drawing-intelligence/page.tsx.
Verify: open /drawing-intelligence from the navigation and click a project card to land on that project''s Drawing Intelligence tab.', 'Pantalla Drawing Intelligence (global)', 'El punto de entrada organizacional del módulo de inteligencia de planos/BIM en `/drawing-intelligence`, accesible desde la navegación o desde el hub del Operador IA. Es un selector de proyectos, no la superficie de análisis en sí. La página de servidor (dinámica forzada) consulta en paralelo dos tablas de Supabase, ambas limitadas a la organización y sin registros eliminados: `projects` (id, slug, título localizado) y `drawing_files` (id, project_id) para contar planos por proyecto — el código anota que `drawing_files` puede no existir hasta aplicar su migración, en cuyo caso los conteos degradan honestamente a cero. El encabezado muestra título y subtítulo de las traducciones `drawingIntelligence` más una insignia destacada (`notStorageNote`) aclarando que el módulo no es un almacén de archivos. Si hay proyectos, una cuadrícula de tarjetas lista cada proyecto con su conteo de planos ("N planos" o "sin planos aún"), y cada tarjeta enlaza a la pestaña Drawing Intelligence de ese proyecto en `/projects/{id}/drawing-intelligence`, donde realmente ocurren la subida y el análisis. Sin proyectos, se muestra un estado vacío con el mensaje `global.noProjects`. Desde esta pantalla no se escribe nada. Pantallas relacionadas: Drawing Intelligence por proyecto, Operador IA, Lista de proyectos.

Fuente: src/app/[locale]/(app)/drawing-intelligence/page.tsx.
Verifica: abre /drawing-intelligence desde la navegación y pulsa una tarjeta de proyecto para llegar a su pestaña de Drawing Intelligence.', '/drawing-intelligence — generated from: src/app/[locale]/(app)/drawing-intelligence/page.tsx'),
    ('screen-execution-map-kpis', 'KPI Engine screen', 'The KPI Engine screen evaluates a project''s metrics against canonical data and is reached from the "KPIs" tab button on the Execution Map. The server page calls `loadKpiDataset`, which validates org ownership (an unauthorized or invalid project id renders a denial panel, a read failure an error panel) and reads `projects`, `roadmap_tasks`, `milestones`, `budget_items` and `resources`. Only hourly `cost_rate` values are used, and budget lines are matched to milestones by `milestone_id` first and by normalised name second. Every card in the grid comes from the built-in `KPI_CATALOG` — 24 definitions covering progress, blocked and overdue tasks, effort ratios, delay percentiles, momentum and forecast, cost and budget consumption, and the Earned Value family `spi`, `cpi`, `schedule_variance_hours`, `cost_variance`, `eac` and `percent_complete_evm`, which depend on `roadmap_tasks.baseline_estimate_hours` and the baseline dates. Each card shows the value, unit, bilingual description and the raw expression; when a denominator is missing it says "not computable" rather than showing a zero. Below, `CustomKpiSection` lists persisted custom KPIs from `kpi_definitions` and lets non-viewers create them via `createCustomKpi` (server-revalidated against the sandbox allow-list) or soft-delete their own via `deleteCustomKpi`. Two honest caveats: `loadKpiDataset` returns a per-milestone `milestoneScopes` dimension that this page never renders — pinned milestone KPIs surface instead on the Gantt and Living Graph — and nothing in the app writes the baseline columns the EVM cards need. Related: screen-execution-map, screen-living-graph.
Source: execution-map/kpis/page.tsx, lib/kpi/load-dataset.ts, lib/kpi/catalog.ts, components/process-mining/custom-kpi-section.tsx.
Verify: Execution Map > "KPIs" (/projects/[projectId]/execution-map/kpis).', 'Pantalla Motor de KPIs', 'La pantalla del Motor de KPIs evalúa las métricas del proyecto sobre datos canónicos y se abre desde el botón "KPIs" del Mapa de Ejecución. La página de servidor llama a `loadKpiDataset`, que valida la pertenencia a la organización (un proyecto no autorizado o con id inválido muestra un panel de denegación; un fallo de lectura, uno de error) y consulta `projects`, `roadmap_tasks`, `milestones`, `budget_items` y `resources`. Solo se usan las tarifas `cost_rate` por hora, y las líneas de presupuesto se enlazan al hito primero por `milestone_id` y después por nombre normalizado. Cada tarjeta proviene del catálogo `KPI_CATALOG`: 24 definiciones que cubren avance, tareas bloqueadas y vencidas, esfuerzo, percentiles de retraso, impulso y pronóstico, coste y consumo de presupuesto, y la familia de Valor Ganado `spi`, `cpi`, `schedule_variance_hours`, `cost_variance`, `eac` y `percent_complete_evm`, que dependen de `roadmap_tasks.baseline_estimate_hours` y de las fechas de línea base. Cada tarjeta muestra el valor, la unidad, la descripción bilingüe y la expresión; cuando falta un denominador dice "no calculable" en lugar de mostrar un cero. Debajo, `CustomKpiSection` lista los KPIs personalizados guardados en `kpi_definitions` y permite a quien no sea viewer crearlos con `createCustomKpi` (revalidado en servidor contra la lista blanca del sandbox) o borrar los propios con `deleteCustomKpi`. Dos advertencias honestas: `loadKpiDataset` devuelve una dimensión por hito, `milestoneScopes`, que esta página nunca dibuja —los KPIs por hito aparecen en el Cronograma y el Grafo Vivo—, y nada en la aplicación escribe las columnas de línea base que necesitan las tarjetas de Valor Ganado. Relacionadas: screen-execution-map, screen-living-graph.
Fuente: execution-map/kpis/page.tsx, lib/kpi/load-dataset.ts, lib/kpi/catalog.ts, components/process-mining/custom-kpi-section.tsx.
Verifica: Mapa de Ejecución > "KPIs" (/projects/[projectId]/execution-map/kpis).', '/projects/[projectId]/execution-map/kpis — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/kpis/page.tsx, src/lib/kpi/load-dataset.ts, src/lib/kpi/build-dataset.ts, src/lib/kpi/catalog.ts, src/lib/kpi/earned-value.ts, src/lib/kpi/custom-actions.ts, src/components/process-mining/custom-kpi-section.tsx'),
    ('screen-execution-map-root-causes', 'Statistical Root Cause Miner screen', 'The CAP-046 F2 consumer, reached from the Execution Map''s "Root Causes" tab button. It asks which attributes of this project''s work correlate with trouble. The server page calls `loadRootCauseAnalysis`, which validates the UUID, resolves the org context and confirms the project belongs to it (deny-by-default, returning a safe denial state with no data), then reads `roadmap_tasks`, `milestones` and `TaskReopened` rows from `project_event_log` (capped at 5,000) through the RLS-scoped client. Problem flags follow the REG-010 canonical helpers: `hasActiveBlocker` for blockage, and a delay rule where a terminal-but-not-completed task is never late and a completed task is late only if `completed_at` exceeds `end_date`. The pure `mineRootCauses` engine then crosses three problem types — delay, blockage, rework — against the dimensions ownership, priority, criticality, milestone, discipline, trade and location, keeping at most five findings per problem.

The view is a findings table with an influence score, a written evidence line, sample size, confidence and example tasks, plus a method note. It is evidence-only by contract: no recommendations, and no per-person dimension, since ownership is analysed solely as assigned versus unassigned. Nothing is written and no LLM is called. The only other control is "Back to Execution Map". Related: screen-execution-map, screen-execution-map-variants, screen-execution-map-kpis.
Source: root-causes/page.tsx, src/lib/process-mining/root-cause/load-analysis.ts, src/components/process-mining/root-cause-view.tsx.
Verify: open a project, go to Execution Map, click "Root Causes".', 'Pantalla Minero Estadístico de Causas Raíz', 'El consumidor de CAP-046 F2, al que se llega desde el botón "Causas Raíz" del Mapa de Ejecución. Pregunta qué atributos del trabajo de este proyecto se correlacionan con los problemas. La página de servidor llama a `loadRootCauseAnalysis`, que valida el UUID, resuelve el contexto de la organización y confirma que el proyecto le pertenece (denegación por omisión, devolviendo un estado seguro sin datos), y después lee `roadmap_tasks`, `milestones` y las filas `TaskReopened` de `project_event_log` (con tope de 5.000) mediante el cliente sujeto a RLS. Las marcas de problema siguen los ayudantes canónicos de REG-010: `hasActiveBlocker` para el bloqueo, y una regla de retraso según la cual una tarea terminal pero no completada nunca va tarde, y una completada solo va tarde si `completed_at` supera a `end_date`. Luego el motor puro `mineRootCauses` cruza tres tipos de problema —retraso, bloqueo y retrabajo— contra las dimensiones responsabilidad, prioridad, criticidad, hito, disciplina, oficio y ubicación, conservando como máximo cinco hallazgos por problema.

La vista es una tabla de hallazgos con puntaje de influencia, una línea de evidencia redactada, tamaño de muestra, confianza y tareas de ejemplo, además de una nota metodológica. Por contrato solo aporta evidencia: no da recomendaciones ni tiene dimensión por persona, ya que la responsabilidad se analiza únicamente como asignada frente a no asignada. No escribe nada ni invoca ningún modelo de lenguaje. El otro único control es "Volver al Mapa de Ejecución". Relacionadas: screen-execution-map, screen-execution-map-variants, screen-execution-map-kpis.
Fuente: root-causes/page.tsx, src/lib/process-mining/root-cause/load-analysis.ts, src/components/process-mining/root-cause-view.tsx.
Verifica: abre un proyecto, entra al Mapa de Ejecución y pulsa "Causas Raíz".', '/projects/[projectId]/execution-map/root-causes — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/root-causes/page.tsx, src/components/process-mining/root-cause-view.tsx, src/lib/process-mining/root-cause/load-analysis.ts, src/lib/process-mining/root-cause/engine.ts, src/lib/execution/task-activity.ts'),
    ('screen-execution-map-variants', 'Execution Variant Analysis screen', 'The CAP-046 F1 consumer, reached from the Execution Map''s "Execution Variants" tab button. It answers which real execution paths the organization''s projects actually follow. The page is a server component that calls `loadVariantAnalysis`, which validates the UUID, resolves the org context, confirms the focus project belongs to that organization (deny-by-default), then reads `projects` and up to 20,000 rows of `project_event_log` through the RLS-scoped client and feeds the pure `analyzeVariants` engine for the `project_lifecycle` process type. It writes nothing: no events, no canonical changes, no LLM call. The only navigation control is a "Back to Execution Map" link; there are no filters or mutations.

The view shows four summary tiles (cases, variants, events used, decided cases), a truncation notice when the event window was capped, a focus card naming this project''s variant share, fitness versus the reference variant and its skipped and inserted activities, and a variant catalog table with sequence, projects, frequency, median duration, rework and success, flagging the reference variant. An honest empty state appears when no reference variant exists. Note the practical gate: it can only be as rich as the event log, and business-event capture is limited to projects listed in `PROCESS_MINING_EVENT_CAPTURE_PROJECT_IDS`. Related: screen-execution-map, screen-execution-map-root-causes, screen-execution-map-kpis, screen-milestone-flow.
Source: variants/page.tsx, src/lib/process-mining/variants/load-analysis.ts, src/components/process-mining/variant-analysis-view.tsx.
Verify: open a project, go to Execution Map, click "Execution Variants".', 'Pantalla Análisis de Variantes de Ejecución', 'El consumidor de CAP-046 F1, al que se llega desde el botón "Variantes de Ejecución" del Mapa de Ejecución. Responde qué caminos de ejecución siguen realmente los proyectos de la organización. La página es un componente de servidor que llama a `loadVariantAnalysis`, el cual valida el UUID, resuelve el contexto de la organización, confirma que el proyecto en foco le pertenece (denegación por omisión) y luego lee `projects` y hasta 20.000 filas de `project_event_log` con el cliente sujeto a RLS, para alimentar el motor puro `analyzeVariants` con el tipo de proceso `project_lifecycle`. No escribe nada: ni eventos, ni cambios canónicos, ni llamadas a un modelo de lenguaje. El único control de navegación es un enlace "Volver al Mapa de Ejecución"; no hay filtros ni mutaciones.

La vista muestra cuatro tarjetas de resumen (casos, variantes, eventos usados y casos decididos), un aviso cuando la ventana de eventos quedó truncada, una tarjeta de foco con la cuota de la variante de este proyecto, su ajuste frente a la variante de referencia y las actividades omitidas e insertadas, y un catálogo de variantes con secuencia, proyectos, frecuencia, duración mediana, retrabajo y éxito, marcando la variante de referencia. Cuando no existe variante de referencia aparece un estado vacío honesto. Ojo con el límite práctico: la pantalla solo puede ser tan rica como el registro de eventos, y la captura de eventos de negocio se limita a los proyectos listados en `PROCESS_MINING_EVENT_CAPTURE_PROJECT_IDS`. Relacionadas: screen-execution-map, screen-execution-map-root-causes, screen-execution-map-kpis, screen-milestone-flow.
Fuente: variants/page.tsx, src/lib/process-mining/variants/load-analysis.ts, src/components/process-mining/variant-analysis-view.tsx.
Verifica: abre un proyecto, entra al Mapa de Ejecución y pulsa "Variantes de Ejecución".', '/projects/[projectId]/execution-map/variants — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/variants/page.tsx, src/components/process-mining/variant-analysis-view.tsx, src/lib/process-mining/variants/load-analysis.ts, src/lib/process-mining/variants/engine.ts, src/lib/events/process-mining-capture-flag.ts'),
    ('screen-execution-map', 'Execution Map screen', 'The Execution Map is a project''s roadmap and execution hub. The server page reads `projects`, `milestones`, `roadmap_tasks` and `task_dependencies`, computes progress (`computeRoadmapProgress`), a next step (`computeNextStep`) and a topological task order, then calls `loadMilestoneKpis`, which reads `milestone_kpi_pins`, `kpi_definitions`, `budget_items` and `resources` so a pinned KPI reads identically here and in the Living Graph. Six tabs render inline: Overview (`RoadmapHero`, `NextStepPanel`, `ExecutionDashboard`), Timeline, Tasks (status changes, edit/archive, reorder milestones, add predecessors), Gantt, Critical Path and Dependencies. Critical Path renders no chart — it is a panel whose button opens the Living Graph with `?overlay=criticalPath`. The Gantt now has real zoom (Fit/Quarter/Month/Week/Day, auto-chosen from plan length via `gantt-zoom.ts`), draggable bars calling `updateTaskDatesAction`, a dashed "Original plan" ghost bar drawn from `roadmap_tasks.baseline_start_date/baseline_end_date` with a drift count, and right-click on a milestone row to pin up to eight KPIs (`pinKpiToMilestone`/`unpinKpiFromMilestone`, writing `milestone_kpi_pins`). Below it, `CashFlowPanel` plots monthly planned/baseline/spent cost. No screen writes the baseline columns — they are read-only until something populates them. Header buttons create tasks and milestones. Five extra tab buttons navigate to living-graph, milestone-flow, variants, root-causes and kpis. Related: screen-living-graph, screen-milestone-flow, screen-execution-map-kpis, screen-project-roadmap.
Source: execution-map/page.tsx, execution-map-client.tsx, components/roadmap/gantt-roadmap.tsx, lib/kpi/load-milestone-kpis.ts.
Verify: open a project and go to Execution Map (/projects/[projectId]/execution-map), then the "Gantt" tab.', 'Pantalla Mapa de Ejecución', 'El Mapa de Ejecución es el centro de hoja de ruta y ejecución del proyecto. La página de servidor lee `projects`, `milestones`, `roadmap_tasks` y `task_dependencies`, calcula el progreso (`computeRoadmapProgress`), el siguiente paso (`computeNextStep`) y un orden topológico de tareas, y luego llama a `loadMilestoneKpis`, que consulta `milestone_kpi_pins`, `kpi_definitions`, `budget_items` y `resources` para que un KPI fijado se lea igual aquí y en el Grafo Vivo. Seis pestañas se muestran en la misma página: Vista General (`RoadmapHero`, `NextStepPanel`, `ExecutionDashboard`), Línea de Tiempo, Tareas (cambio de estado, editar y archivar, reordenar hitos, añadir predecesoras), Cronograma, Ruta Crítica y Dependencias. Ruta Crítica no dibuja nada: es un panel cuyo botón abre el Grafo Vivo con `?overlay=criticalPath`. El Cronograma ya tiene zoom real (Todo/Trimestre/Mes/Semana/Día, elegido automáticamente según la duración del plan mediante `gantt-zoom.ts`), barras arrastrables que llaman a `updateTaskDatesAction`, una barra fantasma punteada de "Plan inicial" tomada de `roadmap_tasks.baseline_start_date/baseline_end_date` con recuento de desviación, y clic derecho sobre la fila de un hito para fijar hasta ocho KPIs (`pinKpiToMilestone`/`unpinKpiFromMilestone`, que escriben en `milestone_kpi_pins`). Debajo, `CashFlowPanel` grafica el coste mensual planificado, de línea base y gastado. Ninguna pantalla escribe las columnas de línea base: hoy solo se leen. Los botones del encabezado crean tareas e hitos. Cinco botones adicionales navegan a living-graph, milestone-flow, variants, root-causes y kpis. Relacionadas: screen-living-graph, screen-milestone-flow, screen-execution-map-kpis, screen-project-roadmap.
Fuente: execution-map/page.tsx, execution-map-client.tsx, components/roadmap/gantt-roadmap.tsx, lib/kpi/load-milestone-kpis.ts.
Verifica: abre un proyecto, entra a Mapa de Ejecución (/projects/[projectId]/execution-map) y luego a la pestaña "Cronograma".', '/projects/[projectId]/execution-map — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/execution-map-client.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/dependency-actions.ts, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts, src/components/roadmap/gantt-roadmap.tsx, src/components/roadmap/cash-flow-panel.tsx, src/lib/kpi/load-milestone-kpis.ts, src/lib/kpi/milestone-pin-actions.ts, src/lib/roadmap/gantt-zoom.ts'),
    ('screen-home-dashboard', 'Home dashboard (PMO Command Center) screen', 'The authenticated home page at `/`, the first screen after login — anonymous visitors are redirected by the middleware to `/landing` instead. What renders depends on a server flag: when `SINGLE_DASHBOARD_MODE` is on and the user is an org `owner` or `admin`, `shouldServeIntelligenceCenterAtRoot` swaps the whole page for `PmoIntelligenceCenterView` (Dashboard 3). Members and viewers always keep the PMO Command Center described here, deliberately, so promoting Dashboard 3 never hands them a 404 at the app''s home. The Command Center calls `getCommandCenterSummary(organizationId, locale)`, a server-only service that aggregates org-scoped rows from `projects`, `roadmap_tasks`, `milestones`, `risks`, `material_requirements`, `rfis`, `budget_items`, `resources`, `decisions`, `process_nodes`, `process_edges`, `task_dependencies` and `audit_logs` — no fabricated data, and child rows whose parent project is soft-deleted are filtered out. Blocked counts run through `hasActiveBlocker` (REG-010). With no projects an empty state offers Import and Projects. Otherwise: six KPI cards that deep-link into prebuilt Reports (`?report=blocked_tasks_report`, `critical_path_report`, `cost_overrun_risk`, …), the Portfolio Health Engine ring with six dimension bars, Today''s PMO Focus, AI Operator Briefing, Critical Path Monitor, Decision Queue, Resource & Labor Capacity, Material & Procurement Risk, Living Graph Signals, Upcoming 14 Days, Budget & Forecast Signals, Recent Activity and Quick Actions. `?auth=confirmed` shows an email-confirmed banner. Everything is read-only. Related: screen-projects-list, screen-reports, screen-team, screen-import, screen-pmo-living-graph.
Source: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts, src/lib/pmo-living-graph/single-dashboard.ts.
Verify: log in and land on / — the Command Center renders, or Dashboard 3 if single-dashboard mode is on and you are owner/admin.', 'Pantalla Dashboard de inicio (PMO Command Center)', 'La página de inicio autenticada en `/`, la primera pantalla tras iniciar sesión; a los visitantes anónimos el middleware los redirige a `/landing`. Lo que se muestra depende de una bandera de servidor: si `SINGLE_DASHBOARD_MODE` está activa y el usuario es `owner` o `admin` de la organización, `shouldServeIntelligenceCenterAtRoot` sustituye toda la página por `PmoIntelligenceCenterView` (Dashboard 3). Los miembros y lectores conservan siempre el PMO Command Center, a propósito, para que promover el Dashboard 3 nunca les devuelva un 404 en la portada. El Command Center llama a `getCommandCenterSummary(organizationId, locale)`, un servicio solo de servidor que agrega filas del tenant desde `projects`, `roadmap_tasks`, `milestones`, `risks`, `material_requirements`, `rfis`, `budget_items`, `resources`, `decisions`, `process_nodes`, `process_edges`, `task_dependencies` y `audit_logs` — sin datos inventados y descartando registros cuyo proyecto padre esté borrado lógicamente. Los bloqueos se calculan con `hasActiveBlocker` (REG-010). Sin proyectos aparece un estado vacío con Importar y Proyectos. Con proyectos: seis tarjetas KPI que enlazan a reportes preconstruidos (`?report=blocked_tasks_report`, `critical_path_report`, `cost_overrun_risk`, …), el anillo del Motor de Salud del Portafolio con seis dimensiones, Foco del PMO Hoy, Resumen del Operador IA, Monitor de Ruta Crítica, Cola de Decisiones, Capacidad de Recursos y Mano de Obra, Riesgo de Materiales y Compras, Señales del Living Graph, Próximos 14 Días, Señales de Presupuesto y Pronóstico, Actividad Reciente y Acciones Rápidas. Con `?auth=confirmed` se muestra un aviso de correo confirmado. Todo es de solo lectura. Relacionadas: screen-projects-list, screen-reports, screen-team, screen-import, screen-pmo-living-graph.
Fuente: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts, src/lib/pmo-living-graph/single-dashboard.ts.
Verifica: inicia sesión y llega a / — se muestra el Command Center, o el Dashboard 3 si el modo de dashboard único está activo y eres owner/admin.', '/ — generated from: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts, src/lib/pmo-living-graph/single-dashboard.ts, src/lib/pmo-living-graph/flags.ts, src/lib/pmo-process-intelligence/flags.ts'),
    ('screen-import', 'Import (Project Import Intelligence) screen', 'The Project Import Intelligence wizard at `/import`, reached from the navigation, the Command Center header or its empty state; `?projectId=` preselects a merge target. The steps are Upload → Analyze → Review → Import → Done, and nothing is written without preview and explicit approval. Upload accepts `.xlsx/.xlsm`, CSV, JSON, `.docx`, PDF, TXT, Markdown and **Microsoft Project `.mpp`** up to 25 MB, into the `project-imports` Storage bucket; two official templates are downloadable from `/templates/`. `.mpp` has no JVM here, so `convertMppToMpxjJson` converts it inside a per-conversion Vercel Sandbox with egress denied, then rejoins the normal pipeline via `mpxjToParsedFile`. `analyzeImportJobAction` runs `parseImportFile`, `extractCanonicalImport` (with `aiExtractCanonicalImport` as fallback for unstructured text) and `validateCanonicalImport`, persisting to `project_import_jobs`, `project_import_raw_data`, `project_import_mappings`, `project_import_entities` and `project_import_validation_results`, audited in `project_import_audit_events`. Review tabs: Summary, Tasks, Milestones, Dependencies, Resources, Materials, Budget, Risks, a **Charter** tab (fills only empty Charter Center fields, never overwrites), Warnings and Raw data — each row toggles with `toggleImportEntityAction`, and a blocker like a circular dependency must be disabled first. Creating a new project now also requires choosing an **owning PMO** (governance unit). `executeImportAction` calls `create_project_v2` and writes `milestones`, `roadmap_tasks`, `task_dependencies`, `resources`, `material_requirements`, `budget_items`, `risks`, `project_charters` and `project_import_created_records`, emitting Living Graph nodes/edges and recalculating the critical path. `maxDuration` is capped at 300 s (Vercel Hobby ceiling), so derived projections may stop early and say so; progress is polled from the `/api/import/progress` route handler — not a server action, which used to queue behind the import itself. Failures roll back automatically. Related: screen-projects-list, screen-project-charter, screen-ai-operator.
Source: import/{page,import-client,actions}, src/app/api/import/progress/route.ts, src/lib/import-intelligence/*.
Verify: open Import, upload the Excel template, review the tabs, pick the owning PMO, press "Approve & import".', 'Pantalla Importación (Project Import Intelligence)', 'El asistente de Importación Inteligente de Proyectos en `/import`, accesible desde la navegación, el encabezado del Command Center o su estado vacío; `?projectId=` preselecciona el proyecto destino. Los pasos son Subir → Analizar → Revisar → Importar → Listo, y nada se escribe sin vista previa y aprobación explícita. Se aceptan `.xlsx/.xlsm`, CSV, JSON, `.docx`, PDF, TXT, Markdown y **Microsoft Project `.mpp`** hasta 25 MB, en el bucket `project-imports`; hay dos plantillas oficiales descargables desde `/templates/`. Como aquí no hay JVM, `convertMppToMpxjJson` convierte el `.mpp` en un Sandbox de Vercel creado por conversión y sin salida a red, y luego vuelve al pipeline normal con `mpxjToParsedFile`. `analyzeImportJobAction` ejecuta `parseImportFile`, `extractCanonicalImport` (con `aiExtractCanonicalImport` como respaldo para texto no estructurado) y `validateCanonicalImport`, persistiendo en `project_import_jobs`, `project_import_raw_data`, `project_import_mappings`, `project_import_entities` y `project_import_validation_results`, con auditoría en `project_import_audit_events`. Pestañas de revisión: Resumen, Tareas, Hitos, Dependencias, Recursos, Materiales, Presupuesto, Riesgos, una pestaña **Charter** (solo rellena campos vacíos del Charter Center, nunca sobrescribe), Advertencias y Datos crudos; cada fila se activa o desactiva con `toggleImportEntityAction` y un bloqueo como una dependencia circular debe desactivarse antes. Crear un proyecto nuevo exige además elegir la **PMO propietaria** (unidad de gobernanza). `executeImportAction` llama a `create_project_v2` y escribe `milestones`, `roadmap_tasks`, `task_dependencies`, `resources`, `material_requirements`, `budget_items`, `risks`, `project_charters` y `project_import_created_records`, emitiendo nodos y aristas del Living Graph y recalculando la ruta crítica. `maxDuration` está topado en 300 s (techo del plan Hobby de Vercel), así que las proyecciones derivadas pueden quedarse cortas y se informa; el progreso se consulta desde el route handler `/api/import/progress`, no desde una server action, que antes quedaba en cola detrás de la propia importación. Los fallos se revierten automáticamente. Relacionadas: screen-projects-list, screen-project-charter, screen-ai-operator.
Fuente: import/{page,import-client,actions}, src/app/api/import/progress/route.ts, src/lib/import-intelligence/*.
Verifica: abre Importar, sube la plantilla Excel, revisa las pestañas, elige la PMO propietaria y pulsa "Aprobar e importar".', '/import — generated from: src/app/[locale]/(app)/import/page.tsx, src/app/[locale]/(app)/import/import-client.tsx, src/app/[locale]/(app)/import/actions.ts, src/app/api/import/progress/route.ts, src/lib/import-intelligence/parse.ts, src/lib/import-intelligence/extract.ts, src/lib/import-intelligence/ai-extract.ts, src/lib/import-intelligence/validate.ts, src/lib/import-intelligence/execute.ts, src/lib/import-intelligence/progress.ts, src/lib/import-intelligence/mpp-convert.server.ts, src/lib/import-intelligence/mpp-model.ts, src/components/import/import-progress-panel.tsx'),
    ('screen-labor-capacity-lookahead', 'Labor Lookahead screen', 'The Labor Lookahead screen answers "are the next weeks of work executable with available labor?" and lives at /projects/[projectId]/labor-capacity/lookahead (the "Lookahead" tab of the labor navigation). The server page (force-dynamic, read-only — no writes) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then precomputes deterministic results for both horizons: `computeLookahead` (3-week and 6-week), lookahead narratives (`buildLookaheadNarrative`), crew idle risk (`computeCrewIdleRisk`) and idle-risk summaries (`buildIdleRiskSummary`). The client offers a 3-Week / 6-Week horizon toggle, summary counts by readiness level, a weekly grid of upcoming activities with readiness badges and checklist status, and a narrative panel. For each not-ready activity it builds a readiness explanation (`buildReadinessExplanation`) covering the constraint, missing prerequisites and a recommended action. The crew idle risk section lists top recommended actions (with action-type labels) and per-crew entries showing days at risk, severity and the suggested mitigation; recommendations are display-only text — nothing is executed automatically. Related screens: screen-labor-capacity, screen-labor-capacity-workface, screen-living-graph (readiness overlay uses the same lookahead engine).
Source: lookahead/page.tsx, lookahead-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verify: project > Labor Capacity > Lookahead tab (/projects/[projectId]/labor-capacity/lookahead).', 'Pantalla Lookahead Laboral', 'La pantalla Lookahead Laboral responde "¿las próximas semanas de trabajo son ejecutables con la mano de obra disponible?" y vive en /projects/[projectId]/labor-capacity/lookahead (pestaña "Lookahead" de la navegación laboral). La página de servidor (force-dynamic, solo lectura, sin escrituras) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y precalcula resultados deterministas para ambos horizontes: `computeLookahead` (3 y 6 semanas), narrativas (`buildLookaheadNarrative`), riesgo de cuadrilla ociosa (`computeCrewIdleRisk`) y sus resúmenes (`buildIdleRiskSummary`). El cliente ofrece un alternador de horizonte de 3 o 6 semanas, conteos por nivel de preparación, una cuadrícula semanal de actividades próximas con insignias de preparación y estado de la lista de verificación, y un panel narrativo. Para cada actividad no lista construye una explicación de preparación (`buildReadinessExplanation`) con la restricción, los prerrequisitos faltantes y la acción recomendada. La sección de riesgo de cuadrilla ociosa lista las principales acciones recomendadas y entradas por cuadrilla con días en riesgo, severidad y mitigación sugerida; las recomendaciones son solo texto informativo, nada se ejecuta automáticamente. Pantallas relacionadas: screen-labor-capacity, screen-labor-capacity-workface, screen-living-graph (la superposición de preparación usa el mismo motor).
Fuente: lookahead/page.tsx, lookahead-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verifica: proyecto > Capacidad Laboral > pestaña Lookahead (/projects/[projectId]/labor-capacity/lookahead).', '/projects/[projectId]/labor-capacity/lookahead — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/lookahead/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/lookahead/lookahead-client.tsx, src/lib/labor/lookahead.ts, src/lib/labor/crew-idle-risk.ts, src/lib/labor/readiness-explanation.ts'),
    ('screen-labor-capacity-workface', 'Workface Readiness Board screen', 'The Workface Readiness Board asks "are upcoming activities ready to execute?" and shows readiness, blockers and risk at a glance, at /projects/[projectId]/labor-capacity/workface (the "Workface Board" tab of the labor navigation). The server page (force-dynamic, read-only — no server actions here) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then precomputes `computeLookahead` and `computeCrewIdleRisk` for both 3-week and 6-week horizons. The client joins lookahead activities with idle-risk data into board rows and sorts them worst-first by readiness severity, then idle risk. Summary cards show total, ready, at-risk and blocked activities plus overall readiness percent. Filters include trade, readiness level, critical-path-only and blocked-only. Each row shows readiness percent, missing prerequisites (unchecked readiness-checklist items), blocker types, idle risk with days at risk, and a critical-path flag; expanding a row reveals the readiness checklist, assigned resources, a recommended action and the downstream impact, with `buildReadinessExplanation` generating the explanation for not-ready activities. Everything is deterministic and display-only; the screen writes nothing. Related screens: screen-labor-capacity, screen-labor-capacity-lookahead, screen-living-graph (readiness overlay).
Source: workface/page.tsx, workface-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verify: project > Labor Capacity > Workface Board tab (/projects/[projectId]/labor-capacity/workface).', 'Pantalla Tablero de Preparación del Frente de Obra', 'El Tablero de Preparación del Frente de Obra pregunta "¿las actividades próximas están listas para ejecutarse?" y muestra preparación, bloqueos y riesgo de un vistazo, en /projects/[projectId]/labor-capacity/workface (pestaña "Frente de Obra" de la navegación laboral). La página de servidor (force-dynamic, solo lectura, sin acciones de servidor) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y precalcula `computeLookahead` y `computeCrewIdleRisk` para los horizontes de 3 y 6 semanas. El cliente une las actividades del lookahead con los datos de riesgo ocioso en filas de tablero y las ordena de peor a mejor por severidad de preparación y luego por riesgo ocioso. Las tarjetas de resumen muestran actividades totales, listas, en riesgo y bloqueadas, más el porcentaje general de preparación. Los filtros incluyen oficio, nivel de preparación, solo-ruta-crítica y solo-bloqueadas. Cada fila muestra el porcentaje de preparación, los prerrequisitos faltantes (elementos sin marcar de la lista de verificación), los tipos de bloqueo, el riesgo ocioso con días en riesgo y la marca de ruta crítica; al expandirla se ven la lista de verificación, los recursos asignados, la acción recomendada y el impacto aguas abajo, con `buildReadinessExplanation` generando la explicación de las actividades no listas. Todo es determinista y solo de visualización; la pantalla no escribe nada. Pantallas relacionadas: screen-labor-capacity, screen-labor-capacity-lookahead, screen-living-graph (superposición de preparación).
Fuente: workface/page.tsx, workface-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verifica: proyecto > Capacidad Laboral > pestaña Frente de Obra (/projects/[projectId]/labor-capacity/workface).', '/projects/[projectId]/labor-capacity/workface — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/workface/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/workface/workface-client.tsx, src/lib/labor/lookahead.ts, src/lib/labor/crew-idle-risk.ts'),
    ('screen-labor-capacity', 'Labor Capacity & Skills Matrix screen', 'The Labor Capacity & Skills Matrix monitors construction labor gaps, utilization and crew readiness across trades and weeks, at /projects/[projectId]/labor-capacity (the "Capacity Matrix" tab of the labor navigation, alongside Lookahead, Workface Board and a Variance tab — note: the Variance tab links to /labor-capacity/variance, a route with no page in the codebase). The server page (force-dynamic, read-only) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then runs the deterministic `computeLaborCapacity` engine; this screen writes nothing. The client shows four summary cards (total trades, shortage weeks, critical trades, max utilization), a deterministic narrative panel built with `buildCapacitySummary` (overall severity, key numbers, cascade risks), and filters by trade, week, milestone, location and critical-path-only. The main table lists weekly capacity gaps per trade: required vs available headcount and hours, gap, utilization, risk level and critical-path flag; expanding a row shows a gap explanation (`buildGapExplanation`) plus the related activities and resources. A skills panel describes resources from the trade taxonomy (type, skill level, constraints, availability windows). An empty state appears when no labor data exists. Related screens: screen-labor-capacity-lookahead, screen-labor-capacity-workface, screen-living-graph (laborCapacity overlay), screen-resource-capacity.
Source: labor-capacity/page.tsx, labor-capacity-client.tsx, components/labor/labor-capacity-nav.tsx.
Verify: open a project > Labor Capacity (/projects/[projectId]/labor-capacity).', 'Pantalla Capacidad Laboral y Matriz de Habilidades', 'La Matriz de Capacidad Laboral y Habilidades monitorea brechas de mano de obra, utilización y preparación de cuadrillas por oficio y semana, en /projects/[projectId]/labor-capacity (pestaña "Matriz de Capacidad" de la navegación laboral, junto a Lookahead, Frente de Obra y una pestaña Varianza — nota: la pestaña Varianza enlaza a /labor-capacity/variance, una ruta sin página en el código). La página de servidor (force-dynamic, solo lectura) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y ejecuta el motor determinista `computeLaborCapacity`; esta pantalla no escribe nada. El cliente muestra cuatro tarjetas de resumen (oficios totales, semanas con déficit, oficios críticos, utilización máxima), un panel narrativo determinista construido con `buildCapacitySummary` (severidad general, cifras clave, riesgos en cascada) y filtros por oficio, semana, hito, ubicación y solo-ruta-crítica. La tabla principal lista las brechas semanales por oficio: personal y horas requeridas vs disponibles, brecha, utilización, nivel de riesgo y marca de ruta crítica; al expandir una fila se ve la explicación de la brecha (`buildGapExplanation`) y las actividades y recursos relacionados. Un panel de habilidades describe los recursos según la taxonomía de oficios (tipo, nivel, restricciones, ventanas de disponibilidad). Si no hay datos laborales aparece un estado vacío. Pantallas relacionadas: screen-labor-capacity-lookahead, screen-labor-capacity-workface, screen-living-graph (superposición de capacidad laboral), screen-resource-capacity.
Fuente: labor-capacity/page.tsx, labor-capacity-client.tsx, components/labor/labor-capacity-nav.tsx.
Verifica: abre un proyecto > Capacidad Laboral (/projects/[projectId]/labor-capacity).', '/projects/[projectId]/labor-capacity — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/labor-capacity-client.tsx, src/components/labor/labor-capacity-nav.tsx, src/lib/labor/capacity.ts, src/lib/labor/explanation.ts'),
    ('screen-landing', 'Landing screen', 'The public marketing page of ProjectOps360°, served at `/landing` outside the authenticated app shell and outside the `[locale]` route group. You reach it by clicking the URL directly, or automatically: the middleware redirects any anonymous visitor hitting the site root (`/` or `/es`) to `/landing` rather than to the login form. It is a server component that awaits `connection()` and then composes Hero (landing nav bar, background image, animated hero graph, logos strip), Capabilities, Methodology, Industries, Comms, AiSection, Quote, About, Pricing, FinalCta and Footer; the layout wraps everything in `LandingI18nProvider` and mounts `LandingPwaInstallPrompt`, the app-install banner. Pricing is no longer hardcoded: `getPublicPricingPlans()` reads the active rows of the `plans` table (`plan_code`, `name`, `price_monthly`, `price_yearly`, `currency`, `is_enterprise`, `sort_order`) with the admin client, joins the plan capability catalog, and the client renders each card, formats the price with `Intl.NumberFormat`, marks `business` as most popular and points every CTA at `signup?plan=<planCode>`. This is the only Supabase read on the page; nothing is written and no server action runs. Copy uses react-i18next deliberately, not next-intl, and `useAuthPaths` mirrors that choice onto the app routes, so a Spanish visitor is sent to `/es/login` and `/es/signup`. Related: screen-login, screen-signup, screen-org-plans.
Source: src/app/landing/{page,layout}.tsx, src/components/landing/*, src/lib/billing/public-plans.ts, src/middleware.ts.
Verify: open the site root while logged out — you land on /landing; scroll to Pricing and check the plans match the `plans` table.', 'Pantalla Landing', 'La página pública de marketing de ProjectOps360°, servida en `/landing` fuera del shell autenticado y fuera del grupo de rutas `[locale]`. Se llega escribiendo la URL o de forma automática: el middleware redirige a `/landing` a cualquier visitante anónimo que entre a la raíz del sitio (`/` o `/es`), en lugar de mandarlo al formulario de inicio de sesión. Es un componente de servidor que espera `connection()` y luego compone Hero (barra de navegación, imagen de fondo, grafo animado y franja de logotipos), Capabilities, Methodology, Industries, Comms, AiSection, Quote, About, Pricing, FinalCta y Footer; el layout envuelve todo en `LandingI18nProvider` y monta `LandingPwaInstallPrompt`, el banner de instalación de la aplicación. Los precios ya no están escritos en el código: `getPublicPricingPlans()` lee con el cliente admin las filas activas de la tabla `plans` (`plan_code`, `name`, `price_monthly`, `price_yearly`, `currency`, `is_enterprise`, `sort_order`), las cruza con el catálogo de capacidades por plan, y el cliente dibuja cada tarjeta, formatea el importe con `Intl.NumberFormat`, marca `business` como el más popular y dirige cada CTA a `signup?plan=<planCode>`. Esa es la única lectura de Supabase de la página; no escribe nada ni ejecuta server actions. Los textos usan react-i18next a propósito, no next-intl, y `useAuthPaths` traslada esa elección a las rutas de la aplicación, de modo que un visitante en español va a `/es/login` y `/es/signup`. Relacionadas: screen-login, screen-signup, screen-org-plans.
Fuente: src/app/landing/{page,layout}.tsx, src/components/landing/*, src/lib/billing/public-plans.ts, src/middleware.ts.
Verifica: abre la raíz del sitio sin sesión iniciada — caes en /landing; baja hasta Precios y comprueba que los planes coinciden con la tabla `plans`.', '/landing — generated from: src/app/landing/page.tsx, src/app/landing/layout.tsx, src/components/landing/hero.tsx, src/components/landing/nav.tsx, src/components/landing/pricing.tsx, src/components/landing/capabilities.tsx, src/components/landing/auth-links.ts, src/lib/billing/public-plans.ts, src/lib/billing/config.ts, src/middleware.ts'),
    ('screen-living-graph-realtime', 'Realtime Living Graph screen', 'The Realtime Living Graph ("Project Execution Map — Realtime") is a full-viewport, live, hierarchy-safe execution graph reached via the "Realtime" link in the Living Graph header. The server page (force-dynamic) loads an initial snapshot through `loadRealtimeGraphSnapshot`, which reads from the canonical owners (never `process_nodes` or raw events) and fails closed — if the project is not in the caller''s organization it renders notFound. The client (RealtimeLivingGraph) narrows the graph NotebookLM-style: you start from a milestone-scoped root and click nodes to reveal the next level; "Expand all" stays scoped to the selected milestone/task, with a large-graph guard that asks you to arm the action before expanding big scopes. It includes milestone focus, a node inspector, a sync bar, and evidence/dependency overlays. Delivery combines a live Supabase channel push with a polling fallback: the client polls a cheap content signature via `getRealtimeGraphSignatureAction` and refetches the approved snapshot delta via `getRealtimeGraphSnapshotAction` only when the signature changes. The screen never mutates canonical data. A header link returns to the Living Graph. Related screens: screen-living-graph, screen-execution-map.
Source: realtime/page.tsx, realtime/actions.ts, components/living-graph-realtime/realtime-living-graph.tsx.
Verify: Living Graph > "Realtime" button (/projects/[projectId]/execution-map/realtime).', 'Pantalla Grafo Vivo en Tiempo Real', 'El Grafo Vivo en Tiempo Real ("Mapa de Ejecución del Proyecto — Tiempo Real") es un grafo de ejecución en vivo, jerárquicamente seguro y a pantalla completa, accesible con el enlace "Tiempo real" del encabezado del Grafo Vivo. La página de servidor (force-dynamic) carga un snapshot inicial con `loadRealtimeGraphSnapshot`, que lee de los dueños canónicos (nunca de `process_nodes` ni de eventos crudos) y falla de forma cerrada: si el proyecto no pertenece a la organización del usuario, muestra notFound. El cliente (RealtimeLivingGraph) estrecha el grafo al estilo NotebookLM: se parte de una raíz por hito y se hace clic en los nodos para revelar el siguiente nivel; "Expandir todo" queda acotado al hito o tarea seleccionados, con una salvaguarda para grafos grandes que pide confirmación antes de expandir ámbitos extensos. Incluye enfoque por hito, un inspector de nodos, una barra de sincronización y superposiciones de evidencia y dependencias. La entrega combina un canal en vivo de Supabase con un respaldo por sondeo: el cliente consulta una firma de contenido barata con `getRealtimeGraphSignatureAction` y solo cuando cambia recarga el delta aprobado con `getRealtimeGraphSnapshotAction`. La pantalla nunca muta datos canónicos. Un enlace regresa al Grafo Vivo. Pantallas relacionadas: screen-living-graph, screen-execution-map.
Fuente: realtime/page.tsx, realtime/actions.ts, components/living-graph-realtime/realtime-living-graph.tsx.
Verifica: Grafo Vivo > botón "Tiempo real" (/projects/[projectId]/execution-map/realtime).', '/projects/[projectId]/execution-map/realtime — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/realtime/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/realtime/actions.ts, src/lib/living-graph-realtime-ui/load-snapshot.ts, src/components/living-graph-realtime/realtime-living-graph.tsx'),
    ('screen-living-graph', 'Living Graph screen', 'The Living Graph is the project''s visual digital twin (ADR-002), reached from the Execution Map''s "Living Graph" tab button. The server page validates org ownership, removes orphan nodes (`removeOrphanGraphNodes`), then loads `process_nodes`, `process_edges`, `milestones`, `roadmap_tasks`, `task_subtasks`, `project_task_attachments`, the `get_process_timeline` RPC, the labor tables (`labor_resources`, `construction_activities`, `activity_dependencies`, `trade_taxonomy`), and — new — `budget_items` and `resources` for cost. Node labels come from the canonical owners, and a completed task is never shown blocked (REG-008). Server engines: labor capacity, lookahead, labor and productivity variance, variance-cause classification and `computeResourceCapacity`. Beyond the classic operational views it now offers five view levels — milestones, activities, events (a read-only canonical-event projection with an explicit disabled/empty/ready/error/truncated banner), knowledge and trust (the Trust lens is off unless its flag is set) — plus the overlays normal, bottleneck, criticalPath, rework, traceabilityGap, risk, sopCandidate, blocker, timeline, simulation, laborCapacity, readiness, variance and workforceCapacity. At the milestones level, `computeMilestoneCostRollups` derives per-milestone hours, duration, budget and spend; a metrics picker chooses which of those appear on a card (default none), and right-clicking a milestone card pins up to eight KPIs into `milestone_kpi_pins` — the same pins the Gantt shows. Layouts persist in localStorage; Recalculate re-runs `backfill_living_graph`. Related: screen-execution-map, screen-execution-map-kpis, screen-living-graph-realtime, screen-milestone-flow.
Source: living-graph/page.tsx, components/graph/living-graph-view.tsx, lib/roadmap/milestone-cost-rollup.ts, lib/kpi/milestone-pin-actions.ts.
Verify: Execution Map > "Living Graph" (/projects/[projectId]/execution-map/living-graph), then right-click a milestone card.', 'Pantalla Grafo Vivo', 'El Grafo Vivo es el gemelo digital visual del proyecto (ADR-002) y se abre desde el botón "Grafo Vivo" del Mapa de Ejecución. La página de servidor valida la pertenencia a la organización, elimina nodos huérfanos (`removeOrphanGraphNodes`) y carga `process_nodes`, `process_edges`, `milestones`, `roadmap_tasks`, `task_subtasks`, `project_task_attachments`, el RPC `get_process_timeline`, las tablas laborales (`labor_resources`, `construction_activities`, `activity_dependencies`, `trade_taxonomy`) y —novedad— `budget_items` y `resources` para el coste. Las etiquetas provienen de los dueños canónicos y una tarea completada nunca aparece bloqueada (REG-008). Motores en servidor: capacidad laboral, lookahead, varianza laboral y de productividad, clasificación de causas y `computeResourceCapacity`. Además de las vistas operativas clásicas hay cinco niveles: hitos, actividades, eventos (proyección de solo lectura del registro canónico, con un aviso explícito de estado desactivado/vacío/listo/error/truncado), conocimiento y confianza (esta última desactivada salvo que su bandera esté activa), junto con las superposiciones normal, cuello de botella, ruta crítica, retrabajo, brecha de trazabilidad, riesgo, candidato a SOP, bloqueos, línea de tiempo, simulación, capacidad laboral, preparación, varianza y capacidad de fuerza de trabajo. En el nivel de hitos, `computeMilestoneCostRollups` calcula horas, duración, presupuesto y gasto por hito; un selector decide cuáles aparecen en la tarjeta (por omisión, ninguno) y el clic derecho sobre una tarjeta fija hasta ocho KPIs en `milestone_kpi_pins`, los mismos que muestra el Cronograma. Los diseños se guardan en localStorage; Recalcular reejecuta `backfill_living_graph`. Relacionadas: screen-execution-map, screen-execution-map-kpis, screen-living-graph-realtime, screen-milestone-flow.
Fuente: living-graph/page.tsx, components/graph/living-graph-view.tsx, lib/roadmap/milestone-cost-rollup.ts, lib/kpi/milestone-pin-actions.ts.
Verifica: Mapa de Ejecución > "Grafo Vivo" (/projects/[projectId]/execution-map/living-graph) y haz clic derecho en la tarjeta de un hito.', '/projects/[projectId]/execution-map/living-graph — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/living-graph/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/living-graph/actions.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-toolbar.tsx, src/components/graph/milestone-kpi-context-menu.tsx, src/components/graph/milestone-metrics-picker.tsx, src/lib/roadmap/living-graph-sync.ts, src/lib/roadmap/milestone-cost-rollup.ts, src/lib/kpi/load-milestone-kpis.ts, src/lib/kpi/milestone-pin-actions.ts'),
    ('screen-login', 'Login screen', 'The sign-in screen at `/login`, inside the `(auth)` route group whose layout centers a card under the ProjectOps360° wordmark. You reach it from the landing CTAs, from the signup screen''s "already have an account" link, or by redirect — `logoutAction` sends you here, and `/auth/callback` redirects here with `?authError=confirmation_failed` or `?authError=recovery_link_invalid` when an email link fails. The server page passes those two values to `LoginForm` as an amber notice banner. The form (next-intl namespace `auth.login`) has email and password (minimum 6 characters), a "Forgot password?" link to `/forgot-password`, a submit button, and a link to `/signup`. Submitting calls `loginAction`, which runs `supabase.auth.signInWithPassword` and on success redirects to `/`; the app layout then intercepts anyone whose `user_metadata.must_change_password` is true and sends them to `/change-password`. Failures map Supabase messages to localized text for invalid credentials, unconfirmed email, or an unexpected error, shown in a red banner. No database table is read or written — the screen only talks to Supabase Auth. Related: screen-signup, screen-change-password, screen-home-dashboard.
Source: (auth)/login/page.tsx, components/auth/login-form.tsx, (auth)/actions.ts.
Verify: open /login logged out, sign in, and confirm the redirect to the home dashboard.', 'Pantalla Inicio de sesión', 'La pantalla de acceso en `/login`, dentro del grupo de rutas `(auth)`, cuyo layout centra una tarjeta bajo el logotipo de ProjectOps360°. Se llega desde los CTA de la landing, desde el enlace "ya tengo cuenta" del registro o por redirección: `logoutAction` te trae aquí y `/auth/callback` redirige con `?authError=confirmation_failed` o `?authError=recovery_link_invalid` cuando falla un enlace de correo. La página de servidor pasa esos dos valores a `LoginForm` como un aviso ámbar. El formulario (namespace `auth.login` de next-intl) tiene correo y contraseña (mínimo 6 caracteres), un enlace "¿Olvidaste tu contraseña?" hacia `/forgot-password`, un botón de envío y un enlace a `/signup`. Al enviar se llama `loginAction`, que ejecuta `supabase.auth.signInWithPassword` y, si funciona, redirige a `/`; después el layout de la aplicación intercepta a quien tenga `user_metadata.must_change_password` en verdadero y lo manda a `/change-password`. Los fallos se traducen a mensajes de credenciales inválidas, correo sin confirmar o error inesperado, en un aviso rojo. No se lee ni escribe ninguna tabla: la pantalla solo habla con Supabase Auth. Relacionadas: screen-signup, screen-change-password, screen-home-dashboard.
Fuente: (auth)/login/page.tsx, components/auth/login-form.tsx, (auth)/actions.ts.
Verifica: abre /login sin sesión, inicia sesión y confirma la redirección al panel de inicio.', '/login — generated from: src/app/[locale]/(auth)/login/page.tsx, src/app/[locale]/(auth)/layout.tsx, src/components/auth/login-form.tsx, src/app/[locale]/(auth)/actions.ts, src/app/auth/callback/route.ts'),
    ('screen-milestone-flow', 'Milestone Process Flow screen', 'The Milestone Process Flow screen shows the engine-derived execution flow between milestones — health, friction and evidence — and is reached from the "Milestone Flow" tab button on the Execution Map. It is a strictly read-only consumer of the Milestone Process Flow (MPF) Engine: the server page calls `loadMilestoneFlowProjection`, which checks the project belongs to the caller''s organization (deny-by-default: an unauthorized request renders a safe denial state with no data), reads `projects`, canonical `milestones`, `project_event_log` events capped at `MAX_EVENTS` = 5,000 and their `project_event_objects` rows, then invokes the deterministic engine. The route derives nothing itself, writes nothing and emits no events; `buildMilestoneFlowViewModel` only formats the projection, and `filterMilestoneFlowTransitions` filters the view-model without mutating it. The view lists milestone-to-milestone transitions with health badges, confidence level, primary reason codes, duration segment bars and finding indicators. Filters are health-status chips with counts, segment type, finding type, severity, and the checkboxes only-uncertainty, only-warnings and only-open-findings. Selecting a transition opens a `TransitionDetailPanel` with its segments and findings. Dedicated panels cover the error and unauthorized states, and a `loading.tsx` skeleton covers the fetch. A header link returns to the Execution Map. Unlike the neighbouring Execution Map tabs, this screen gained no cost, KPI-pin or baseline features. Related: screen-execution-map, screen-living-graph, screen-execution-map-kpis.
Source: milestone-flow/page.tsx, lib/milestone-flow-ui/load-projection.ts, components/milestone-flow/milestone-flow-view.tsx.
Verify: Execution Map > "Milestone Flow" (/projects/[projectId]/execution-map/milestone-flow).', 'Pantalla Flujo entre Hitos', 'La pantalla Flujo entre Hitos muestra el flujo de ejecución entre hitos derivado por motor —salud, fricción y evidencia— y se abre desde el botón "Flujo entre Hitos" del Mapa de Ejecución. Es un consumidor estrictamente de solo lectura del Motor de Flujo de Procesos entre Hitos (MPF): la página de servidor llama a `loadMilestoneFlowProjection`, que comprueba que el proyecto pertenece a la organización del usuario (denegación por defecto: una petición no autorizada muestra un estado seguro sin datos), lee `projects`, los `milestones` canónicos, los eventos de `project_event_log` con un tope de `MAX_EVENTS` = 5.000 y sus filas de `project_event_objects`, y luego invoca el motor determinista. La ruta no deriva nada por sí misma, no escribe ni emite eventos; `buildMilestoneFlowViewModel` solo da formato a la proyección y `filterMilestoneFlowTransitions` filtra el modelo de vista sin mutarlo. La vista lista las transiciones entre hitos con insignias de salud, nivel de confianza, códigos de razón, barras de segmentos de duración e indicadores de hallazgos. Los filtros son chips por estado de salud con conteos, tipo de segmento, tipo de hallazgo, severidad y las casillas solo-incertidumbre, solo-advertencias y solo-hallazgos-abiertos. Al seleccionar una transición se abre un `TransitionDetailPanel` con sus segmentos y hallazgos. Hay paneles propios para error y para acceso no autorizado, y un esqueleto `loading.tsx` durante la carga. Un enlace del encabezado regresa al Mapa de Ejecución. A diferencia de las pestañas vecinas, esta pantalla no incorporó coste, fijado de KPIs ni línea base. Relacionadas: screen-execution-map, screen-living-graph, screen-execution-map-kpis.
Fuente: milestone-flow/page.tsx, lib/milestone-flow-ui/load-projection.ts, components/milestone-flow/milestone-flow-view.tsx.
Verifica: Mapa de Ejecución > "Flujo entre Hitos" (/projects/[projectId]/execution-map/milestone-flow).', '/projects/[projectId]/execution-map/milestone-flow — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/milestone-flow/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/milestone-flow/loading.tsx, src/lib/milestone-flow-ui/load-projection.ts, src/lib/milestone-flow-ui/selectors.ts, src/components/milestone-flow/milestone-flow-view.tsx, src/components/milestone-flow/transition-detail-panel.tsx'),
    ('screen-navigator-preview', 'Navigator preview screen', 'A temporary development-only route. The file comment states it explicitly: "TEMPORARY preview route to visually verify the ProjectOpsNavigatorButton renders and the drawer opens. NOT part of the shipped feature — deleted after verification." The page wraps its content in a `NextIntlClientProvider` (locale "en", empty messages) and renders a faux app header — a ProjectOps360° label, a fake "Search everything… ⌘K" pill, a bell icon, and an avatar — so the real `ProjectOpsNavigatorButton` can be inspected in a realistic context. Clicking the bilingual Navigator button (Compass icon) opens the `ProjectOpsNavigatorDrawer`, the contextual help/guidance drawer whose sibling components include GuidedOnboardingProgress, ModuleGuidanceCard, NavigatorActions, NavigatorChecklist, and ProjectLifecycleMap. The main area only shows a heading and an instruction to click the button. The page reads and writes no Supabase data and calls no server actions. It lives outside the `[locale]` group and outside the authenticated `(app)` shell, so it is reachable without the normal app chrome. Related: the Navigator button itself ships in the real app header.

Source: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx.
Verify: open /navigator-preview and click the Navigator button in the faux header to open the drawer.', 'Pantalla Vista previa del Navigator', 'Una ruta temporal solo para desarrollo. El comentario del archivo lo dice explícitamente: es una "vista previa TEMPORAL para verificar visualmente que el ProjectOpsNavigatorButton se renderiza y el drawer se abre. NO forma parte de la funcionalidad publicada — se elimina tras la verificación". La página envuelve su contenido en un `NextIntlClientProvider` (locale "en", mensajes vacíos) y muestra un encabezado de aplicación ficticio — etiqueta ProjectOps360°, una píldora falsa de búsqueda "⌘K", un icono de campana y un avatar — para inspeccionar el `ProjectOpsNavigatorButton` real en un contexto realista. Al hacer clic en el botón bilingüe Navigator (icono de brújula) se abre el `ProjectOpsNavigatorDrawer`, el panel contextual de ayuda cuyos componentes hermanos incluyen GuidedOnboardingProgress, ModuleGuidanceCard, NavigatorActions, NavigatorChecklist y ProjectLifecycleMap. El área principal solo muestra un título y la instrucción de pulsar el botón. No lee ni escribe datos en Supabase ni llama server actions, y vive fuera del grupo `[locale]` y del shell autenticado `(app)`.

Fuente: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx.
Verifica: abre /navigator-preview y pulsa el botón Navigator del encabezado ficticio para abrir el drawer.', '/navigator-preview — generated from: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx, src/components/navigator/ProjectOpsNavigatorDrawer.tsx'),
    ('screen-org-billing', 'Organization Billing screen', 'The Billing & plan hub at `/organization/billing`, reached from the sidebar bottom navigation (`bottomNav`, credit-card icon). It is fully server-rendered — there is no client component and the page writes nothing. The header shows the organization name plus links to Members, Teams and External contacts; a "Plans & pricing" link appears only when `isPlatformAdmin(org.email)` passes, which now checks the `admin_authorized_users` table and a hardcoded platform owner, **not** the old org-owner fallback. The Current plan card shows the plan label, a status badge (`active`, `trialing`, `past_due`, `canceled`, `suspended`), and either the monthly/yearly price or "Custom contract" for enterprise plans. The "Manage subscription" button is permanently disabled with a "Payment integration coming soon" title — **no Stripe or any payment provider is wired up**. Usage & limits compares six metrics against entitlements via `checkLimit` with progress bars and near/at-limit warnings; be aware that `getUsage` currently hardcodes AI credits and memory storage to `0` (metering is unbuilt), so only billable users, active projects, free viewers and documents indexed are real. Reads go through `getOrgBilling` and `getPlansWithEntitlements` over `subscriptions`, `plans`, `plan_entitlements`, `organization_members`, `projects` and `project_memory_items`. Related: screen-org-members, screen-org-plans, screen-org-teams, screen-org-external-contacts.
Source: organization/billing/page.tsx, lib/billing/service.ts, lib/billing/config.ts.
Verify: click Billing at the bottom of the sidebar, or open /organization/billing.', 'Pantalla Facturación de la organización', 'El centro de Facturación y plan en `/organization/billing`, accesible desde la navegación inferior de la barra lateral (`bottomNav`, icono de tarjeta). Se renderiza por completo en el servidor: no hay componente cliente y la página no escribe nada. El encabezado muestra el nombre de la organización y enlaces a Miembros, Equipos y Contactos externos; el enlace "Planes y precios" solo aparece cuando `isPlatformAdmin(org.email)` lo permite, comprobación que ahora consulta la tabla `admin_authorized_users` y un propietario de plataforma fijo en código, **no** el antiguo respaldo de propietario de organización. La tarjeta de Plan actual muestra la etiqueta del plan, una insignia de estado (`active`, `trialing`, `past_due`, `canceled`, `suspended`) y el precio mensual o anual, o bien "Contrato personalizado" en los planes enterprise. El botón "Gestionar suscripción" está deshabilitado de forma permanente con el aviso "Integración de pagos próximamente": **no hay Stripe ni ningún proveedor de pago conectado**. Uso y límites compara seis métricas con los derechos del plan mediante `checkLimit`, con barras de progreso y avisos de cercanía o tope; conviene saber que `getUsage` fija hoy en `0` los créditos de IA y el almacenamiento de memoria (la medición no está construida), así que solo son reales los usuarios facturables, los proyectos activos, los observadores gratuitos y los documentos indexados. Las lecturas pasan por `getOrgBilling` y `getPlansWithEntitlements` sobre `subscriptions`, `plans`, `plan_entitlements`, `organization_members`, `projects` y `project_memory_items`. Relacionadas: screen-org-members, screen-org-plans, screen-org-teams, screen-org-external-contacts.
Fuente: organization/billing/page.tsx, lib/billing/service.ts, lib/billing/config.ts.
Verifica: pulsa Facturación al final de la barra lateral, o abre /organization/billing.', '/organization/billing — generated from: src/app/[locale]/(app)/organization/billing/page.tsx, src/lib/billing/service.ts, src/lib/billing/config.ts, src/lib/admin-console/access.server.ts, src/config/navigation.ts'),
    ('screen-org-external-contacts', 'External Contacts screen', 'The External Contacts screen manages people who work with the organization without a full login — vendors, clients, inspectors, consultants and sponsors. The header explicitly states they do not consume a billable seat. You reach it from the Billing screen header (Contacts button) at /organization/external-contacts. The screen has two parts: an inline add form (name, email, company, contact type from CONTACT_TYPES in lib/team-roles/config, plus an Add button) and a table listing each contact''s name, type, company, and email/phone, with a trash icon per row. Adding calls createContactAction, which inserts into the external_contacts table (fields include name, email, company_name, contact_type, phone, notes, can_login, access_status "active") and logs the creation with logAudit. Deleting calls deleteContactAction, a soft delete that sets deleted_at. An updateContactAction exists in actions.ts for editing fields and access status, but the current table UI does not expose inline editing — only add and delete. The page loads contacts server-side through getExternalContacts in lib/team-roles/service, scoped to the organization from getOrgContext; actions only require an authenticated org context. Related screens: Teams (external contacts can be added as team members), Members (internal, seat-based users) and Billing.
Source: src/app/[locale]/(app)/organization/external-contacts/{page.tsx,contacts-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verify: open Billing (sidebar) then click Contacts, or go to /organization/external-contacts.', 'Pantalla Contactos externos', 'La pantalla de Contactos externos administra a las personas que trabajan con la organización sin un acceso completo — proveedores, clientes, inspectores, consultores y patrocinadores. El encabezado indica explícitamente que no consumen asiento facturable. Se accede desde el encabezado de Facturación (botón Contactos), en /organization/external-contacts. La pantalla tiene dos partes: un formulario de alta en línea (nombre, correo, empresa, tipo de contacto según CONTACT_TYPES en lib/team-roles/config, y botón Agregar) y una tabla con nombre, tipo, empresa y correo/teléfono de cada contacto, con un icono de papelera por fila. Agregar llama a createContactAction, que inserta en la tabla external_contacts (campos como name, email, company_name, contact_type, phone, notes, can_login y access_status "active") y registra la creación con logAudit. Eliminar llama a deleteContactAction, un borrado suave que escribe deleted_at. Existe una acción updateContactAction en actions.ts para editar campos y estado de acceso, pero la interfaz actual no expone edición en línea — solo agregar y eliminar. La página carga los contactos en el servidor mediante getExternalContacts de lib/team-roles/service, acotados a la organización; las acciones solo requieren contexto autenticado. Pantallas relacionadas: Equipos, Miembros y Facturación.
Fuente: src/app/[locale]/(app)/organization/external-contacts/{page.tsx,contacts-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verifica: abre Facturación en la barra lateral y pulsa Contactos, o navega a /organization/external-contacts.', '/organization/external-contacts — generated from: src/app/[locale]/(app)/organization/external-contacts/page.tsx, src/app/[locale]/(app)/organization/external-contacts/contacts-client.tsx, src/app/[locale]/(app)/organization/external-contacts/actions.ts, src/lib/team-roles/service.ts'),
    ('screen-org-members', 'Organization Members screen', 'The Members & seats screen at `/organization/members`, reached from the Members button in the Billing header. The server page reads `organization_members` for the current org, joins display names from `profiles`, counts active rows in `project_team_members`, and resolves emails through the `admin_get_user_emails` RPC — scoped to this tenant''s user ids so global Auth users are never enumerated. Three stat cards (billable seats, free viewers/external, pending invites) sit above filter chips (All, Billable, Free, Pending) and a table with member, seat, workspace role, status, project count and a Billable badge. Owners and admins (`canManage`) get inline selects that call `updateMemberSeatAction`, a pencil to rename via `renameWorkspaceUserAction`, and two provisioning panels: "Invite by email" (`inviteMemberAction`, a Supabase Auth invite redirecting to `/change-password?invite=1`, status `invited` until accepted — it returns `email_not_configured` when SMTP is not set up) and "Create login with a temporary password" (`createMemberWithPasswordAction`, minimum 12 characters with a generator button; no SMTP needed, the user is forced to change it on first login, and the credentials are shown once with a copy button). Writes are gated to owner/admin and recorded with `logAudit`. Only `owner`, `admin`, `full_seat` and `contributor_seat` are billable. Related: screen-org-billing, screen-team, screen-change-password.
Source: organization/members/{page.tsx,members-client.tsx,actions.ts}, lib/billing/config.ts.
Verify: open Billing from the sidebar, click Members, or go to /organization/members.', 'Pantalla Miembros de la organización', 'La pantalla de Miembros y asientos en `/organization/members`, accesible desde el botón Miembros del encabezado de Facturación. La página de servidor lee `organization_members` de la organización actual, toma los nombres de `profiles`, cuenta las filas activas de `project_team_members` y resuelve los correos con el RPC `admin_get_user_emails`, limitado a los identificadores de este inquilino para no enumerar jamás a los usuarios globales de Auth. Tres tarjetas de resumen (asientos facturables, gratuitos u observadores/externos, invitaciones pendientes) preceden a los filtros (Todos, Facturables, Gratis, Pendientes) y a una tabla con miembro, asiento, rol de workspace, estado, número de proyectos e insignia de facturable. Propietarios y administradores (`canManage`) disponen de selectores en línea que llaman a `updateMemberSeatAction`, un lápiz para renombrar con `renameWorkspaceUserAction` y dos paneles de alta: "Invitar por correo" (`inviteMemberAction`, invitación de Supabase Auth que redirige a `/change-password?invite=1`, con estado `invited` hasta aceptarse; devuelve `email_not_configured` cuando no hay SMTP configurado) y "Crear acceso con clave temporal" (`createMemberWithPasswordAction`, mínimo 12 caracteres con botón generador; no necesita SMTP, obliga a cambiar la clave en el primer ingreso y muestra las credenciales una sola vez con botón de copiar). Las escrituras se restringen a propietario o administrador y quedan registradas con `logAudit`. Solo `owner`, `admin`, `full_seat` y `contributor_seat` son facturables. Relacionadas: screen-org-billing, screen-team, screen-change-password.
Fuente: organization/members/{page.tsx,members-client.tsx,actions.ts}, lib/billing/config.ts.
Verifica: abre Facturación desde la barra lateral, pulsa Miembros o navega a /organization/members.', '/organization/members — generated from: src/app/[locale]/(app)/organization/members/page.tsx, src/app/[locale]/(app)/organization/members/members-client.tsx, src/app/[locale]/(app)/organization/members/actions.ts, src/lib/billing/config.ts, src/lib/auth/email-redirects.server.ts'),
    ('screen-org-plans', 'Plans & Pricing admin screen', 'A platform-admin-only editor for the global subscription plans (Personal, Team, Business/PMO, Enterprise) at `/organization/plans`, reached from the "Plans & pricing" button on Billing. The page awaits `isPlatformAdmin(org.email)` from `lib/admin-console/access.server` — the `admin_authorized_users` table plus a hardcoded platform owner, **not** the deprecated org-owner fallback — and calls `notFound()` for everyone else. The intro warns that plans are global: an edit changes every organization, including the public landing pricing. Each plan is a card with pricing/metadata (name, currency, monthly and yearly price, description, Active checkbox), numeric `LIMIT_FIELDS` where an empty box means unlimited, and `FEATURE_FIELDS` checkboxes. For enterprise plans the two price inputs are replaced by a read-only "Contact Sales" field. Below them a read-only "Intelligence capabilities" matrix, loaded by `getPlanCapabilityCatalog` from the `plan_capabilities` table and grouped by the tier that first includes each capability, documents what the plan unlocks — it is display-only and not editable here. One Save button per card calls `updatePlanAction` (updates `plans`) then `updateEntitlementsAction` (updates or inserts the `plan_entitlements` row); both re-check the platform-admin gate and write `logAudit` entries. Editing prices here changes nothing in any payment provider — no billing integration exists. Related: screen-org-billing, screen-org-members.
Source: organization/plans/{page.tsx,plans-admin-client.tsx,actions.ts}, lib/billing/service.ts, lib/billing/plan-capabilities.server.ts.
Verify: as a platform admin open Billing and click Plans & pricing, or go to /organization/plans (anyone else gets a 404).', 'Pantalla Planes y precios (administración)', 'Un editor exclusivo para administradores de plataforma de los planes globales de suscripción (Personal, Team, Business/PMO, Enterprise) en `/organization/plans`, accesible desde el botón "Planes y precios" de Facturación. La página espera a `isPlatformAdmin(org.email)` de `lib/admin-console/access.server` —la tabla `admin_authorized_users` más un propietario de plataforma fijo en código, **no** el respaldo obsoleto de propietario de organización— y devuelve `notFound()` a cualquier otro usuario. La introducción advierte que los planes son globales: una edición afecta a todas las organizaciones, incluida la tarifa pública de la landing. Cada plan es una tarjeta con precios y metadatos (nombre, moneda, precio mensual y anual, descripción, casilla de Activo), los campos numéricos de `LIMIT_FIELDS` donde vacío significa ilimitado, y las casillas de `FEATURE_FIELDS`. En los planes enterprise los dos campos de precio se sustituyen por un campo de solo lectura "Contactar ventas". Debajo, una matriz de solo lectura de "Capacidades de inteligencia", cargada por `getPlanCapabilityCatalog` desde la tabla `plan_capabilities` y agrupada por el nivel a partir del cual se incluye cada capacidad, documenta lo que desbloquea el plan; es informativa y no se edita aquí. Un botón Guardar por tarjeta llama a `updatePlanAction` (actualiza `plans`) y luego a `updateEntitlementsAction` (actualiza o inserta la fila de `plan_entitlements`); ambas vuelven a comprobar la autorización y registran auditoría con `logAudit`. Cambiar precios aquí no modifica nada en ningún proveedor de pago: no existe integración de facturación. Relacionadas: screen-org-billing, screen-org-members.
Fuente: organization/plans/{page.tsx,plans-admin-client.tsx,actions.ts}, lib/billing/service.ts, lib/billing/plan-capabilities.server.ts.
Verifica: como administrador de plataforma abre Facturación y pulsa Planes y precios, o navega a /organization/plans (los demás reciben un 404).', '/organization/plans — generated from: src/app/[locale]/(app)/organization/plans/page.tsx, src/app/[locale]/(app)/organization/plans/plans-admin-client.tsx, src/app/[locale]/(app)/organization/plans/actions.ts, src/lib/billing/service.ts, src/lib/billing/plan-capabilities.server.ts, src/lib/billing/config.ts, src/lib/admin-console/access.server.ts'),
    ('screen-org-teams', 'Organization Teams screen', 'The Teams & Groups screen manages reusable company teams (development, QA, steering committee, etc.) that can later be added wholesale to a project. You reach it from the Billing screen header (Teams button) at /organization/teams. At the top, a create panel takes a team name and a team type (from TEAM_TYPES in lib/team-roles/config) and calls createTeamAction. Below, each team renders as a card showing its name, type badge and member count, with a Member button that opens an add-member form and a trash button that soft-deletes the team (deleteTeamAction sets deleted_at). The add form lets you pick either an internal user (from the company directory) or an external contact — mutually exclusive — plus an optional default project role (datalist fed by PROJECT_ROLES), then calls addTeamMemberAction; members appear as removable chips (removeTeamMemberAction). Server-side, the page loads data through lib/team-roles/service: getCompanyTeams (organization_teams and organization_team_members tables), getCompanyDirectory (organization_members + profiles) and getExternalContacts (external_contacts). Actions require an authenticated org context (getOrgContext) and audit team creation via logAudit; there is no explicit owner/admin gate in these actions beyond authentication. Related screens: Members (internal users), External Contacts (people without login), Billing, and the project Team & Roles page where teams get applied.
Source: src/app/[locale]/(app)/organization/teams/{page.tsx,teams-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verify: open Billing (sidebar) then click Teams, or go to /organization/teams.', 'Pantalla Equipos de la organización', 'La pantalla de Equipos y grupos administra equipos reutilizables de la empresa (desarrollo, QA, comité directivo, etc.) que luego pueden agregarse completos a un proyecto. Se accede desde el encabezado de Facturación (botón Equipos), en /organization/teams. Arriba, un panel de creación pide nombre y tipo de equipo (TEAM_TYPES en lib/team-roles/config) y llama a createTeamAction. Debajo, cada equipo se muestra como una tarjeta con nombre, insignia de tipo y número de miembros, con un botón Miembro que abre el formulario para agregar personas y un botón de papelera que hace borrado suave (deleteTeamAction escribe deleted_at). El formulario permite elegir un usuario interno (del directorio de la empresa) o un contacto externo — excluyentes entre sí — más un rol de proyecto por defecto opcional (lista PROJECT_ROLES), y llama a addTeamMemberAction; los miembros aparecen como fichas removibles (removeTeamMemberAction). En el servidor, la página carga datos con lib/team-roles/service: getCompanyTeams (tablas organization_teams y organization_team_members), getCompanyDirectory (organization_members + profiles) y getExternalContacts (external_contacts). Las acciones requieren contexto de organización autenticado y auditan la creación con logAudit; no hay un control adicional de propietario/administrador más allá de la autenticación. Pantallas relacionadas: Miembros, Contactos externos, Facturación y la página de Equipo y roles del proyecto.
Fuente: src/app/[locale]/(app)/organization/teams/{page.tsx,teams-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verifica: abre Facturación en la barra lateral y pulsa Equipos, o navega a /organization/teams.', '/organization/teams — generated from: src/app/[locale]/(app)/organization/teams/page.tsx, src/app/[locale]/(app)/organization/teams/teams-client.tsx, src/app/[locale]/(app)/organization/teams/actions.ts, src/lib/team-roles/service.ts, src/lib/team-roles/config.ts'),
    ('screen-phase0', 'Phase 0 control screen', 'An internal builder checklist at `/phase0`, not an end-user project feature. It tracks the "Phase 0" plan for building ProjectOps360° itself: the task list is a static array in `src/data/phase0-tasks.ts` (tasks like "Confirm solo-builder scope", "Create single source of truth folder", "Define Definition of Done"), each with category, goal, priority, hour estimate, sprint, dependencies, acceptance criteria, an AI prompt, and a deliverable. The `Phase0ControlClient` renders a page header, a `ProgressHeader` showing completed/total counts, the last-saved timestamp and a reset button, then tasks grouped by category as `TaskCard`s where the user can change each task''s status and write notes. Crucially, all state is local: `usePhase0Progress` persists status overrides and notes in browser localStorage — nothing is read from or written to Supabase, and there are no server actions, so progress is per-browser and per-device. The screen is bilingual through the next-intl `phase0Control` namespace. Because the data ships in the bundle, every user sees the same task definitions; only their own local status/notes differ. Related screens: none functionally — this is a standalone internal tracking surface, distinct from project workboards and the Product Intelligence cockpit.

Source: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts.
Verify: navigate to /phase0 in the app; change a task status and reload — the change persists via localStorage.', 'Pantalla Control de Fase 0', 'Una lista de control interna del constructor en `/phase0`; no es una funcionalidad de proyectos para usuarios finales. Da seguimiento al plan "Fase 0" de la construcción del propio ProjectOps360°: la lista de tareas es un arreglo estático en `src/data/phase0-tasks.ts` (tareas como "Confirmar alcance de constructor en solitario" o "Definir Definition of Done"), cada una con categoría, objetivo, prioridad, estimación en horas, sprint, dependencias, criterios de aceptación, un prompt de IA y un entregable. El `Phase0ControlClient` muestra un encabezado, un `ProgressHeader` con el conteo de completadas/total, la marca de último guardado y un botón de reinicio, y luego las tareas agrupadas por categoría como `TaskCard`, donde se puede cambiar el estado de cada tarea y escribir notas. Lo importante: todo el estado es local — `usePhase0Progress` persiste estados y notas en localStorage del navegador; no se lee ni escribe nada en Supabase y no hay server actions, así que el progreso es por navegador y dispositivo. Es bilingüe mediante el namespace `phase0Control` de next-intl. Como los datos van en el bundle, todos ven las mismas tareas; solo difieren estados y notas locales. Pantallas relacionadas: ninguna funcionalmente — es una superficie interna independiente.

Fuente: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts.
Verifica: navega a /phase0, cambia el estado de una tarea y recarga — el cambio persiste vía localStorage.', '/phase0 — generated from: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts, src/hooks/use-phase0-progress.ts'),
    ('screen-pmo-living-graph', 'PMO Intelligence Center (Dashboard 3 — PMO Living Graph) screen', 'The portfolio-wide graph dashboard. Two gates decide whether you see it at all. `canAccessPmoLivingGraph` requires the `PMO_LIVING_GRAPH_ENABLED` flag — **default OFF** — *and* an `owner`/`admin` role; anyone else gets `notFound()`, so with the flag off the route simply does not exist. Separately, `SINGLE_DASHBOARD_MODE` (also default OFF) promotes this dashboard to `/`: when it is on, `/pmo-living-graph` **redirects to the root** rather than rendering, keeping bookmarks and Isabella deep links alive, while `/process-intelligence` retires and the dashboard switcher disappears. Both routes render one implementation, `PmoIntelligenceCenterView`, so they cannot drift.

That component reads the URL scope on the server (organization always from the session, never the query string), calls `loadPmoIntelligence` and flattens it with `toDashboardSlice`. Per ADR-012 it composes and never computes. The read model queries `organizations`, `projects`, `milestones`, `roadmap_tasks`, `task_subtasks`, `risks`, `decisions`, `resources`, `stakeholders`, `kpi_definitions`, `budget_items`, `task_dependencies`, `traceability_links` and `project_resource_allocations`. It distinguishes "unavailable" (a query failed) from "empty" and shows different screens for each. The toolbar has search, node-kind filters, a confidence-floor slider, Focus mode, Find path, blast radius at 1–3 hops, and Save/Reset layout, which persist positions per organization and user. Read-only by contract; insight feedback writes to `audit_logs`. Related: screen-process-intelligence, screen-living-graph.
Source: src/app/[locale]/(app)/pmo-living-graph/page.tsx, src/components/pmo-living-graph/intelligence-center-view.tsx, src/lib/pmo-living-graph/{flags,single-dashboard}.ts.
Verify: as an owner/admin with `PMO_LIVING_GRAPH_ENABLED=true` and single-dashboard mode off, open /pmo-living-graph and double-click a project node.', 'Pantalla PMO Intelligence Center (Dashboard 3 — PMO Living Graph)', 'El tablero de grafo a nivel de portafolio. Dos controles deciden si llegas a verlo. `canAccessPmoLivingGraph` exige la bandera `PMO_LIVING_GRAPH_ENABLED` —**apagada por omisión**— *y* un rol `owner`/`admin`; cualquier otro recibe `notFound()`, así que con la bandera apagada la ruta sencillamente no existe. Aparte, `SINGLE_DASHBOARD_MODE` (también apagada por omisión) promueve este tablero a `/`: cuando está encendida, `/pmo-living-graph` **redirige a la raíz** en lugar de renderizar, con lo que sobreviven los marcadores y los enlaces profundos de Isabella, mientras `/process-intelligence` se retira y desaparece el conmutador de tableros. Ambas rutas renderizan una sola implementación, `PmoIntelligenceCenterView`, para que no puedan divergir.

Ese componente lee el alcance desde la URL en el servidor (la organización siempre viene de la sesión, nunca de la cadena de consulta), llama a `loadPmoIntelligence` y lo aplana con `toDashboardSlice`. Según ADR-012 compone y nunca calcula. El modelo de lectura consulta `organizations`, `projects`, `milestones`, `roadmap_tasks`, `task_subtasks`, `risks`, `decisions`, `resources`, `stakeholders`, `kpi_definitions`, `budget_items`, `task_dependencies`, `traceability_links` y `project_resource_allocations`. Distingue "no disponible" (una consulta falló) de "vacío" y muestra pantallas distintas para cada caso. La barra de herramientas tiene búsqueda, filtros por tipo de nodo, un deslizador de confianza mínima, modo Enfoque, Buscar ruta, radio de impacto de 1 a 3 saltos y Guardar/Restablecer diseño, que persisten las posiciones por organización y usuario. Es de solo lectura por contrato; la retroalimentación sobre hallazgos se escribe en `audit_logs`. Relacionadas: screen-process-intelligence, screen-living-graph.
Fuente: src/app/[locale]/(app)/pmo-living-graph/page.tsx, src/components/pmo-living-graph/intelligence-center-view.tsx, src/lib/pmo-living-graph/{flags,single-dashboard}.ts.
Verifica: como owner/admin con `PMO_LIVING_GRAPH_ENABLED=true` y el modo de tablero único apagado, abre /pmo-living-graph y haz doble clic en un nodo de proyecto.', '/pmo-living-graph — generated from: src/app/[locale]/(app)/pmo-living-graph/page.tsx, src/components/pmo-living-graph/intelligence-center-view.tsx, src/components/pmo-living-graph/portfolio-graph-shell.tsx, src/components/pmo-living-graph/graph-toolbar.tsx, src/lib/pmo-living-graph/flags.ts, src/lib/pmo-living-graph/single-dashboard.ts, src/lib/pmo-living-graph/read-model.server.ts, src/lib/pmo-intelligence/read-model.server.ts, src/lib/pmo-intelligence/dashboard-model.ts'),
    ('screen-process-intelligence', 'Process Intelligence Command Center screen', 'Do not confuse this with `/product-intelligence` (screen-product-intelligence), which is a repo-documentation cockpit. **Process** Intelligence is Dashboard 2: portfolio process analytics mined from the Project Event Graph.

It is doubly gated and often dark. `canAccessProcessIntelligence` requires the `PMO_PROCESS_INTELLIGENCE_DASHBOARD_ENABLED` flag — **default OFF** — plus an `owner`/`admin` role. On top of that, `isProcessIntelligenceRouteRetired()` makes the route `notFound()` for everyone whenever `SINGLE_DASHBOARD_MODE` is on; that check runs first, before the org read. The module behind it stays alive either way, because Dashboard 3 composes the same read models.

When it does render, the server calls `loadPmoPiFlowModel` (organization scope, and a second focused call when `?project=` is set), `loadPmoPiFinanceOverlay`, `loadPmoPiOverlays`, `loadPmoPiHierarchy` and `buildInsights`, then hands them to `CommandCenterShell`. Reads cover `project_event_log`, `projects`, `project_charters`, `stakeholders`, `risks`, `task_dependencies`, `milestones` and `roadmap_tasks`. Seven overlays switch the same canvas: Process, Risk, Finance, Resources, Dependencies, Benefits and What-if, selectable via `?overlay=`. A technical event explorer, a realtime refresh and an Isabella insights panel are included; insight feedback goes through `recordInsightFeedbackAction`. An unauthorized or foreign `?project=` is treated as no access. Related: screen-pmo-living-graph, screen-product-intelligence.
Source: src/app/[locale]/(app)/process-intelligence/page.tsx, src/components/pmo-process-intelligence/command-center-shell.tsx, src/lib/pmo-process-intelligence/flags.ts.
Verify: as an owner/admin with the flag on and single-dashboard mode off, open /process-intelligence and switch the overlay to Finance.', 'Pantalla Centro de Mando de Inteligencia de Procesos', 'No la confundas con `/product-intelligence` (screen-product-intelligence), que es una cabina sobre la documentación del repositorio. Inteligencia **de Procesos** es el Dashboard 2: analítica de procesos del portafolio, minada del Project Event Graph.

Tiene doble control y a menudo está apagada. `canAccessProcessIntelligence` exige la bandera `PMO_PROCESS_INTELLIGENCE_DASHBOARD_ENABLED` —**apagada por omisión**— más un rol `owner`/`admin`. Además, `isProcessIntelligenceRouteRetired()` hace que la ruta devuelva `notFound()` para todos cuando `SINGLE_DASHBOARD_MODE` está encendido; esa comprobación ocurre primero, antes de leer el contexto de la organización. El módulo subyacente sigue vivo en cualquier caso, porque el Dashboard 3 compone los mismos modelos de lectura.

Cuando sí se renderiza, el servidor llama a `loadPmoPiFlowModel` (alcance de organización, y una segunda llamada enfocada si viene `?project=`), `loadPmoPiFinanceOverlay`, `loadPmoPiOverlays`, `loadPmoPiHierarchy` y `buildInsights`, y se los pasa a `CommandCenterShell`. Las lecturas abarcan `project_event_log`, `projects`, `project_charters`, `stakeholders`, `risks`, `task_dependencies`, `milestones` y `roadmap_tasks`. Siete capas reproyectan el mismo lienzo: Proceso, Riesgo, Finanzas, Recursos, Dependencias, Beneficios y What-if, seleccionables con `?overlay=`. Incluye un explorador técnico de eventos, refresco en tiempo real y un panel de hallazgos de Isabella; la retroalimentación pasa por `recordInsightFeedbackAction`. Un `?project=` ajeno o no autorizado se trata como falta de acceso. Relacionadas: screen-pmo-living-graph, screen-product-intelligence.
Fuente: src/app/[locale]/(app)/process-intelligence/page.tsx, src/components/pmo-process-intelligence/command-center-shell.tsx, src/lib/pmo-process-intelligence/flags.ts.
Verifica: como owner/admin con la bandera encendida y el modo de tablero único apagado, abre /process-intelligence y cambia la capa a Finanzas.', '/process-intelligence — generated from: src/app/[locale]/(app)/process-intelligence/page.tsx, src/app/[locale]/(app)/process-intelligence/actions.ts, src/components/pmo-process-intelligence/command-center-shell.tsx, src/lib/pmo-process-intelligence/flags.ts, src/lib/pmo-process-intelligence/read-model.server.ts, src/lib/pmo-process-intelligence/overlays-read.server.ts, src/lib/pmo-process-intelligence/hierarchy-read.server.ts, src/lib/pmo-process-intelligence/insights.ts, src/lib/pmo-living-graph/single-dashboard.ts'),
    ('screen-product-intelligence', 'Product Intelligence (Product Brain Control Center) screen', 'Do not confuse this with `/process-intelligence` (screen-process-intelligence). **Product** Intelligence is an internal governance cockpit about *the product''s own documentation* — decisions, regressions, UX contracts — and reads no tenant data at all. **Process** Intelligence is a portfolio analytics dashboard over the Project Event Graph. Different audiences, different gates, different data.

Access to `/product-intelligence` is enforced server-side by a strict email allowlist, `isProductBrainAllowedEmail` (TASK 10A). A non-allowlisted user gets `notFound()`, so the route''s existence is never revealed and no Product Brain data is loaded or serialized. For allowed users the server calls `getAllProductBrainDocs()` — the curated markdown corpus from `docs/product-brain` — resolving `?doc=` against `DEFAULT_DOC_ID`, and passes the structured `PRODUCT_BRAIN_ITEMS` registry. The client `ProductBrainControlCenter` is tabbed: Dashboard, Decisions, Regressions, UX Contracts, Modules, ADRs/CAPs, Test Map, Guardrails and Documents. It offers full-text search, a module filter, detail panels with GitHub source links, and two server actions, `askIsabellaAboutItemAction` and `exportProductBrainAction`. Nothing here queries Supabase business tables. Related: screen-process-intelligence (the differently-named analytics dashboard), screen-admin-living-graph-observability.
Source: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain/access.server.ts.
Verify: with an allowlisted email, open /product-intelligence; any other account gets a 404.', 'Pantalla Inteligencia de Producto (Product Brain Control Center)', 'No la confundas con `/process-intelligence` (screen-process-intelligence). Inteligencia **de Producto** es una cabina de gobernanza interna sobre *la documentación del propio producto* — decisiones, regresiones, contratos UX — y no lee ningún dato de los clientes. Inteligencia **de Procesos** es un tablero analítico de portafolio sobre el Project Event Graph. Audiencias distintas, controles distintos, datos distintos.

El acceso a `/product-intelligence` se aplica en el servidor con una lista blanca estricta de correos, `isProductBrainAllowedEmail` (TASK 10A). Quien no esté en ella recibe `notFound()`, de modo que nunca se revela que la ruta existe y no se carga ni se serializa ningún dato del Product Brain. Para los usuarios permitidos el servidor llama a `getAllProductBrainDocs()` — el corpus markdown curado de `docs/product-brain` — resolviendo `?doc=` contra `DEFAULT_DOC_ID`, y entrega el registro estructurado `PRODUCT_BRAIN_ITEMS`. El cliente `ProductBrainControlCenter` está organizado en pestañas: Panel, Decisiones, Regresiones, Contratos UX, Módulos, ADRs/CAPs, Mapa de tests, Guardrails y Documentos. Ofrece búsqueda de texto completo, filtro por módulo, paneles de detalle con enlaces al código en GitHub y dos server actions, `askIsabellaAboutItemAction` y `exportProductBrainAction`. Nada de esto consulta tablas de negocio en Supabase. Relacionadas: screen-process-intelligence (el tablero analítico de nombre casi idéntico), screen-admin-living-graph-observability.
Fuente: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain/access.server.ts.
Verifica: con un correo de la lista blanca, abre /product-intelligence; cualquier otra cuenta recibe un 404.', '/product-intelligence — generated from: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain/loader.ts, src/lib/product-brain/access.server.ts, src/lib/product-brain-center/registry.ts'),
    ('screen-project-audit', 'Project Audit Log screen', 'A read-only governance trail of who changed what in the project. The page is fully server-rendered with no client component and no write actions. It verifies the project belongs to the user''s organization, then loads the 50 most recent rows from the audit_logs table for that org and project (columns: actor, action, entity type, entity id, metadata, timestamp), plus display names from the profiles table for each actor. Each entry renders as a card with a colored action badge (green create, blue update, red delete — unknown actions fall back to the update style), the localized entity-type label, an optional title from the metadata, "by <actor>" (or an unknown-actor label) and a localized date-time. Two metadata enrichments are shown: for traceability_links entries, pills with the link type and the source → target entity types; and a purple "AI" pill when metadata.source is "ai_extraction", marking records produced by AI extraction flows. An empty state with a shield icon appears when no records exist, and a breadcrumb returns to the project overview. Entries are produced by the logAudit helper called from server actions across modules (charter, decisions, documents, memory, links). Related screens: any screen whose actions write audit entries.
Source: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx.
Verify: open a project and go to /projects/[projectId]/audit.', 'Pantalla Bitácora de Auditoría del Proyecto', 'Rastro de gobernanza de solo lectura sobre quién cambió qué en el proyecto. La página se renderiza por completo en el servidor, sin componente de cliente ni acciones de escritura. Verifica que el proyecto pertenezca a la organización del usuario y carga las 50 filas más recientes de la tabla audit_logs para esa organización y proyecto (actor, acción, tipo de entidad, id, metadatos, fecha), más los nombres visibles desde la tabla profiles para cada actor. Cada entrada es una tarjeta con insignia de acción coloreada (verde crear, azul actualizar, rojo eliminar — acciones desconocidas usan el estilo de actualizar), la etiqueta localizada del tipo de entidad, un título opcional tomado de los metadatos, "por <actor>" (o etiqueta de actor desconocido) y fecha-hora localizada. Se muestran dos enriquecimientos: para entradas de traceability_links, píldoras con el tipo de enlace y los tipos origen → destino; y una píldora morada "AI" cuando metadata.source es "ai_extraction", que marca registros creados por flujos de extracción con IA. Hay estado vacío con icono de escudo cuando no hay registros, y una miga de pan regresa al resumen del proyecto. Las entradas las produce el helper logAudit invocado desde acciones de servidor de otros módulos (charter, decisiones, documentos, memoria, enlaces). Relacionadas: cualquier pantalla cuyas acciones escriban auditoría.
Fuente: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx.
Verifica: abre un proyecto y ve a /projects/[projectId]/audit.', '/projects/[projectId]/audit — generated from: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx, src/lib/audit/index.ts'),
    ('screen-project-budget', 'Project Budget screen', 'Three stacked sections, only one of which every project sees. The always-present part is the estimate table built from `material_requirements` (quantity, unit, estimated unit cost, total, `metadata.category`, `needs_review`, `origin`), grouped into categories with subtotals, a grand total and counts of unquantified and uncosted lines; currency is hardcoded `USD`. Quantity and unit cost are editable inline and auto-save via `updateBudgetLineAction`, which recomputes the extended cost, clears `needs_review` and stamps `metadata.manually_edited` so a regenerated estimate does not wipe the human number. "Download PDF" prints with a `BUD` document code. Lines cannot be created or deleted here. Above it, the financial control layer is **flag-gated and only appears for pilot projects**: `getFinancialFeatureStateFromProcess` requires the project id in `FINANCIAL_PILOT_PROJECT_IDS` plus `FINANCIAL_FOUNDATION_ENABLED` and the matching `FINANCIAL_WRITERS_ENABLED` / `FINANCIAL_UI_ENABLED`. When writers are on, Financial Setup edits a cost-plan draft (`financial_estimate_versions`, `financial_boe_versions`, `financial_baseline_versions`, `financial_baseline_lines`, rate cards in `resources`) with Save draft / Submit for review / Approve & activate, authorized through `project_team_members` capabilities — the approver must be independent of the preparer. When UI is on, the Financial Cockpit reads the `financial_project_cockpit` view for baseline, funding, commitment, actuals, accrual, EAC/P50/P80, CPI/SPI and data-quality warnings. Related: screen-project-closeout, screen-reports.
Source: budget/page.tsx, budget-client.tsx, actions.ts, financial-setup.tsx, financial-cockpit.tsx, setup-actions.ts.
Verify: open a project and go to /projects/[projectId]/budget; the financial sections appear only if the project is in FINANCIAL_PILOT_PROJECT_IDS.', 'Pantalla Presupuesto del Proyecto', 'Tres secciones apiladas, de las cuales solo una la ve todo proyecto. La parte siempre presente es la tabla de estimado construida desde `material_requirements` (cantidad, unidad, costo unitario estimado, total, `metadata.category`, `needs_review`, `origin`), agrupada en categorías con subtotales, gran total y conteos de líneas sin cantidad y sin costo; la moneda está fija en `USD`. Cantidad y costo unitario se editan en línea y se guardan solos con `updateBudgetLineAction`, que recalcula el total, quita `needs_review` y marca `metadata.manually_edited` para que un estimado regenerado no borre el número humano. "Descargar PDF" imprime con el código de documento `BUD`. Aquí no se crean ni eliminan líneas. Encima, la capa de control financiero está **detrás de banderas y solo aparece en proyectos piloto**: `getFinancialFeatureStateFromProcess` exige que el id del proyecto esté en `FINANCIAL_PILOT_PROJECT_IDS` más `FINANCIAL_FOUNDATION_ENABLED` y la bandera correspondiente `FINANCIAL_WRITERS_ENABLED` / `FINANCIAL_UI_ENABLED`. Con los escritores activos, Configuración financiera edita un borrador de plan de costos (`financial_estimate_versions`, `financial_boe_versions`, `financial_baseline_versions`, `financial_baseline_lines`, tarifas en `resources`) con Guardar borrador / Enviar a revisión / Aprobar y activar, autorizado por las capacidades de `project_team_members` — el aprobador debe ser independiente de quien preparó. Con la UI activa, el Cockpit Financiero lee la vista `financial_project_cockpit`: baseline, financiamiento, compromiso, reales, devengo, EAC/P50/P80, CPI/SPI y avisos de calidad de datos. Relacionadas: screen-project-closeout, screen-reports.
Fuente: budget/page.tsx, budget-client.tsx, actions.ts, financial-setup.tsx, financial-cockpit.tsx, setup-actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/budget; las secciones financieras solo salen si el proyecto está en FINANCIAL_PILOT_PROJECT_IDS.', '/projects/[projectId]/budget — generated from: src/app/[locale]/(app)/projects/[projectId]/budget/page.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/budget-client.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/actions.ts, src/app/[locale]/(app)/projects/[projectId]/budget/financial-setup.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/financial-cockpit.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/setup-actions.ts, src/lib/financial/flags.ts, src/lib/financial/read-model.server.ts, src/lib/financial/setup-read-model.server.ts'),
    ('screen-project-charter-print', 'Project Charter Print / PDF screen', 'A printable document version of the Project Charter, reached from the "Print / PDF" button on the Charter screen. The server page loads the project, the charter (project_charters) and its child tables — project_charter_roles, project_governance_rules, project_approval_matrix and project_signoffs — all read-only; this screen writes nothing. The client renders a formal document inside #charter-report-print (print CSS in globals.css isolates it): full logo header, a deterministic folio code CHR-<shortId>-V<version> derived from the charter id and version so the on-page folio matches the PDF filename (Pops360-Charter-CHR-…), the status badge, all charter text sections from CHARTER_SECTIONS, the governance tables, and signature blocks for Project Sponsor, Project Manager and Steering Committee (names auto-matched from defined charter roles) plus a client signature line — it is intended for client approval and signature. A non-printing toolbar offers "Back to Charter" and a "Download PDF" button that calls printWithFilename to open the browser print dialog with the canonical filename. Related screens: the editable Charter (/charter) and the stakeholder summary (/charter/summary).
Source: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, charter-print-client.tsx.
Verify: open /projects/[projectId]/charter and click "Print / PDF".', 'Pantalla Charter imprimible / PDF', 'Versión de documento imprimible del Charter del Proyecto, a la que se llega con el botón "Imprimir / PDF" de la pantalla de Charter. La página de servidor carga el proyecto, el charter (project_charters) y sus tablas hijas — project_charter_roles, project_governance_rules, project_approval_matrix y project_signoffs — todo en modo lectura; esta pantalla no escribe nada. El cliente muestra un documento formal dentro de #charter-report-print (el CSS de impresión en globals.css lo aísla): encabezado con logotipo, un folio determinista CHR-<shortId>-V<versión> derivado del id y la versión del charter para que el folio en pantalla coincida con el nombre del PDF (Pops360-Charter-CHR-…), la insignia de estado, todas las secciones de texto del charter, las tablas de gobernanza y bloques de firma para Patrocinador, Gerente de Proyecto y Comité Directivo (nombres tomados de los roles definidos) más una línea de firma del cliente — está pensado para aprobación y firma del cliente. Una barra no imprimible ofrece "Volver al Charter" y "Descargar PDF", que llama a printWithFilename para abrir el diálogo de impresión con el nombre canónico. Pantallas relacionadas: el Charter editable (/charter) y el resumen para stakeholders (/charter/summary).
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, charter-print-client.tsx.
Verifica: abre /projects/[projectId]/charter y pulsa "Imprimir / PDF".', '/projects/[projectId]/charter/print — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/print/charter-print-client.tsx, src/lib/print-document.ts'),
    ('screen-project-charter-summary', 'Charter Stakeholder Summary screen', 'A read-only "Project Foundation & Status Context" view for stakeholders, reached from the "Stakeholder view" button on the Charter screen. It is a fully server-rendered summary — no editing. The header shows the project name, the charter status badge (from CHARTER_STATUS_META) and the version, with a note that this view helps stakeholders understand current status relative to the approved charter. Below, a StakeholderSummaryButton (from charter-extra.tsx) generates an AI stakeholder summary on demand via the stakeholderSummaryAction server action. Six field cards show project purpose (project_goal or executive_summary), major deliverables, in scope, success criteria, governance model and reporting cadence, each showing "To be defined." when empty. Then a "Key milestones" card lists up to 8 milestones with target dates, and two cards list up to 6 open risks (status open/identified/mitigating) and up to 6 pending decisions (status proposed). Data read: project_charters, milestones, risks and decisions tables, scoped to the org and project. A back link returns to the Charter. Related screens: Charter editor (/charter) and printable charter (/charter/print).
Source: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, ../charter-extra.tsx.
Verify: open /projects/[projectId]/charter and click "Stakeholder view".', 'Pantalla Resumen del Charter para Stakeholders', 'Vista de solo lectura "Fundación y Contexto de Estado del Proyecto" para interesados, a la que se llega con el botón "Vista stakeholders" del Charter. Se renderiza por completo en el servidor — sin edición. El encabezado muestra el nombre del proyecto, la insignia de estado del charter (CHARTER_STATUS_META) y la versión, con una nota que indica que esta vista ayuda a entender el estado actual respecto al charter aprobado. Debajo, el botón StakeholderSummaryButton (de charter-extra.tsx) genera bajo demanda un resumen con IA mediante la acción de servidor stakeholderSummaryAction. Seis tarjetas muestran propósito del proyecto (project_goal o executive_summary), entregables principales, dentro del alcance, criterios de éxito, modelo de gobernanza y cadencia de reportes; cada una muestra "Por definir." si está vacía. Luego, una tarjeta de "Hitos clave" lista hasta 8 hitos con fecha objetivo, y dos tarjetas listan hasta 6 riesgos abiertos (open/identified/mitigating) y hasta 6 decisiones pendientes (proposed). Datos leídos: tablas project_charters, milestones, risks y decisions, acotadas a la organización y al proyecto. Un enlace regresa al Charter. Relacionadas: editor del Charter (/charter) y charter imprimible (/charter/print).
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, ../charter-extra.tsx.
Verifica: abre /projects/[projectId]/charter y pulsa "Vista stakeholders".', '/projects/[projectId]/charter/summary — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-extra.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/actions.ts'),
    ('screen-project-charter', 'Project Charter & Governance screen', 'The living Project Charter: the governance foundation of a project. The page auto-creates a charter row if none exists (covers older projects) and loads it with versions, roles, governance rules, approval matrix, sign-offs, project team members and the unified People Directory for person suggestions. The editor shows a section navigation (charter text sections from CHARTER_SECTIONS) plus governance tabs: Roles, Approval Matrix, Governance Rules, Sign-Off and an AI tools tab (gap analysis, scope-creep detection, charter Q&A, stakeholder summary, generate full governance set). Header actions: "Generate with AI" fills only empty fields; "Submit for approval" is gated (100% required fields, at least one role, one approval rule and one sign-off); reviewers can Approve (locks the charter, snapshots a version into project_charter_versions and pushes sections into Project Memory) or Reject (status revision_required with notes). Editing an approved charter opens a new revision and bumps the version. A completion bar shows missing fields and governance blockers. Data: project_charters, project_charter_versions, project_charter_roles, project_governance_rules, project_approval_matrix, project_signoffs, project_team_members; transitions write audit_logs. Links go to the Stakeholder view (/charter/summary), Print/PDF (/charter/print) and the Delivery framework (/delivery?setup=true). The ?onboard=true param shows an onboarding banner.
Source: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, charter-client.tsx, charter-extra.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/charter.', 'Pantalla Charter y Gobernanza del Proyecto', 'El Charter vivo del proyecto: su base de gobernanza. La página crea automáticamente el charter si no existe (cubre proyectos antiguos) y lo carga con versiones, roles, reglas de gobernanza, matriz de aprobación, firmas, miembros del equipo y el Directorio de Personas unificado para sugerir personas. El editor muestra navegación por secciones de texto (CHARTER_SECTIONS) más pestañas de gobernanza: Roles, Matriz de Aprobación, Reglas de Gobernanza, Firmas (Sign-Off) y una pestaña de herramientas de IA (análisis de brechas, detección de scope creep, preguntas al charter, resumen para stakeholders, generación del modelo de gobernanza completo). Acciones del encabezado: "Generar con IA" rellena solo campos vacíos; "Enviar a aprobación" tiene compuerta de preparación (100% de campos requeridos, al menos un rol, una regla de aprobación y una firma); el revisor puede Aprobar (bloquea el charter, guarda una versión en project_charter_versions y sincroniza secciones a la Memoria del Proyecto) o Rechazar (estado revision_required con notas). Editar un charter aprobado abre una nueva revisión y sube la versión. Una barra de completitud señala campos faltantes y bloqueos de gobernanza. Datos: project_charters, project_charter_versions, project_charter_roles, project_governance_rules, project_approval_matrix, project_signoffs, project_team_members; las transiciones registran audit_logs. Enlaces a la vista de stakeholders (/charter/summary), Imprimir/PDF (/charter/print) y el Marco de ejecución (/delivery?setup=true). El parámetro ?onboard=true muestra un banner de bienvenida.
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, charter-client.tsx, charter-extra.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/charter.', '/projects/[projectId]/charter — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-client.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-extra.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/actions.ts, src/lib/charter/service.ts, src/lib/charter/fields.ts'),
    ('screen-project-closeout', 'Project Closeout Report screen', 'The end-of-project report with live metrics and an AI-generated executive narrative. The server page computes metrics, readiness, milestone durations and the archive through `lib/rhythm/closeout`, then reads the `meetings` table twice: the latest **completed** `closing` meeting carries the narrative inside `ai_summary.closeout`, and the latest closing meeting of any status drives the UX-010 guided rail (schedule → complete → generate summary → review → export). Sections: executive summary (with a notice that it needs a completed "Closing Project" meeting in the Rhythm Center), Results KPIs, key accomplishments, milestone durations, detail cards for schedule, budget, risks/RFIs/submittals and governance, lessons learned, open items, next steps and the archive. Actions: `generateCloseoutNarrativeAction` stores the narrative on the closing meeting (blocked for viewers — the page passes `canRunCloseout = role !== "viewer"`), "Download PDF" prints with a `CLS` code then `markCloseoutExportedAction` stamps `exportedAt`, and `resolveRiskAction` (REG-017) resolves open `risks` inline. Behind **two independent, default-OFF per-project env flags** (`RISK_EVENT_CAPTURE_PROJECT_IDS` and `RISK_EVENT_CAPTURE_AFFORDANCES_PROJECT_IDS`, both must list the project), each risk row also exposes Assess, Materialize and Reopen: these append `risk_assessed`, `risk_materialized` and `risk_reopened` to `project_event_log` with a client-supplied command id for idempotency; only Reopen changes the record, atomically back to `open`. A footer notes untracked metrics are omitted. Related: screen-project-rhythm-center, screen-project-budget.
Source: closeout/page.tsx, closeout-client.tsx, actions.ts, src/lib/rhythm/closeout.ts.
Verify: open a project and go to /projects/[projectId]/closeout; the Assess/Materialize/Reopen risk controls appear only when both risk-event flags list that project.', 'Pantalla Reporte de Cierre del Proyecto', 'El reporte de fin de proyecto con métricas en vivo y una narrativa ejecutiva generada con IA. La página de servidor calcula métricas, preparación, duración de hitos y el archivo mediante `lib/rhythm/closeout`, y lee dos veces la tabla `meetings`: la reunión `closing` **completada** más reciente lleva la narrativa en `ai_summary.closeout`, y la reunión de cierre más reciente de cualquier estado alimenta el riel guiado UX-010 (programar → completar → generar resumen → revisar → exportar). Secciones: resumen ejecutivo (con aviso de que requiere una reunión "Cierre del Proyecto" completada en el Rhythm Center), KPIs de resultados, logros clave, duración de hitos, tarjetas de cronograma, presupuesto, riesgos/RFIs/submittals y gobernanza, lecciones aprendidas, asuntos abiertos, próximos pasos y archivo. Acciones: `generateCloseoutNarrativeAction` guarda la narrativa en la reunión de cierre (bloqueada para viewers — la página pasa `canRunCloseout = role !== "viewer"`), "Descargar PDF" imprime con código `CLS` y luego `markCloseoutExportedAction` registra `exportedAt`, y `resolveRiskAction` (REG-017) resuelve riesgos abiertos de `risks` en línea. Detrás de **dos banderas de entorno por proyecto, independientes y apagadas por defecto** (`RISK_EVENT_CAPTURE_PROJECT_IDS` y `RISK_EVENT_CAPTURE_AFFORDANCES_PROJECT_IDS`, ambas deben listar el proyecto), cada riesgo ofrece además Evaluar, Materializar y Reabrir: agregan `risk_assessed`, `risk_materialized` y `risk_reopened` a `project_event_log` con un id de comando enviado por el cliente para idempotencia; solo Reabrir modifica el registro, de forma atómica de vuelta a `open`. Un pie aclara que se omiten métricas no rastreadas. Relacionadas: screen-project-rhythm-center, screen-project-budget.
Fuente: closeout/page.tsx, closeout-client.tsx, actions.ts, src/lib/rhythm/closeout.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/closeout; los controles Evaluar/Materializar/Reabrir solo aparecen si ambas banderas de eventos de riesgo listan ese proyecto.', '/projects/[projectId]/closeout — generated from: src/app/[locale]/(app)/projects/[projectId]/closeout/page.tsx, src/app/[locale]/(app)/projects/[projectId]/closeout/closeout-client.tsx, src/app/[locale]/(app)/projects/[projectId]/closeout/actions.ts, src/lib/rhythm/closeout.ts, src/lib/events/risk-capture-flag.ts'),
    ('screen-project-communications', 'Project Communications screen', 'The Communications screen (/projects/[projectId]/communications) is the project''s communication log. The server page fans out three parallel queries: the project (organization-scoped), all non-deleted `communication_items` ordered by `item_date` descending, and the project''s `stakeholders` for linking. `CommunicationsListClient` shows a header with a "Create" button, a filter bar (source type, status draft/logged, a "requires follow-up" toggle, and date range) and cards for each entry. Source types confirmed in code: email, meeting, phone, teams, slack, in_person, document, manual_note and other; each card shows a source-type badge, status badge and a follow-up indicator when `requires_follow_up` is set. `CreateCommunicationDialog` and `EditCommunicationDialog` feed the server actions in `actions.ts` — createCommunicationAction, updateCommunicationAction and archiveCommunicationAction (soft delete with confirmation). Writes go to `communication_items` with per-locale i18n fields, then fire-and-forget generate a semantic-search embedding (`generateAndStoreEmbedding`) and emit a Living Graph `communication_flow` event via `emitAndAutoLink`. Communications can also be linked from the meeting detail screen through `traceability_links`. Related screens: Meetings, Meeting detail, Stakeholders, Documents.
Source: communications/page.tsx, communications-list-client.tsx, communications/actions.ts.
Verify: open a project and go to Communications (/projects/[projectId]/communications).', 'Pantalla Comunicaciones del proyecto', 'La pantalla de Comunicaciones (/projects/[projectId]/communications) es la bitácora de comunicaciones del proyecto. La página de servidor lanza tres consultas en paralelo: el proyecto (limitado a la organización), los `communication_items` no eliminados ordenados por `item_date` descendente y los `stakeholders` del proyecto para vincular. `CommunicationsListClient` muestra un encabezado con botón "Crear", una barra de filtros (tipo de fuente, estado borrador/registrado, un interruptor de "requiere seguimiento" y rango de fechas) y tarjetas por cada entrada. Tipos de fuente confirmados en el código: correo, reunión, teléfono, Teams, Slack, presencial, documento, nota manual y otro; cada tarjeta muestra insignias de tipo y estado y un indicador cuando `requires_follow_up` está activo. Los diálogos de creación y edición usan las acciones de servidor createCommunicationAction, updateCommunicationAction y archiveCommunicationAction (borrado lógico con confirmación). Las escrituras van a `communication_items` con campos i18n por idioma y, de forma asíncrona, generan un embedding para búsqueda semántica y emiten un evento `communication_flow` del Living Graph. Las comunicaciones también se vinculan desde el detalle de reunión mediante `traceability_links`. Relacionadas: Reuniones, Detalle de reunión, Stakeholders, Documentos.
Fuente: communications/page.tsx, communications-list-client.tsx, communications/actions.ts.
Verifica: abre un proyecto y entra a Comunicaciones (/projects/[projectId]/communications).', '/projects/[projectId]/communications — generated from: src/app/[locale]/(app)/projects/[projectId]/communications/page.tsx, src/app/[locale]/(app)/projects/[projectId]/communications/communications-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/communications/actions.ts, src/components/communications/index.ts, src/components/communications/communication-card.tsx, src/components/communications/communication-filters.tsx, src/components/communications/create-communication-dialog.tsx, src/components/communications/edit-communication-dialog.tsx, src/components/communications/source-type-badge.tsx, src/components/communications/status-badge.tsx'),
    ('screen-project-decision-detail', 'Project Decision Detail screen', 'Full view of a single decision, reached by clicking a decision card in the project decision log. The server page loads the decision (decisions table), project stakeholders, and the decision''s traceability links in both directions from traceability_links (source_type or target_type = "decision"). Linked-record titles are resolved by batch queries against decisions, meetings, communication_items and documents; the page also loads full catalogs of those four entity types to feed the "Add link" dialog. The detail client displays date, decision maker, source (meeting, communication, document, manual, other), evidence, description, rationale, linked stakeholders and a Linked Records section where the user can add or remove typed links (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) with optional context notes. Edit and archive reuse the list-screen actions: updateDecisionAction rewrites the i18n fields, logs to audit_logs and regenerates the vector embedding; archiveDecisionAction soft-deletes after confirmation. A back link returns to the decisions list. Related screens: decisions list (/decisions), Project Memory (memory items can link to decisions) and the project audit log.
Source: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, decision-detail-client.tsx, ../actions.ts.
Verify: open /projects/[projectId]/decisions and click any decision.', 'Pantalla Detalle de Decisión', 'Vista completa de una decisión, a la que se llega al hacer clic en una tarjeta de la bitácora de decisiones. La página de servidor carga la decisión (tabla decisions), los stakeholders del proyecto y los enlaces de trazabilidad de la decisión en ambas direcciones desde traceability_links (source_type o target_type = "decision"). Los títulos de los registros vinculados se resuelven con consultas por lote a decisions, meetings, communication_items y documents; la página también carga catálogos completos de esos cuatro tipos para el diálogo "Agregar enlace". El cliente muestra fecha, decisor, origen (reunión, comunicación, documento, manual, otro), evidencia, descripción, justificación, stakeholders vinculados y una sección de Registros Vinculados donde se pueden agregar o quitar enlaces tipificados (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) con notas de contexto opcionales. Editar y archivar reutilizan las acciones de la lista: updateDecisionAction reescribe los campos i18n, registra en audit_logs y regenera el embedding vectorial; archiveDecisionAction hace borrado suave tras confirmación. Un enlace regresa a la lista. Relacionadas: lista de decisiones (/decisions), Memoria del Proyecto (los elementos de memoria pueden vincularse a decisiones) y la bitácora de auditoría.
Fuente: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, decision-detail-client.tsx, ../actions.ts.
Verifica: abre /projects/[projectId]/decisions y haz clic en cualquier decisión.', '/projects/[projectId]/decisions/[decisionId] — generated from: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/decision-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/actions.ts, src/components/links/linked-records.tsx'),
    ('screen-project-decisions', 'Project Decisions screen', 'The project decision log. The server page loads all non-deleted decisions for the project ordered by decision date (newest first) plus the project stakeholders for the linked-stakeholders multi-select. The client shows a filterable card list: filters by status (proposed, accepted, rejected, deferred, revoked), impact area (scope, schedule, budget, risk, quality, communication, document, other) and date range, applied client-side. A create button opens CreateDecisionDialog with title, description, rationale, decision maker, decision date, source (meeting, communication, document, manual, other, with optional source record), impact area, evidence URL, status and linked stakeholders. Server actions: createDecisionAction and updateDecisionAction write to the decisions table with per-locale i18n fields (title_i18n, description_i18n, rationale_i18n), log to audit_logs, generate a vector embedding (generateAndStoreEmbedding) for semantic search, and can create a traceability link to the source record; archiveDecisionAction soft-deletes with confirmation. Each card links to the decision detail screen. An empty state prompts creating the first decision. Related screens: decision detail (/decisions/[decisionId]), Project Memory (decisions tab) and the Charter summary (pending decisions).
Source: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, decision-list-client.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/decisions.', 'Pantalla Decisiones del Proyecto', 'La bitácora de decisiones del proyecto. La página de servidor carga todas las decisiones no eliminadas ordenadas por fecha de decisión (más recientes primero) y los stakeholders del proyecto para el selector múltiple de vinculación. El cliente muestra una lista de tarjetas con filtros por estado (propuesta, aceptada, rechazada, diferida, revocada), área de impacto (alcance, cronograma, presupuesto, riesgo, calidad, comunicación, documento, otro) y rango de fechas, aplicados en el cliente. El botón de crear abre CreateDecisionDialog con título, descripción, justificación, decisor, fecha, origen (reunión, comunicación, documento, manual, otro, con registro origen opcional), área de impacto, URL de evidencia, estado y stakeholders vinculados. Acciones de servidor: createDecisionAction y updateDecisionAction escriben en la tabla decisions con campos i18n por idioma (title_i18n, description_i18n, rationale_i18n), registran en audit_logs, generan un embedding vectorial (generateAndStoreEmbedding) para búsqueda semántica y pueden crear un enlace de trazabilidad al registro origen; archiveDecisionAction hace borrado suave con confirmación. Cada tarjeta enlaza al detalle. Hay estado vacío que invita a crear la primera decisión. Relacionadas: detalle de decisión (/decisions/[decisionId]), Memoria del Proyecto (pestaña de decisiones) y resumen del Charter (decisiones pendientes).
Fuente: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, decision-list-client.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/decisions.', '/projects/[projectId]/decisions — generated from: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/decision-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/actions.ts, src/components/decisions/create-decision-dialog.tsx, src/components/decisions/decision-filters.tsx'),
    ('screen-project-delivery', 'Adaptive Delivery Framework screen', 'Configures and runs how the project executes. With no active framework (or `?setup=true`, linked from the Charter), a wizard asks a diagnostic: project type, uncertainty, governance, documentation, change control, stakeholder feedback, vendor dependency and cadence. For implementation-type projects only (`PLATFORM_RELEVANT_TYPES` = `erp`, `data_bi`) it also asks an optional **Platform** (SAP, Oracle, Dynamics, Salesforce, Workday, Other). `recommendFrameworkAction` runs the pure rule engine: its first and highest-precedence rule fires on `platform === "sap"` **and** `projectType === "erp"` — never on the project type alone — recommending `sap_activate` at 84% (92% under heavy governance or regulatory). A blank platform recommends nothing vendor-specific. `saveFrameworkAction` writes `project_delivery_frameworks` (including the new `platform` column) and rebuilds `project_board_columns`; `activateFrameworkAction` sets status active. Overview shows adaptive metrics from live `roadmap_tasks` counts, attribute cards, editable WIP limits, suggested `MEETING_RHYTHM` with `scheduleFrameworkMeetingsAction` writing `project_events`, and — only when the method is `sap_activate` — a panel listing the six SAP Activate phases (Discover, Prepare, Explore, Realize, Deploy, Run) with their Q0–Q5 quality gates and exit criteria. Other tabs: Backlog (`project_backlog_items`, AI generate/prioritize, AI `milestones`, promote to tasks), Cycles (`project_execution_cycles`, `project_cycle_items`) and AI & Health (`project_scope_creep_alerts`). Related: screen-project-charter, screen-project-workboard, screen-project-rhythm-center.
Source: delivery/page.tsx, delivery-client.tsx, delivery-tabs.tsx, actions.ts, src/lib/delivery/config.ts, recommend.ts.
Verify: open a project and go to /projects/[projectId]/delivery (or /delivery?setup=true); pick "ERP / System Implementation" + Platform "SAP" and press Recommend framework.', 'Pantalla Marco de Ejecución Adaptativo (Delivery)', 'Configura y opera cómo se ejecuta el proyecto. Si no hay marco activo (o con `?setup=true`, enlazado desde el Charter), un asistente hace un diagnóstico: tipo de proyecto, incertidumbre, gobernanza, documentación, control de cambios, feedback de stakeholders, dependencia de proveedores y cadencia. Solo para proyectos de implementación (`PLATFORM_RELEVANT_TYPES` = `erp`, `data_bi`) pregunta además una **Plataforma** opcional (SAP, Oracle, Dynamics, Salesforce, Workday, Otra). `recommendFrameworkAction` ejecuta el motor de reglas puro: su primera regla, la de mayor precedencia, se dispara con `platform === "sap"` **y** `projectType === "erp"` — nunca por el tipo de proyecto solo — y recomienda `sap_activate` con 84% de confianza (92% con gobernanza fuerte o regulatoria). Una plataforma sin responder no recomienda nada específico de un proveedor. `saveFrameworkAction` guarda en `project_delivery_frameworks` (incluida la nueva columna `platform`) y reconstruye `project_board_columns`; `activateFrameworkAction` activa el marco. El Resumen muestra métricas adaptativas desde los conteos vivos de `roadmap_tasks`, tarjetas de atributos, límites de WIP editables, el ritmo sugerido `MEETING_RHYTHM` con `scheduleFrameworkMeetingsAction` escribiendo en `project_events` y —solo si el método es `sap_activate`— un panel con las seis fases de SAP Activate (Descubrimiento, Preparación, Exploración, Realización, Despliegue, Soporte) con sus quality gates Q0–Q5 y criterios de salida. Otras pestañas: Backlog (`project_backlog_items`, generar/priorizar con IA, hitos con IA, promover a tareas), Ciclos (`project_execution_cycles`, `project_cycle_items`) e IA y Salud (`project_scope_creep_alerts`). Relacionadas: screen-project-charter, screen-project-workboard, screen-project-rhythm-center.
Fuente: delivery/page.tsx, delivery-client.tsx, delivery-tabs.tsx, actions.ts, src/lib/delivery/config.ts, recommend.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/delivery (o /delivery?setup=true); elige "ERP / Implementación de sistemas" + Plataforma "SAP" y pulsa Recomendar marco.', '/projects/[projectId]/delivery — generated from: src/app/[locale]/(app)/projects/[projectId]/delivery/page.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/delivery-client.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/delivery-tabs.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/actions.ts, src/lib/delivery/config.ts, src/lib/delivery/recommend.ts, src/lib/delivery/service.ts, supabase/migrations/20260904000000_sap_activate_delivery_method.sql, supabase/migrations/20260908000000_delivery_platform.sql'),
    ('screen-project-document-detail', 'Project Document Detail screen', 'Detail view of a single project document, reached by clicking an item in the documents list. The server page loads the document row (documents table) scoped to the organization; for documents with storage_type "upload" it generates a one-hour signed URL from the Supabase Storage "documents" bucket so the file can be opened securely. The detail client shows the document title, status badge (draft, review, approved, archived), owner (or "no owner"), storage type (upload or external URL), document type (evidence, contract, specification, report, presentation, other), description, version, and either an "Open file" button (using the signed URL) or an "Open link" button for external URLs. Edit opens the edit dialog, which can replace the uploaded file or switch storage mode, and saves through updateDocumentAction (zod validation, i18n fields, audit_logs entry, path revalidation). Archive soft-deletes via archiveDocumentAction after a confirmation prompt. A back link returns to the documents list. This screen writes nothing on load; all mutations go through the shared server actions. Related screens: documents list (/documents), Project Memory (documents tab), decision detail (documents appear as linkable records).
Source: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, document-detail-client.tsx, ../actions.ts.
Verify: open /projects/[projectId]/documents and click any document.', 'Pantalla Detalle de Documento', 'Vista de detalle de un documento del proyecto, a la que se llega al hacer clic en un elemento de la lista de documentos. La página de servidor carga la fila del documento (tabla documents) acotada a la organización; para documentos con storage_type "upload" genera una URL firmada de una hora desde el bucket "documents" de Supabase Storage para abrir el archivo de forma segura. El cliente muestra título, insignia de estado (borrador, revisión, aprobado, archivado), responsable (o "sin responsable"), tipo de almacenamiento (carga o URL externa), tipo de documento (evidencia, contrato, especificación, reporte, presentación, otro), descripción, versión, y un botón "Abrir archivo" (con la URL firmada) o "Abrir enlace" para URLs externas. Editar abre el diálogo de edición, que puede reemplazar el archivo subido o cambiar el modo de almacenamiento, y guarda mediante updateDocumentAction (validación zod, campos i18n, registro en audit_logs, revalidación de ruta). Archivar hace borrado suave con archiveDocumentAction tras confirmación. Un enlace regresa a la lista. La pantalla no escribe nada al cargar; todas las mutaciones pasan por las acciones de servidor compartidas. Relacionadas: lista de documentos (/documents), Memoria del Proyecto (pestaña documentos) y detalle de decisión.
Fuente: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, document-detail-client.tsx, ../actions.ts.
Verifica: abre /projects/[projectId]/documents y haz clic en cualquier documento.', '/projects/[projectId]/documents/[documentId] — generated from: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/document-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/actions.ts'),
    ('screen-project-documents', 'Project Documents screen', 'The project document repository. The server page loads all non-deleted rows from the documents table for the project, newest first. The client renders a filterable list with client-side filters for status (draft, review, approved, archived), document type (evidence, contract, specification, report, presentation, other) and storage type (upload vs external_url). The create button opens CreateDocumentDialog, which supports two storage modes: uploading a file to the Supabase Storage "documents" bucket (the dialog uploads directly from the browser, then the server action stores the file path, name, size and mime type) or registering an external URL. createDocumentAction and updateDocumentAction validate with zod (a file is required for uploads, a URL for external links), write i18n title/description to the documents table, log to audit_logs and revalidate the project layout; archiveDocumentAction soft-deletes with confirmation. Each item links to the document detail screen. Empty state prompts adding the first document. Related screens: document detail (/documents/[documentId]), Project Memory (documents tab and linking), and decisions (documents can be a decision source or linked record).
Source: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, document-list-client.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/documents.', 'Pantalla Documentos del Proyecto', 'El repositorio documental del proyecto. La página de servidor carga todas las filas no eliminadas de la tabla documents para el proyecto, de más reciente a más antigua. El cliente muestra una lista con filtros en el cliente por estado (borrador, revisión, aprobado, archivado), tipo de documento (evidencia, contrato, especificación, reporte, presentación, otro) y tipo de almacenamiento (carga de archivo o URL externa). El botón de crear abre CreateDocumentDialog, que admite dos modos: subir un archivo al bucket "documents" de Supabase Storage (el diálogo sube directamente desde el navegador y la acción de servidor guarda ruta, nombre, tamaño y tipo mime) o registrar una URL externa. createDocumentAction y updateDocumentAction validan con zod (archivo obligatorio para cargas, URL para enlaces externos), escriben título y descripción i18n en la tabla documents, registran en audit_logs y revalidan el layout del proyecto; archiveDocumentAction hace borrado suave con confirmación. Cada elemento enlaza al detalle del documento. Hay estado vacío que invita a agregar el primer documento. Relacionadas: detalle de documento (/documents/[documentId]), Memoria del Proyecto (pestaña de documentos y vinculación) y decisiones (un documento puede ser origen o registro vinculado).
Fuente: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, document-list-client.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/documents.', '/projects/[projectId]/documents — generated from: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/document-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/actions.ts, src/components/documents/create-document-dialog.tsx'),
    ('screen-project-drawing-intelligence', 'Drawing Intelligence (BIM) screen', 'The project Drawing Intelligence screen ("BIM — Building Information Module") turns construction drawings into structured project knowledge with evidence traceability, at /projects/[projectId]/drawing-intelligence. The server page (force-dynamic) loads `drawing_files`, `drawing_processing_jobs`, `drawing_extractions`, `drawing_insights`, `drawing_versions`, `drawing_evidence` and `roadmap_tasks` (for linking), degrading gracefully if tables are missing, and checks whether the Autodesk APS connector is configured (server-side env check only). The client has thirteen tabs: Upload (functional manual upload zone via `registerDrawingFileAction`; Autodesk connector card; Procore and Google Drive cards are explicitly "coming soon"), Library (files table with status/processing badges and buttons to process (`processDrawingFileAction`), retry (`retryDrawingProcessingJobAction`), archive (`archiveDrawingFileAction`) and open a detail panel (`getDrawingDetailAction`)), Extractions, then insight tabs filtered by type — Risks, RFIs, Submittals (incl. inspections), Schedule and Cost impacts, and Actions — where insights can change status (`updateDrawingInsightStatusAction`) and be linked to tasks (`linkDrawingInsightToTaskAction`). Takeoff generates quantity estimates (`generateTakeoffEstimateAction`); Versions shows `drawing_versions` changes; Evidence and Logs render placeholder "coming soon" panels when data is absent. Processing progress is polled via `getDrawingProcessingProgressAction`. Related screens: screen-execution-map (linked tasks), global drawing-intelligence page.
Source: drawing-intelligence/page.tsx, drawing-intelligence-client.tsx, actions.ts.
Verify: open a project > Drawing Intelligence / BIM (/projects/[projectId]/drawing-intelligence).', 'Pantalla Inteligencia de Planos (BIM)', 'La pantalla de Inteligencia de Planos del proyecto ("BIM — Módulo de Información de Construcción") convierte planos de construcción en conocimiento estructurado con trazabilidad de evidencia, en /projects/[projectId]/drawing-intelligence. La página de servidor (force-dynamic) carga `drawing_files`, `drawing_processing_jobs`, `drawing_extractions`, `drawing_insights`, `drawing_versions`, `drawing_evidence` y `roadmap_tasks` (para vincular), degradando con gracia si faltan tablas, y verifica si el conector Autodesk APS está configurado (solo variables de entorno del servidor). El cliente tiene trece pestañas: Carga (zona de carga manual funcional mediante `registerDrawingFileAction`; tarjeta del conector Autodesk; Procore y Google Drive aparecen explícitamente como "próximamente"), Biblioteca (tabla de archivos con insignias de estado y procesamiento, y botones para procesar (`processDrawingFileAction`), reintentar (`retryDrawingProcessingJobAction`), archivar (`archiveDrawingFileAction`) y abrir un panel de detalle (`getDrawingDetailAction`)), Extracciones, y luego pestañas de insights filtradas por tipo — Riesgos, RFIs, Submittals (incluye inspecciones), impactos de Cronograma y Costo, y Acciones — donde los insights pueden cambiar de estado (`updateDrawingInsightStatusAction`) y vincularse a tareas (`linkDrawingInsightToTaskAction`). Takeoff genera estimaciones de cantidades (`generateTakeoffEstimateAction`); Versiones muestra los cambios de `drawing_versions`; Evidencia y Registros muestran paneles de "próximamente" cuando no hay datos. El progreso de procesamiento se sondea con `getDrawingProcessingProgressAction`. Pantallas relacionadas: screen-execution-map (tareas vinculadas) y la página global de drawing-intelligence.
Fuente: drawing-intelligence/page.tsx, drawing-intelligence-client.tsx, actions.ts.
Verifica: abre un proyecto > Inteligencia de Planos / BIM (/projects/[projectId]/drawing-intelligence).', '/projects/[projectId]/drawing-intelligence — generated from: src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/page.tsx, src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/drawing-intelligence-client.tsx, src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/actions.ts, src/components/drawing-intelligence/drawing-upload-zone.tsx'),
    ('screen-project-github-integration', 'GitHub Integration Settings screen', 'Where a software project connects a GitHub repository, reached from Project Settings → GitHub Intelligence (card visible only for software projects with the feature flag on). The page is guarded by assertGitHubIntelligenceAvailable: non-software projects see an explicit "software projects only" message; disabled/forbidden cases 404. It advertises two guarantees as badges — Read-only and Software projects only — and states the integration reads repository metadata and receives webhooks but never writes to GitHub. If repositories are connected (getConnectionStatus), they are listed with last-sync time/status and, for managers/admins (guard.canManage), Refresh (manualSyncAction) and Disconnect (disconnectRepositoryAction) buttons, plus an "Open dashboard" link to /projects/[projectId]/github. Otherwise a connect section shows: with a configured GitHub App, StartInstallButton (startInstallationAction → GitHub App install flow); without one, a dev-mode ConnectSampleButton (devConnectSampleRepositoryAction) that loads synthetic sample data. After the install callback (?installation_id, validated against the github_installations table), a RepoPicker lists installable repositories (listInstallationRepositories) and saves the choice via selectRepositoriesAction. A diagnostics section shows whether the GitHub App env vars, webhook secret and GITHUB_INTELLIGENCE_ENABLED flag are configured. Related screens: GitHub Intelligence dashboard, Project Settings.
Source: settings/integrations/github/page.tsx, github/actions.ts, components/github-intelligence/*.
Verify: software project → Settings → GitHub Intelligence card.', 'Pantalla Configuración de la integración con GitHub', 'Donde un proyecto de software conecta un repositorio de GitHub; se llega desde Configuración del Proyecto → GitHub Intelligence (tarjeta visible solo en proyectos de software con el flag activo). La página está protegida por assertGitHubIntelligenceAvailable: los proyectos que no son de software ven un mensaje explícito ("solo proyectos de software"); los casos deshabilitados o sin permiso devuelven 404. Muestra dos garantías como insignias — Solo lectura y Solo proyectos de software — y aclara que la integración lee metadatos y recibe webhooks pero nunca escribe en GitHub. Si hay repositorios conectados (getConnectionStatus), se listan con fecha y estado de la última sincronización y, para gestores/administradores, botones Actualizar (manualSyncAction) y Desconectar (disconnectRepositoryAction), más un enlace "Abrir dashboard" hacia /projects/[projectId]/github. Si no, aparece la sección de conexión: con una GitHub App configurada, el botón de instalación (startInstallationAction); sin ella, un botón de modo desarrollo (devConnectSampleRepositoryAction) que carga datos sintéticos de muestra. Tras el callback de instalación (?installation_id, validado contra la tabla github_installations), un selector de repositorios (listInstallationRepositories) guarda la elección con selectRepositoriesAction. Una sección de diagnóstico indica si están configuradas las variables de la GitHub App, el webhook secret y el flag GITHUB_INTELLIGENCE_ENABLED. Relacionadas: dashboard de GitHub Intelligence, Configuración del Proyecto.
Fuente: settings/integrations/github/page.tsx, github/actions.ts, components/github-intelligence/*.
Verifica: proyecto de software → Settings → tarjeta GitHub Intelligence.', '/projects/[projectId]/settings/integrations/github — generated from: src/app/[locale]/(app)/projects/[projectId]/settings/integrations/github/page.tsx, src/app/[locale]/(app)/projects/[projectId]/github/actions.ts, src/components/github-intelligence/github-action-buttons.tsx, src/components/github-intelligence/repo-picker.tsx, src/lib/github-intelligence/software-project-guard.ts, src/lib/github-intelligence/config.ts, src/lib/github-intelligence/read-model.ts, src/lib/github-intelligence/installation.ts'),
    ('screen-project-github', 'GitHub Intelligence dashboard screen', 'The read-only execution-evidence dashboard for a connected repository, at /projects/[projectId]/github (linked from the integration settings). Guarded by assertGitHubIntelligenceAvailable: non-software projects get an explicit unavailable state; disabled/forbidden access 404s. With no repository connected it shows an empty state pointing managers to the sample-data button and the integration settings. Once connected, loadDashboardData (lib/github-intelligence/read-model) builds everything server-side. Filters are URL query params: a date window (7/14/30 days or all history, default all) and a repository switcher when several repos are connected. Five metric cards summarize commits, active branches, open PRs (with merged count), workflow/CI health and the Release Readiness score with a good/watch/at_risk/blocked band (lib/github-intelligence/readiness). The main visualization is the GitHubLivingGraph (branches/commits/PRs over time). Below sit three panels: an Activity Summary (commits, merged PRs, failed workflows, releases), an "Isabella Insight" card — a deterministic summary/risk/recommendation, labeled in code as a deterministic placeholder, with a note that readiness never replaces human approval — and a Release Path checklist (branch active, PR open, CI passing, staging deploy, production ready) derived from the metrics. The Refresh button calls manualSyncAction; the integration never writes to GitHub. Related screens: GitHub integration settings, Project Settings.
Source: github/page.tsx, github/actions.ts, lib/github-intelligence/read-model.ts.
Verify: software project with a connected repo → GitHub tab, or via Settings → GitHub Intelligence → Open dashboard.', 'Pantalla Dashboard de GitHub Intelligence', 'El dashboard de evidencia de ejecución, de solo lectura, para un repositorio conectado, en /projects/[projectId]/github (enlazado desde la configuración de la integración). Protegido por assertGitHubIntelligenceAvailable: los proyectos que no son de software ven un estado de no disponible explícito; los accesos deshabilitados o sin permiso devuelven 404. Sin repositorio conectado muestra un estado vacío que dirige a los gestores al botón de datos de muestra y a la configuración. Conectado, loadDashboardData (lib/github-intelligence/read-model) construye todo en el servidor. Los filtros son parámetros de URL: ventana de tiempo (7/14/30 días o todo el historial, por defecto todo) y selector de repositorio si hay varios. Cinco tarjetas resumen commits, ramas activas, PRs abiertos (con fusionados), salud de CI/workflows y el puntaje de Release Readiness con banda good/watch/at_risk/blocked (lib/github-intelligence/readiness). La visualización principal es el GitHubLivingGraph (ramas/commits/PRs en el tiempo). Debajo hay tres paneles: Resumen de actividad (commits, PRs fusionados, workflows fallidos, releases), la "Perspectiva de Isabella" — un resumen/riesgo/recomendación determinista, marcado en el código como placeholder determinista, con la nota de que la readiness no reemplaza la aprobación humana — y la Ruta al release (rama activa, PR abierto, CI en verde, deploy a staging, listo para producción) derivada de las métricas. El botón Actualizar llama a manualSyncAction; la integración nunca escribe en GitHub. Relacionadas: configuración de la integración con GitHub, Configuración del Proyecto.
Fuente: github/page.tsx, github/actions.ts, lib/github-intelligence/read-model.ts.
Verifica: proyecto de software con repositorio conectado → pestaña GitHub, o Settings → GitHub Intelligence → Abrir dashboard.', '/projects/[projectId]/github — generated from: src/app/[locale]/(app)/projects/[projectId]/github/page.tsx, src/app/[locale]/(app)/projects/[projectId]/github/actions.ts, src/components/github-intelligence/github-living-graph.tsx, src/lib/github-intelligence/read-model.ts, src/lib/github-intelligence/readiness.ts, src/lib/github-intelligence/summary.ts, src/lib/github-intelligence/software-project-guard.ts'),
    ('screen-project-meeting-detail', 'Project Meeting Detail screen', 'Reached by clicking a meeting card on the Meetings list (/projects/[projectId]/meetings/[meetingId]), this screen shows one record from the `meetings` table: date, duration, location, attendees, agenda, summary, notes, linked stakeholders, and status badge, with Edit and Archive actions. The server page also builds a bidirectional traceability panel: it reads `traceability_links` where the meeting is source or target, resolves titles from `decisions`, `meetings`, `communication_items` and `documents`, and passes entity catalogs so the "Add link" dialog can connect the meeting to decisions, other meetings, communications or documents with a link type (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) and context notes. Two AI features stand out: "Extract decisions" (ai-extract-actions.ts) and "Extract action items" (ai-action-extract-actions.ts) run the meeting''s notes/agenda through `runAi`, return suggestions with confidence and source excerpts, and let the user edit, approve or reject each one; approving inserts into `decisions` or `action_items` and creates a `traceability_link` back to the meeting, with audit logging via `logAudit`. Related screens: Meetings list, Decisions, Communications, Documents.
Source: meetings/[meetingId]/page.tsx, meeting-detail-client.tsx, ai-extract-actions.ts, ai-action-extract-actions.ts.
Verify: open a project, go to Meetings and click any meeting.', 'Pantalla Detalle de reunión', 'Se llega al pulsar una tarjeta en la lista de Reuniones (/projects/[projectId]/meetings/[meetingId]). Muestra un registro de la tabla `meetings`: fecha, duración, lugar, asistentes, agenda, resumen, notas, stakeholders vinculados y estado, con acciones de Editar y Archivar. La página de servidor construye además un panel de trazabilidad bidireccional: lee `traceability_links` donde la reunión es origen o destino, resuelve títulos desde `decisions`, `meetings`, `communication_items` y `documents`, y entrega catálogos para que el diálogo "Agregar vínculo" conecte la reunión con decisiones, otras reuniones, comunicaciones o documentos, con tipo de vínculo (relacionado, causado por, depende de, reemplaza, derivado de, contradice) y notas de contexto. Destacan dos funciones de IA: "Extraer decisiones" y "Extraer acciones" procesan las notas y la agenda con `runAi`, devuelven sugerencias con nivel de confianza y extracto fuente, y permiten editarlas, aprobarlas o rechazarlas; al aprobar se insertan en `decisions` o `action_items` y se crea un `traceability_link` hacia la reunión, con registro de auditoría (`logAudit`). Relacionadas: lista de Reuniones, Decisiones, Comunicaciones, Documentos.
Fuente: meetings/[meetingId]/page.tsx, meeting-detail-client.tsx, ai-extract-actions.ts, ai-action-extract-actions.ts.
Verifica: abre un proyecto, entra a Reuniones y pulsa cualquier reunión.', '/projects/[projectId]/meetings/[meetingId] — generated from: src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/meeting-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/ai-extract-actions.ts, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/ai-action-extract-actions.ts'),
    ('screen-project-meetings', 'Project Meetings screen', 'The Meetings screen is the classic meeting log of a project, reached from the project workspace at /projects/[projectId]/meetings. The server page verifies the project belongs to the user''s organization, then loads all non-deleted rows from the `meetings` table (newest `meeting_date` first) plus the project''s `stakeholders` for a multi-select. The `MeetingListClient` renders a header with a "Create" button, a filter bar (status: scheduled / in_progress / completed / cancelled, plus date-from and date-to), and meeting cards that link to the meeting detail screen. `CreateMeetingDialog` and `EditMeetingDialog` capture title, agenda, notes, summary, date, duration, location, attendees (free text), status and linked stakeholders. Server actions in `actions.ts` (createMeetingAction, updateMeetingAction, archiveMeetingAction) validate with Zod, write to `meetings` using per-locale i18n JSON fields and `linked_stakeholder_ids`, then fire-and-forget generate a semantic-search embedding and emit a Living Graph `communication_flow` event via `emitAndAutoLink`. Archiving is a soft delete with confirmation. Note the Product Brain designates Rhythm Center (/rhythm) as the canonical meeting module; this screen is the simpler standalone log. Related screens: meeting detail, Rhythm Center, Communications.
Source: meetings/page.tsx, meeting-list-client.tsx, meetings/actions.ts.
Verify: open a project and go to Meetings (/projects/[projectId]/meetings).', 'Pantalla Reuniones del proyecto', 'La pantalla de Reuniones es la bitácora clásica de reuniones de un proyecto, accesible desde el espacio del proyecto en /projects/[projectId]/meetings. La página de servidor verifica que el proyecto pertenezca a la organización del usuario y carga las filas no eliminadas de la tabla `meetings` (ordenadas por `meeting_date` descendente) junto con los `stakeholders` del proyecto para un selector múltiple. El componente `MeetingListClient` muestra un encabezado con botón "Crear", una barra de filtros (estado: programada / en curso / completada / cancelada, más fecha desde y hasta) y tarjetas de reunión que enlazan al detalle. Los diálogos de creación y edición capturan título, agenda, notas, resumen, fecha, duración, lugar, asistentes (texto libre), estado y stakeholders vinculados. Las acciones de servidor (createMeetingAction, updateMeetingAction, archiveMeetingAction) validan con Zod, escriben en `meetings` con campos i18n por idioma y `linked_stakeholder_ids`, y de forma asíncrona generan un embedding para búsqueda semántica y emiten un evento `communication_flow` del Living Graph. Archivar es un borrado lógico con confirmación. El Product Brain designa al Rhythm Center (/rhythm) como módulo canónico de reuniones; esta pantalla es la bitácora simple. Relacionadas: detalle de reunión, Rhythm Center, Comunicaciones.
Fuente: meetings/page.tsx, meeting-list-client.tsx, meetings/actions.ts.
Verifica: abre un proyecto y entra a Reuniones (/projects/[projectId]/meetings).', '/projects/[projectId]/meetings — generated from: src/app/[locale]/(app)/projects/[projectId]/meetings/page.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/meeting-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/actions.ts, src/components/meetings/index.ts, src/components/meetings/create-meeting-dialog.tsx, src/components/meetings/edit-meeting-dialog.tsx, src/components/meetings/meeting-card.tsx, src/components/meetings/meeting-filters.tsx, src/components/meetings/meeting-status-badge.tsx'),
    ('screen-project-memory', 'Project Memory screen', 'Per-project institutional memory (CAP-006/CAP-008), opened from the project''s Memory tab. The server page verifies the project belongs to the org (404 otherwise) and loads up to 500 `project_memory_items` with their `traceability_links` (`source_type` "memory"), the linkable entities (`roadmap_tasks`, `milestones`, `risks`, `stakeholders`, `decisions`, `documents`, `communication_items`, `meetings`) and the counts for the secondary tabs. Seven tabs: Memory (the capture timeline), Overview (a merged read-only activity feed), Communications, Meetings, Decisions, Documents and Search. The "ProjectOps Scribe" button opens the capture modal: type, paste or Dictate via the browser Web Speech API; `analyzeScribeAction` extracts action items, decisions, follow-ups and risks with verbatim source excerpts, and only items the user approves are written by `saveScribeEntryAction`. Note what Scribe actually creates — approved action items become **`project_backlog_items`**, tagged with the project''s `project_delivery_frameworks` row so they surface under Delivery → Refinement, not `roadmap_tasks`; decisions go to `decisions` and risks to `risks`. Every capture carries a client-generated `captureOperationId` used as the idempotency anchor (`…:item:N`) so a retried save cannot duplicate risks; the action refuses to create entities without it. Provenance is recorded in `project_scribe_items` and, where the link type allows, in `traceability_links`. Item actions: create, update, archive, reclassify with AI, reindex, link/unlink, and an artifacts view (`getMemoryArtifactsAction`) showing what a note produced. Indexing runs fire-and-forget via pgvector (`ai_status`/`index_status`); `searchMemoryAction` combines the `match_documents` RPC with keyword search scoped to org and project. `?item=` deep-links an item.
Source: memory/{page,memory-client,actions,scribe-actions,search-action}, src/components/memory/scribe-modal.tsx.
Verify: open a project, go to /projects/[projectId]/memory and click "ProjectOps Scribe".', 'Pantalla Memoria del Proyecto', 'Memoria institucional por proyecto (CAP-006/CAP-008), accesible desde la pestaña Memoria del proyecto. La página de servidor comprueba que el proyecto pertenezca a la organización (si no, devuelve 404) y carga hasta 500 `project_memory_items` con sus `traceability_links` (`source_type` "memory"), las entidades vinculables (`roadmap_tasks`, `milestones`, `risks`, `stakeholders`, `decisions`, `documents`, `communication_items`, `meetings`) y los conteos de las pestañas secundarias. Siete pestañas: Memoria (la línea de tiempo de capturas), Resumen (un feed de actividad combinado y de solo lectura), Comunicaciones, Reuniones, Decisiones, Documentos y Buscar. El botón "ProjectOps Scribe" abre el modal de captura: escribir, pegar o Dictar con la Web Speech API del navegador; `analyzeScribeAction` extrae acciones, decisiones, seguimientos y riesgos con extractos textuales del origen, y solo los elementos aprobados los escribe `saveScribeEntryAction`. Conviene saber qué crea realmente Scribe: las acciones aprobadas se convierten en **`project_backlog_items`**, etiquetados con el `project_delivery_frameworks` del proyecto para que aparezcan en Delivery → Refinamiento, no en `roadmap_tasks`; las decisiones van a `decisions` y los riesgos a `risks`. Cada captura lleva un `captureOperationId` generado en el cliente que sirve de ancla de idempotencia (`…:item:N`) para que un reintento no duplique riesgos; la acción se niega a crear entidades sin él. La procedencia queda en `project_scribe_items` y, cuando el tipo de enlace lo permite, en `traceability_links`. Acciones sobre elementos: crear, actualizar, archivar, reclasificar con IA, reindexar, vincular o desvincular, y una vista de artefactos (`getMemoryArtifactsAction`) que muestra qué produjo cada nota. El indexado corre en segundo plano sobre pgvector (`ai_status`/`index_status`); `searchMemoryAction` combina el RPC `match_documents` con búsqueda por palabras clave acotada a organización y proyecto. `?item=` abre un elemento concreto.
Fuente: memory/{page,memory-client,actions,scribe-actions,search-action}, src/components/memory/scribe-modal.tsx.
Verifica: abre un proyecto, ve a /projects/[projectId]/memory y pulsa "ProjectOps Scribe".', '/projects/[projectId]/memory — generated from: src/app/[locale]/(app)/projects/[projectId]/memory/page.tsx, src/app/[locale]/(app)/projects/[projectId]/memory/memory-client.tsx, src/app/[locale]/(app)/projects/[projectId]/memory/actions.ts, src/app/[locale]/(app)/projects/[projectId]/memory/scribe-actions.ts, src/app/[locale]/(app)/projects/[projectId]/memory/search-action.ts, src/components/memory/scribe-modal.tsx, src/components/memory/memory-detail-panel.tsx'),
    ('screen-project-overview', 'Project Overview (Command Center) screen', 'The project home, reached by opening any project from the projects list. `ProjectHeaderClient` shows title, status, type and dates with Edit and Archive; owners and admins additionally get **Delete permanently**, a two-step dialog that first calls `getProjectDeletionImpactAction` to show exactly how many tasks, milestones, dependencies and `project_event_log` rows will be destroyed, then `deleteProjectPermanentlyAction` — the only sanctioned way to remove append-only event-log rows, recorded in `compliance_archive.project_purges`. Below sit three navigation strips: Charter & Governance (status, completion %, locked state), Delivery Framework (method, active cycle, open scope-creep alerts — hidden entirely when no framework row exists) and Team & Roles (member count, missing critical roles, score). An AI Communication Summary generates on demand. `ProjectDashboard` renders stat cards, recent-item lists, unresolved action items, traceability health, a roadmap snapshot, an effort block (estimated / logged / remaining / variance from `getProjectEffortSummary`, where actual hours are summed from `subtask_time_entries` and never typed in) and a deterministic Status card built with `buildProjectBriefing` (REG-013/REG-015) where waiting on a dependency is never counted as blocked. Reads `projects`, `project_charters`, `project_delivery_frameworks`, `project_scope_creep_alerts`, `project_execution_cycles`, `communication_items`, `meetings`, `decisions`, `documents`, `action_items`, `milestones`, `roadmap_tasks`, `task_dependencies`, `risks`, `traceability_links`. Related: screen-project-workboard, screen-project-status, screen-project-charter, screen-project-delivery, screen-project-team.
Source: projects/[projectId]/page.tsx, dashboard-client.tsx, project-header-client.tsx, lib/project-briefing/briefing-engine.ts.
Verify: open a project from the projects list; the Command Center is the landing tab.', 'Pantalla Resumen del Proyecto (Command Center)', 'La página de inicio del proyecto; se llega al abrir cualquier proyecto desde la lista. `ProjectHeaderClient` muestra título, estado, tipo y fechas con Editar y Archivar; además, los roles owner y admin ven **Eliminar permanentemente**, un diálogo de dos pasos que primero llama a `getProjectDeletionImpactAction` para mostrar cuántas tareas, hitos, dependencias y filas de `project_event_log` se destruirán, y luego a `deleteProjectPermanentlyAction`: la única vía autorizada para borrar filas del registro de eventos de solo anexado, que queda asentada en `compliance_archive.project_purges`. Debajo hay tres franjas de navegación: Charter y Gobernanza (estado, % de completitud, si está aprobado), Marco de Ejecución (método, ciclo activo, alertas de scope creep abiertas; se oculta por completo si no existe la fila del marco) y Equipo y Roles (miembros, roles críticos faltantes, puntaje). El Resumen de Comunicaciones con IA se genera bajo demanda. `ProjectDashboard` muestra tarjetas de conteo, listas de elementos recientes, acciones sin resolver, salud de trazabilidad, un snapshot del roadmap, un bloque de esfuerzo (estimado / registrado / restante / variación desde `getProjectEffortSummary`, donde las horas reales se suman de `subtask_time_entries` y nunca se escriben a mano) y una tarjeta de Estado determinista calculada con `buildProjectBriefing` (REG-013/REG-015), donde esperar una dependencia nunca cuenta como bloqueo. Lee `projects`, `project_charters`, `project_delivery_frameworks`, `project_scope_creep_alerts`, `project_execution_cycles`, `communication_items`, `meetings`, `decisions`, `documents`, `action_items`, `milestones`, `roadmap_tasks`, `task_dependencies`, `risks` y `traceability_links`. Relacionadas: screen-project-workboard, screen-project-status, screen-project-charter, screen-project-delivery, screen-project-team.
Fuente: projects/[projectId]/page.tsx, dashboard-client.tsx, project-header-client.tsx, lib/project-briefing/briefing-engine.ts.
Verifica: abre un proyecto desde la lista; el Command Center es la pestaña inicial.', '/projects/[projectId] — generated from: src/app/[locale]/(app)/projects/[projectId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/dashboard-client.tsx, src/app/[locale]/(app)/projects/[projectId]/project-header-client.tsx, src/app/[locale]/(app)/projects/[projectId]/project-detail-client.tsx, src/app/[locale]/(app)/projects/actions.ts, src/lib/time-tracking/project-effort.ts, src/lib/project-briefing/briefing-engine.ts, src/lib/roadmap/progress.ts'),
    ('screen-project-rhythm-center', 'Project Rhythm Center screen', 'The Rhythm Center (/projects/[projectId]/rhythm) is the canonical calendar-plus-meetings module — not to be confused with /rythm (no "h"), the retired Rythm Meeting Intelligence route that now merely redirects here (REG-011). The server page loads `project_events`, `meetings` linked by event_id, `meeting_attendees`, meeting-linked `decisions` and `action_items`, `project_memory_items` (source_system "rhythm_center", used to badge memory-synced meetings) and `stakeholders`. `RhythmClient` offers List and Calendar views, filters (event type, status, date range), an "Upcoming events" panel and a "New meeting" dialog that creates a meeting from templates (kickoff, status_update, stakeholder_review, project_review, closing, other), auto-generating agenda, objective and expected outcome. Clicking an event opens the `MeetingDrawer`: AI Summary, agenda sections with per-section notes, general notes, decisions, action items and attendees, plus "Generate summary" (runAi) and "Complete & save to memory", which calls `completeMeeting` in `lib/rhythm/service` to write `project_memory_items`. Server actions cover event CRUD, `createMeetingFromTemplateAction` (inserts event + meeting + attendees), attendee CRUD, adding decisions/action items, and audit logging. Related screens: Meetings, Workboard, Decisions, Project Memory, Closeout.
Source: rhythm/page.tsx, rhythm-client.tsx, meeting-drawer.tsx, rhythm/actions.ts, lib/rhythm/service.ts.
Verify: open a project and go to Rhythm Center (/projects/[projectId]/rhythm).', 'Pantalla Centro de Ritmo del proyecto', 'El Centro de Ritmo (/projects/[projectId]/rhythm) es el módulo canónico de calendario y reuniones — no confundir con /rythm (sin "h"), la ruta retirada de Rythm Meeting Intelligence que hoy solo redirige aquí (REG-011). La página de servidor carga `project_events`, `meetings` enlazadas por event_id, `meeting_attendees`, `decisions` y `action_items` ligados a reuniones, `project_memory_items` (source_system "rhythm_center", para marcar reuniones sincronizadas con memoria) y `stakeholders`. `RhythmClient` ofrece vistas Lista y Calendario, filtros (tipo de evento, estado, rango de fechas), un panel de "Próximos eventos" y un diálogo "Nueva reunión" que crea reuniones desde plantillas (arranque, actualización de estado, revisión con stakeholders, revisión de proyecto, cierre, otra), generando automáticamente agenda, objetivo y resultado esperado. Al pulsar un evento se abre el `MeetingDrawer`: Resumen IA, secciones de agenda con notas por sección, notas generales, decisiones, acciones y asistentes, más "Generar resumen" (runAi) y "Completar y guardar en memoria", que llama a `completeMeeting` en `lib/rhythm/service` para escribir en `project_memory_items`. Las acciones de servidor cubren CRUD de eventos, `createMeetingFromTemplateAction` (inserta evento + reunión + asistentes), CRUD de asistentes, alta de decisiones/acciones y auditoría. Relacionadas: Reuniones, Workboard, Decisiones, Memoria del Proyecto, Closeout.
Fuente: rhythm/page.tsx, rhythm-client.tsx, meeting-drawer.tsx, rhythm/actions.ts, lib/rhythm/service.ts.
Verifica: abre un proyecto y entra al Centro de Ritmo (/projects/[projectId]/rhythm).', '/projects/[projectId]/rhythm — generated from: src/app/[locale]/(app)/projects/[projectId]/rhythm/page.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/rhythm-client.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/meeting-drawer.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/actions.ts, src/lib/rhythm/templates.ts, src/lib/rhythm/service.ts'),
    ('screen-project-roadmap', 'Project Roadmap screen (redirect to Execution Map)', 'The `/roadmap` route still renders no UI of its own: its `page.tsx` is nothing but a server `redirect` to `/projects/[projectId]/execution-map`, preserving the locale through `localizedHref`. Anyone who opens the Roadmap URL — a bookmark, an old link, an Isabella suggestion — lands on the Execution Map, which loads `projects`, `milestones`, `roadmap_tasks` and `task_dependencies`, computes per-milestone counts, overall progress and a recommended next step, and additionally evaluates the KPIs pinned to each milestone through `loadMilestoneKpis`. The folder nevertheless remains central, because `roadmap/actions.ts` holds the canonical server actions for the whole task and milestone domain: `createTaskAction`, `updateTaskAction` (preserve-on-absent), `updateTaskStatusAction` with `dependency_not_met` enforcement and blocker reasons, `reorderTasksAction`, `createMilestoneAction`, `updateMilestoneAction`, `reorderMilestoneAction`, `archiveTaskAction`, `archiveMilestoneAction`, `recordPromptSentAction`, `getTaskAuditTrailAction`, plus `getTaskFormOptionsAction` and `createPersonResourceAction`, which feed the task form''s people, resources, team, materials and dependency pickers. Between them they read and write `milestones`, `roadmap_tasks`, `task_dependencies`, `resources`, `project_team_members`, `material_requirements`, `profiles` and `audit_logs`, always scoped by organization and project. The Workboard, the Execution Map and the Living Graph all import from here. Related: screen-execution-map, screen-project-workboard, screen-living-graph.
Source: roadmap/page.tsx (redirect only), roadmap/actions.ts, execution-map/page.tsx.
Verify: open a project and visit /projects/{id}/roadmap; the browser lands on /projects/{id}/execution-map.', 'Pantalla Roadmap del proyecto (redirección al Mapa de Ejecución)', 'La ruta `/roadmap` sigue sin tener interfaz propia: su `page.tsx` es únicamente un `redirect` de servidor a `/projects/[projectId]/execution-map`, conservando el idioma mediante `localizedHref`. Quien abra la URL del Roadmap —un marcador, un enlace antiguo o una sugerencia de Isabella— aterriza en el Mapa de Ejecución, que carga `projects`, `milestones`, `roadmap_tasks` y `task_dependencies`, calcula los conteos por hito, el progreso general y el siguiente paso recomendado, y además evalúa los KPIs fijados a cada hito con `loadMilestoneKpis`. Aun así la carpeta sigue siendo central, porque `roadmap/actions.ts` contiene las acciones de servidor canónicas de todo el dominio de tareas e hitos: `createTaskAction`, `updateTaskAction` (conserva los valores ausentes), `updateTaskStatusAction` con validación `dependency_not_met` y motivos de bloqueo, `reorderTasksAction`, `createMilestoneAction`, `updateMilestoneAction`, `reorderMilestoneAction`, `archiveTaskAction`, `archiveMilestoneAction`, `recordPromptSentAction`, `getTaskAuditTrailAction`, además de `getTaskFormOptionsAction` y `createPersonResourceAction`, que alimentan los selectores de personas, recursos, equipo, materiales y dependencias del formulario de tarea. Entre todas leen y escriben `milestones`, `roadmap_tasks`, `task_dependencies`, `resources`, `project_team_members`, `material_requirements`, `profiles` y `audit_logs`, siempre acotadas por organización y proyecto. El Workboard, el Mapa de Ejecución y el Grafo Vivo importan desde aquí. Relacionadas: screen-execution-map, screen-project-workboard, screen-living-graph.
Fuente: roadmap/page.tsx (solo redirección), roadmap/actions.ts, execution-map/page.tsx.
Verifica: abre un proyecto y visita /projects/{id}/roadmap; el navegador termina en /projects/{id}/execution-map.', '/projects/[projectId]/roadmap — generated from: src/app/[locale]/(app)/projects/[projectId]/roadmap/page.tsx, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts, src/app/[locale]/(app)/projects/[projectId]/execution-map/page.tsx'),
    ('screen-project-rythm-intelligence', 'Rythm Meeting Intelligence screen (redirect alias)', 'Important naming note: /rythm (no "h") is NOT the Rhythm Center. It was the standalone "Rythm Meeting Intelligence" module — audio-focused meetings (in-person recording, video call, uploaded audio) with a transcription pipeline (draft, recording, audio_uploaded, ready_for_transcription, transcribing, transcribed, summary_ready, failed), backed by `lib/rythm/meeting-service` and tables like `project_rythm_audio_files`, transcripts and processing jobs. Per REG-011 in the regression log, the standalone dashboard queried a `project_rythm_meetings` table that never reached production and crashed, so today `rythm/page.tsx` contains no UI at all: it is a backward-compatible alias that immediately calls `redirect()` to the canonical Rhythm Center at /projects/[projectId]/rhythm, preserving old bookmarks and deep links. The dormant `rythm/actions.ts` (createMeetingAction, registerAudioAction, status updates, processing jobs) and `lib/rythm` remain in the repo but are not reachable from this route. Anyone asking about "Rythm" screens should be pointed to the Rhythm Center, where the meeting capability now lives. Related screens: Rhythm Center (/rhythm), Meetings.
Source: rythm/page.tsx, rythm/actions.ts, docs/product-brain/04-module-map.md.
Verify: navigating to /projects/[projectId]/rythm redirects to /projects/[projectId]/rhythm.', 'Pantalla Rythm Meeting Intelligence (alias de redirección)', 'Nota importante de nombres: /rythm (sin "h") NO es el Centro de Ritmo. Era el módulo independiente "Rythm Meeting Intelligence" — reuniones centradas en audio (grabación presencial, videollamada, audio subido) con un pipeline de transcripción (draft, recording, audio_uploaded, ready_for_transcription, transcribing, transcribed, summary_ready, failed), apoyado en `lib/rythm/meeting-service` y tablas como `project_rythm_audio_files`, transcripciones y trabajos de procesamiento. Según REG-011 del registro de regresiones, el tablero independiente consultaba una tabla `project_rythm_meetings` que nunca llegó a producción y fallaba; por eso hoy `rythm/page.tsx` no contiene interfaz alguna: es un alias retrocompatible que llama de inmediato a `redirect()` hacia el Centro de Ritmo canónico en /projects/[projectId]/rhythm, preservando marcadores y enlaces antiguos. El archivo `rythm/actions.ts` (crear reunión, registrar audio, estados, trabajos de procesamiento) y `lib/rythm` siguen en el repositorio pero inactivos desde esta ruta. Ante preguntas sobre "Rythm", dirige al usuario al Centro de Ritmo, donde vive hoy la capacidad de reuniones. Relacionadas: Centro de Ritmo (/rhythm), Reuniones.
Fuente: rythm/page.tsx, rythm/actions.ts, docs/product-brain/04-module-map.md.
Verifica: al navegar a /projects/[projectId]/rythm se redirige a /projects/[projectId]/rhythm.', '/projects/[projectId]/rythm — generated from: src/app/[locale]/(app)/projects/[projectId]/rythm/page.tsx, src/app/[locale]/(app)/projects/[projectId]/rythm/actions.ts, src/lib/rythm/index.ts, src/lib/rythm/meeting-service.ts, src/lib/rythm/recording-service.ts, src/lib/rythm/storage-service.ts, src/lib/rythm/types.ts, docs/product-brain/04-module-map.md'),
    ('screen-project-rythm-meeting-detail', 'Rythm meeting detail screen (redirect alias)', 'This route (/projects/[projectId]/rythm/[meetingId], "rythm" without the "h") was the detail view of a single meeting in the retired Rythm Meeting Intelligence module — the audio/transcription-oriented meeting surface, distinct from the Rhythm Center calendar module at /rhythm. According to the REG-011 consolidation documented in the file itself and in the product brain''s regression log, this detail surface queried the `project_rythm_meetings` table, which never reached production, so the page crashed with a server error. The current `rythm/[meetingId]/page.tsx` therefore renders nothing: it is a pure backward-compatibility alias whose only code is a `redirect()` to the canonical Rhythm Center at /projects/[projectId]/rhythm (the meetingId segment is accepted but not used to deep-link into a specific event). Old bookmarks and deep links to individual Rythm meetings land safely on the Rhythm Center list, where the user can locate the meeting and open its drawer. There is no data read or write on this route beyond resolving locale and projectId for the redirect. Related screens: Rhythm Center (/rhythm), Rythm alias index (/rythm), Meetings and meeting detail under /meetings.
Source: rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md.
Verify: opening /projects/[projectId]/rythm/<any-id> redirects to /projects/[projectId]/rhythm.', 'Pantalla Detalle de reunión Rythm (alias de redirección)', 'Esta ruta (/projects/[projectId]/rythm/[meetingId], "rythm" sin "h") era la vista de detalle de una reunión del módulo retirado Rythm Meeting Intelligence — la superficie de reuniones orientada a audio y transcripción, distinta del módulo de calendario Centro de Ritmo en /rhythm. Según la consolidación REG-011 documentada en el propio archivo y en el registro de regresiones del product brain, esta vista consultaba la tabla `project_rythm_meetings`, que nunca llegó a producción, por lo que la página fallaba con un error de servidor. El `rythm/[meetingId]/page.tsx` actual no muestra nada: es un alias de retrocompatibilidad cuyo único código es un `redirect()` al Centro de Ritmo canónico en /projects/[projectId]/rhythm (el segmento meetingId se acepta pero no se usa para abrir un evento específico). Los marcadores y enlaces antiguos a reuniones Rythm aterrizan sin errores en la lista del Centro de Ritmo, donde el usuario puede ubicar la reunión y abrir su panel. Esta ruta no lee ni escribe datos más allá de resolver locale y projectId para redirigir. Relacionadas: Centro de Ritmo (/rhythm), alias índice de Rythm (/rythm), Reuniones y detalle bajo /meetings.
Fuente: rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md.
Verifica: abrir /projects/[projectId]/rythm/<cualquier-id> redirige a /projects/[projectId]/rhythm.', '/projects/[projectId]/rythm/[meetingId] — generated from: src/app/[locale]/(app)/projects/[projectId]/rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md'),
    ('screen-project-search', 'Project Search screen', 'Cross-entity search inside one project, reached from the project''s Search tab. The server page only verifies the project belongs to the user''s organization and passes translations; all searching happens in the SearchClient through the searchProjectAction server action. A single query box with type filter chips (All, Communication, Meeting, Decision, Document, Task, Memory) runs two strategies in one action: keyword matching (case-insensitive substring with generated snippets) over communication_items, meetings, decisions, documents, roadmap_tasks and project_memory_items, and semantic search — the query is embedded with OpenAI text-embedding-3-small and matched via the match_documents Supabase RPC (pgvector). Semantic hits are listed first sorted by similarity and tagged with a "semantic match" badge; keyword duplicates are removed, then keyword results follow sorted by date. Semantic search is non-fatal: if no API key or the RPC fails, keyword results still return. Each result links to the entity''s detail screen. A second server action, backfillEmbeddingsAction, generates missing embeddings in idempotent batches of 50 for roadmap_tasks, communication_items, meetings, decisions and documents — the only write this screen can trigger (embedding columns). Related screens: Communications, Meetings, Decisions, Documents, Workboard, Project Memory.
Source: search/page.tsx, search/search-client.tsx, search/search-action.ts, search/embedding-backfill-action.ts.
Verify: open a project → Search tab, type a keyword and toggle the type filters.', 'Pantalla Búsqueda del proyecto', 'Búsqueda entre entidades dentro de un proyecto; se llega desde la pestaña Search del proyecto. La página de servidor solo verifica que el proyecto pertenezca a la organización y pasa traducciones; la búsqueda ocurre en el SearchClient mediante la acción de servidor searchProjectAction. Un cuadro de consulta con filtros por tipo (Todos, Comunicación, Reunión, Decisión, Documento, Tarea, Memoria) ejecuta dos estrategias en una sola acción: coincidencia por palabra clave (subcadena sin distinguir mayúsculas, con fragmentos generados) sobre communication_items, meetings, decisions, documents, roadmap_tasks y project_memory_items, y búsqueda semántica: la consulta se convierte en embedding con OpenAI text-embedding-3-small y se compara con la RPC match_documents de Supabase (pgvector). Los resultados semánticos van primero, ordenados por similitud y con la insignia de coincidencia semántica; se eliminan duplicados y luego siguen los resultados por palabra clave ordenados por fecha. La búsqueda semántica no es bloqueante: si falta la clave de API o falla la RPC, se devuelven igual los resultados por palabra clave. Cada resultado enlaza al detalle de su entidad. Una segunda acción, backfillEmbeddingsAction, genera embeddings faltantes en lotes idempotentes de 50; es la única escritura posible desde esta pantalla (columnas de embedding). Relacionadas: Comunicaciones, Reuniones, Decisiones, Documentos, Workboard, Memoria del proyecto.
Fuente: search/page.tsx, search/search-client.tsx, search/search-action.ts, search/embedding-backfill-action.ts.
Verifica: abre un proyecto → pestaña Search, escribe una palabra y prueba los filtros por tipo.', '/projects/[projectId]/search — generated from: src/app/[locale]/(app)/projects/[projectId]/search/page.tsx, src/app/[locale]/(app)/projects/[projectId]/search/search-client.tsx, src/app/[locale]/(app)/projects/[projectId]/search/search-action.ts, src/app/[locale]/(app)/projects/[projectId]/search/embedding-backfill-action.ts'),
    ('screen-project-settings', 'Project Settings screen', 'A lightweight settings hub for one project, reached from the project''s Settings tab. It is navigation, not forms: the page performs no writes at all, and it holds no toggle for anything the project owns. After validating the project against the user''s organization (`notFound()` otherwise), it renders the header "Manage stakeholders, audit trail, and project metadata" and a grid of link cards: Stakeholders, carrying a live `count` from the `stakeholders` table and linking to `/projects/[projectId]/stakeholders`; Audit Log, linking to `/projects/[projectId]/audit`; and Import / Merge Project Data, which leaves the project and opens the org-level import wizard preloaded with this project (`/import?projectId=…`) to upload Excel, CSV or PDF and merge it. A fourth card, GitHub Intelligence, renders only when `isGitHubIntelligenceFlagEnabled()` is true **and** the project''s `project_type` is `software_development`; otherwise it is hidden entirely rather than disabled, so a construction project shows three cards and gives no hint the integration exists. Below, a read-only Project Metadata section shows start date, target end date and creation date formatted per locale — start and target dates are omitted when null. All page copy is inline `isEs` ternaries rather than message keys. Editing, archiving and permanent deletion of the project live on the Overview header, not here. Related: screen-project-stakeholders, screen-project-audit, screen-import, screen-project-github-integration, screen-project-overview.
Source: settings/page.tsx, lib/env.ts.
Verify: open a project → Settings tab; the GitHub card only appears for software projects with the flag enabled.', 'Pantalla Configuración del Proyecto', 'Un centro ligero de configuración por proyecto; se llega desde la pestaña Settings del proyecto. Es navegación, no formularios: la página no escribe absolutamente nada y no contiene ningún interruptor de las propiedades del proyecto. Tras validar que el proyecto pertenece a la organización del usuario (si no, `notFound()`), muestra el encabezado "Gestiona stakeholders, auditoría y metadatos del proyecto" y una cuadrícula de tarjetas de enlace: Stakeholders, con un conteo en vivo de la tabla `stakeholders` y enlace a `/projects/[projectId]/stakeholders`; Registro de Auditoría, con enlace a `/projects/[projectId]/audit`; e Importar / Fusionar datos, que sale del proyecto y abre el asistente de importación de la organización precargado con este proyecto (`/import?projectId=…`) para subir Excel, CSV o PDF y fusionarlos. Una cuarta tarjeta, GitHub Intelligence, aparece solo cuando `isGitHubIntelligenceFlagEnabled()` es verdadero **y** el `project_type` del proyecto es `software_development`; en cualquier otro caso queda oculta por completo en lugar de deshabilitada, así que un proyecto de construcción ve tres tarjetas y no recibe ninguna pista de que la integración existe. Debajo, la sección de Metadatos del Proyecto es de solo lectura y muestra fecha de inicio, fecha objetivo y fecha de creación según el idioma; las dos primeras se omiten si son nulas. Todos los textos son ternarios `isEs` en línea, no claves de traducción. Editar, archivar y eliminar permanentemente el proyecto viven en el encabezado del Resumen, no aquí. Relacionadas: screen-project-stakeholders, screen-project-audit, screen-import, screen-project-github-integration, screen-project-overview.
Fuente: settings/page.tsx, lib/env.ts.
Verifica: abre un proyecto → pestaña Settings; la tarjeta de GitHub solo aparece en proyectos de software con el flag activo.', '/projects/[projectId]/settings — generated from: src/app/[locale]/(app)/projects/[projectId]/settings/page.tsx, src/lib/env.ts'),
    ('screen-project-stakeholders', 'Project Stakeholders screen', 'The project stakeholder registry. The server page fans out two queries in parallel: the project row (org-scoped) and all non-deleted rows from the stakeholders table ordered by name, then hands them to StakeholderListClient with localized labels. The client shows a title, description and a create button that opens CreateStakeholderDialog; created stakeholders are appended to the list. Stakeholders render in a responsive card grid (2 columns on small screens, 3 on large): each StakeholderCard shows the person with influence and interest badges mapped to high/medium/low localized labels (InfluenceBadge component), plus edit and archive actions — archiving asks for confirmation and soft-deletes the row. When the list is empty, a dashed empty state invites creating the first stakeholder. Data: reads and writes only the stakeholders table (per-project, org-scoped, soft delete via deleted_at). Stakeholders registered here feed other governance screens: the decisions module links decisions to stakeholders, the Charter suggests them in governance roles through the unified People Directory, and Project Memory offers them as linkable entities.
Source: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, stakeholder-list-client.tsx, src/components/stakeholders/*.
Verify: open a project and go to /projects/[projectId]/stakeholders.', 'Pantalla Stakeholders del Proyecto', 'El registro de interesados del proyecto. La página de servidor lanza dos consultas en paralelo: la fila del proyecto (acotada a la organización) y todas las filas no eliminadas de la tabla stakeholders ordenadas por nombre, y las entrega a StakeholderListClient con etiquetas localizadas. El cliente muestra título, descripción y un botón de crear que abre CreateStakeholderDialog; los creados se agregan a la lista. Los stakeholders se muestran en una cuadrícula de tarjetas adaptable (2 columnas en pantallas pequeñas, 3 en grandes): cada StakeholderCard muestra a la persona con insignias de influencia e interés mapeadas a etiquetas alta/media/baja (componente InfluenceBadge), más acciones de editar y archivar — archivar pide confirmación y hace borrado suave. Con la lista vacía, un estado vacío con borde punteado invita a crear el primer stakeholder. Datos: lee y escribe solo la tabla stakeholders (por proyecto, acotada a la organización, borrado suave con deleted_at). Los stakeholders registrados aquí alimentan otras pantallas de gobernanza: el módulo de decisiones vincula decisiones a stakeholders, el Charter los sugiere en roles de gobernanza mediante el Directorio de Personas unificado, y la Memoria del Proyecto los ofrece como entidades vinculables.
Fuente: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, stakeholder-list-client.tsx, src/components/stakeholders/*.
Verifica: abre un proyecto y ve a /projects/[projectId]/stakeholders.', '/projects/[projectId]/stakeholders — generated from: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, src/app/[locale]/(app)/projects/[projectId]/stakeholders/stakeholder-list-client.tsx, src/components/stakeholders/stakeholder-card.tsx, src/components/stakeholders/create-stakeholder-dialog.tsx'),
    ('screen-project-status', 'Project Status Report screen', 'A plain-language, printable status report, reached from the project''s Status tab (also surfaced from the Command Center''s Reports & Executive Outputs area). The server page loads milestones, roadmap_tasks, task_dependencies, material_requirements, a budget_items count, profiles and resources (for owner names) plus the project_charters goal/status for context, and feeds everything into the deterministic buildStatusReport engine (lib/execution/status-report.ts) — no AI is involved. The report shows a headline sentence, a completion progress ring and stat buckets (done/started/blocked/not started), the milestone phases with completed/in-progress/upcoming states, blocked tasks surfaced first with their blocker_reason, attention items (blocked work, missing info), materials status and a daily plan with action types unblock/do_now/start/assign. Crucially, tasks whose only issue is an unfinished predecessor are counted as "waiting", never as blocked — blocked means an explicit impediment, matching the product''s independent status dimensions. The only button is "Download PDF", which uses the browser print dialog via printWithFilename with a generated document name; print CSS isolates the #status-report-print block. The screen is read-only and writes nothing. Related screens: Command Center (Status card uses the briefing engine), Closeout Report, Workboard.
Source: status/page.tsx, status/status-report-client.tsx, lib/execution/status-report.ts.
Verify: open a project → Status tab; click Download PDF to print.', 'Pantalla Reporte de Estado del proyecto', 'Un reporte de estado en lenguaje claro e imprimible; se llega desde la pestaña Status del proyecto (también enlazado desde el área de reportes del Command Center). El servidor carga milestones, roadmap_tasks, task_dependencies, material_requirements, el conteo de budget_items, profiles y resources (nombres de responsables) y el objetivo/estado del charter (project_charters) como contexto, y lo pasa al motor determinista buildStatusReport (lib/execution/status-report.ts); no interviene IA. El reporte muestra una frase titular, un anillo de progreso y contadores (hechas/iniciadas/bloqueadas/sin iniciar), las fases por hito con estados completado/en curso/próximo, las tareas bloqueadas primero con su blocker_reason, elementos de atención (trabajo bloqueado, información faltante), estado de materiales y un plan diario con acciones de tipo desbloquear/hacer ahora/iniciar/asignar. Un punto clave: las tareas cuyo único problema es una predecesora sin terminar se cuentan como "en espera", nunca como bloqueadas; bloqueado implica un impedimento explícito, acorde con las dimensiones de estado independientes del producto. El único botón es "Descargar PDF", que usa el diálogo de impresión del navegador (printWithFilename) con un nombre de documento generado. La pantalla es de solo lectura y no escribe nada. Relacionadas: Command Center, Reporte de Cierre, Workboard.
Fuente: status/page.tsx, status/status-report-client.tsx, lib/execution/status-report.ts.
Verifica: abre un proyecto → pestaña Status; usa Descargar PDF para imprimir.', '/projects/[projectId]/status — generated from: src/app/[locale]/(app)/projects/[projectId]/status/page.tsx, src/app/[locale]/(app)/projects/[projectId]/status/status-report-client.tsx, src/lib/execution/status-report.ts'),
    ('screen-project-task-detail', 'Task Execution Map (task detail) screen', 'The drill-down for one task, reached from a Workboard card''s Execution Map link; a "Back to Workboard" breadcrumb returns there. Where the Living Graph is the project-level visualization, this is the task-level mind map: a parent node (title, status, progress, owner, critical flag, estimate vs actual hours), its subtasks, and its external predecessor tasks drawn as dotted dependency nodes. The server page reads one `roadmap_tasks` row, its `task_subtasks` (sorted by `sort_order` then `created_at`), the `task_dependencies` rows where this task is the successor, then the predecessor titles. Owner display names come from `profiles` through the admin client — names only, because RLS hides other users'' rows and an assignee may be a cross-org member. The assignable-owner dropdown comes from `getAssignableProjectOwners` (org workspace users plus project team members) merged with everyone currently assigned, so the real owner is never missing from the list even after leaving the team. `ExecutionMapClient` offers a canvas view and a table view, a subtask detail panel and a form dialog. Writes go through `lib/subtasks/actions.ts`: create, update, complete, block/unblock with a reason (an explicit impediment, distinct from waiting on a dependency), reassign, delete, override or clear the parent''s calculated progress, and close the parent with incomplete subtasks. Editing requires org `owner` or `admin` (`canManage`); `viewer` cannot upload. Related: screen-project-workboard, screen-execution-map, screen-living-graph.
Source: tasks/[taskId]/page.tsx, components/task-execution-map/execution-map-client.tsx, lib/subtasks/actions.ts.
Verify: Workboard → open a card''s Execution Map link.', 'Pantalla Mapa de Ejecución de tarea (detalle de tarea)', 'El detalle de una tarea; se llega desde el enlace Execution Map de una tarjeta del Workboard, con una miga "Volver al Workboard" para regresar. Mientras el Grafo Vivo es la visualización a nivel de proyecto, esta es el mapa mental de UNA tarea: un nodo padre (título, estado, progreso, responsable, marca de ruta crítica, horas estimadas y reales), sus subtareas y sus tareas predecesoras externas dibujadas como nodos punteados de dependencia. La página de servidor lee una fila de `roadmap_tasks`, sus `task_subtasks` (ordenadas por `sort_order` y luego `created_at`), las filas de `task_dependencies` donde esta tarea es la sucesora y después los títulos de las predecesoras. Los nombres de responsables provienen de `profiles` mediante el cliente admin — solo nombres, porque RLS oculta las filas de otros usuarios y un asignado puede pertenecer a otra organización. La lista de responsables asignables sale de `getAssignableProjectOwners` (usuarios del workspace más miembros del equipo del proyecto) combinada con quienes ya están asignados, de modo que el responsable real nunca desaparece del desplegable aunque haya dejado el equipo. `ExecutionMapClient` ofrece vista de lienzo y de tabla, panel de detalle y formulario de subtareas. Las escrituras pasan por `lib/subtasks/actions.ts`: crear, actualizar, completar, bloquear y desbloquear con motivo (un impedimento explícito, distinto de esperar una dependencia), reasignar, eliminar, sobrescribir o limpiar el progreso calculado del padre y cerrar el padre con subtareas incompletas. Editar exige rol `owner` o `admin` (`canManage`); un `viewer` no puede subir archivos. Relacionadas: screen-project-workboard, screen-execution-map, screen-living-graph.
Fuente: tasks/[taskId]/page.tsx, components/task-execution-map/execution-map-client.tsx, lib/subtasks/actions.ts.
Verifica: Workboard → enlace Execution Map de una tarjeta de tarea.', '/projects/[projectId]/tasks/[taskId] — generated from: src/app/[locale]/(app)/projects/[projectId]/tasks/[taskId]/page.tsx, src/components/task-execution-map/execution-map-client.tsx, src/lib/subtasks/actions.ts, src/lib/subtasks/map-model.ts, src/lib/people/service.ts'),
    ('screen-project-team', 'Project Team & Roles screen', 'The Team screen (/projects/[projectId]/team) is the Project Team & Roles Center. The server page uses `lib/team-roles/service` to load the project team (`project_team_members`), RACI assignments (`project_raci_assignments`), stakeholder access (`stakeholder_access`), the company directory and company teams (`organization_team_members` + `profiles`), external contacts and the project''s `milestones`, then computes a team-completeness score (has PM, has approver, missing critical roles). `TeamClient` has three tabs: "Members & roles" (with a List view and a drag-and-drop Role Assignment Board built on dnd-kit, where people from the directory are dragged into role buckets with optimistic UI and undo), "RACI", and "Stakeholder access". Server actions cover adding individual members or whole company teams, editing/removing/restoring members, assigning and moving people between roles (with server-side dedup), AI role recommendations (recommendRolesAction / addRecommendedRolesAction), RACI add/delete plus generateRaciDraftAction, and granting or revoking read-only stakeholder viewers. A code comment confirms project teams are operational: adding people here never creates a paid seat. Related screens: Stakeholders, Organization members/teams, Charter governance roles, Resources.
Source: team/page.tsx, team-client.tsx, role-board.tsx, team/actions.ts, lib/team-roles/service.ts.
Verify: open a project and go to Team (/projects/[projectId]/team).', 'Pantalla Equipo y roles del proyecto', 'La pantalla de Equipo (/projects/[projectId]/team) es el Centro de Equipo y Roles del proyecto. La página de servidor usa `lib/team-roles/service` para cargar el equipo (`project_team_members`), asignaciones RACI (`project_raci_assignments`), accesos de stakeholders (`stakeholder_access`), el directorio y los equipos de la empresa (`organization_team_members` + `profiles`), contactos externos y los `milestones` del proyecto, y calcula un puntaje de completitud del equipo (hay PM, hay aprobador, roles críticos faltantes). `TeamClient` tiene tres pestañas: "Miembros y roles" (con vista Lista y un tablero de asignación de roles con arrastrar y soltar basado en dnd-kit, donde se arrastran personas del directorio a cubetas de rol, con interfaz optimista y deshacer), "RACI" y "Acceso de stakeholders". Las acciones de servidor permiten agregar miembros individuales o equipos completos de la empresa, editar/quitar/restaurar miembros, asignar y mover personas entre roles (con deduplicación en servidor), recomendaciones de roles con IA (recommendRolesAction / addRecommendedRolesAction), alta/baja de RACI más generateRaciDraftAction, y otorgar o revocar visores de solo lectura para stakeholders. Un comentario del código confirma que agregar personas aquí nunca crea un asiento de pago. Relacionadas: Stakeholders, Miembros/equipos de la organización, roles de gobernanza del Charter, Recursos.
Fuente: team/page.tsx, team-client.tsx, role-board.tsx, team/actions.ts, lib/team-roles/service.ts.
Verifica: abre un proyecto y entra a Equipo (/projects/[projectId]/team).', '/projects/[projectId]/team — generated from: src/app/[locale]/(app)/projects/[projectId]/team/page.tsx, src/app/[locale]/(app)/projects/[projectId]/team/team-client.tsx, src/app/[locale]/(app)/projects/[projectId]/team/role-board.tsx, src/app/[locale]/(app)/projects/[projectId]/team/actions.ts, src/lib/team-roles/service.ts, src/lib/team-roles/board-model.ts, src/lib/team-roles/config.ts'),
    ('screen-project-workboard', 'Project Workboard screen', 'The Kanban execution board, reached from the project''s Workboard tab. Columns map to the nine `TaskStatus` values (not_started, prompt_ready, sent_to_ai, in_progress, implemented, tested, done, blocked, deferred); `workboardColumnLabels` relabels them for the project''s delivery method, but the board always operates on the same statuses. `topologicalSortTasks` puts predecessors before successors, grouped by milestone. Cards show owner (`assigned_to` person or `assigned_resource_id` crew, names resolved with the admin client from `profiles`, `project_team_members` and `resources`), priority, dependencies, subtask badges from `task_subtasks`, and a link to the Task Execution Map. The filter bar toggles Sprint/Milestone; `milestoneFilterOptions` now lists **every** milestone with its task count, including zero-count ones — previously only milestones that already had tasks appeared, which circularly hid the gates you needed to plan into — and `isEmptyByMilestoneFilter` renders an explicit "This milestone has no tasks planned yet" panel instead of a blank board. A density toggle (`density.ts`, Compact = 212px columns, UX-013) avoids browser zoom. Drag-and-drop calls `updateTaskStatusAction` and `reorderTasksAction`; a move can fail with `dependency_not_met`. `updateTaskAction` can move a task to another `milestone_id` and recalculates both milestones — but note it does **not** write the `task_milestone_reassignments` audit table, which exists in migration `20260907000000` with no code reading or writing it yet. Reads `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`, `task_subtasks`, `project_delivery_frameworks`. Related: screen-project-task-detail, screen-execution-map, screen-project-overview.
Source: workboard/page.tsx, workboard-client.tsx, roadmap/actions.ts, lib/workboard/milestone-filter.ts.
Verify: open a project → Workboard tab → switch the filter to Milestone and look for a chip with a `0` count.', 'Pantalla Workboard del proyecto', 'El tablero Kanban de ejecución; se llega desde la pestaña Workboard del proyecto. Las columnas corresponden a los nueve valores de `TaskStatus` (not_started, prompt_ready, sent_to_ai, in_progress, implemented, tested, done, blocked, deferred); `workboardColumnLabels` cambia las etiquetas según el método de entrega, pero el tablero siempre opera sobre los mismos estados. `topologicalSortTasks` coloca las predecesoras antes que las sucesoras, agrupadas por hito. Las tarjetas muestran responsable (`assigned_to` persona o `assigned_resource_id` cuadrilla, con nombres resueltos mediante el cliente admin desde `profiles`, `project_team_members` y `resources`), prioridad, dependencias, insignias de subtareas desde `task_subtasks` y un enlace al Mapa de Ejecución de la tarea. La barra de filtros alterna Sprint/Hito; `milestoneFilterOptions` ahora lista **todos** los hitos con su conteo de tareas, incluidos los que tienen cero — antes solo aparecían los hitos que ya tenían tareas, lo que ocultaba de forma circular las compuertas donde hacía falta planificar — e `isEmptyByMilestoneFilter` muestra un aviso explícito ("Este hito todavía no tiene tareas planificadas") en lugar de un tablero vacío. Un botón de densidad (`density.ts`, Compacto = columnas de 212px, UX-013) evita usar el zoom del navegador. Arrastrar y soltar llama a `updateTaskStatusAction` y `reorderTasksAction`; el movimiento puede fallar con `dependency_not_met`. `updateTaskAction` puede mover una tarea a otro `milestone_id` y recalcula ambos hitos, pero **no** escribe la tabla de auditoría `task_milestone_reassignments`, que existe en la migración `20260907000000` sin código que la lea ni la escriba. Lee `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`, `task_subtasks` y `project_delivery_frameworks`. Relacionadas: screen-project-task-detail, screen-execution-map, screen-project-overview.
Fuente: workboard/page.tsx, workboard-client.tsx, roadmap/actions.ts, lib/workboard/milestone-filter.ts.
Verifica: abre un proyecto → pestaña Workboard → cambia el filtro a Hito y busca un chip con conteo `0`.', '/projects/[projectId]/workboard — generated from: src/app/[locale]/(app)/projects/[projectId]/workboard/page.tsx, src/app/[locale]/(app)/projects/[projectId]/workboard/workboard-client.tsx, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts, src/lib/workboard/milestone-filter.ts, src/lib/workboard/density.ts, src/lib/workboard/task-schemas.ts, src/lib/roadmap/topological-sort.ts, src/lib/subtasks/map-model.ts, src/lib/delivery/config.ts'),
    ('screen-projects-list', 'Projects list screen', 'The organization''s portfolio at `/projects`, reached from the main navigation. The server page queries `projects` scoped to the current organization (soft-deleted rows excluded, newest first) and resolves localized status labels with next-intl. `ProjectListClient` renders a create button opening `CreateProjectDialog`, then either an empty state or a grid of `ProjectCard`s linking to `/projects/{id}`. Each card shows a status badge and start date; the ellipsis icon that appears on hover is decorative — there is no card menu, and the card title reads `title_i18n.en` regardless of the active locale, so a Spanish-only project falls back to its slug. Creation is now governed: the dialog first calls `loadProjectCreationScopeAction` (RPC `v2_creatable_organizations`) and, when more than one organization is creatable, forces an organization choice; `loadGovernanceUnitsAction` (RPC `v2_creatable_units`) then populates a **required** governance unit selector — `createProjectAction` returns `governance_unit_required` without one. The insert itself no longer uses the admin client: it calls the `create_project_v2` RPC on the session client so RLS and the capability resolver decide, writing the project, its `multi_pmo_v2` contract and one owner assignment in a single transaction (`42501` surfaces as `not_authorized`). Afterwards `createCharterForProject` and, if "use template" is ticked, `instantiateTemplate` run best-effort; a failure there is logged but never loses the project. Related: screen-project-overview, screen-import, screen-home-dashboard.
Source: projects/page.tsx, project-list-client.tsx, actions.ts, create-project-dialog.tsx.
Verify: open Projects from the navigation and press create — the governance unit field must be filled before Save succeeds.', 'Pantalla Lista de proyectos', 'El portafolio de la organización en `/projects`, accesible desde la navegación principal. La página de servidor consulta `projects` limitada a la organización actual (excluyendo eliminados, más recientes primero) y resuelve etiquetas de estado con next-intl. `ProjectListClient` muestra un botón de crear que abre `CreateProjectDialog` y, después, un estado vacío o una cuadrícula de tarjetas `ProjectCard` que enlazan a `/projects/{id}`. Cada tarjeta muestra una insignia de estado y la fecha de inicio; el icono de tres puntos que aparece al pasar el cursor es decorativo — no hay menú en la tarjeta — y el título se lee de `title_i18n.en` sin importar el idioma activo, por lo que un proyecto solo en español acaba mostrando su slug. La creación ahora está gobernada: el diálogo llama primero a `loadProjectCreationScopeAction` (RPC `v2_creatable_organizations`) y, si hay más de una organización disponible, obliga a elegir una; luego `loadGovernanceUnitsAction` (RPC `v2_creatable_units`) llena un selector de unidad de gobernanza **obligatorio**, ya que `createProjectAction` devuelve `governance_unit_required` si falta. La inserción ya no usa el cliente admin: invoca el RPC `create_project_v2` sobre el cliente de sesión para que decidan RLS y el resolutor de capacidades, escribiendo el proyecto, su contrato `multi_pmo_v2` y una asignación de owner en una sola transacción (`42501` se traduce a `not_authorized`). Después se ejecutan `createCharterForProject` y, si se marcó usar plantilla, `instantiateTemplate`, ambos con tolerancia a fallos: un error se registra pero nunca pierde el proyecto. Relacionadas: screen-project-overview, screen-import, screen-home-dashboard.
Fuente: projects/page.tsx, project-list-client.tsx, actions.ts, create-project-dialog.tsx.
Verifica: abre Proyectos desde la navegación y pulsa crear — la unidad de gobernanza debe estar seleccionada para que Guardar funcione.', '/projects — generated from: src/app/[locale]/(app)/projects/page.tsx, src/app/[locale]/(app)/projects/project-list-client.tsx, src/app/[locale]/(app)/projects/actions.ts, src/components/projects/project-card.tsx, src/components/projects/create-project-dialog.tsx'),
    ('screen-reports', 'Reports screen', 'The org-level "Reports & Intelligence" studio at `/reports`. The server page preloads saved reports and projects via `listSavedReportsAction` and `listProjectsForReportsAction`, honouring `?report=` and `?project=` deep links. Six tabs: Overview, Report Library, Report Builder, Saved Reports, Data Explorer and KPI Dictionary. The builder only queries curated datasets from the semantic layer `lib/reports/registry` — never raw SQL — currently `project_health`, `task_execution`, `budget_performance`, `financial_control`, `risk_register`, `material_requirements`, `rfi_log` and `project_memory`. `task_execution` now carries `record_type`/`parent_task` plus effort columns `estimated_hours`, `actual_hours`, `logged_hours`, `hours_variance` and `hours_variance_pct`, where actual hours come from the time log and only fall back to a hand-entered value; `financial_control` exposes baseline, funding, commitment, actuals, EAC/P50/P80, CPI/SPI and data-quality counters. You pick columns, filters (multi-value and `Paul*` wildcards, accent-insensitive — REG-038/039), grouping, sorting, visualization, calculated formula fields (with `suggestCalculatedFieldAction`), an "Include subtasks" toggle and a scope of all projects or one. Run calls `runReportAction`, logging `report_runs`, paging at 200 rows (5,000 for print) under a 5,000-row cap; result rows deep-link to the underlying record. CSV export uses `exportReportCsvAction` (logged in `report_exports`), Print renders a hidden print-only pane, and save/list/duplicate/delete use `saved_reports` with private/project/organization visibility. Related: screen-home-dashboard, screen-project-status, screen-project-budget.
Source: reports/page.tsx, reports-client.tsx, actions.ts, src/lib/reports/registry.ts, query-service.ts.
Verify: open Reports from the app navigation, pick a dataset in Report Builder, select columns, and press Run.', 'Pantalla Reportes', 'El estudio organizacional "Reportes e Inteligencia" en `/reports`. La página de servidor precarga reportes guardados y proyectos con `listSavedReportsAction` y `listProjectsForReportsAction`, respetando los enlaces directos `?report=` y `?project=`. Seis pestañas: Resumen, Biblioteca de Reportes, Constructor de Reportes, Reportes Guardados, Explorador de Datos y Diccionario de KPIs. El constructor solo consulta datasets curados de la capa semántica `lib/reports/registry` — nunca SQL crudo —: hoy `project_health`, `task_execution`, `budget_performance`, `financial_control`, `risk_register`, `material_requirements`, `rfi_log` y `project_memory`. `task_execution` ya incluye `record_type`/`parent_task` y las columnas de esfuerzo `estimated_hours`, `actual_hours`, `logged_hours`, `hours_variance` y `hours_variance_pct`, donde las horas reales vienen del registro de tiempo y solo caen al valor capturado a mano si no hay ninguno; `financial_control` expone baseline, financiamiento, compromiso, reales, EAC/P50/P80, CPI/SPI y contadores de calidad de datos. Eliges columnas, filtros (multivalor y comodines tipo `Paul*`, sin distinguir acentos — REG-038/039), agrupación, orden, visualización, campos calculados por fórmula (con `suggestCalculatedFieldAction`), un interruptor "Incluir subtareas" y un alcance de todos los proyectos o uno. Ejecutar llama `runReportAction`, registra en `report_runs` y pagina de 200 filas (5.000 al imprimir) bajo un tope de 5.000; cada fila enlaza al registro de origen. La exportación CSV usa `exportReportCsvAction` (registrada en `report_exports`), Imprimir arma un panel oculto solo para impresión, y guardar/listar/duplicar/eliminar usan `saved_reports` con visibilidad privada/proyecto/organización. Relacionadas: screen-home-dashboard, screen-project-status, screen-project-budget.
Fuente: reports/page.tsx, reports-client.tsx, actions.ts, src/lib/reports/registry.ts, query-service.ts.
Verifica: abre Reportes desde la navegación, elige un dataset en el Constructor, selecciona columnas y pulsa Ejecutar.', '/reports — generated from: src/app/[locale]/(app)/reports/page.tsx, src/app/[locale]/(app)/reports/reports-client.tsx, src/app/[locale]/(app)/reports/actions.ts, src/lib/reports/registry.ts, src/lib/reports/query-service.ts, src/lib/reports/filter-engine.ts'),
    ('screen-resource-capacity', 'Resource Capacity Intelligence screen', 'The Resource Capacity Intelligence screen (pillar P3, CAP-009) answers "do we have enough real workforce capacity to deliver the plan?", at /projects/[projectId]/resource-capacity. The server page runs the deterministic engine `computeResourceCapacity` (lib/capacity/service, 4-week window), which reads `project_resource_allocations` plus team/task/milestone data, and adapts its wording per project type (construction types say "Crew", software says "Resource"). Eight overview cards show Health Index (with band color), effective vs nominal capacity, assigned workload with average utilization, remaining hours, overallocated hours, workforce availability with overhead percent, bottleneck count and at-risk milestones. The Capacity Editor lets PM/PMO users add team members as allocations and edit weekly capacity hours, availability percent, overhead percent and role inline, via `getCapacityEditorDataAction`, `saveAllocationAction` and `removeAllocationAction` (writes to `project_resource_allocations`). A PMO Summary section (deterministic `buildCapacitySummary` from lib/capacity/insight) presents a headline, bullets, bottlenecks, missing-data warnings and recommended actions explicitly labeled as requiring your approval. Below are the per-resource capacity table (effective, assigned, remaining, utilization, overhead, status), a milestone capacity-risk section and a health-deductions breakdown explaining every point subtracted. The same engine feeds the Living Graph''s workforceCapacity overlay. Related: screen-living-graph, screen-labor-capacity.
Source: resource-capacity/page.tsx, capacity-editor.tsx, actions.ts, lib/capacity/service.ts.
Verify: open a project > Resource Capacity (/projects/[projectId]/resource-capacity).', 'Pantalla Inteligencia de Capacidad de Recursos', 'La pantalla Inteligencia de Capacidad de Recursos (pilar P3, CAP-009) responde "¿tenemos capacidad real de fuerza de trabajo para entregar el plan?", en /projects/[projectId]/resource-capacity. La página de servidor ejecuta el motor determinista `computeResourceCapacity` (lib/capacity/service, ventana de 4 semanas), que lee `project_resource_allocations` junto con datos de equipo, tareas e hitos, y adapta la terminología por tipo de proyecto (en construcción dice "Cuadrilla"; en software, "Recurso"). Ocho tarjetas muestran el Índice de Salud (con color por banda), capacidad efectiva vs nominal, carga asignada con utilización promedio, horas restantes, horas sobreasignadas, disponibilidad con porcentaje de overhead, cuellos de botella e hitos en riesgo. El Editor de Capacidad permite a PM/PMO agregar miembros del equipo como asignaciones y editar en línea horas semanales, porcentaje de disponibilidad, porcentaje de overhead y rol, mediante `getCapacityEditorDataAction`, `saveAllocationAction` y `removeAllocationAction` (escribe en `project_resource_allocations`). La sección Resumen PMO (determinista, `buildCapacitySummary` de lib/capacity/insight) presenta un titular, viñetas, cuellos de botella, advertencias de datos faltantes y acciones recomendadas marcadas explícitamente como sujetas a tu aprobación. Debajo están la tabla de capacidad por recurso (efectiva, asignada, restante, utilización, overhead, estado), la sección de riesgo de capacidad por hito y el desglose de deducciones del índice de salud, que explica cada punto restado. El mismo motor alimenta la superposición de capacidad de fuerza de trabajo del Grafo Vivo. Relacionadas: screen-living-graph, screen-labor-capacity.
Fuente: resource-capacity/page.tsx, capacity-editor.tsx, actions.ts, lib/capacity/service.ts.
Verifica: abre un proyecto > Capacidad de Recursos (/projects/[projectId]/resource-capacity).', '/projects/[projectId]/resource-capacity — generated from: src/app/[locale]/(app)/projects/[projectId]/resource-capacity/page.tsx, src/app/[locale]/(app)/projects/[projectId]/resource-capacity/capacity-editor.tsx, src/app/[locale]/(app)/projects/[projectId]/resource-capacity/actions.ts, src/lib/capacity/service.ts, src/lib/capacity/insight.ts'),
    ('screen-settings', 'Settings screen', 'The personal preferences screen at `/settings` inside the authenticated app, opened from the app navigation. After resolving the session with `getOrgContext()`, the server page renders three cards with inline bilingual copy. The Organization card is new and is the only part that touches the database: owners and admins get `OrganizationNameForm`, whose `renameOrganizationAction` validates the name at 2–120 characters and updates `organizations.name_i18n`, writing the same string to both `en` and `es` because a company name is a proper noun and UX-012 forbids auto-translating user content; the update runs through the user''s RLS session, so the `is_pmo_level` policy is the real authority and the role check is only a friendly pre-check. Everyone else sees the name as read-only text with a note that only owners or admins can rename it. The Appearance card hosts `ThemeControl` (Light, Dark, System) which persists to localStorage and re-applies on OS preference changes. The Language card hosts `LanguageControl`: choosing English or Español writes the `NEXT_LOCALE` cookie for a year and hard-navigates to the equivalent localized URL through `buildLocaleSwitchPath`, reloading the whole app. Billing, plans, members, teams and external contacts are not here — they live under `/organization/*`, and per-project configuration lives in each project''s Settings tab. Related: screen-team, screen-org-members, screen-org-billing, screen-org-plans, screen-project-settings.
Source: src/app/[locale]/(app)/settings/{page,actions}, src/components/settings/{organization-name-form,theme-control,language-control}.tsx.
Verify: open Settings from the navigation, rename the organization as an owner/admin, toggle the theme, then switch the language and watch the app reload.', 'Pantalla Configuración', 'La pantalla de preferencias personales en `/settings`, dentro de la aplicación autenticada, accesible desde la navegación. Tras resolver la sesión con `getOrgContext()`, la página de servidor muestra tres tarjetas con textos bilingües en línea. La tarjeta Organización es nueva y es la única parte que toca la base de datos: los propietarios y administradores ven `OrganizationNameForm`, cuya acción `renameOrganizationAction` valida el nombre entre 2 y 120 caracteres y actualiza `organizations.name_i18n` escribiendo la misma cadena en `en` y en `es`, porque el nombre de una empresa es un nombre propio y UX-012 prohíbe traducir automáticamente contenido del usuario; la actualización pasa por la sesión RLS del usuario, así que la política `is_pmo_level` es la autoridad real y la comprobación de rol es solo una validación previa amable. El resto de usuarios ve el nombre como texto de solo lectura con una nota de que únicamente propietarios o administradores pueden renombrarlo. La tarjeta Apariencia contiene `ThemeControl` (Claro, Oscuro, Sistema), que guarda la elección en localStorage y la reaplica cuando cambia la preferencia del sistema operativo. La tarjeta Idioma contiene `LanguageControl`: elegir English o Español escribe la cookie `NEXT_LOCALE` durante un año y navega de forma dura a la URL localizada equivalente mediante `buildLocaleSwitchPath`, recargando toda la aplicación. Facturación, planes, miembros, equipos y contactos externos no están aquí: viven en `/organization/*`, y la configuración por proyecto en la pestaña Ajustes de cada proyecto. Relacionadas: screen-team, screen-org-members, screen-org-billing, screen-org-plans, screen-project-settings.
Fuente: src/app/[locale]/(app)/settings/{page,actions}, src/components/settings/{organization-name-form,theme-control,language-control}.tsx.
Verifica: abre Configuración desde la navegación, renombra la organización como propietario o administrador, cambia el tema y después el idioma; la aplicación se recarga.', '/settings — generated from: src/app/[locale]/(app)/settings/page.tsx, src/app/[locale]/(app)/settings/actions.ts, src/components/settings/organization-name-form.tsx, src/components/settings/theme-control.tsx, src/components/settings/language-control.tsx'),
    ('screen-signup', 'Signup screen', 'The account-creation screen at `/signup` in the `(auth)` route group, reached from the landing page or the login screen''s "no account" link. The server page renders a localized title and subtitle (namespace `auth.signup`) above the client `SignupForm`, which collects display name, an optional company name (max 120 characters, with a hint under it), email, password and confirmation (minimum 6 characters). The client checks that both passwords match and meet the length rule before calling `signupAction`, which runs `supabase.auth.signUp` storing `display_name` and — when provided — `company_name` in user metadata; the database''s `handle_new_user()` trigger consumes `company_name` to name the new organization. `emailRedirectTo` is built by `getAuthEmailCallbackUrl` → `resolveAuthSiteUrl`, which in a production deployment always uses `NEXT_PUBLIC_SITE_URL` or falls back to `https://projectops360.com`, so confirmation links never point at a preview URL. On success the form is replaced by an envelope icon and a "check your email" message with a link back to login; clicking the emailed link hits `/auth/callback`, which exchanges the code and lands on `/?auth=confirmed`, or bounces to login with `authError=confirmation_failed`. Confirmation email delivery depends on SMTP being configured in Supabase. Related: screen-login, screen-change-password.
Source: (auth)/signup/page.tsx, components/auth/signup-form.tsx, (auth)/actions.ts, lib/auth/email-redirects.ts.
Verify: open /signup, register a new email, and confirm the "check your email" state appears.', 'Pantalla Registro', 'La pantalla de creación de cuenta en `/signup`, dentro del grupo `(auth)`; se llega desde la landing o desde el enlace "sin cuenta" del inicio de sesión. La página de servidor muestra título y subtítulo localizados (namespace `auth.signup`) sobre el componente cliente `SignupForm`, que pide nombre para mostrar, un nombre de empresa opcional (máximo 120 caracteres, con una nota debajo), correo, contraseña y confirmación (mínimo 6 caracteres). El cliente valida que las contraseñas coincidan y cumplan la longitud antes de llamar a `signupAction`, que ejecuta `supabase.auth.signUp` guardando `display_name` y, si se indicó, `company_name` en los metadatos; el disparador `handle_new_user()` de la base de datos usa `company_name` para nombrar la nueva organización. `emailRedirectTo` se construye con `getAuthEmailCallbackUrl` → `resolveAuthSiteUrl`, que en un despliegue de producción siempre usa `NEXT_PUBLIC_SITE_URL` o recurre a `https://projectops360.com`, de modo que los enlaces de confirmación nunca apunten a una URL de vista previa. Si tiene éxito, el formulario se sustituye por un icono de sobre y el mensaje "revisa tu correo" con enlace de vuelta al acceso; al pulsar el enlace del correo se entra a `/auth/callback`, que canjea el código y aterriza en `/?auth=confirmed`, o devuelve al acceso con `authError=confirmation_failed`. La entrega del correo depende de que SMTP esté configurado en Supabase. Relacionadas: screen-login, screen-change-password.
Fuente: (auth)/signup/page.tsx, components/auth/signup-form.tsx, (auth)/actions.ts, lib/auth/email-redirects.ts.
Verifica: abre /signup, regístrate con un correo nuevo y confirma que aparece el estado "revisa tu correo".', '/signup — generated from: src/app/[locale]/(auth)/signup/page.tsx, src/components/auth/signup-form.tsx, src/app/[locale]/(auth)/actions.ts, src/lib/auth/email-redirects.ts, src/lib/auth/email-redirects.server.ts, src/app/auth/callback/route.ts'),
    ('screen-team', 'Team screen', 'The org-wide people directory at `/team`, opened from the app navigation. The server page uses the Supabase admin client to load `organization_members` for the current org (excluding `removed`), their `profiles` fetched by user id — deliberately not filtered by the profile''s home org, which used to hide members — plus `resources` of type person/crew/team/role/vendor/subcontractor, and `projects` for name resolution. Emails now come from the `admin_get_user_emails` RPC restricted to this tenant''s member ids, never from enumerating global Auth users. `TeamClient` shows two populations: Workspace users as cards (name, role, email, non-active status, a "you" marker) and People, crews & vendors as a table, grouped by normalised name so duplicates can be merged. Managers (`owner` or `admin`) get "Create login" and "Add person" plus, per row, edit, merge (`mergeTeamResourcesAction`), archive (`archiveTeamResourceAction`) and invite-as-user (`inviteResourceAsUserAction`); `createMemberWithPasswordAction` creates a member with a generated readable temporary password. The Manage-user dialog edits name, seat, workspace role, status, department, job title and a **financial rate card** — the cost rate is stored on the user''s linked `resources` row, and saving one creates an org-wide person resource if none exists — plus Remove. Two things it deliberately does not do: email is read-only (identity changes must go through the account owner) and permanent deletion is no longer offered here, only in the Admin console. Writes: `resources`, `organization_members`, `profiles`, `project_team_members`, Supabase Auth users, `audit_logs`. Related: screen-org-members, screen-project-team, screen-change-password, screen-admin-console.
Source: team/{page,team-client,actions}, ../organization/members/actions.ts.
Verify: open Team from the navigation; as an owner/admin click "Manage" on a workspace user and set a cost rate.', 'Pantalla Equipo', 'El directorio de personas de toda la organización en `/team`, accesible desde la navegación. La página de servidor usa el cliente admin de Supabase para cargar `organization_members` de la organización actual (excluyendo los `removed`), sus `profiles` buscados por id de usuario —a propósito sin filtrar por la organización personal del perfil, que antes ocultaba miembros—, más `resources` de tipo persona/cuadrilla/equipo/rol/proveedor/subcontratista y `projects` para resolver nombres. Los correos ahora provienen del RPC `admin_get_user_emails` acotado a los ids de miembros de este tenant, nunca de enumerar usuarios globales de Auth. `TeamClient` muestra dos poblaciones: los usuarios del workspace como tarjetas (nombre, rol, correo, estado si no está activo y marcador "tú") y las personas, cuadrillas y proveedores como tabla, agrupados por nombre normalizado para poder fusionar duplicados. Los gestores (`owner` o `admin`) disponen de "Crear acceso" y "Agregar persona" y, por fila, editar, fusionar (`mergeTeamResourcesAction`), archivar (`archiveTeamResourceAction`) e invitar como usuario (`inviteResourceAsUserAction`); `createMemberWithPasswordAction` crea un miembro con una contraseña temporal legible generada. El diálogo Gestionar usuario edita nombre, asiento, rol de workspace, estado, departamento, cargo y una **tarifa financiera** — la tarifa se guarda en el `resources` vinculado al usuario y, si no existe, guardarla crea un recurso de persona a nivel organización — además de Remover. Dos cosas que a propósito no hace: el correo es de solo lectura (el cambio de identidad debe hacerlo el titular de la cuenta) y el borrado permanente ya no se ofrece aquí, solo en la consola de administración. Escribe en: `resources`, `organization_members`, `profiles`, `project_team_members`, usuarios de Supabase Auth y `audit_logs`. Relacionadas: screen-org-members, screen-project-team, screen-change-password, screen-admin-console.
Fuente: team/{page,team-client,actions}, ../organization/members/actions.ts.
Verifica: abre Equipo desde la navegación; como propietario o administrador pulsa "Gestionar" en un usuario del workspace y define una tarifa de costo.', '/team — generated from: src/app/[locale]/(app)/team/page.tsx, src/app/[locale]/(app)/team/team-client.tsx, src/app/[locale]/(app)/team/actions.ts, src/app/[locale]/(app)/organization/members/actions.ts, src/lib/billing/config.ts')
),
ins_pkg AS (
  INSERT INTO public.knowledge_packages (organization_id, slug, domain, status, default_language)
  SELECT NULL, d.slug, 'app_screens', 'published', 'en' FROM data d
  ON CONFLICT DO NOTHING
  RETURNING id, slug
),
allpkg AS (
  -- freshly inserted packages (invisible to a same-statement table scan) ∪ pre-existing ones
  SELECT id, slug FROM ins_pkg
  UNION
  SELECT p.id, p.slug
  FROM public.knowledge_packages p
  JOIN data d ON d.slug = p.slug
  WHERE p.organization_id IS NULL AND p.deleted_at IS NULL
),
changed AS (
  SELECT a.id AS package_id, d.*
  FROM allpkg a
  JOIN data d ON d.slug = a.slug
  WHERE NOT EXISTS (
    SELECT 1
    FROM public.knowledge_package_versions v
    JOIN public.knowledge_localizations le ON le.version_id = v.id AND le.language = 'en'
    JOIN public.knowledge_localizations ls ON ls.version_id = v.id AND ls.language = 'es'
    WHERE v.package_id = a.id AND v.is_current
      AND le.title = d.title_en AND le.body = d.body_en
      AND ls.title = d.title_es AND ls.body = d.body_es
  )
),
demoted AS (
  UPDATE public.knowledge_package_versions v
  SET is_current = false
  WHERE v.is_current AND v.package_id IN (SELECT package_id FROM changed)
  RETURNING v.id
),
retired_chunks AS (
  UPDATE public.knowledge_chunks c
  SET deleted_at = now()
  WHERE c.deleted_at IS NULL AND c.version_id IN (SELECT id FROM demoted)
  RETURNING c.id
),
ver AS (
  INSERT INTO public.knowledge_package_versions
    (package_id, organization_id, version_no, is_current, confidence_tier, source_refs)
  SELECT
    c.package_id, NULL,
    coalesce((SELECT max(vv.version_no) FROM public.knowledge_package_versions vv
              WHERE vv.package_id = c.package_id), 0) + 1,
    true, 'learned_pattern', jsonb_build_array(c.source_ref)
  FROM changed c
  -- force 'demoted'/'retired_chunks' to run first (one-current partial index)
  WHERE (SELECT count(*) FROM demoted) >= 0
    AND (SELECT count(*) FROM retired_chunks) >= 0
  RETURNING id, package_id
),
loc AS (
  INSERT INTO public.knowledge_localizations
    (version_id, package_id, organization_id, language, title, body)
  SELECT v.id, v.package_id, NULL, x.language, x.title, x.body
  FROM ver v
  JOIN changed c ON c.package_id = v.package_id
  CROSS JOIN LATERAL (VALUES
    ('en', c.title_en, c.body_en),
    ('es', c.title_es, c.body_es)
  ) AS x(language, title, body)
  RETURNING id, version_id, package_id, organization_id, language, body
)
INSERT INTO public.knowledge_chunks
  (localization_id, version_id, package_id, organization_id, language,
   ordinal, body, content_hash, index_status)
SELECT
  l.id, l.version_id, l.package_id, l.organization_id, l.language,
  0, l.body, encode(digest(l.body, 'sha256'), 'hex'), 'pending'
FROM loc l;
