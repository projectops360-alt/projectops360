-- ============================================================================
-- ProjectOps360° — Isabella App Screens corpus (GENERATED — do not edit)
-- Generated by docs/app-knowledge/scripts/generate-app-screens-seed.mjs
-- Idempotent + version-aware: unchanged packages are untouched; changed ones
-- get a new current version and re-pending chunks. Requires pgcrypto (digest).
-- ============================================================================
WITH data(slug, title_en, body_en, title_es, body_es, source_ref) AS (
  VALUES
    ('screen-admin-backfill', 'Admin Backfill Console screen', 'The secure, admin-only Backfill Administration Console at `/admin/backfill` — described in code as "the single approved surface for executing Historical Backfill" into the Project Event Graph. Access is gated server-side by `canRunBackfill`: only org owners/admins or emails on the `BACKFILL_ADMIN_EMAILS` platform allowlist may enter; everyone else gets a 404 (no data, no nav leak). The server loads up to 500 org projects (id, localized title, type, status) with the admin client. The `BackfillConsole` lets the admin pick a scope — a single project or the entire organization — then enforces a safety sequence: a dry run must be executed for the chosen scope first, a written reason is required, and a confirm dialog precedes execution. Both modes call `runBackfillAction`, which re-checks authorization and invokes `backfillProject` from `lib/events/backfill`; that engine maps existing projects, milestones, tasks, dependencies, decisions, documents, and drawings into events for the event ledger. The operation is idempotent and additive, and per the UI it never modifies canonical data or `process_nodes`/`process_edges`. Results render as progress bars and can be downloaded as a JSON report named after the execution id. Related screens: Living Graph (consumer of the event graph) and the Living Graph Observability panel.

Source: src/app/[locale]/(app)/admin/backfill/{page,backfill-console,actions}, src/lib/events/{backfill,backfill-access}.ts.
Verify: as an org owner/admin, open /admin/backfill, run a dry run on one project, then execute with a reason.', 'Pantalla Consola de Backfill (admin)', 'La Consola de Administración de Backfill, segura y solo para administradores, en `/admin/backfill` — descrita en el código como "la única superficie aprobada para ejecutar el Backfill Histórico" hacia el Project Event Graph. El acceso se controla en el servidor con `canRunBackfill`: solo owners/admins de la organización o correos de la lista blanca de plataforma `BACKFILL_ADMIN_EMAILS`; el resto recibe un 404 (sin datos ni fuga de navegación). El servidor carga hasta 500 proyectos de la organización (id, título localizado, tipo, estado) con el cliente admin. La `BackfillConsole` permite elegir alcance — un proyecto o toda la organización — y aplica una secuencia de seguridad: primero un dry run obligatorio para ese alcance, una razón escrita requerida y un diálogo de confirmación antes de ejecutar. Ambos modos llaman `runBackfillAction`, que revalida la autorización e invoca `backfillProject` de `lib/events/backfill`; ese motor convierte proyectos, hitos, tareas, dependencias, decisiones, documentos y planos existentes en eventos del libro de eventos. La operación es idempotente y aditiva y, según la UI, nunca modifica datos canónicos ni `process_nodes`/`process_edges`. Los resultados se muestran con barras de progreso y pueden descargarse como reporte JSON con el id de ejecución. Pantallas relacionadas: Living Graph y el panel de Observabilidad del Living Graph.

Fuente: src/app/[locale]/(app)/admin/backfill/{page,backfill-console,actions}, src/lib/events/{backfill,backfill-access}.ts.
Verifica: como owner/admin, abre /admin/backfill, ejecuta un dry run sobre un proyecto y luego ejecuta con una razón.', '/admin/backfill — generated from: src/app/[locale]/(app)/admin/backfill/page.tsx, src/app/[locale]/(app)/admin/backfill/backfill-console.tsx, src/app/[locale]/(app)/admin/backfill/actions.ts, src/lib/events/backfill-access.ts, src/lib/events/backfill.ts'),
    ('screen-admin-console', 'Admin Console', 'A platform-wide administration cockpit at `/admin`, with no navigation link — you type the URL directly. Access is a strict server-side gate: `requirePlatformAdmin` checks active rows in `admin_authorized_users` first, then the hardcoded emergency owner (`ADMIN_CONSOLE_ALLOWED_EMAILS`); anyone else gets a 404 before a single business query runs, and denials are logged. The page loads cross-org data with the service role: KPI cards (companies, users, projects, tasks, active admins) and six tabs — Overview (top companies and projects), Companies (expandable rows that load per-company users via the SECURITY DEFINER RPC `admin_list_company_users`, plus inline rename through the `admin_rename_organization` RPC), Users & Projects (filterable table of projects per owner), Project Tasks (per-project drill-down with search, status filter and pagination via `getProjectTasksAction`), Plans (read-only global plan catalog from `plans`/`subscriptions`, linking to /organization/plans for edits), and Admin Access (grant, revoke or reactivate system admins in `admin_authorized_users` — revocation is soft, and the emergency owner can never be locked out). Server actions re-validate the gate on every call, and writes are recorded in `audit_logs`. User emails resolve via the `admin_get_user_emails` RPC. Related screens: Admin Backfill (`/admin/backfill`) and Living Graph Observability (`/admin/living-graph-observability`).
Source: src/app/[locale]/(app)/admin/page.tsx, actions.ts; src/components/admin-console/admin-console.tsx; src/lib/admin-console/access.server.ts, queries.ts.
Verify: sign in as an authorized platform admin and open /admin; a non-admin account gets a 404.', 'Consola de administración', 'Un centro de administración de toda la plataforma en `/admin`, sin enlace en la navegación: se accede escribiendo la URL. El acceso es una puerta estricta del lado del servidor: `requirePlatformAdmin` consulta primero las filas activas de `admin_authorized_users` y después al propietario de emergencia codificado (`ADMIN_CONSOLE_ALLOWED_EMAILS`); cualquier otro usuario recibe un 404 antes de ejecutar consulta alguna, y las denegaciones se registran. La página carga datos de todas las organizaciones con el service role: tarjetas KPI (compañías, usuarios, proyectos, tareas, administradores activos) y seis pestañas — Resumen (compañías y proyectos principales), Compañías (filas expandibles que cargan los usuarios de cada compañía mediante la RPC SECURITY DEFINER `admin_list_company_users`, más renombrado en línea con la RPC `admin_rename_organization`), Usuarios y proyectos (tabla filtrable de proyectos por propietario), Tareas por proyecto (detalle con búsqueda, filtro de estado y paginación vía `getProjectTasksAction`), Planes (catálogo global de solo lectura desde `plans`/`subscriptions`, con enlace a /organization/plans para editar) y Acceso de administradores (conceder, revocar o reactivar administradores del sistema en `admin_authorized_users` — la revocación es blanda y el propietario de emergencia nunca queda fuera). Las server actions revalidan la puerta en cada llamada y las escrituras quedan en `audit_logs`. Los correos se resuelven con la RPC `admin_get_user_emails`. Pantallas relacionadas: Backfill de administración (`/admin/backfill`) y Observabilidad del Living Graph (`/admin/living-graph-observability`).
Fuente: src/app/[locale]/(app)/admin/page.tsx, actions.ts; src/components/admin-console/admin-console.tsx; src/lib/admin-console/access.server.ts, queries.ts.
Verifica: inicia sesión como administrador de plataforma autorizado y abre /admin; una cuenta no autorizada recibe un 404.', '/admin — generated from: src/app/[locale]/(app)/admin/page.tsx, src/app/[locale]/(app)/admin/actions.ts, src/components/admin-console/admin-console.tsx, src/lib/admin-console/access.server.ts, src/lib/admin-console/queries.ts'),
    ('screen-admin-living-graph-observability', 'Living Graph Observability screen', 'An internal admin diagnostics panel at `/admin/living-graph-observability` for the Living Graph Realtime Engine (LGRE). Access uses the same strict server-side email allowlist as the Product Brain cockpit (`isProductBrainAllowedEmail`); unauthorized users get a 404 and no diagnostics data is loaded. The code is explicit about safety: only safe aggregates reach the client — infra health booleans plus an org-scoped project list (id and display title, up to 50) — never payloads, ledger rows, or secrets. The server computes environment health via `getLivingGraphEnvironmentHealth` (which reads `project_event_log`); failures degrade to null. The client panel owns exactly one scoped realtime subscription (via `useLiveGraphSync`) and reports session counters it genuinely instruments: subscription count, active channels, reconnects, stale and fresh-recovery counts, permission losses, notices, errors, and warnings. Metrics it does not track (delta sync, recalculation, rendering — which live in-memory per graph consumer) are honestly rendered as "not instrumented", never fake zeros. Controls include a project selector, a refresh button (`router.refresh()`), and connection-state indicators. Copy comes from the `livingGraphObservability` translation namespace. Related screens: the project Living Graph (`/projects/{id}/execution-map/living-graph`) and the Backfill Console that feeds the event graph.

Source: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx.
Verify: with an allowlisted email, open /admin/living-graph-observability and select a project to watch realtime counters.', 'Pantalla Observabilidad del Living Graph', 'Un panel interno de diagnóstico para administradores en `/admin/living-graph-observability`, dedicado al motor de tiempo real del Living Graph (LGRE). El acceso usa la misma lista blanca estricta de correos del cockpit de Product Brain (`isProductBrainAllowedEmail`), aplicada en el servidor; los no autorizados reciben un 404 y no se carga ningún dato de diagnóstico. El código es explícito en seguridad: al cliente solo llegan agregados seguros — booleanos de salud de infraestructura y una lista de proyectos de la organización (id y título, hasta 50) — nunca payloads, filas del ledger ni secretos. El servidor calcula la salud del entorno con `getLivingGraphEnvironmentHealth` (que lee `project_event_log`); los fallos degradan a null. El panel cliente posee exactamente una suscripción realtime acotada (vía `useLiveGraphSync`) y reporta los contadores de sesión que realmente instrumenta: suscripciones, canales activos, reconexiones, conteos de datos obsoletos y recuperación, pérdidas de permiso, avisos, errores y advertencias. Las métricas que no rastrea (delta sync, recálculo, render) se muestran honestamente como "no instrumentadas", nunca ceros falsos. Los controles incluyen selector de proyecto, botón de refrescar e indicadores de conexión. Pantallas relacionadas: el Living Graph del proyecto y la Consola de Backfill que alimenta el grafo de eventos.

Fuente: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx.
Verifica: con un correo en la lista blanca, abre /admin/living-graph-observability y elige un proyecto para ver los contadores en tiempo real.', '/admin/living-graph-observability — generated from: src/app/[locale]/(app)/admin/living-graph-observability/page.tsx, src/components/admin/living-graph-observability-panel.tsx, src/lib/living-graph/observability/environment-health.server.ts'),
    ('screen-ai-operator', 'AI Operator screen', 'A lightweight hub at `/ai-operator` that groups the app''s AI-powered intake modules. After confirming the session with `getOrgContext()`, the page shows a centered Bot icon with a title and description pulled from the next-intl `placeholders.aiOperator` namespace — the "placeholders" namespace is an honest signal that this surface is still an early hub rather than a full module. Below the header are exactly two navigation cards. The first, "Project Import Intelligence", links to `/import` and describes uploading Excel, CSV, JSON, Word, or PDF files to turn them into a full project (bilingual copy is inlined in the component). The second links to the global Drawing Intelligence screen at `/drawing-intelligence`, with its title and description coming from the `drawingIntelligence.aiOperatorCard` translations. Both cards are simple `Link`s with hover affordances and arrow icons. The screen reads no Supabase tables beyond the auth/org check, writes nothing, and calls no server actions — it is purely a launcher. There is no chat or agent execution on this page despite the name. Related screens: Import (`/import`), global Drawing Intelligence (`/drawing-intelligence`), and the Home dashboard whose "AI Operator Briefing" section is a separate, data-driven feature.

Source: src/app/[locale]/(app)/ai-operator/page.tsx.
Verify: open /ai-operator from the app; the two cards navigate to Import and Drawing Intelligence.', 'Pantalla Operador IA', 'Un hub ligero en `/ai-operator` que agrupa los módulos de ingesta con IA de la aplicación. Tras confirmar la sesión con `getOrgContext()`, la página muestra un icono de robot centrado con título y descripción tomados del namespace `placeholders.aiOperator` de next-intl — que el namespace se llame "placeholders" es una señal honesta de que esta superficie sigue siendo un hub inicial y no un módulo completo. Bajo el encabezado hay exactamente dos tarjetas de navegación. La primera, "Importación Inteligente de Proyectos", enlaza a `/import` y describe subir archivos Excel, CSV, JSON, Word o PDF para convertirlos en un proyecto completo (texto bilingüe en el propio componente). La segunda enlaza a la pantalla global de Drawing Intelligence en `/drawing-intelligence`, con título y descripción de las traducciones `drawingIntelligence.aiOperatorCard`. Ambas tarjetas son simples `Link` con efectos al pasar el cursor. La pantalla no lee tablas de Supabase más allá de la verificación de sesión, no escribe nada y no llama server actions — es puramente un lanzador. A pesar del nombre, aquí no hay chat ni ejecución de agentes. Pantallas relacionadas: Importación (`/import`), Drawing Intelligence global (`/drawing-intelligence`) y el dashboard de inicio, cuyo "Resumen del Operador IA" es una funcionalidad aparte basada en datos.

Fuente: src/app/[locale]/(app)/ai-operator/page.tsx.
Verifica: abre /ai-operator en la aplicación; las dos tarjetas navegan a Importación y Drawing Intelligence.', '/ai-operator — generated from: src/app/[locale]/(app)/ai-operator/page.tsx'),
    ('screen-change-password', 'Change password screen', 'A standalone password-change screen at `/change-password` that deliberately lives outside the `(app)` route group (no app shell), so the forced-change gate in the app layout can redirect here without a redirect loop. It requires an authenticated session: the server page calls `supabase.auth.getUser()` and redirects to `/login` if there is none. It then reads `user_metadata.must_change_password`; when true (the "forced" flow, typically after an admin created the account with a temporary password), the copy explains the user must choose a new password to continue. The centered card form has two fields — new password and confirmation, both minimum 8 characters — and a "Save and continue" button. Submitting calls the `changeOwnPasswordAction` server action, which uses the user''s own session (never the service role) to run `supabase.auth.updateUser`, setting the new password and clearing `must_change_password` in metadata. Localized errors cover weak password, reusing the same password, mismatch, expired session, and generic update failure. On success the client navigates to `/` and refreshes. Related screens: Login, Team (whose admin actions create users with temporary passwords), and the Home dashboard.

Source: src/app/[locale]/change-password/page.tsx, change-password-form.tsx, actions.ts.
Verify: sign in with an account flagged must_change_password (e.g. created from Team with a temp password); you are redirected to /change-password.', 'Pantalla Cambio de contraseña', 'Una pantalla independiente de cambio de contraseña en `/change-password`, que vive deliberadamente fuera del grupo `(app)` (sin shell de la aplicación) para que la puerta de cambio forzado del layout pueda redirigir aquí sin bucles. Requiere sesión autenticada: la página de servidor llama `supabase.auth.getUser()` y redirige a `/login` si no la hay. Luego lee `user_metadata.must_change_password`; cuando es verdadero (flujo "forzado", típico tras crear la cuenta con contraseña temporal), el texto explica que hay que elegir una nueva contraseña para continuar. El formulario en tarjeta centrada tiene dos campos — nueva contraseña y confirmación, mínimo 8 caracteres — y un botón "Guardar y continuar". Al enviar se llama la server action `changeOwnPasswordAction`, que usa la propia sesión del usuario (nunca el service role) para ejecutar `supabase.auth.updateUser`, fijando la contraseña y limpiando `must_change_password` en los metadatos. Los errores localizados cubren contraseña débil, reutilizar la misma, no coincidencia, sesión expirada y fallo genérico. Al terminar, el cliente navega a `/` y refresca. Pantallas relacionadas: Login, Equipo (cuyas acciones de administración crean usuarios con contraseña temporal) y el dashboard de inicio.

Fuente: src/app/[locale]/change-password/page.tsx, change-password-form.tsx, actions.ts.
Verifica: inicia sesión con una cuenta marcada must_change_password (por ejemplo, creada desde Equipo con contraseña temporal); serás redirigido a /change-password.', '/change-password — generated from: src/app/[locale]/change-password/page.tsx, src/app/[locale]/change-password/change-password-form.tsx, src/app/[locale]/change-password/actions.ts'),
    ('screen-drawing-intelligence-global', 'Drawing Intelligence (global) screen', 'The org-level entry point for the Drawing/BIM Intelligence module at `/drawing-intelligence`, reached from the navigation or from the AI Operator hub. It is a project selector, not the analysis surface itself. The server page (forced dynamic) queries two Supabase tables in parallel, both scoped to the organization and excluding soft-deleted rows: `projects` (id, slug, localized title) and `drawing_files` (id, project_id) to count drawings per project — the code notes that `drawing_files` may not exist until its migration is applied, in which case counts honestly degrade to zero. The header shows the module title and subtitle from the `drawingIntelligence` translations plus a highlighted badge (`notStorageNote`) clarifying this module is not a file-storage feature. If the org has projects, a card grid lists each project with its drawing count ("N drawings" or "no drawings yet"), and each card links to that project''s own Drawing Intelligence tab at `/projects/{id}/drawing-intelligence`, where uploads and analysis actually happen. With no projects, a dashed empty state shows the `global.noProjects` message. Nothing is written from this screen. Related screens: per-project Drawing Intelligence, AI Operator, Projects list.

Source: src/app/[locale]/(app)/drawing-intelligence/page.tsx.
Verify: open /drawing-intelligence from the navigation and click a project card to land on that project''s Drawing Intelligence tab.', 'Pantalla Drawing Intelligence (global)', 'El punto de entrada organizacional del módulo de inteligencia de planos/BIM en `/drawing-intelligence`, accesible desde la navegación o desde el hub del Operador IA. Es un selector de proyectos, no la superficie de análisis en sí. La página de servidor (dinámica forzada) consulta en paralelo dos tablas de Supabase, ambas limitadas a la organización y sin registros eliminados: `projects` (id, slug, título localizado) y `drawing_files` (id, project_id) para contar planos por proyecto — el código anota que `drawing_files` puede no existir hasta aplicar su migración, en cuyo caso los conteos degradan honestamente a cero. El encabezado muestra título y subtítulo de las traducciones `drawingIntelligence` más una insignia destacada (`notStorageNote`) aclarando que el módulo no es un almacén de archivos. Si hay proyectos, una cuadrícula de tarjetas lista cada proyecto con su conteo de planos ("N planos" o "sin planos aún"), y cada tarjeta enlaza a la pestaña Drawing Intelligence de ese proyecto en `/projects/{id}/drawing-intelligence`, donde realmente ocurren la subida y el análisis. Sin proyectos, se muestra un estado vacío con el mensaje `global.noProjects`. Desde esta pantalla no se escribe nada. Pantallas relacionadas: Drawing Intelligence por proyecto, Operador IA, Lista de proyectos.

Fuente: src/app/[locale]/(app)/drawing-intelligence/page.tsx.
Verifica: abre /drawing-intelligence desde la navegación y pulsa una tarjeta de proyecto para llegar a su pestaña de Drawing Intelligence.', '/drawing-intelligence — generated from: src/app/[locale]/(app)/drawing-intelligence/page.tsx'),
    ('screen-execution-map-kpis', 'KPI Engine screen', 'The KPI Engine screen (CAP-046 F3) shows project metrics computed by the single KPI engine that Isabella and reports also use, so no metric drift exists. It is reached from the Execution Map tab bar: the "KPIs" button navigates to /projects/[projectId]/execution-map/kpis; a "Back to Execution Map" link returns. The server page loads a read-only, RLS-scoped dataset from tables `roadmap_tasks` and `milestones` (after validating the project in `projects`), with blocker/overdue/delayed flags derived by the canonical REG-010 helpers in task-activity.ts. It renders the 12 built-in catalog KPIs as cards (overall_progress, blocked_tasks, overdue_tasks, unassigned_tasks, avg_task_progress, median_task_duration, effort_ratio, estimate_correlation, milestone_delay_p90, completion_trend, completion_momentum, forecast_completions_next_week), each with value, unit, description and its expression; values that cannot be computed show an honest "not computable" state instead of fake zeros. Below, the Custom KPI section lists persisted custom definitions from table `kpi_definitions` (project-scoped plus org-wide), evaluated against the same dataset and sandbox. Non-viewer roles can create a custom KPI (name ES/EN, expression, unit, target, direction — server actions createCustomKpi/deleteCustomKpi validate the expression against an allow-list before persisting) and soft-delete their own (or any, as org owner/admin). A hint notes Isabella can translate natural language into expressions. Related screens: screen-execution-map, screen-execution-map-variants, screen-execution-map-root-causes.
Source: kpis/page.tsx, lib/kpi/load-dataset.ts, lib/kpi/catalog.ts, lib/kpi/custom-actions.ts, components/process-mining/custom-kpi-section.tsx.
Verify: open a project, go to Execution Map and click the "KPIs" tab (/projects/[projectId]/execution-map/kpis).', 'Pantalla Motor de KPIs', 'La pantalla Motor de KPIs (CAP-046 F3) muestra las métricas del proyecto calculadas por el mismo motor de KPIs que usan Isabella y los reportes, de modo que no hay divergencia de métricas. Se llega desde la barra de pestañas del Mapa de Ejecución: el botón "KPIs" navega a /projects/[projectId]/execution-map/kpis, y un enlace "Volver al Mapa de Ejecución" regresa. La página de servidor carga un conjunto de datos de solo lectura, limitado por RLS, desde las tablas `roadmap_tasks` y `milestones` (tras validar el proyecto en `projects`), con los indicadores de bloqueo, atraso y demora derivados por las reglas canónicas REG-010 de task-activity.ts. Renderiza como tarjetas los 12 KPIs integrados del catálogo (progreso general, tareas bloqueadas, vencidas, sin asignar, progreso promedio, duración mediana, razón de esfuerzo, correlación de estimados, percentil 90 de demora de hitos, tendencia, impulso y pronóstico de completadas), cada una con valor, unidad, descripción y expresión; lo que no puede calcularse muestra un estado honesto de "no calculable" en lugar de ceros falsos. Debajo, la sección de KPIs personalizados lista las definiciones guardadas en la tabla `kpi_definitions`, evaluadas con el mismo motor. Los roles distintos de espectador pueden crear un KPI personalizado (las acciones de servidor validan la expresión contra una lista permitida antes de guardar) y borrarlo de forma suave (creador o administrador). Pantallas relacionadas: screen-execution-map, screen-execution-map-variants, screen-execution-map-root-causes.
Fuente: kpis/page.tsx, lib/kpi/load-dataset.ts, lib/kpi/catalog.ts, lib/kpi/custom-actions.ts, components/process-mining/custom-kpi-section.tsx.
Verifica: abre un proyecto, entra al Mapa de Ejecución y pulsa la pestaña "KPIs" (/projects/[projectId]/execution-map/kpis).', '/projects/[projectId]/execution-map/kpis — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/kpis/page.tsx, src/lib/kpi/load-dataset.ts, src/lib/kpi/catalog.ts, src/lib/kpi/custom-actions.ts, src/components/process-mining/custom-kpi-section.tsx'),
    ('screen-execution-map-root-causes', 'Statistical Root Cause Miner screen', 'The Root Cause Miner screen (CAP-046 F2) surfaces statistical evidence about which task attributes correlate with three execution problems: delay, blockage and rework. It is reached from the Execution Map tab bar via the "Root Causes" button, which navigates to /projects/[projectId]/execution-map/root-causes; a "Back to Execution Map" link returns. The page is read-only and evidence-only by contract (PD-019): it renders no recommendations anywhere. The server adapter validates the project against `projects` in the caller''s organization (deny-by-default, safe denial state when unauthorized), then reads the project''s tasks from `roadmap_tasks` (priority, owner, criticality, discipline, trade, location, milestone, dates), milestone names from `milestones`, and rework signals from `project_event_log` — up to 5,000 non-compensating `TaskReopened` business events counted per task. Blocked and delayed flags use the canonical REG-010 helpers (terminal tasks are never blocked; delay requires a real planned finish). The pure mineRootCauses engine produces the output the view renders: a prevalence strip with problem counts and baseline rates per problem type, a method/honesty disclosure note, and a findings table with an influence score bar, a bilingual evidence explanation with example task titles, sample size and phi correlation (n and φ), and a high/medium/low confidence badge, plus any stated limitations. There are no buttons, filters or write actions. Related screens: screen-execution-map, screen-execution-map-variants, screen-execution-map-kpis.
Source: root-causes/page.tsx, lib/process-mining/root-cause/load-analysis.ts, components/process-mining/root-cause-view.tsx.
Verify: open a project, go to Execution Map and click "Root Causes" (/projects/[projectId]/execution-map/root-causes).', 'Pantalla Minero Estadístico de Causas Raíz', 'La pantalla del Minero de Causas Raíz (CAP-046 F2) presenta evidencia estadística sobre qué atributos de las tareas se correlacionan con tres problemas de ejecución: demora, bloqueo y retrabajo. Se llega desde la barra de pestañas del Mapa de Ejecución con el botón "Causas Raíz", que navega a /projects/[projectId]/execution-map/root-causes; un enlace "Volver al Mapa de Ejecución" regresa. La página es de solo lectura y solo evidencia por contrato (PD-019): no muestra recomendaciones en ninguna parte. El adaptador de servidor valida el proyecto en `projects` dentro de la organización del usuario (denegación por defecto, estado seguro sin datos si no hay permiso), luego lee las tareas desde `roadmap_tasks` (prioridad, responsable, criticidad, disciplina, oficio, ubicación, hito, fechas), los nombres de hitos desde `milestones` y las señales de retrabajo desde `project_event_log`: hasta 5000 eventos de negocio `TaskReopened` no compensatorios contados por tarea. Los indicadores de bloqueo y demora usan las reglas canónicas REG-010 (una tarea terminal nunca está bloqueada; la demora exige una fecha de fin planificada real). El motor puro mineRootCauses genera lo que la vista muestra: un resumen de prevalencia por tipo de problema con conteos y tasas base, una nota de método y honestidad, y una tabla de hallazgos con barra de puntaje de influencia, explicación bilingüe con ejemplos de tareas, tamaño de muestra y correlación phi (n y φ), insignia de confianza y limitaciones declaradas. No hay botones, filtros ni acciones de escritura. Pantallas relacionadas: screen-execution-map, screen-execution-map-variants, screen-execution-map-kpis.
Fuente: root-causes/page.tsx, lib/process-mining/root-cause/load-analysis.ts, components/process-mining/root-cause-view.tsx.
Verifica: abre un proyecto, entra al Mapa de Ejecución y pulsa "Causas Raíz" (/projects/[projectId]/execution-map/root-causes).', '/projects/[projectId]/execution-map/root-causes — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/root-causes/page.tsx, src/lib/process-mining/root-cause/load-analysis.ts, src/components/process-mining/root-cause-view.tsx'),
    ('screen-execution-map-variants', 'Execution Variant Analysis screen', 'The Execution Variant Analysis screen (CAP-046 F1) shows how the current project''s execution path compares with every other project the caller can see. It is reached from the Execution Map tab bar via the "Execution Variants" button, which navigates to /projects/[projectId]/execution-map/variants; a "Back to Execution Map" link returns. The server page is strictly read-only: the adapter validates the focus project against `projects` in the caller''s organization (deny-by-default; unauthorized users get a safe denial state with no data), then reads all visible projects plus up to 20,000 business events from `project_event_log` — the Process Event Graph, the only event source — ordered by occurred_at and sequence_number, and feeds them as cases to the pure analyzeVariants engine for the `project_lifecycle` process type. Outcomes come only from canonical project status (completed = success, cancelled = failure, otherwise open). The view renders a summary strip (analyzed cases, variants, events used, decided cases), a truncation notice when the event window was capped, a focus-project card with its variant share, fitness versus the reference variant and skipped/inserted activities, an honest empty state when no reference exists, and a variant catalog table with activity-signature chips, project count, frequency, median duration, rework rate and success rate. No filters, no mutations, no event emission. Related screens: screen-execution-map, screen-execution-map-root-causes, screen-execution-map-kpis.
Source: variants/page.tsx, lib/process-mining/variants/load-analysis.ts, components/process-mining/variant-analysis-view.tsx.
Verify: open a project, go to Execution Map and click "Execution Variants" (/projects/[projectId]/execution-map/variants).', 'Pantalla Análisis de Variantes de Ejecución', 'La pantalla de Análisis de Variantes de Ejecución (CAP-046 F1) muestra cómo se compara la ruta de ejecución del proyecto actual con la de los demás proyectos visibles para el usuario. Se llega desde la barra de pestañas del Mapa de Ejecución con el botón "Variantes de Ejecución", que navega a /projects/[projectId]/execution-map/variants; un enlace "Volver al Mapa de Ejecución" regresa. La página de servidor es estrictamente de solo lectura: el adaptador valida el proyecto en `projects` dentro de la organización del usuario (denegación por defecto; sin permiso se muestra un estado seguro sin datos), luego lee todos los proyectos visibles y hasta 20 000 eventos de negocio de `project_event_log` — el Grafo de Eventos del Proyecto, única fuente de eventos — ordenados por fecha y número de secuencia, y los entrega como casos al motor puro analyzeVariants para el tipo de proceso `project_lifecycle`. Los desenlaces provienen solo del estado canónico del proyecto (completado = éxito, cancelado = fracaso, el resto abierto). La vista muestra un resumen (casos, variantes, eventos usados, casos decididos), un aviso de truncamiento cuando se alcanzó el límite, una tarjeta del proyecto con su variante, ajuste frente a la referencia y actividades omitidas o insertadas, un estado vacío honesto cuando no hay referencia, y un catálogo de variantes con secuencia de actividades, cantidad de proyectos, frecuencia, duración mediana, tasa de retrabajo y tasa de éxito. Sin filtros ni mutaciones. Pantallas relacionadas: screen-execution-map, screen-execution-map-root-causes, screen-execution-map-kpis.
Fuente: variants/page.tsx, lib/process-mining/variants/load-analysis.ts, components/process-mining/variant-analysis-view.tsx.
Verifica: abre un proyecto, entra al Mapa de Ejecución y pulsa "Variantes de Ejecución" (/projects/[projectId]/execution-map/variants).', '/projects/[projectId]/execution-map/variants — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/variants/page.tsx, src/lib/process-mining/variants/load-analysis.ts, src/components/process-mining/variant-analysis-view.tsx'),
    ('screen-execution-map', 'Execution Map screen', 'The Execution Map is the roadmap/execution hub of a project, reached from the project navigation at /projects/[projectId]/execution-map. The server page loads the project, its milestones, roadmap tasks and task dependencies (tables `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`), computes overall progress (`computeRoadmapProgress`), a recommended next step (`computeNextStep`), and sorts tasks topologically by dependency within milestone order. The client renders six tabs: Overview (RoadmapHero with phase/progress/blockers, NextStepPanel with actions such as resolve blocker or mark completed, and an ExecutionDashboard of status counts), Timeline (VisualRoadmapTimeline), Tasks (TaskListByMilestone with status updates, AI-prompt copy, edit/archive of tasks and milestones, reorder milestones, add predecessors), Gantt (GanttRoadmap where dragging task bars calls `updateTaskDatesAction`), Critical Path (a pointer panel that redirects to the Living Graph, where critical path actually renders), and Dependencies (DependenciesView backed by `createDependencyAction`, `deleteDependencyAction`, `getDependenciesAction` in dependency-actions.ts). Header buttons open dialogs to create tasks and milestones (actions in the sibling roadmap/actions.ts). Two extra tab buttons navigate to the Living Graph and Milestone Flow routes, which live on their own pages. Related screens: screen-living-graph, screen-milestone-flow, screen-living-graph-realtime.
Source: execution-map/page.tsx, execution-map-client.tsx, dependency-actions.ts.
Verify: open a project and go to Execution Map (/projects/[projectId]/execution-map).', 'Pantalla Mapa de Ejecución', 'El Mapa de Ejecución es el centro de hoja de ruta y ejecución del proyecto, accesible desde la navegación del proyecto en /projects/[projectId]/execution-map. La página de servidor carga el proyecto, sus hitos, tareas y dependencias (tablas `projects`, `milestones`, `roadmap_tasks`, `task_dependencies`), calcula el progreso general (`computeRoadmapProgress`), el siguiente paso recomendado (`computeNextStep`) y ordena las tareas topológicamente según dependencias. El cliente muestra seis pestañas: Vista General (RoadmapHero con fase, progreso y bloqueos; NextStepPanel con acciones como resolver bloqueo o marcar completada; y un panel ExecutionDashboard con conteos por estado), Línea de Tiempo, Tareas (lista por hito con cambio de estado, copia de prompts de IA, edición y archivado de tareas e hitos, reordenar hitos y agregar predecesoras), Cronograma (Gantt donde arrastrar barras llama a `updateTaskDatesAction`), Ruta Crítica (un panel indicativo que redirige al Grafo Vivo, donde realmente se visualiza) y Dependencias (crear y eliminar dependencias con las acciones de dependency-actions.ts). Los botones del encabezado abren diálogos para crear tareas e hitos (acciones en roadmap/actions.ts). Dos botones adicionales navegan al Grafo Vivo y al Flujo entre Hitos, que tienen sus propias rutas. Pantallas relacionadas: screen-living-graph, screen-milestone-flow, screen-living-graph-realtime.
Fuente: execution-map/page.tsx, execution-map-client.tsx, dependency-actions.ts.
Verifica: abre un proyecto y entra a Mapa de Ejecución (/projects/[projectId]/execution-map).', '/projects/[projectId]/execution-map — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/execution-map-client.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/dependency-actions.ts, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts'),
    ('screen-forgot-password', 'Forgot password screen', 'A public password-recovery screen at `/forgot-password`, inside the `(auth)` route group, reached from the "Forgot password?" link on the Login form — no session is required. The server page only renders a localized title and subtitle above the client `ForgotPasswordForm`. The form has a single required email field and a submit button that shows a "sending" state while pending; a "Back to login" link is always available. Submitting calls the `requestPasswordResetAction` server action, which normalizes the email (trim + lowercase), validates it against a simple email regex, and then calls Supabase Auth `resetPasswordForEmail` with a `redirectTo` pointing at `/change-password?recovery=1` (built by `getAuthEmailCallbackUrl`), so the recovery link in the email lands the user on the Change Password screen in recovery mode. The action returns exactly two error codes — `invalid_email` when the address fails validation and `delivery_failed` when Supabase reports an error — rendered as localized banners; no user database table is read or written, everything goes through Supabase Auth. On success the form is replaced by a confirmation message telling the user to check their inbox, plus the back-to-login link. Related screens: Login and Change Password.
Source: src/app/[locale]/(auth)/forgot-password/page.tsx, src/components/auth/forgot-password-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verify: open /login, click "Forgot password?", submit your email; you see the confirmation message and receive a recovery email linking to /change-password.', 'Pantalla de recuperación de contraseña', 'Una pantalla pública de recuperación de contraseña en `/forgot-password`, dentro del grupo de rutas `(auth)`, a la que se llega desde el enlace "¿Olvidaste tu contraseña?" del formulario de inicio de sesión — no requiere sesión. La página de servidor solo muestra un título y subtítulo localizados sobre el componente cliente `ForgotPasswordForm`. El formulario tiene un único campo obligatorio de correo electrónico y un botón de envío que muestra un estado de "enviando" mientras procesa; el enlace "Volver al inicio de sesión" está siempre visible. Al enviar se llama la server action `requestPasswordResetAction`, que normaliza el correo (recorte y minúsculas), lo valida con una expresión regular sencilla y llama a `resetPasswordForEmail` de Supabase Auth con un `redirectTo` que apunta a `/change-password?recovery=1` (construido por `getAuthEmailCallbackUrl`), de modo que el enlace del correo lleva al usuario a la pantalla de cambio de contraseña en modo recuperación. La acción devuelve exactamente dos códigos de error — `invalid_email` si el correo no es válido y `delivery_failed` si Supabase reporta un fallo — mostrados como avisos localizados; no se lee ni escribe ninguna tabla propia, todo pasa por Supabase Auth. Si tiene éxito, el formulario se sustituye por un mensaje de confirmación que invita a revisar la bandeja de entrada, junto al enlace de regreso. Pantallas relacionadas: Inicio de sesión y Cambio de contraseña.
Fuente: src/app/[locale]/(auth)/forgot-password/page.tsx, src/components/auth/forgot-password-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verifica: abre /login, pulsa "¿Olvidaste tu contraseña?", envía tu correo; verás el mensaje de confirmación y recibirás un correo de recuperación que enlaza a /change-password.', '/forgot-password — generated from: src/app/[locale]/(auth)/forgot-password/page.tsx, src/components/auth/forgot-password-form.tsx, src/app/[locale]/(auth)/actions.ts'),
    ('screen-home-dashboard', 'Home dashboard (PMO Command Center) screen', 'The authenticated home page at `/`, the first screen after login. It is the org-level PMO Command Center: the server page resolves the org via `getOrgContext()` and calls `getCommandCenterSummary(organizationId, locale)` from `src/lib/command-center/service.ts`, a server-only service that aggregates real, org-scoped data (projects, tasks, milestones, risks, materials, RFIs, budget) with the Supabase admin client — explicitly no fabricated data, with honest empty states. If the org has no projects, an empty state invites the user to create or import one. Otherwise the layout shows: six KPI cards (Portfolio Health, Active Projects, Blocked Tasks, Critical Path Risks, Budget Variance, PM Decisions), each drilling into its related view; the Portfolio Health Engine (overall ring plus per-dimension bars: schedule, budget, resources, materials, risk, critical path, banded green/amber/red); Today''s PMO Focus; the AI Operator Briefing (recommendations with confidence); the Critical Path Monitor; the Decision Queue; Resource & Labor Capacity utilization bars; Material & Procurement Risk; Living Graph Signals; Upcoming 14 Days; Budget & Forecast Signals; and Recent Activity. Everything is read-only here; rows link out to Projects, Reports, Team, and project-level views. Bilingual copy is inlined (EN/ES).

Source: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts.
Verify: log in and land on / — the Command Center renders with KPI cards, or an empty state if the org has no projects.', 'Pantalla Dashboard de inicio (PMO Command Center)', 'La página de inicio autenticada en `/`, la primera pantalla tras iniciar sesión. Es el Command Center de PMO a nivel organización: la página de servidor resuelve la organización con `getOrgContext()` y llama `getCommandCenterSummary(organizationId, locale)` de `src/lib/command-center/service.ts`, un servicio solo de servidor que agrega datos reales del tenant (proyectos, tareas, hitos, riesgos, materiales, RFIs, presupuesto) con el cliente admin de Supabase — explícitamente sin datos fabricados y con estados vacíos honestos. Si la organización no tiene proyectos, un estado vacío invita a crear o importar uno. Si los hay, se muestran: seis tarjetas KPI (Salud del Portafolio, Proyectos Activos, Tareas Bloqueadas, Riesgos de Ruta Crítica, Variación de Presupuesto, Decisiones del PM), cada una con enlace a su vista; el Motor de Salud del Portafolio (anillo general y barras por dimensión: cronograma, presupuesto, recursos, materiales, riesgo, ruta crítica); Foco del PMO Hoy; el Resumen del Operador IA; el Monitor de Ruta Crítica; la Cola de Decisiones; Capacidad de Recursos y Mano de Obra; Riesgo de Materiales y Compras; Señales del Living Graph; Próximos 14 Días; Señales de Presupuesto y Pronóstico; y Actividad Reciente. Todo es de solo lectura; las filas enlazan a Proyectos, Reportes, Equipo y vistas de proyecto.

Fuente: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts.
Verifica: inicia sesión y llega a / — el Command Center se muestra con tarjetas KPI, o un estado vacío si no hay proyectos.', '/ — generated from: src/app/[locale]/(app)/page.tsx, src/lib/command-center/service.ts'),
    ('screen-import', 'Import (Project Import Intelligence) screen', 'The Project Import Intelligence wizard at `/import`, reached from the navigation, the AI Operator hub, or the Command Center empty state; `?projectId=` preselects a merge target. The client wizard flows Upload → Analyze → Review → Import → Done, and by design "nothing is imported without preview and explicit approval". Upload accepts Excel (.xlsx), CSV, JSON, Word (.docx), PDF, TXT, and Markdown up to 25 MB (stored in the `project-imports` Supabase Storage bucket), with a mode choice: create a new project or merge into an existing one, plus project type. `analyzeImportJobAction` runs the `lib/import-intelligence` engines — `parseImportFile`, `extractCanonicalImport`/`aiExtractCanonicalImport`, `buildFieldMappings`, `validateCanonicalImport` — persisting to `project_import_jobs`, `project_import_raw_data`, `project_import_mappings`, `project_import_entities`, and `project_import_validation_results`, with audit rows in `project_import_audit_events`. The Review step shows tabs (summary, tasks, milestones, dependencies, resources, materials, budget, risks, warnings, raw data) where each extracted row has confidence, validation status, and an include/exclude toggle (`toggleImportEntityAction`). Approving calls `executeImportAction` → `executeImport`, which creates the real project entities, skips duplicates, and reports whether the critical path could be calculated; `rollbackImport` exists in the library. Related screens: Projects list, AI Operator, project Command Center.

Source: src/app/[locale]/(app)/import/{page,import-client,actions}.tsx|ts, src/lib/import-intelligence/*.
Verify: open Import, upload an .xlsx or CSV, review the extracted tabs, and press "Approve & import".', 'Pantalla Importación (Project Import Intelligence)', 'El asistente de Importación Inteligente de Proyectos en `/import`, accesible desde la navegación, el hub del Operador IA o el estado vacío del Command Center; `?projectId=` preselecciona el proyecto destino. El asistente fluye Subir → Analizar → Revisar → Importar → Listo, y por diseño "nada se importa sin vista previa y aprobación explícita". La subida acepta Excel (.xlsx), CSV, JSON, Word (.docx), PDF, TXT y Markdown hasta 25 MB (guardados en el bucket de Storage `project-imports`), con modo de creación de proyecto nuevo o fusión en uno existente, y tipo de proyecto. `analyzeImportJobAction` ejecuta los motores de `lib/import-intelligence` — `parseImportFile`, `extractCanonicalImport`/`aiExtractCanonicalImport`, `buildFieldMappings`, `validateCanonicalImport` — persistiendo en `project_import_jobs`, `project_import_raw_data`, `project_import_mappings`, `project_import_entities` y `project_import_validation_results`, con auditoría en `project_import_audit_events`. La revisión muestra pestañas (resumen, tareas, hitos, dependencias, recursos, materiales, presupuesto, riesgos, advertencias, datos crudos) donde cada fila tiene confianza, estado de validación e interruptor de incluir/excluir (`toggleImportEntityAction`). Aprobar llama `executeImportAction` → `executeImport`, que crea las entidades reales, omite duplicados e informa si se pudo calcular la ruta crítica; `rollbackImport` existe en la librería. Pantallas relacionadas: Lista de proyectos, Operador IA, Command Center del proyecto.

Fuente: src/app/[locale]/(app)/import/{page,import-client,actions}.tsx|ts, src/lib/import-intelligence/*.
Verifica: abre Importar, sube un .xlsx o CSV, revisa las pestañas extraídas y pulsa "Aprobar e importar".', '/import — generated from: src/app/[locale]/(app)/import/page.tsx, src/app/[locale]/(app)/import/import-client.tsx, src/app/[locale]/(app)/import/actions.ts, src/lib/import-intelligence/ai-extract.ts, src/lib/import-intelligence/execute.ts, src/lib/import-intelligence/extract.ts, src/lib/import-intelligence/parse.ts, src/lib/import-intelligence/validate.ts'),
    ('screen-labor-capacity-lookahead', 'Labor Lookahead screen', 'The Labor Lookahead screen answers "are the next weeks of work executable with available labor?" and lives at /projects/[projectId]/labor-capacity/lookahead (the "Lookahead" tab of the labor navigation). The server page (force-dynamic, read-only — no writes) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then precomputes deterministic results for both horizons: `computeLookahead` (3-week and 6-week), lookahead narratives (`buildLookaheadNarrative`), crew idle risk (`computeCrewIdleRisk`) and idle-risk summaries (`buildIdleRiskSummary`). The client offers a 3-Week / 6-Week horizon toggle, summary counts by readiness level, a weekly grid of upcoming activities with readiness badges and checklist status, and a narrative panel. For each not-ready activity it builds a readiness explanation (`buildReadinessExplanation`) covering the constraint, missing prerequisites and a recommended action. The crew idle risk section lists top recommended actions (with action-type labels) and per-crew entries showing days at risk, severity and the suggested mitigation; recommendations are display-only text — nothing is executed automatically. Related screens: screen-labor-capacity, screen-labor-capacity-workface, screen-living-graph (readiness overlay uses the same lookahead engine).
Source: lookahead/page.tsx, lookahead-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verify: project > Labor Capacity > Lookahead tab (/projects/[projectId]/labor-capacity/lookahead).', 'Pantalla Lookahead Laboral', 'La pantalla Lookahead Laboral responde "¿las próximas semanas de trabajo son ejecutables con la mano de obra disponible?" y vive en /projects/[projectId]/labor-capacity/lookahead (pestaña "Lookahead" de la navegación laboral). La página de servidor (force-dynamic, solo lectura, sin escrituras) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y precalcula resultados deterministas para ambos horizontes: `computeLookahead` (3 y 6 semanas), narrativas (`buildLookaheadNarrative`), riesgo de cuadrilla ociosa (`computeCrewIdleRisk`) y sus resúmenes (`buildIdleRiskSummary`). El cliente ofrece un alternador de horizonte de 3 o 6 semanas, conteos por nivel de preparación, una cuadrícula semanal de actividades próximas con insignias de preparación y estado de la lista de verificación, y un panel narrativo. Para cada actividad no lista construye una explicación de preparación (`buildReadinessExplanation`) con la restricción, los prerrequisitos faltantes y la acción recomendada. La sección de riesgo de cuadrilla ociosa lista las principales acciones recomendadas y entradas por cuadrilla con días en riesgo, severidad y mitigación sugerida; las recomendaciones son solo texto informativo, nada se ejecuta automáticamente. Pantallas relacionadas: screen-labor-capacity, screen-labor-capacity-workface, screen-living-graph (la superposición de preparación usa el mismo motor).
Fuente: lookahead/page.tsx, lookahead-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verifica: proyecto > Capacidad Laboral > pestaña Lookahead (/projects/[projectId]/labor-capacity/lookahead).', '/projects/[projectId]/labor-capacity/lookahead — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/lookahead/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/lookahead/lookahead-client.tsx, src/lib/labor/lookahead.ts, src/lib/labor/crew-idle-risk.ts, src/lib/labor/readiness-explanation.ts'),
    ('screen-labor-capacity-workface', 'Workface Readiness Board screen', 'The Workface Readiness Board asks "are upcoming activities ready to execute?" and shows readiness, blockers and risk at a glance, at /projects/[projectId]/labor-capacity/workface (the "Workface Board" tab of the labor navigation). The server page (force-dynamic, read-only — no server actions here) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then precomputes `computeLookahead` and `computeCrewIdleRisk` for both 3-week and 6-week horizons. The client joins lookahead activities with idle-risk data into board rows and sorts them worst-first by readiness severity, then idle risk. Summary cards show total, ready, at-risk and blocked activities plus overall readiness percent. Filters include trade, readiness level, critical-path-only and blocked-only. Each row shows readiness percent, missing prerequisites (unchecked readiness-checklist items), blocker types, idle risk with days at risk, and a critical-path flag; expanding a row reveals the readiness checklist, assigned resources, a recommended action and the downstream impact, with `buildReadinessExplanation` generating the explanation for not-ready activities. Everything is deterministic and display-only; the screen writes nothing. Related screens: screen-labor-capacity, screen-labor-capacity-lookahead, screen-living-graph (readiness overlay).
Source: workface/page.tsx, workface-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verify: project > Labor Capacity > Workface Board tab (/projects/[projectId]/labor-capacity/workface).', 'Pantalla Tablero de Preparación del Frente de Obra', 'El Tablero de Preparación del Frente de Obra pregunta "¿las actividades próximas están listas para ejecutarse?" y muestra preparación, bloqueos y riesgo de un vistazo, en /projects/[projectId]/labor-capacity/workface (pestaña "Frente de Obra" de la navegación laboral). La página de servidor (force-dynamic, solo lectura, sin acciones de servidor) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y precalcula `computeLookahead` y `computeCrewIdleRisk` para los horizontes de 3 y 6 semanas. El cliente une las actividades del lookahead con los datos de riesgo ocioso en filas de tablero y las ordena de peor a mejor por severidad de preparación y luego por riesgo ocioso. Las tarjetas de resumen muestran actividades totales, listas, en riesgo y bloqueadas, más el porcentaje general de preparación. Los filtros incluyen oficio, nivel de preparación, solo-ruta-crítica y solo-bloqueadas. Cada fila muestra el porcentaje de preparación, los prerrequisitos faltantes (elementos sin marcar de la lista de verificación), los tipos de bloqueo, el riesgo ocioso con días en riesgo y la marca de ruta crítica; al expandirla se ven la lista de verificación, los recursos asignados, la acción recomendada y el impacto aguas abajo, con `buildReadinessExplanation` generando la explicación de las actividades no listas. Todo es determinista y solo de visualización; la pantalla no escribe nada. Pantallas relacionadas: screen-labor-capacity, screen-labor-capacity-lookahead, screen-living-graph (superposición de preparación).
Fuente: workface/page.tsx, workface-client.tsx, lib/labor/lookahead.ts, lib/labor/crew-idle-risk.ts.
Verifica: proyecto > Capacidad Laboral > pestaña Frente de Obra (/projects/[projectId]/labor-capacity/workface).', '/projects/[projectId]/labor-capacity/workface — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/workface/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/workface/workface-client.tsx, src/lib/labor/lookahead.ts, src/lib/labor/crew-idle-risk.ts'),
    ('screen-labor-capacity', 'Labor Capacity & Skills Matrix screen', 'The Labor Capacity & Skills Matrix monitors construction labor gaps, utilization and crew readiness across trades and weeks, at /projects/[projectId]/labor-capacity (the "Capacity Matrix" tab of the labor navigation, alongside Lookahead, Workface Board and a Variance tab — note: the Variance tab links to /labor-capacity/variance, a route with no page in the codebase). The server page (force-dynamic, read-only) fetches `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` and `trade_taxonomy`, then runs the deterministic `computeLaborCapacity` engine; this screen writes nothing. The client shows four summary cards (total trades, shortage weeks, critical trades, max utilization), a deterministic narrative panel built with `buildCapacitySummary` (overall severity, key numbers, cascade risks), and filters by trade, week, milestone, location and critical-path-only. The main table lists weekly capacity gaps per trade: required vs available headcount and hours, gap, utilization, risk level and critical-path flag; expanding a row shows a gap explanation (`buildGapExplanation`) plus the related activities and resources. A skills panel describes resources from the trade taxonomy (type, skill level, constraints, availability windows). An empty state appears when no labor data exists. Related screens: screen-labor-capacity-lookahead, screen-labor-capacity-workface, screen-living-graph (laborCapacity overlay), screen-resource-capacity.
Source: labor-capacity/page.tsx, labor-capacity-client.tsx, components/labor/labor-capacity-nav.tsx.
Verify: open a project > Labor Capacity (/projects/[projectId]/labor-capacity).', 'Pantalla Capacidad Laboral y Matriz de Habilidades', 'La Matriz de Capacidad Laboral y Habilidades monitorea brechas de mano de obra, utilización y preparación de cuadrillas por oficio y semana, en /projects/[projectId]/labor-capacity (pestaña "Matriz de Capacidad" de la navegación laboral, junto a Lookahead, Frente de Obra y una pestaña Varianza — nota: la pestaña Varianza enlaza a /labor-capacity/variance, una ruta sin página en el código). La página de servidor (force-dynamic, solo lectura) consulta `labor_resources`, `construction_activities`, `activity_dependencies`, `milestones` y `trade_taxonomy`, y ejecuta el motor determinista `computeLaborCapacity`; esta pantalla no escribe nada. El cliente muestra cuatro tarjetas de resumen (oficios totales, semanas con déficit, oficios críticos, utilización máxima), un panel narrativo determinista construido con `buildCapacitySummary` (severidad general, cifras clave, riesgos en cascada) y filtros por oficio, semana, hito, ubicación y solo-ruta-crítica. La tabla principal lista las brechas semanales por oficio: personal y horas requeridas vs disponibles, brecha, utilización, nivel de riesgo y marca de ruta crítica; al expandir una fila se ve la explicación de la brecha (`buildGapExplanation`) y las actividades y recursos relacionados. Un panel de habilidades describe los recursos según la taxonomía de oficios (tipo, nivel, restricciones, ventanas de disponibilidad). Si no hay datos laborales aparece un estado vacío. Pantallas relacionadas: screen-labor-capacity-lookahead, screen-labor-capacity-workface, screen-living-graph (superposición de capacidad laboral), screen-resource-capacity.
Fuente: labor-capacity/page.tsx, labor-capacity-client.tsx, components/labor/labor-capacity-nav.tsx.
Verifica: abre un proyecto > Capacidad Laboral (/projects/[projectId]/labor-capacity).', '/projects/[projectId]/labor-capacity — generated from: src/app/[locale]/(app)/projects/[projectId]/labor-capacity/page.tsx, src/app/[locale]/(app)/projects/[projectId]/labor-capacity/labor-capacity-client.tsx, src/components/labor/labor-capacity-nav.tsx, src/lib/labor/capacity.ts, src/lib/labor/explanation.ts'),
    ('screen-landing', 'Landing screen', 'The public marketing page of ProjectOps360°, served at `/landing` outside the authenticated app shell and outside the `[locale]` route group. It is a static composition of eleven landing components rendered in order: Hero (with the landing navigation bar, an animated hero graph, and a logos strip), Capabilities, Methodology, Industries, Comms, AiSection, Quote, About, Pricing, FinalCta, and Footer. The Pricing section presents four plans defined in code — Personal (per month), Team and Business (per user/month, Business is the featured plan), and Enterprise (no listed price) — with feature checklists. Copy is translated with react-i18next (unlike the app, which uses next-intl), and call-to-action buttons resolve their destinations through the `useAuthPaths` helper, pointing visitors to the login and signup screens. The page reads and writes no Supabase data and calls no server actions; it is purely presentational marketing content with decorative animated backgrounds. Related screens: Login (`/login`) and Signup (`/signup`), which the hero, pricing, and final CTA link into.

Source: src/app/landing/page.tsx, src/components/landing/*.tsx.
Verify: open /landing while logged out and scroll through the sections; the CTAs lead to /login and /signup.', 'Pantalla Landing', 'La página pública de marketing de ProjectOps360°, servida en `/landing` fuera del shell autenticado de la aplicación y fuera del grupo de rutas `[locale]`. Es una composición estática de once componentes de landing renderizados en orden: Hero (con la barra de navegación, un grafo animado y una franja de logotipos), Capabilities, Methodology, Industries, Comms, AiSection, Quote, About, Pricing, FinalCta y Footer. La sección de precios presenta cuatro planes definidos en el código — Personal (por mes), Team y Business (por usuario/mes; Business es el plan destacado) y Enterprise (sin precio listado) — con listas de características. Los textos se traducen con react-i18next (a diferencia de la aplicación, que usa next-intl), y los botones de llamada a la acción resuelven su destino mediante el helper `useAuthPaths`, dirigiendo a los visitantes a las pantallas de inicio de sesión y registro. La página no lee ni escribe datos en Supabase y no llama server actions; es contenido de marketing puramente presentacional. Pantallas relacionadas: Login (`/login`) y Signup (`/signup`).

Fuente: src/app/landing/page.tsx, src/components/landing/*.tsx.
Verifica: abre /landing sin sesión iniciada y recorre las secciones; los CTA llevan a /login y /signup.', '/landing — generated from: src/app/landing/page.tsx, src/components/landing/hero.tsx, src/components/landing/pricing.tsx, src/components/landing/capabilities.tsx'),
    ('screen-living-graph-realtime', 'Realtime Living Graph screen', 'The Realtime Living Graph ("Project Execution Map — Realtime") is a full-viewport, live, hierarchy-safe execution graph reached via the "Realtime" link in the Living Graph header. The server page (force-dynamic) loads an initial snapshot through `loadRealtimeGraphSnapshot`, which reads from the canonical owners (never `process_nodes` or raw events) and fails closed — if the project is not in the caller''s organization it renders notFound. The client (RealtimeLivingGraph) narrows the graph NotebookLM-style: you start from a milestone-scoped root and click nodes to reveal the next level; "Expand all" stays scoped to the selected milestone/task, with a large-graph guard that asks you to arm the action before expanding big scopes. It includes milestone focus, a node inspector, a sync bar, and evidence/dependency overlays. Delivery combines a live Supabase channel push with a polling fallback: the client polls a cheap content signature via `getRealtimeGraphSignatureAction` and refetches the approved snapshot delta via `getRealtimeGraphSnapshotAction` only when the signature changes. The screen never mutates canonical data. A header link returns to the Living Graph. Related screens: screen-living-graph, screen-execution-map.
Source: realtime/page.tsx, realtime/actions.ts, components/living-graph-realtime/realtime-living-graph.tsx.
Verify: Living Graph > "Realtime" button (/projects/[projectId]/execution-map/realtime).', 'Pantalla Grafo Vivo en Tiempo Real', 'El Grafo Vivo en Tiempo Real ("Mapa de Ejecución del Proyecto — Tiempo Real") es un grafo de ejecución en vivo, jerárquicamente seguro y a pantalla completa, accesible con el enlace "Tiempo real" del encabezado del Grafo Vivo. La página de servidor (force-dynamic) carga un snapshot inicial con `loadRealtimeGraphSnapshot`, que lee de los dueños canónicos (nunca de `process_nodes` ni de eventos crudos) y falla de forma cerrada: si el proyecto no pertenece a la organización del usuario, muestra notFound. El cliente (RealtimeLivingGraph) estrecha el grafo al estilo NotebookLM: se parte de una raíz por hito y se hace clic en los nodos para revelar el siguiente nivel; "Expandir todo" queda acotado al hito o tarea seleccionados, con una salvaguarda para grafos grandes que pide confirmación antes de expandir ámbitos extensos. Incluye enfoque por hito, un inspector de nodos, una barra de sincronización y superposiciones de evidencia y dependencias. La entrega combina un canal en vivo de Supabase con un respaldo por sondeo: el cliente consulta una firma de contenido barata con `getRealtimeGraphSignatureAction` y solo cuando cambia recarga el delta aprobado con `getRealtimeGraphSnapshotAction`. La pantalla nunca muta datos canónicos. Un enlace regresa al Grafo Vivo. Pantallas relacionadas: screen-living-graph, screen-execution-map.
Fuente: realtime/page.tsx, realtime/actions.ts, components/living-graph-realtime/realtime-living-graph.tsx.
Verifica: Grafo Vivo > botón "Tiempo real" (/projects/[projectId]/execution-map/realtime).', '/projects/[projectId]/execution-map/realtime — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/realtime/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/realtime/actions.ts, src/lib/living-graph-realtime-ui/load-snapshot.ts, src/components/living-graph-realtime/realtime-living-graph.tsx'),
    ('screen-living-graph-view-blocker', 'Living Graph — Blocker View', 'The Blocker View highlights work that is explicitly blocked and the work it is holding back. It is activated from the Living Graph''s view selector (the "Overlay" dropdown in the toolbar, inside Execution Map). The rule is deterministic: a node lights up if it has an explicit active impediment (`is_blocked`) or is a `blocker_event` node; nodes reachable downstream of a blocked node (within 6 dependency hops) stay at normal emphasis because they are exposed to the blockage; everything else is dimmed. Per ADR-006 (REG-006/REG-008), "blocked" is never derived from dependencies: an item merely waiting on an unfinished predecessor is counted separately as "waiting on dependency", and a completed or cancelled task is never shown as blocked even if a stale blocked flag remains. The toolbar summary therefore reports blocked and waiting counts separately. Selecting a blocked node shows the insight "this node is blocked and is holding back N downstream nodes" in the detail panel. This overlay has no Sprint #3 clarity card or dedicated empty state: with zero blocked items the whole graph simply appears dimmed. Recommended action: open each blocked item, resolve or escalate the recorded impediment, and let waiting items clear themselves by finishing predecessors. Source: src/lib/graph/living-graph-analysis.ts, src/lib/graph/living-graph-status.ts, src/components/graph/living-graph-view.tsx. Verify: open a project → Execution Map → Living Graph → view selector → Blocker View.', 'Living Graph — Vista de bloqueos', 'La Vista de bloqueos resalta el trabajo bloqueado explícitamente y el trabajo que está frenando. Se activa desde el selector de vistas del Living Graph (menú "Capa" de la barra de herramientas, dentro de Execution Map). La regla es determinista: un nodo se resalta si tiene un impedimento activo explícito (`is_blocked`) o es un nodo `blocker_event`; los nodos alcanzables aguas abajo de un nodo bloqueado (hasta 6 saltos de dependencia) quedan con énfasis normal porque están expuestos al bloqueo; el resto se atenúa. Según ADR-006 (REG-006/REG-008), "bloqueado" nunca se deriva de dependencias: un elemento que solo espera un predecesor sin terminar se cuenta aparte como "esperando dependencia", y una tarea completada o cancelada nunca aparece bloqueada aunque conserve una marca obsoleta. Por eso la barra muestra los conteos de bloqueados y en espera por separado. Al seleccionar un nodo bloqueado, el panel de detalle muestra "este nodo está bloqueado y frena N nodos aguas abajo". Esta capa no tiene tarjeta de claridad de Sprint #3 ni estado vacío propio: sin elementos bloqueados, todo el grafo simplemente se ve atenuado. Acción recomendada: abre cada elemento bloqueado, resuelve o escala el impedimento registrado, y deja que los elementos en espera se liberen al terminar sus predecesores. Fuente: src/lib/graph/living-graph-analysis.ts, src/lib/graph/living-graph-status.ts, src/components/graph/living-graph-view.tsx. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Blocker View.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/lib/graph/living-graph-status.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-toolbar.tsx, messages/en.json'),
    ('screen-living-graph-view-bottleneck', 'Living Graph — Bottleneck View', 'Bottleneck View highlights nodes where work funnels through and delays would ripple widely. The rule is deterministic (`bottleneckScore` in living-graph-analysis.ts): a 0–1 composite of connection density (the node''s in+out links relative to the busiest node, 45%), duration (up to 14 days, 25%), downstream reach (up to 10 dependent nodes, 20%), plus blocked pressure (+0.35 if the node is blocked, +0.15 per incoming "blocked" edge). Nodes scoring ≥ 0.6 are highlighted with their accent ring, 0.35–0.6 stay normal, and everything below dims — so the busy hubs pop out. In plain terms: a bottleneck is a long or blocked step that many things feed into and many things wait on. The shared legend marks the bottleneck indicator in orange (#f97316), and the health header counts "Active Bottlenecks" (score ≥ 0.6). Activate it from the toolbar''s "Layer" view selector. It requires edges (dependencies), node durations or dates, and blocked flags; sparse graphs with few edges simply dim everything — this view has no dedicated clarity card or empty state (it is not one of the Sprint #3 advanced overlays). Recommended action per the node insight: split responsibilities or parallelize the work feeding through the highlighted node.
Source: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/lib/graph/living-graph-styles.ts.
Verify: open a project → Execution Map → Living Graph → view selector → Bottleneck View.', 'Living Graph — Vista de cuellos de botella', 'La Vista de cuellos de botella resalta los nodos por donde se embudo el trabajo y donde un retraso se propagaría más. La regla es determinista (`bottleneckScore` en living-graph-analysis.ts): un compuesto 0–1 de densidad de conexiones (enlaces entrantes+salientes respecto al nodo más conectado, 45%), duración (hasta 14 días, 25%), alcance descendente (hasta 10 nodos dependientes, 20%), más presión de bloqueo (+0.35 si el nodo está bloqueado, +0.15 por cada arista entrante de tipo "blocked"). Los nodos con puntuación ≥ 0.6 se resaltan, entre 0.35 y 0.6 quedan normales y el resto se atenúa. En lenguaje llano: un cuello de botella es un paso largo o bloqueado del que muchas cosas dependen y al que muchas cosas alimentan. La leyenda marca el indicador en naranja (#f97316) y la cabecera de salud cuenta los "cuellos de botella activos" (≥ 0.6). Se activa desde el selector de vistas ("Capa") de la barra de herramientas. Requiere aristas (dependencias), duraciones o fechas y marcas de bloqueo; en grafos con pocas aristas simplemente se atenúa todo — esta vista no tiene tarjeta de claridad ni estado vacío propio (no es uno de los overlays avanzados del Sprint #3). Acción recomendada: dividir responsabilidades o paralelizar el trabajo que pasa por el nodo resaltado.
Fuente: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/lib/graph/living-graph-styles.ts.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de cuellos de botella.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-legend.tsx, src/lib/graph/living-graph-styles.ts'),
    ('screen-living-graph-view-critical-path', 'Living Graph — Critical Path View', 'Critical Path View isolates the chain of work that determines the project end date: any delay on it delays everything. Emphasis is binary — nodes with `onCriticalPath` are highlighted, all others are dimmed, and the connecting critical edges render with visible flow only in this view. A node is on the path if the source data flags it critical (`node.isCritical`) or if it belongs to the client-side approximation in living-graph-analysis.ts: the longest duration-weighted path through the acyclic part of the graph (Kahn topological order + dynamic programming, counting node durations plus edge lag days; cycle-closing edges are skipped, and a "path" of fewer than two nodes is discarded). Separately, the full CPM engine (src/lib/execution/critical-path.ts) computes forward/backward passes, total float ≤ 0 and near-critical tasks (float ≤ 3 days) over roadmap tasks and dependencies — the graph approximation is the fallback when those flags are absent. The critical color is rose (#f43f5e); critical nodes also show a small rose dot in every other view. The view is deep-linkable via `?overlay=criticalPath`. It requires connected dependencies with durations or dates; a graph without chains dims everything (no dedicated empty-state card). Action: protect and monitor highlighted work, combine with the "Critical only" filter or What-if Simulation to test delays.
Source: src/lib/graph/living-graph-analysis.ts, src/lib/execution/critical-path.ts, src/components/graph/living-graph-view.tsx.
Verify: open a project → Execution Map → Living Graph → view selector → Critical Path View.', 'Living Graph — Vista de ruta crítica', 'La Vista de ruta crítica aísla la cadena de trabajo que determina la fecha final del proyecto: cualquier retraso ahí retrasa todo. El énfasis es binario: los nodos con `onCriticalPath` se resaltan, el resto se atenúa, y las aristas críticas muestran flujo visible solo en esta vista. Un nodo está en la ruta si los datos de origen lo marcan crítico (`node.isCritical`) o si pertenece a la aproximación de living-graph-analysis.ts: la ruta más larga ponderada por duración sobre la parte acíclica del grafo (orden topológico de Kahn + programación dinámica, sumando duraciones de nodos y días de lag de aristas; se omiten aristas que cierran ciclos y se descarta una "ruta" de menos de dos nodos). Aparte, el motor CPM completo (src/lib/execution/critical-path.ts) calcula pases hacia adelante/atrás, holgura total ≤ 0 y tareas casi críticas (holgura ≤ 3 días) sobre tareas y dependencias del roadmap; la aproximación del grafo es el respaldo cuando faltan esas marcas. El color crítico es rosa (#f43f5e); los nodos críticos muestran además un punto rosa en las demás vistas. Admite enlace directo con `?overlay=criticalPath`. Requiere dependencias conectadas con duraciones o fechas; un grafo sin cadenas se atenúa por completo (sin tarjeta de estado vacío propia). Acción: proteger y vigilar el trabajo resaltado, y combinar con el filtro "solo crítico" o la simulación what-if.
Fuente: src/lib/graph/living-graph-analysis.ts, src/lib/execution/critical-path.ts, src/components/graph/living-graph-view.tsx.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de ruta crítica.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/lib/execution/critical-path.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-node.tsx, src/lib/graph/living-graph-styles.ts'),
    ('screen-living-graph-view-labor-capacity', 'Living Graph — Labor Capacity View', 'The Labor Capacity View shows where construction labor shortages threaten the plan. Activate it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "Labor Capacity View"). It only has data when the project has labor resources and construction activities; the server computes it non-fatally, so without that data the overlay changes nothing except dimming nodes. The rule is deterministic per trade and week: required vs available headcount ratio classifies shortage risk — over 1.0 is low, over 1.10 medium, over 1.25 high, over 1.50 critical (zero available with demand is critical). At Activities/Events level, high/critical weekly gaps become synthetic "labor risk" nodes ("<Trade> shortage <week>") connected by labor-constrained edges to affected milestones, and by delayed edges to their tasks when the gap touches the critical path. Nodes in affected milestones carry the worst gap as metadata and a badge. Emphasis maps shortage risk to a 0–1 score (critical=1, high=0.7, medium=0.4, low=0.15): 0.5+ highlights, 0.2+ stays normal, the rest dims. The detail panel explains the gap: trade, week, missing headcount, required vs available. No Sprint #3 clarity card exists for this overlay. Action: rebalance crews, adjust schedules, or resolve gaps in the labor module. Source: src/lib/labor/capacity.ts, src/lib/graph/labor-graph-mapping.ts, src/lib/graph/living-graph-analysis.ts. Verify: open a project → Execution Map → Living Graph → view selector → Labor Capacity View.', 'Living Graph — Vista de capacidad laboral', 'La Vista de capacidad laboral muestra dónde la escasez de mano de obra de construcción amenaza el plan. Se activa desde el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Vista de capacidad laboral"). Solo tiene datos cuando el proyecto tiene recursos laborales y actividades de construcción; el servidor la calcula de forma no fatal, así que sin esos datos la capa solo atenúa los nodos. La regla es determinista por oficio y semana: la razón entre headcount requerido y disponible clasifica el riesgo de escasez — mayor a 1.0 es bajo, mayor a 1.10 medio, mayor a 1.25 alto, mayor a 1.50 crítico (disponible cero con demanda es crítico). En nivel Actividades/Eventos, las brechas semanales altas/críticas se convierten en nodos sintéticos de "riesgo laboral" ("escasez de <oficio> <semana>"), conectados con aristas labor-constrained a los hitos afectados y con aristas delayed a sus tareas cuando la brecha toca la ruta crítica. Los nodos de hitos afectados llevan la peor brecha como metadato y una insignia. El énfasis mapea el riesgo a un puntaje 0–1 (crítico=1, alto=0.7, medio=0.4, bajo=0.15): 0.5+ se resalta, 0.2+ queda normal, el resto se atenúa. El panel de detalle explica la brecha: oficio, semana, headcount faltante, requerido vs disponible. Esta capa no tiene tarjeta de claridad de Sprint #3. Acción: rebalancea cuadrillas, ajusta el cronograma o resuelve brechas en el módulo laboral. Fuente: src/lib/labor/capacity.ts, src/lib/graph/labor-graph-mapping.ts, src/lib/graph/living-graph-analysis.ts. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Labor Capacity View.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/labor/capacity.ts, src/lib/graph/labor-graph-mapping.ts, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-detail-panel.tsx'),
    ('screen-living-graph-view-normal', 'Living Graph — Normal View', 'Normal View is the default, neutral layer of the Living Graph. It applies no emphasis at all: in `nodeOverlayEmphasis` every node resolves to "normal", so nothing is highlighted or dimmed. You see the full project graph as-is — nodes colored by type (task, milestone gate, decision, document link, risk event, etc.), edges styled by relationship type (caused, enabled, blocked in red, delayed as dashed amber curves, informed as thin dotted lines). Baseline indicators still appear on every node regardless of view: a red lock for blocked items, an amber link icon for waiting-on-dependency, a rose dot for critical-path membership, risk-level dots, evidence-gap icons, and progress bars. It is selected by default and restored from saved view preferences; switch back to it from the "Layer" view selector in the toolbar. There is no per-view calculation — the deterministic analysis pass (`analyzeGraph`) still runs to power the status icons and the health header, but nothing changes visual weight. Data required: project process events (nodes and edges); with no events the graph itself is empty, and there is no view-specific empty state. Use it to orient yourself with search, filters and detail levels (Milestones/Activities/Events) before switching to a diagnostic view. Not a placeholder — fully functional.
Source: src/components/graph/living-graph-toolbar.tsx, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-node.tsx.
Verify: open a project → Execution Map → Living Graph → view selector → Normal View.', 'Living Graph — Vista normal', 'La Vista normal es la capa neutral por defecto del Living Graph. No aplica ningún énfasis: en `nodeOverlayEmphasis` todos los nodos resuelven a "normal", así que nada se resalta ni se atenúa. Se ve el grafo completo tal cual: nodos coloreados por tipo (tarea, hito, decisión, enlace de documento, evento de riesgo, etc.) y aristas según el tipo de relación (caused, enabled, blocked en rojo, delayed como curva discontinua ámbar, informed como línea punteada fina). Los indicadores base aparecen en cualquier vista: candado rojo si está bloqueado, icono ámbar si espera una dependencia, punto rosa si está en la ruta crítica, puntos de riesgo, icono de brecha de evidencia y barras de progreso. Es la vista seleccionada por defecto y se restaura desde las preferencias guardadas; se vuelve a ella con el selector de vistas ("Capa") de la barra de herramientas. No hay cálculo propio: el análisis determinista (`analyzeGraph`) sigue ejecutándose para los iconos de estado y la cabecera de salud, pero no cambia el peso visual. Requiere los eventos de proceso del proyecto; sin eventos el grafo queda vacío y no hay estado vacío específico de esta vista. Úsala para orientarte con búsqueda, filtros y niveles de detalle antes de pasar a una vista diagnóstica. No es un placeholder.
Fuente: src/components/graph/living-graph-toolbar.tsx, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-node.tsx.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista normal.', '/projects/[projectId]/execution-map/living-graph — generated from: src/components/graph/living-graph-toolbar.tsx, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-node.tsx, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-legend.tsx'),
    ('screen-living-graph-view-rework', 'Living Graph — Rework View', 'Rework View surfaces work that went backwards — steps the project had to revisit after moving forward. The rule is deterministic and binary (`reworkSignal` in living-graph-analysis.ts): a node is highlighted if it participates in a detected cycle (a loop found by bounded depth-first search, cycles up to 8 nodes, at most 10 reported) or if it touches a "delayed" edge in either direction; every other node is dimmed. In user terms: rework is a step caught in a loop or connected to a delay signal. Delayed edges themselves render as dashed, curved amber lines, and the shared legend marks the rework indicator in amber (#f59e0b). The executive health header counts "Rework Signals", and each of them lowers the composite health score. Activate the view from the toolbar''s "Layer" view selector. It requires edges with meaningful types — specifically "delayed" edges or actual loops in the event flow; a clean linear project dims entirely, and this view has no dedicated clarity card or empty state (it is not one of the Sprint #3 advanced overlays). The node insight explains that work likely came back to the highlighted step; the recommended action is to review what caused the loop and add an acceptance gate before that step so the same work is not redone again.
Source: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/lib/graph/living-graph-styles.ts.
Verify: open a project → Execution Map → Living Graph → view selector → Rework View.', 'Living Graph — Vista de retrabajo', 'La Vista de retrabajo muestra el trabajo que retrocedió: pasos que el proyecto tuvo que revisitar después de avanzar. La regla es determinista y binaria (`reworkSignal` en living-graph-analysis.ts): un nodo se resalta si participa en un ciclo detectado (bucle hallado por búsqueda en profundidad acotada, ciclos de hasta 8 nodos, máximo 10 reportados) o si toca una arista "delayed" en cualquier dirección; el resto de nodos se atenúa. En lenguaje de usuario: retrabajo es un paso atrapado en un bucle o conectado a una señal de retraso. Las aristas "delayed" se dibujan como curvas discontinuas en ámbar, y la leyenda marca el indicador de retrabajo en ámbar (#f59e0b). La cabecera ejecutiva cuenta las "señales de retrabajo" y cada una baja la puntuación compuesta de salud. Se activa desde el selector de vistas ("Capa") de la barra de herramientas. Requiere aristas con tipos significativos — en concreto aristas "delayed" o bucles reales en el flujo de eventos; un proyecto lineal y limpio se atenúa por completo, y esta vista no tiene tarjeta de claridad ni estado vacío propio (no es un overlay avanzado del Sprint #3). La acción recomendada: revisar qué causó el bucle y añadir una puerta de aceptación antes de ese paso para no repetir el mismo trabajo.
Fuente: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/lib/graph/living-graph-styles.ts.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de retrabajo.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/lib/graph/living-graph-styles.ts, src/components/graph/living-graph-legend.tsx'),
    ('screen-living-graph-view-risk', 'Living Graph — Risk View', 'Risk View shows project risks and the work they threaten, so you act on the risks that matter. Emphasis is deterministic (`riskScore` in living-graph-analysis.ts): the node''s declared risk level (low 0.2 / medium 0.55 / high 1.0, weighted 50%) + 0.3 if blocked + up to 0.2 for downstream reach (10+ dependents) + 0.15 if it also shows a rework signal, capped at 1. Score ≥ 0.5 highlights, 0.25–0.5 stays normal, below dims. Plainly: a risky node is high-risk or blocked work that many other things depend on. This is a Sprint #3 "advanced overlay" with its own clarity card (overlay-metadata.ts): legend — red high risk, amber medium, green low, slate unlinked risk; data requirements — risks, risk→task/milestone links, severity/impact, status; recommended layout hierarchical. States are explicit: empty ("No risks to show") when there are no risk-event nodes and no medium/high-risk work; incomplete when some risk nodes have zero connections ("Some risks are not linked to tasks or milestones yet"). Isabella can narrate this card but never invents risks. Known limitation: node-level risk detail (severity/probability/owner) is not yet mapped into node metadata. Action: review high-impact risks and link unlinked risks to the affected task or milestone.
Source: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, docs/product-brain/28-sprint-03-overlay-clarity.md.
Verify: open a project → Execution Map → Living Graph → view selector → Risk View.', 'Living Graph — Vista de riesgo', 'La Vista de riesgo muestra los riesgos del proyecto y el trabajo que amenazan, para actuar sobre los que importan. El énfasis es determinista (`riskScore` en living-graph-analysis.ts): nivel de riesgo declarado del nodo (bajo 0.2 / medio 0.55 / alto 1.0, ponderado al 50%) + 0.3 si está bloqueado + hasta 0.2 por alcance descendente (10+ dependientes) + 0.15 si además tiene señal de retrabajo, con tope 1. Con ≥ 0.5 se resalta, entre 0.25 y 0.5 queda normal y por debajo se atenúa. En llano: un nodo riesgoso es trabajo de alto riesgo o bloqueado del que dependen muchas otras cosas. Es un "overlay avanzado" del Sprint #3 con tarjeta de claridad propia (overlay-metadata.ts): leyenda — rojo riesgo alto, ámbar medio, verde bajo, gris riesgo sin vincular; datos requeridos — riesgos, enlaces riesgo→tarea/hito, severidad/impacto, estado; layout recomendado jerárquico. Los estados son explícitos: vacío ("Sin riesgos que mostrar") cuando no hay nodos de riesgo ni trabajo de riesgo medio/alto; incompleto cuando algunos riesgos no tienen conexiones ("Algunos riesgos aún no están vinculados a tareas o hitos"). Isabella puede narrar la tarjeta pero nunca inventa riesgos. Limitación conocida: el detalle a nivel de nodo (severidad/probabilidad/responsable) aún no está mapeado en los metadatos. Acción: revisar los riesgos de alto impacto y vincular los no enlazados a la tarea o hito afectado.
Fuente: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, docs/product-brain/28-sprint-03-overlay-clarity.md.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de riesgo.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, src/components/graph/living-graph-view.tsx, docs/product-brain/28-sprint-03-overlay-clarity.md'),
    ('screen-living-graph-view-sop-candidate', 'Living Graph — SOP Candidate View', 'SOP Candidate View highlights clean, well-evidenced, repeatable work worth turning into a Standard Operating Procedure. The rule is deterministic (`sopCandidateScore` in living-graph-analysis.ts): a node only qualifies if it is completed (status done/completed/tested), not blocked, and shows no rework signal; its score is then 0.4 base + up to 0.35 for evidence (the inverse of its traceability gap) + up to 0.25 for connectivity (about six links saturates it). Score ≥ 0.6 highlights green, anything above 0 stays normal, zero dims. Plainly: an SOP candidate is a finished step that ran without loops or blockers and left a documented trail. This is a Sprint #3 advanced overlay with its own clarity card (overlay-metadata.ts): legend — green "suggested candidate", slate "not yet linked to a process"; data requirements — completed work, evidence/traceability, repeated patterns; recommended layout: force. The state is deterministic: empty ("No SOP candidates detected — complete and document more work") when no node reaches 0.6; the incomplete note reminds you candidates come from repeated patterns and may not yet be a formal process. The health header counts SOP Candidates. Note: candidates are heuristic suggestions — there is no formalize/dismiss workflow inside the graph yet; the metadata''s user action ("formalize as an SOP, or dismiss") is guidance, not a button. Action: review each candidate and document it as a reusable SOP.
Source: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, docs/product-brain/28-sprint-03-overlay-clarity.md.
Verify: open a project → Execution Map → Living Graph → view selector → SOP Candidate View.', 'Living Graph — Vista de candidatos a SOP', 'La Vista de candidatos a SOP resalta trabajo limpio, bien evidenciado y repetible que merece convertirse en un Procedimiento Operativo Estándar. La regla es determinista (`sopCandidateScore` en living-graph-analysis.ts): un nodo solo califica si está completado (estado done/completed/tested), no está bloqueado y no tiene señal de retrabajo; su puntuación es 0.4 base + hasta 0.35 por evidencia (el inverso de su brecha de trazabilidad) + hasta 0.25 por conectividad (unas seis conexiones la saturan). Con ≥ 0.6 se resalta en verde, por encima de 0 queda normal y con 0 se atenúa. En llano: un candidato a SOP es un paso terminado que corrió sin bucles ni bloqueos y dejó rastro documentado. Es un overlay avanzado del Sprint #3 con tarjeta de claridad propia (overlay-metadata.ts): leyenda — verde "candidato sugerido", gris "aún sin proceso formal"; datos requeridos — trabajo completado, evidencia/trazabilidad, patrones repetidos; layout recomendado: force. El estado es determinista: vacío ("Sin candidatos a SOP detectados — completa y documenta más trabajo") cuando ningún nodo llega a 0.6; el aviso de incompleto recuerda que los candidatos surgen de patrones repetidos y pueden no ser aún un proceso formal. La cabecera de salud cuenta los candidatos. Nota: son sugerencias heurísticas — todavía no existe un flujo de formalizar/descartar dentro del grafo; la acción de los metadatos es una guía, no un botón. Acción: revisar cada candidato y documentarlo como SOP reutilizable.
Fuente: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, docs/product-brain/28-sprint-03-overlay-clarity.md.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de candidatos a SOP.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/lib/graph/overlay-metadata.ts, src/components/graph/living-graph-view.tsx, docs/product-brain/28-sprint-03-overlay-clarity.md'),
    ('screen-living-graph-view-timeline-playback', 'Living Graph — Timeline Playback', 'Timeline Playback answers "how did the project get here?" by replaying recorded process events in chronological order. Activate it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "Timeline Playback"); the timeline layout is recommended. A transport bar appears under the canvas with play/pause, step back/forward, reset, a speed selector (0.5×–4×), a scrubber, and the current position plus the date and label of the event at the playhead. While playing, the playhead advances one event every 1.2 seconds divided by the speed. The visual rule is deterministic: nodes whose timestamp is after the playhead render as "not yet occurred" (dimmed, with their incoming edges hidden), nodes at or before it render as past, and the node matching the current event glows as active (matched per detail level; at the Milestones level no single node is active). The clarity legend reads: brand color = changed/active node, green = completed, slate = not yet occurred. Empty state is honest by design (PD-004): if all events fall on fewer than two distinct calendar days — typical of a one-shot import — the overlay shows "requires project history" instead of fake playback; limited history plays only what was captured. Switching overlays resets playback. Action: replay the evolution to understand decisions and delays. Source: src/lib/graph/overlay-metadata.ts, src/components/graph/living-graph-timeline.tsx, src/components/graph/living-graph-view.tsx. Verify: open a project → Execution Map → Living Graph → view selector → Timeline Playback.', 'Living Graph — Vista Reproducción de Línea de Tiempo', 'La Reproducción de Línea de Tiempo responde "¿cómo llegó aquí el proyecto?" reproduciendo los eventos de proceso registrados en orden cronológico. Se activa desde el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Reproducción temporal"); se recomienda el layout de línea de tiempo. Bajo el lienzo aparece una barra de transporte con reproducir/pausar, paso atrás/adelante, reiniciar, selector de velocidad (0.5×–4×), un deslizador y la posición actual con la fecha y etiqueta del evento en el cabezal. Al reproducir, el cabezal avanza un evento cada 1.2 segundos divididos por la velocidad. La regla visual es determinista: los nodos con fecha posterior al cabezal se muestran como "aún no ocurridos" (atenuados y con sus aristas entrantes ocultas), los anteriores como pasado, y el nodo del evento actual brilla como activo (según el nivel de detalle; en el nivel Milestones no hay nodo activo único). La leyenda indica: color de marca = nodo cambiado/activo, verde = completado, gris = aún no ocurrido. El estado vacío es honesto por diseño (PD-004): si todos los eventos caen en menos de dos días calendario distintos — típico de una importación única — la capa muestra "requiere historial del proyecto" en lugar de una reproducción falsa; con historial limitado solo se reproduce lo capturado. Cambiar de capa reinicia la reproducción. Acción: reproduce la evolución para entender decisiones y retrasos. Fuente: src/lib/graph/overlay-metadata.ts, src/components/graph/living-graph-timeline.tsx, src/components/graph/living-graph-view.tsx. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Timeline Playback.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/overlay-metadata.ts, src/components/graph/living-graph-timeline.tsx, src/components/graph/living-graph-view.tsx, docs/product-brain/28-sprint-03-overlay-clarity.md'),
    ('screen-living-graph-view-traceability-gap', 'Living Graph — Traceability Gap View', 'Traceability Gap View highlights work with little or no linked evidence — nodes whose history would be hard to audit later. The rule is deterministic (`traceabilityGapScore` in living-graph-analysis.ts): gap = 1 − evidence. Evidence uses the node''s own `traceabilityScore` when the backend provides one; otherwise it is computed as 0.35 per "informed" (informational/evidence) edge, +0.4 if the node is connected to a document-link or decision-cascade node, +0.2 if it has a description, capped at 1. Document-link nodes themselves always score gap 0 — they are evidence. Nodes with gap ≥ 0.6 are highlighted, 0.3–0.6 stay normal, below 0.3 dim. In plain terms: a traceability gap is a step with no attached documents, decisions or informational links. The indicator color is yellow (#eab308), and high-gap nodes show a file-question icon in every view; the health header counts "Trace Gaps" (≥ 0.6), which also feed the process-confidence metric. Activate it from the toolbar''s "Layer" view selector. It requires informational edges and document/decision nodes; a project that captures no evidence will highlight almost everything rather than show an empty state — this view has no Sprint #3 clarity card. Action: link the relevant document, decision or evidence to each highlighted node.
Source: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-node.tsx, src/lib/graph/living-graph-styles.ts.
Verify: open a project → Execution Map → Living Graph → view selector → Traceability Gap View.', 'Living Graph — Vista de brechas de trazabilidad', 'La Vista de brechas de trazabilidad resalta trabajo con poca o ninguna evidencia vinculada — nodos cuya historia sería difícil de auditar después. La regla es determinista (`traceabilityGapScore` en living-graph-analysis.ts): brecha = 1 − evidencia. La evidencia usa el `traceabilityScore` del nodo si el backend lo provee; si no, se calcula como 0.35 por cada arista "informed" (informativa/evidencia), +0.4 si el nodo está conectado a un nodo de documento o de cascada de decisión, +0.2 si tiene descripción, con tope 1. Los nodos de tipo documento siempre puntúan brecha 0 — ellos son la evidencia. Con brecha ≥ 0.6 el nodo se resalta, entre 0.3 y 0.6 queda normal y por debajo se atenúa. En lenguaje llano: una brecha de trazabilidad es un paso sin documentos, decisiones ni enlaces informativos adjuntos. El color del indicador es amarillo (#eab308) y los nodos con brecha alta muestran un icono de archivo con interrogación en todas las vistas; la cabecera de salud cuenta las brechas (≥ 0.6), que además alimentan la métrica de confianza del proceso. Se activa desde el selector de vistas ("Capa"). Requiere aristas informativas y nodos de documento/decisión; un proyecto sin evidencia capturada resaltará casi todo en vez de mostrar un estado vacío — esta vista no tiene tarjeta de claridad del Sprint #3. Acción: vincular el documento, decisión o evidencia pertinente a cada nodo resaltado.
Fuente: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-node.tsx, src/lib/graph/living-graph-styles.ts.
Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Vista de brechas de trazabilidad.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-node.tsx, src/lib/graph/living-graph-styles.ts'),
    ('screen-living-graph-view-variance', 'Living Graph — Variance View', 'The Variance View answers "what changed versus the plan?". Activate it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "Variance View"); it recommends the Activities level. Deterministic rule: for tracked construction activities, variance % = (actual − estimated hours) ÷ estimated × 100, classified as on track (≤10%), minor (≤25%), major (≤50%) or critical (>50%). A separate 0–100 schedule-risk score maps to none/low/medium/high/critical, and each deviation gets a likely cause (e.g. material delay, unresolved RFI, crew shortage, rework/quality) with a confidence, plus a trend versus trade peers. Emphasis: nodes without variance data dim; high/critical schedule risk or major/critical severity highlight; minor stays normal; on-track dims. Its Sprint #3 clarity card explains purpose and legend — red "Behind plan", orange "Over effort", green "Ahead/under", slate "No baseline" — and resolves an explicit empty state: without baseline/variance data it says the view "requires baseline/variance data" with a CTA to open the Delivery Framework; partial data shows "some items have no baseline yet". Selecting a node opens a variance block with severity + %, schedule risk + score, likely cause + confidence, and trend. Note the engine currently derives from construction/labor activity tracking. Action: review deviations and decide corrective action or re-baseline. Source: src/lib/graph/overlay-metadata.ts, src/lib/labor/labor-variance.ts, src/lib/labor/productivity-variance.ts, src/components/graph/variance-detail-block.tsx. Verify: open a project → Execution Map → Living Graph → view selector → Variance View.', 'Living Graph — Vista de Variación', 'La Vista de Variación responde "¿qué cambió respecto al plan?". Se activa desde el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Vista de Varianza"); recomienda el nivel Actividades. Regla determinista: para actividades de construcción con seguimiento, % de variación = (horas reales − estimadas) ÷ estimadas × 100, clasificada como en plan (≤10%), menor (≤25%), mayor (≤50%) o crítica (>50%). Un puntaje de riesgo de cronograma 0–100 se mapea a ninguno/bajo/medio/alto/crítico, y cada desviación recibe una causa probable (p. ej. retraso de material, RFI sin resolver, escasez de cuadrilla, retrabajo/calidad) con confianza, más una tendencia frente a pares del mismo oficio. Énfasis: los nodos sin datos de variación se atenúan; riesgo alto/crítico o severidad mayor/crítica se resalta; menor queda normal; en plan se atenúa. Su tarjeta de claridad de Sprint #3 explica propósito y leyenda — rojo "Atrasado", naranja "Sobre-esfuerzo", verde "Adelantado/bajo plan", gris "Sin línea base" — y resuelve un estado vacío explícito: sin línea base/datos de variación indica que la vista "requiere línea base" con un CTA para abrir el Marco de Entrega; con datos parciales avisa que "algunos elementos aún no tienen línea base". Al seleccionar un nodo se abre un bloque con severidad + %, riesgo + puntaje, causa probable + confianza y tendencia. Nota: el motor hoy se deriva del seguimiento de actividades de construcción/laborales. Acción: revisa las desviaciones y decide acción correctiva o re-establecer la línea base. Fuente: src/lib/graph/overlay-metadata.ts, src/lib/labor/labor-variance.ts, src/lib/labor/productivity-variance.ts, src/components/graph/variance-detail-block.tsx. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Variance View.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/overlay-metadata.ts, src/lib/labor/labor-variance.ts, src/lib/labor/productivity-variance.ts, src/lib/labor/variance-cause-classification.ts, src/lib/graph/labor-graph-mapping.ts, src/components/graph/variance-detail-block.tsx, docs/product-brain/28-sprint-03-overlay-clarity.md'),
    ('screen-living-graph-view-whatif-simulation', 'Living Graph — What-if Simulation', 'What-if Simulation answers "what happens if something changes?" without touching real data. Activate it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "What-if Simulation"), then click any node; until you do, a banner and the clarity card say "pick a node to simulate". A floating cockpit panel offers six deterministic scenarios: delay 1 day / 3 days / 1 week, mark as blocked (assumes ~5 days held), remove blocker (recovers ~3 days), and duration +50% of the node''s own duration. The engine propagates the delay downstream through outgoing dependencies (up to 12 hops), attenuating 15% per hop to account for parallel slack, and reports: estimated propagated days, affected node count, how many affected nodes sit on the critical path, up to three milestones at risk, the strongest downstream dependency, and a risk delta (high when the critical path is hit or 5+ nodes are affected; medium at 2+; low otherwise). The panel compares "Current plan" vs "Simulated plan" in days, and affected nodes get an orange impact ring; red marks critical-path impact, slate means "not applied (estimate)". It is a sandbox by contract (PD-005): no project data changes, and "Apply simulation" does not exist yet. Action: test a scenario, then mitigate — resequence, resolve upstream blockers, or re-baseline. Source: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-simulation-panel.tsx, src/lib/graph/overlay-metadata.ts. Verify: open a project → Execution Map → Living Graph → view selector → What-if Simulation.', 'Living Graph — Vista Simulación What-if', 'La Simulación What-if responde "¿qué pasa si algo cambia?" sin tocar datos reales. Se activa desde el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Simulación what-if") y luego se hace clic en cualquier nodo; mientras no lo hagas, un aviso y la tarjeta de claridad piden "elige un nodo para simular". Un panel flotante ofrece seis escenarios deterministas: retrasar 1 día / 3 días / 1 semana, marcar como bloqueado (asume ~5 días retenidos), quitar bloqueo (recupera ~3 días) y duración +50% de la duración propia del nodo. El motor propaga el retraso aguas abajo por las dependencias salientes (hasta 12 saltos), atenuando 15% por salto por la holgura del trabajo paralelo, y reporta: días propagados estimados, cantidad de nodos afectados, cuántos están en la ruta crítica, hasta tres hitos en riesgo, la dependencia aguas abajo más fuerte y un delta de riesgo (alto si toca la ruta crítica o afecta 5+ nodos; medio con 2+; bajo en otro caso). El panel compara "Plan actual" vs "Plan simulado" en días, y los nodos afectados llevan un anillo naranja de impacto; el rojo marca ruta crítica afectada y el gris "no aplicado (estimación)". Es un sandbox por contrato (PD-005): ningún dato del proyecto cambia y "Aplicar simulación" aún no existe. Acción: prueba un escenario y mitiga — re-secuencia, resuelve bloqueos aguas arriba o re-planifica la línea base. Fuente: src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-simulation-panel.tsx, src/lib/graph/overlay-metadata.ts. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → What-if Simulation.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/graph/overlay-metadata.ts, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-simulation-panel.tsx, src/components/graph/living-graph-view.tsx, docs/product-brain/28-sprint-03-overlay-clarity.md'),
    ('screen-living-graph-view-workface-readiness', 'Living Graph — Workface Readiness', 'Workface Readiness (overlay id `readiness`) answers "can this construction activity actually start?". Select it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "Workface Readiness"). It is fed by the deterministic lookahead engine: each construction activity gets a readiness level — blocked (activity or a predecessor is blocked), not ready (no crew assigned, a high/critical labor gap for its trade, or 3+ required checklist items incomplete), at risk (incomplete predecessor, partial crew, medium/low labor gap, or 1–2 missing items), otherwise ready. Readiness % is the share of completed required items in a 9-point checklist: RFI answered, submittal approved, drawing current, material onsite, area released, safety/permit ready, predecessor complete, QA prerequisite, crew assigned. Only not-ready activities are enriched; selecting one shows a detail block with level, percentage bar, missing prerequisites, a short summary, and a recommended action ("Resolve blockers" or "Expedite missing prerequisites"). Be aware this view is limited today: the overlay defines no emphasis rule, so choosing it does not dim or highlight nodes — the value lives in the enriched detail panel of construction-activity nodes. It requires labor resources plus construction activities; without them nothing is enriched, and there is no clarity card or empty state. Action: clear the listed prerequisites before crews mobilize. Source: src/lib/labor/workface-readiness.ts, src/lib/labor/lookahead.ts, src/lib/graph/labor-graph-mapping.ts. Verify: open a project → Execution Map → Living Graph → view selector → Workface Readiness.', 'Living Graph — Vista Preparación de Frente de Obra', 'Preparación de Frente de Obra (id de capa `readiness`) responde "¿esta actividad de construcción realmente puede empezar?". Se elige en el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Preparación de Frente de Obra"). La alimenta el motor determinista de lookahead: cada actividad recibe un nivel — bloqueada (la actividad o un predecesor está bloqueado), no lista (sin cuadrilla asignada, brecha laboral alta/crítica de su oficio, o 3+ ítems requeridos incompletos), en riesgo (predecesor incompleto, cuadrilla parcial, brecha media/baja, o 1–2 ítems faltantes), y si no, lista. El % de preparación es la proporción de ítems requeridos completados de una lista de 9 puntos: RFI respondida, submittal aprobado, plano vigente, material en sitio, área liberada, seguridad/permiso listo, predecesor completo, requisito QA y cuadrilla asignada. Solo se enriquecen las actividades no listas; al seleccionar una, el panel de detalle muestra nivel, barra de porcentaje, prerrequisitos faltantes, un resumen y una acción recomendada ("Resolver bloqueadores" o "Acelerar prerrequisitos faltantes"). Ojo: hoy esta vista es limitada — la capa no define regla de énfasis, así que elegirla no atenúa ni resalta nodos; el valor está en el panel de detalle enriquecido de los nodos de actividad de construcción. Requiere recursos laborales y actividades de construcción; sin ellos no se enriquece nada, y no hay tarjeta de claridad ni estado vacío. Acción: despeja los prerrequisitos listados antes de movilizar cuadrillas. Fuente: src/lib/labor/workface-readiness.ts, src/lib/labor/lookahead.ts, src/lib/graph/labor-graph-mapping.ts. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Workface Readiness.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/labor/workface-readiness.ts, src/lib/labor/lookahead.ts, src/lib/graph/labor-graph-mapping.ts, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-detail-panel.tsx'),
    ('screen-living-graph-view-workforce-intelligence', 'Living Graph — Workforce Intelligence', 'Workforce Intelligence (overlay id `workforceCapacity`) shows people''s workload for any project type, using real team/allocation data over a 4-week window. Activate it from the Living Graph view selector (Execution Map → Living Graph → Overlay → "Workforce Intelligence"). The math is deterministic: effective hours = weekly capacity × availability% × (1 − overhead%); utilization = assigned task estimates (prorated by date overlap per week) ÷ effective hours. Status bands: available ≤69%, healthy 70–89%, near capacity 90–100%, overallocated 101–120%, critical >120%; resources without capacity data are "needs review" — nothing is invented. A floating "Workforce" roster panel lists each person with a colored utilization bar (red critical, orange overallocated, amber near capacity, gray needs review, green ok), sorted worst first. On the Milestones level, milestone cards and tasks are enriched with their assignee''s status (overloaded work highlights; a banner suggests switching to Activities). On Activities/Events, the canvas becomes a curated view: only people cards (idle people read "100% available") connected to their assigned tasks by status-colored edges. Nodes with no workforce data dim; critical/overallocated highlight. If the project has no resources, the overlay simply stays empty — there is no clarity card. Action: rebalance assignments, add estimates and owners, and relieve overloaded people. Source: src/lib/capacity/formulas.ts, src/lib/capacity/service.ts, src/lib/graph/workforce-graph-mapping.ts. Verify: open a project → Execution Map → Living Graph → view selector → Workforce Intelligence.', 'Living Graph — Vista Inteligencia de Fuerza Laboral', 'Inteligencia de Fuerza Laboral (id de capa `workforceCapacity`) muestra la carga de trabajo de las personas en cualquier tipo de proyecto, con datos reales de equipo/asignaciones en una ventana de 4 semanas. Se activa desde el selector de vistas del Living Graph (Execution Map → Living Graph → Capa → "Inteligencia de Fuerza Laboral"). El cálculo es determinista: horas efectivas = capacidad semanal × %disponibilidad × (1 − %overhead); utilización = estimaciones de tareas asignadas (prorrateadas por solapamiento de fechas por semana) ÷ horas efectivas. Bandas de estado: disponible ≤69%, saludable 70–89%, cerca del límite 90–100%, sobreasignado 101–120%, crítico >120%; los recursos sin datos de capacidad quedan en "requiere revisión" — nada se inventa. Un panel flotante "Fuerza laboral" lista a cada persona con una barra de utilización coloreada (rojo crítico, naranja sobreasignado, ámbar cerca del límite, gris requiere revisión, verde ok), ordenada de peor a mejor. En el nivel Milestones, las tarjetas de hito y tareas se enriquecen con el estado de su asignado (el trabajo sobrecargado se resalta; un aviso sugiere cambiar a Actividades). En Actividades/Eventos, el lienzo se vuelve una vista curada: solo tarjetas de personas (las ociosas dicen "100% disponible") conectadas a sus tareas asignadas con aristas coloreadas por estado. Los nodos sin datos de fuerza laboral se atenúan; crítico/sobreasignado se resalta. Si el proyecto no tiene recursos, la capa queda vacía — no hay tarjeta de claridad. Acción: rebalancea asignaciones, agrega estimaciones y responsables, y alivia a las personas sobrecargadas. Fuente: src/lib/capacity/formulas.ts, src/lib/capacity/service.ts, src/lib/graph/workforce-graph-mapping.ts. Verifica: abre un proyecto → Execution Map → Living Graph → selector de vistas → Workforce Intelligence.', '/projects/[projectId]/execution-map/living-graph — generated from: src/lib/capacity/formulas.ts, src/lib/capacity/service.ts, src/lib/graph/workforce-graph-mapping.ts, src/lib/graph/living-graph-analysis.ts, src/components/graph/living-graph-view.tsx'),
    ('screen-living-graph', 'Living Graph screen', 'The Living Graph is the project''s visual digital twin and primary intelligence surface (ADR-002), reached from the Execution Map''s "Living Graph" tab button. The server page validates org ownership, auto-cleans orphan graph nodes (`removeOrphanGraphNodes`), then loads `process_nodes`, `process_edges`, `milestones`, the `get_process_timeline` RPC, labor tables (`labor_resources`, `construction_activities`, `activity_dependencies`, `trade_taxonomy`), full `roadmap_tasks` and `task_subtasks`. Node labels always come from the canonical owners (tasks/milestones), never stale snapshots, and completed tasks are never shown blocked (REG-008). It runs deterministic engines server-side: `computeLaborCapacity`, `computeLookahead`, `computeLaborVariance`, `computeProductivityVariance`, variance-cause classification and `computeResourceCapacity` (4 weeks). The client (LivingGraphView, React Flow) offers overlays — normal, bottleneck, criticalPath, rework, traceabilityGap, risk, sopCandidate, blocker, timeline, simulation, laborCapacity, readiness, variance, workforceCapacity — plus view levels (milestones/activities/events), layout modes with saved layouts (localStorage), search, timeline playback, a what-if simulation panel, an executive Insights panel, in-graph task/milestone editing, subtask expansion, and a Recalculate action (`refreshLivingGraphAction`, which removes orphans and re-runs the `backfill_living_graph` RPC). Header links go to the Realtime view and back to the Execution Map; an auto-refresh component polls a content signature. Related: screen-execution-map, screen-living-graph-realtime, screen-milestone-flow, screen-resource-capacity, screen-labor-capacity.
Source: living-graph/page.tsx, living-graph/actions.ts, components/graph/living-graph-view.tsx.
Verify: Execution Map > "Living Graph" (/projects/[projectId]/execution-map/living-graph).', 'Pantalla Grafo Vivo', 'El Grafo Vivo es el gemelo digital visual del proyecto y su superficie principal de inteligencia (ADR-002); se llega desde el botón "Grafo Vivo" del Mapa de Ejecución. La página de servidor valida la pertenencia a la organización, limpia nodos huérfanos (`removeOrphanGraphNodes`) y carga `process_nodes`, `process_edges`, `milestones`, el RPC `get_process_timeline`, las tablas laborales (`labor_resources`, `construction_activities`, `activity_dependencies`, `trade_taxonomy`), todas las `roadmap_tasks` y `task_subtasks`. Las etiquetas de nodo provienen siempre de los dueños canónicos (tareas e hitos) y una tarea completada nunca aparece bloqueada (REG-008). Ejecuta motores deterministas en el servidor: `computeLaborCapacity`, `computeLookahead`, `computeLaborVariance`, `computeProductivityVariance`, clasificación de causas de varianza y `computeResourceCapacity` (4 semanas). El cliente (LivingGraphView, React Flow) ofrece superposiciones — normal, cuellos de botella, ruta crítica, retrabajo, brecha de trazabilidad, riesgo, candidato a SOP, bloqueos, línea de tiempo, simulación, capacidad laboral, preparación, varianza y capacidad de fuerza de trabajo —, niveles de vista (hitos/actividades/eventos), modos de diseño con posiciones guardadas (localStorage), búsqueda, reproducción de línea de tiempo, panel de simulación "qué pasaría si", panel ejecutivo de Insights, edición de tareas e hitos dentro del grafo, expansión de subtareas y el botón Recalcular (`refreshLivingGraphAction`, que elimina huérfanos y reejecuta el RPC `backfill_living_graph`). El encabezado enlaza a la vista en tiempo real y de regreso al Mapa de Ejecución; un componente de autoactualización sondea una firma de contenido. Relacionadas: screen-execution-map, screen-living-graph-realtime, screen-milestone-flow, screen-resource-capacity, screen-labor-capacity.
Fuente: living-graph/page.tsx, living-graph/actions.ts, components/graph/living-graph-view.tsx.
Verifica: Mapa de Ejecución > "Grafo Vivo" (/projects/[projectId]/execution-map/living-graph).', '/projects/[projectId]/execution-map/living-graph — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/living-graph/page.tsx, src/app/[locale]/(app)/projects/[projectId]/execution-map/living-graph/actions.ts, src/components/graph/living-graph-view.tsx, src/components/graph/living-graph-toolbar.tsx, src/lib/roadmap/living-graph-sync.ts'),
    ('screen-login', 'Login screen', 'The sign-in screen, at `/login` inside the `(auth)` route group. Users reach it from the landing page CTAs, from the signup screen''s "back to login" link, or by being redirected when unauthenticated (for example, `logoutAction` redirects here). The server page renders a localized title and subtitle (next-intl namespace `auth.login`) above the client `LoginForm`. The form has two fields — email and password (minimum 6 characters) — plus a submit button and a link to the signup screen for users without an account. Submitting calls the `loginAction` server action, which runs `supabase.auth.signInWithPassword` and, on success, redirects to `/` (the home dashboard / PMO Command Center). On failure the form maps Supabase error messages to localized errors: invalid credentials, email not confirmed, or a generic unexpected error, shown in a red banner. No database tables are touched directly; everything goes through Supabase Auth. There is no password-recovery link on this screen in the current code. Related screens: Signup (`/signup`), Change password (`/change-password`, used for forced password changes after login), and the Home dashboard it redirects to.

Source: src/app/[locale]/(auth)/login/page.tsx, src/components/auth/login-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verify: open /login while logged out, sign in with valid credentials, and confirm the redirect to the home dashboard.', 'Pantalla Inicio de sesión', 'La pantalla de inicio de sesión, en `/login` dentro del grupo de rutas `(auth)`. Se llega desde los CTA de la landing, desde el enlace "volver a iniciar sesión" del registro, o por redirección cuando no hay sesión (por ejemplo, `logoutAction` redirige aquí). La página de servidor muestra título y subtítulo localizados (namespace `auth.login` de next-intl) sobre el componente cliente `LoginForm`. El formulario tiene dos campos — correo y contraseña (mínimo 6 caracteres) — más un botón de envío y un enlace al registro para quien no tiene cuenta. Al enviar se llama la server action `loginAction`, que ejecuta `supabase.auth.signInWithPassword` y, si tiene éxito, redirige a `/` (el dashboard PMO Command Center). Si falla, el formulario traduce los errores de Supabase a mensajes localizados: credenciales inválidas, correo no confirmado o error inesperado, mostrados en un aviso rojo. No se tocan tablas directamente; todo pasa por Supabase Auth. En el código actual no hay enlace de recuperación de contraseña. Pantallas relacionadas: Registro (`/signup`), Cambio de contraseña (`/change-password`) y el dashboard de inicio.

Fuente: src/app/[locale]/(auth)/login/page.tsx, src/components/auth/login-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verifica: abre /login sin sesión, inicia sesión con credenciales válidas y confirma la redirección al dashboard de inicio.', '/login — generated from: src/app/[locale]/(auth)/login/page.tsx, src/components/auth/login-form.tsx, src/app/[locale]/(auth)/actions.ts'),
    ('screen-milestone-flow', 'Milestone Process Flow screen', 'The Milestone Process Flow screen shows the engine-derived execution flow between milestones — health, friction and evidence — and is reached from the "Milestone Flow" tab button on the Execution Map. It is a strictly read-only consumer of the Milestone Process Flow (MPF) Engine: the server page calls `loadMilestoneFlowProjection`, which validates the project belongs to the caller''s organization (deny-by-default: unauthorized access renders a safe denial state with no data), reads canonical `milestones` plus `project_event_log` events (capped at 5,000), and invokes the deterministic engine. The route derives nothing itself, mutates nothing, and emits no events; `buildMilestoneFlowViewModel` only formats the projection for display. The view lists milestone-to-milestone transitions with health status badges, confidence level and primary reason codes, duration segment bars, and finding indicators. Filter controls include health-status chips with counts, segment type, finding type, severity, and toggles such as only-uncertainty, only-warnings and only-open-findings. Selecting a transition opens a TransitionDetailPanel with its segments and findings. Error and unauthorized states render dedicated panels. A header link returns to the Execution Map. Related screens: screen-execution-map, screen-living-graph.
Source: milestone-flow/page.tsx, lib/milestone-flow-ui/load-projection.ts, components/milestone-flow/milestone-flow-view.tsx.
Verify: Execution Map > "Milestone Flow" (/projects/[projectId]/execution-map/milestone-flow).', 'Pantalla Flujo entre Hitos', 'La pantalla Flujo entre Hitos muestra el flujo de ejecución entre hitos derivado por motor — salud, fricción y evidencia — y se llega desde el botón "Flujo entre Hitos" del Mapa de Ejecución. Es un consumidor estrictamente de solo lectura del Motor de Flujo de Procesos entre Hitos (MPF): la página de servidor llama a `loadMilestoneFlowProjection`, que valida que el proyecto pertenezca a la organización del usuario (denegación por defecto: un acceso no autorizado muestra un estado seguro sin datos), lee los `milestones` canónicos y los eventos de `project_event_log` (máximo 5,000) e invoca el motor determinista. La ruta no deriva nada por sí misma, no muta datos ni emite eventos; `buildMilestoneFlowViewModel` solo da formato a la proyección. La vista lista las transiciones entre hitos con insignias de estado de salud, nivel de confianza y códigos de razón, barras de segmentos de duración e indicadores de hallazgos. Los filtros incluyen chips por estado de salud con conteos, tipo de segmento, tipo de hallazgo, severidad y alternadores como solo-incertidumbre, solo-advertencias y solo-hallazgos-abiertos. Al seleccionar una transición se abre un panel de detalle con sus segmentos y hallazgos. Los estados de error y de no autorizado tienen paneles propios. Un enlace del encabezado regresa al Mapa de Ejecución. Pantallas relacionadas: screen-execution-map, screen-living-graph.
Fuente: milestone-flow/page.tsx, lib/milestone-flow-ui/load-projection.ts, components/milestone-flow/milestone-flow-view.tsx.
Verifica: Mapa de Ejecución > "Flujo entre Hitos" (/projects/[projectId]/execution-map/milestone-flow).', '/projects/[projectId]/execution-map/milestone-flow — generated from: src/app/[locale]/(app)/projects/[projectId]/execution-map/milestone-flow/page.tsx, src/lib/milestone-flow-ui/load-projection.ts, src/lib/milestone-flow-ui/selectors.ts, src/components/milestone-flow/milestone-flow-view.tsx, src/components/milestone-flow/transition-detail-panel.tsx'),
    ('screen-navigator-preview', 'Navigator preview screen', 'A temporary development-only route. The file comment states it explicitly: "TEMPORARY preview route to visually verify the ProjectOpsNavigatorButton renders and the drawer opens. NOT part of the shipped feature — deleted after verification." The page wraps its content in a `NextIntlClientProvider` (locale "en", empty messages) and renders a faux app header — a ProjectOps360° label, a fake "Search everything… ⌘K" pill, a bell icon, and an avatar — so the real `ProjectOpsNavigatorButton` can be inspected in a realistic context. Clicking the bilingual Navigator button (Compass icon) opens the `ProjectOpsNavigatorDrawer`, the contextual help/guidance drawer whose sibling components include GuidedOnboardingProgress, ModuleGuidanceCard, NavigatorActions, NavigatorChecklist, and ProjectLifecycleMap. The main area only shows a heading and an instruction to click the button. The page reads and writes no Supabase data and calls no server actions. It lives outside the `[locale]` group and outside the authenticated `(app)` shell, so it is reachable without the normal app chrome. Related: the Navigator button itself ships in the real app header.

Source: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx.
Verify: open /navigator-preview and click the Navigator button in the faux header to open the drawer.', 'Pantalla Vista previa del Navigator', 'Una ruta temporal solo para desarrollo. El comentario del archivo lo dice explícitamente: es una "vista previa TEMPORAL para verificar visualmente que el ProjectOpsNavigatorButton se renderiza y el drawer se abre. NO forma parte de la funcionalidad publicada — se elimina tras la verificación". La página envuelve su contenido en un `NextIntlClientProvider` (locale "en", mensajes vacíos) y muestra un encabezado de aplicación ficticio — etiqueta ProjectOps360°, una píldora falsa de búsqueda "⌘K", un icono de campana y un avatar — para inspeccionar el `ProjectOpsNavigatorButton` real en un contexto realista. Al hacer clic en el botón bilingüe Navigator (icono de brújula) se abre el `ProjectOpsNavigatorDrawer`, el panel contextual de ayuda cuyos componentes hermanos incluyen GuidedOnboardingProgress, ModuleGuidanceCard, NavigatorActions, NavigatorChecklist y ProjectLifecycleMap. El área principal solo muestra un título y la instrucción de pulsar el botón. No lee ni escribe datos en Supabase ni llama server actions, y vive fuera del grupo `[locale]` y del shell autenticado `(app)`.

Fuente: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx.
Verifica: abre /navigator-preview y pulsa el botón Navigator del encabezado ficticio para abrir el drawer.', '/navigator-preview — generated from: src/app/navigator-preview/page.tsx, src/components/navigator/ProjectOpsNavigatorButton.tsx, src/components/navigator/ProjectOpsNavigatorDrawer.tsx'),
    ('screen-org-billing', 'Organization Billing screen', 'The Billing & Plan screen is the organization-level billing hub, reachable from the sidebar bottom navigation (Billing, credit-card icon) at /organization/billing. It is a fully server-rendered page (no client component). The header shows the organization name and quick links to Members, Teams and Contacts, plus a Plans & Pricing link that appears only for platform admins (isPlatformAdmin: PLATFORM_ADMIN_EMAILS allowlist, or org owner as fallback). The Current Plan card shows the plan label, subscription status badge (active, trialing, past_due, canceled, suspended) and monthly/yearly price; the "Manage subscription" button is intentionally disabled with a "Payment integration coming soon" tooltip — no payment flow exists yet. The Usage & Limits grid compares six metrics against plan entitlements — billable users, active projects, free viewers, documents indexed, AI credits per month and memory storage MB — with progress bars and near-limit/at-limit warnings computed by checkLimit; pending invites are noted as not billable. A Plan Features card lists FEATURE_FIELDS with check/cross icons, and a read-only comparison grid shows all active plans. Data is read via getOrgBilling and getPlansWithEntitlements in lib/billing/service (tables: subscriptions, plans, plan_entitlements, organization_members, projects, project_memory_items). The page writes nothing. Related screens: Members, Teams, External Contacts, Plans (admin).
Source: src/app/[locale]/(app)/organization/billing/page.tsx, src/lib/billing/service.ts, src/lib/billing/config.ts.
Verify: click Billing in the sidebar bottom navigation, or go to /organization/billing.', 'Pantalla Facturación de la organización', 'La pantalla de Facturación y plan es el centro de facturación a nivel de organización, accesible desde la navegación inferior de la barra lateral (Facturación, icono de tarjeta) en /organization/billing. Es una página renderizada por completo en el servidor. El encabezado muestra el nombre de la organización y enlaces rápidos a Miembros, Equipos y Contactos, más un enlace a Planes y precios visible solo para administradores de plataforma (isPlatformAdmin: lista PLATFORM_ADMIN_EMAILS o, en su defecto, el propietario de la organización). La tarjeta de Plan actual muestra la etiqueta del plan, el estado de la suscripción (activa, en prueba, vencida, cancelada, suspendida) y el precio mensual o anual; el botón "Gestionar suscripción" está deshabilitado a propósito con el aviso "Integración de pagos próximamente" — todavía no existe flujo de pago. La sección de Uso y límites compara seis métricas contra los límites del plan — usuarios facturables, proyectos activos, observadores gratis, documentos indexados, créditos de IA por mes y memoria en MB — con barras de progreso y avisos de límite calculados por checkLimit; las invitaciones pendientes no cuentan como asiento. Una tarjeta lista las funciones del plan y una cuadrícula de solo lectura compara los planes activos. Los datos se leen con getOrgBilling y getPlansWithEntitlements (tablas: subscriptions, plans, plan_entitlements, organization_members, projects, project_memory_items). La página no escribe nada. Pantallas relacionadas: Miembros, Equipos, Contactos externos y Planes (administración).
Fuente: src/app/[locale]/(app)/organization/billing/page.tsx, src/lib/billing/service.ts, src/lib/billing/config.ts.
Verifica: pulsa Facturación en la parte inferior de la barra lateral, o navega a /organization/billing.', '/organization/billing — generated from: src/app/[locale]/(app)/organization/billing/page.tsx, src/lib/billing/service.ts, src/lib/billing/config.ts, src/config/navigation.ts'),
    ('screen-org-external-contacts', 'External Contacts screen', 'The External Contacts screen manages people who work with the organization without a full login — vendors, clients, inspectors, consultants and sponsors. The header explicitly states they do not consume a billable seat. You reach it from the Billing screen header (Contacts button) at /organization/external-contacts. The screen has two parts: an inline add form (name, email, company, contact type from CONTACT_TYPES in lib/team-roles/config, plus an Add button) and a table listing each contact''s name, type, company, and email/phone, with a trash icon per row. Adding calls createContactAction, which inserts into the external_contacts table (fields include name, email, company_name, contact_type, phone, notes, can_login, access_status "active") and logs the creation with logAudit. Deleting calls deleteContactAction, a soft delete that sets deleted_at. An updateContactAction exists in actions.ts for editing fields and access status, but the current table UI does not expose inline editing — only add and delete. The page loads contacts server-side through getExternalContacts in lib/team-roles/service, scoped to the organization from getOrgContext; actions only require an authenticated org context. Related screens: Teams (external contacts can be added as team members), Members (internal, seat-based users) and Billing.
Source: src/app/[locale]/(app)/organization/external-contacts/{page.tsx,contacts-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verify: open Billing (sidebar) then click Contacts, or go to /organization/external-contacts.', 'Pantalla Contactos externos', 'La pantalla de Contactos externos administra a las personas que trabajan con la organización sin un acceso completo — proveedores, clientes, inspectores, consultores y patrocinadores. El encabezado indica explícitamente que no consumen asiento facturable. Se accede desde el encabezado de Facturación (botón Contactos), en /organization/external-contacts. La pantalla tiene dos partes: un formulario de alta en línea (nombre, correo, empresa, tipo de contacto según CONTACT_TYPES en lib/team-roles/config, y botón Agregar) y una tabla con nombre, tipo, empresa y correo/teléfono de cada contacto, con un icono de papelera por fila. Agregar llama a createContactAction, que inserta en la tabla external_contacts (campos como name, email, company_name, contact_type, phone, notes, can_login y access_status "active") y registra la creación con logAudit. Eliminar llama a deleteContactAction, un borrado suave que escribe deleted_at. Existe una acción updateContactAction en actions.ts para editar campos y estado de acceso, pero la interfaz actual no expone edición en línea — solo agregar y eliminar. La página carga los contactos en el servidor mediante getExternalContacts de lib/team-roles/service, acotados a la organización; las acciones solo requieren contexto autenticado. Pantallas relacionadas: Equipos, Miembros y Facturación.
Fuente: src/app/[locale]/(app)/organization/external-contacts/{page.tsx,contacts-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verifica: abre Facturación en la barra lateral y pulsa Contactos, o navega a /organization/external-contacts.', '/organization/external-contacts — generated from: src/app/[locale]/(app)/organization/external-contacts/page.tsx, src/app/[locale]/(app)/organization/external-contacts/contacts-client.tsx, src/app/[locale]/(app)/organization/external-contacts/actions.ts, src/lib/team-roles/service.ts'),
    ('screen-org-members', 'Organization Members screen', 'The Members & Seats screen manages the internal members of the organization and their billing seat types. You reach it from the Billing screen header (Members button) at /organization/members. Three summary cards show billable seats, free members (viewers/external) and pending invites, followed by filter chips (All, Billable, Free, Pending) and a members table with name, email, seat type, workspace role, status, active project count and a Billable badge. Owners and admins (canManage) see two provisioning panels: "Invite by email" (inviteMemberAction, sends a Supabase auth invite; status stays "invited" until accepted) and "Create login with a temporary password" (createMemberWithPasswordAction, no SMTP needed; the user must change the password on first login, and credentials are shown once with a copy button). Inline controls edit seat type, workspace role and status (updateMemberSeatAction) and rename a member (renameWorkspaceUserAction). Data comes from organization_members, profiles, project_team_members and the Supabase auth admin API for emails; writes go through server actions gated to owner/admin and logged via logAudit. Seat types come from SEAT_TYPES in lib/billing/config: only owner, admin, full_seat and contributor_seat are billable; viewers and external are free. Related screens: Billing, Teams, External Contacts, and the workspace /team page, which reuses these same actions.
Source: src/app/[locale]/(app)/organization/members/{page.tsx,members-client.tsx,actions.ts}, src/lib/billing/config.ts.
Verify: open Billing (sidebar) then click Members, or go to /organization/members.', 'Pantalla Miembros de la organización', 'La pantalla de Miembros y asientos administra a los miembros internos de la organización y su tipo de asiento de facturación. Se accede desde el encabezado de la pantalla de Facturación (botón Miembros), en /organization/members. Tres tarjetas de resumen muestran asientos facturables, miembros gratuitos (observadores/externos) e invitaciones pendientes, seguidas de filtros (Todos, Facturables, Gratis, Pendientes) y una tabla con nombre, correo, tipo de asiento, rol de workspace, estado, cantidad de proyectos activos y una insignia de facturable. Propietarios y administradores ven dos paneles de alta: "Invitar por correo" (inviteMemberAction, invitación por Supabase Auth; el estado queda "invitado" hasta aceptarse) y "Crear acceso con clave temporal" (createMemberWithPasswordAction, sin SMTP; el usuario debe cambiar la clave en su primer ingreso y las credenciales se muestran una sola vez con botón de copiar). Controles en línea permiten editar asiento, rol de workspace y estado (updateMemberSeatAction) y renombrar miembros (renameWorkspaceUserAction). Los datos provienen de organization_members, profiles, project_team_members y la API admin de Supabase Auth para correos; las escrituras pasan por acciones de servidor restringidas a propietario/administrador y se registran con logAudit. Solo los asientos owner, admin, full_seat y contributor_seat son facturables. Pantallas relacionadas: Facturación, Equipos, Contactos externos y la página /team, que reutiliza estas mismas acciones.
Fuente: src/app/[locale]/(app)/organization/members/{page.tsx,members-client.tsx,actions.ts}, src/lib/billing/config.ts.
Verifica: abre Facturación en la barra lateral y pulsa Miembros, o navega a /organization/members.', '/organization/members — generated from: src/app/[locale]/(app)/organization/members/page.tsx, src/app/[locale]/(app)/organization/members/members-client.tsx, src/app/[locale]/(app)/organization/members/actions.ts, src/lib/billing/config.ts'),
    ('screen-org-plans', 'Plans & Pricing admin screen', 'The Plans & Pricing screen is a platform-admin-only editor for the global subscription plans (Personal, Team, Business/PMO, Enterprise). It lives at /organization/plans and is reached from the "Plans & pricing" button on the Billing screen, which is shown only to platform admins; the page itself calls isPlatformAdmin server-side (PLATFORM_ADMIN_EMAILS allowlist, or org owner as fallback) and returns notFound() for everyone else. A prominent warning explains that prices are global: edits apply to all organizations. Each plan renders as a card with three editable areas: pricing/metadata (name, description, currency, monthly and yearly price, active checkbox), limits (LIMIT_FIELDS numeric inputs where empty means unlimited) and features (FEATURE_FIELDS checkboxes). One Save button per card calls updatePlanAction (updates the plans table) and updateEntitlementsAction (updates or inserts the plan_entitlements row), both gated to platform admins and audited via logAudit. Data is loaded server-side with getPlansWithEntitlements from lib/billing/service, which joins plans with plan_entitlements ordered by sort_order. Enterprise plans get an Enterprise badge; inactive plans show an Inactive badge. A back link returns to /organization/billing, where the resulting limits drive the Usage & Limits cards. Related screens: Billing (consumer of these entitlements) and Members (billable seats counted against max_billable_users).
Source: src/app/[locale]/(app)/organization/plans/{page.tsx,plans-admin-client.tsx,actions.ts}, src/lib/billing/service.ts.
Verify: as a platform admin, open Billing and click Plans & pricing, or go to /organization/plans (others get a 404).', 'Pantalla Planes y precios (administración)', 'La pantalla de Planes y precios es un editor exclusivo para administradores de plataforma de los planes globales de suscripción (Personal, Team, Business/PMO, Enterprise). Vive en /organization/plans y se accede con el botón "Planes y precios" de la pantalla de Facturación, visible solo para administradores de plataforma; la página valida isPlatformAdmin en el servidor (lista PLATFORM_ADMIN_EMAILS o, en su defecto, propietario de la organización) y devuelve notFound() a cualquier otro usuario. Un aviso destacado explica que los precios son globales: los cambios aplican a todas las organizaciones. Cada plan se muestra como una tarjeta con tres áreas editables: precios y metadatos (nombre, descripción, moneda, precio mensual y anual, casilla de activo), límites (campos numéricos de LIMIT_FIELDS donde vacío significa ilimitado) y funciones (casillas de FEATURE_FIELDS). Un botón Guardar por tarjeta llama a updatePlanAction (actualiza la tabla plans) y a updateEntitlementsAction (actualiza o inserta la fila de plan_entitlements), ambas restringidas a administradores de plataforma y auditadas con logAudit. Los datos se cargan con getPlansWithEntitlements de lib/billing/service, que une plans con plan_entitlements ordenados por sort_order. Los planes Enterprise llevan insignia Enterprise y los inactivos, insignia de Inactivo. Un enlace regresa a /organization/billing, donde estos límites alimentan las tarjetas de uso. Pantallas relacionadas: Facturación y Miembros.
Fuente: src/app/[locale]/(app)/organization/plans/{page.tsx,plans-admin-client.tsx,actions.ts}, src/lib/billing/service.ts.
Verifica: como administrador de plataforma, abre Facturación y pulsa Planes y precios, o navega a /organization/plans (otros usuarios reciben 404).', '/organization/plans — generated from: src/app/[locale]/(app)/organization/plans/page.tsx, src/app/[locale]/(app)/organization/plans/plans-admin-client.tsx, src/app/[locale]/(app)/organization/plans/actions.ts, src/lib/billing/service.ts, src/lib/billing/config.ts'),
    ('screen-org-teams', 'Organization Teams screen', 'The Teams & Groups screen manages reusable company teams (development, QA, steering committee, etc.) that can later be added wholesale to a project. You reach it from the Billing screen header (Teams button) at /organization/teams. At the top, a create panel takes a team name and a team type (from TEAM_TYPES in lib/team-roles/config) and calls createTeamAction. Below, each team renders as a card showing its name, type badge and member count, with a Member button that opens an add-member form and a trash button that soft-deletes the team (deleteTeamAction sets deleted_at). The add form lets you pick either an internal user (from the company directory) or an external contact — mutually exclusive — plus an optional default project role (datalist fed by PROJECT_ROLES), then calls addTeamMemberAction; members appear as removable chips (removeTeamMemberAction). Server-side, the page loads data through lib/team-roles/service: getCompanyTeams (organization_teams and organization_team_members tables), getCompanyDirectory (organization_members + profiles) and getExternalContacts (external_contacts). Actions require an authenticated org context (getOrgContext) and audit team creation via logAudit; there is no explicit owner/admin gate in these actions beyond authentication. Related screens: Members (internal users), External Contacts (people without login), Billing, and the project Team & Roles page where teams get applied.
Source: src/app/[locale]/(app)/organization/teams/{page.tsx,teams-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verify: open Billing (sidebar) then click Teams, or go to /organization/teams.', 'Pantalla Equipos de la organización', 'La pantalla de Equipos y grupos administra equipos reutilizables de la empresa (desarrollo, QA, comité directivo, etc.) que luego pueden agregarse completos a un proyecto. Se accede desde el encabezado de Facturación (botón Equipos), en /organization/teams. Arriba, un panel de creación pide nombre y tipo de equipo (TEAM_TYPES en lib/team-roles/config) y llama a createTeamAction. Debajo, cada equipo se muestra como una tarjeta con nombre, insignia de tipo y número de miembros, con un botón Miembro que abre el formulario para agregar personas y un botón de papelera que hace borrado suave (deleteTeamAction escribe deleted_at). El formulario permite elegir un usuario interno (del directorio de la empresa) o un contacto externo — excluyentes entre sí — más un rol de proyecto por defecto opcional (lista PROJECT_ROLES), y llama a addTeamMemberAction; los miembros aparecen como fichas removibles (removeTeamMemberAction). En el servidor, la página carga datos con lib/team-roles/service: getCompanyTeams (tablas organization_teams y organization_team_members), getCompanyDirectory (organization_members + profiles) y getExternalContacts (external_contacts). Las acciones requieren contexto de organización autenticado y auditan la creación con logAudit; no hay un control adicional de propietario/administrador más allá de la autenticación. Pantallas relacionadas: Miembros, Contactos externos, Facturación y la página de Equipo y roles del proyecto.
Fuente: src/app/[locale]/(app)/organization/teams/{page.tsx,teams-client.tsx,actions.ts}, src/lib/team-roles/service.ts.
Verifica: abre Facturación en la barra lateral y pulsa Equipos, o navega a /organization/teams.', '/organization/teams — generated from: src/app/[locale]/(app)/organization/teams/page.tsx, src/app/[locale]/(app)/organization/teams/teams-client.tsx, src/app/[locale]/(app)/organization/teams/actions.ts, src/lib/team-roles/service.ts, src/lib/team-roles/config.ts'),
    ('screen-phase0', 'Phase 0 control screen', 'An internal builder checklist at `/phase0`, not an end-user project feature. It tracks the "Phase 0" plan for building ProjectOps360° itself: the task list is a static array in `src/data/phase0-tasks.ts` (tasks like "Confirm solo-builder scope", "Create single source of truth folder", "Define Definition of Done"), each with category, goal, priority, hour estimate, sprint, dependencies, acceptance criteria, an AI prompt, and a deliverable. The `Phase0ControlClient` renders a page header, a `ProgressHeader` showing completed/total counts, the last-saved timestamp and a reset button, then tasks grouped by category as `TaskCard`s where the user can change each task''s status and write notes. Crucially, all state is local: `usePhase0Progress` persists status overrides and notes in browser localStorage — nothing is read from or written to Supabase, and there are no server actions, so progress is per-browser and per-device. The screen is bilingual through the next-intl `phase0Control` namespace. Because the data ships in the bundle, every user sees the same task definitions; only their own local status/notes differ. Related screens: none functionally — this is a standalone internal tracking surface, distinct from project workboards and the Product Intelligence cockpit.

Source: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts.
Verify: navigate to /phase0 in the app; change a task status and reload — the change persists via localStorage.', 'Pantalla Control de Fase 0', 'Una lista de control interna del constructor en `/phase0`; no es una funcionalidad de proyectos para usuarios finales. Da seguimiento al plan "Fase 0" de la construcción del propio ProjectOps360°: la lista de tareas es un arreglo estático en `src/data/phase0-tasks.ts` (tareas como "Confirmar alcance de constructor en solitario" o "Definir Definition of Done"), cada una con categoría, objetivo, prioridad, estimación en horas, sprint, dependencias, criterios de aceptación, un prompt de IA y un entregable. El `Phase0ControlClient` muestra un encabezado, un `ProgressHeader` con el conteo de completadas/total, la marca de último guardado y un botón de reinicio, y luego las tareas agrupadas por categoría como `TaskCard`, donde se puede cambiar el estado de cada tarea y escribir notas. Lo importante: todo el estado es local — `usePhase0Progress` persiste estados y notas en localStorage del navegador; no se lee ni escribe nada en Supabase y no hay server actions, así que el progreso es por navegador y dispositivo. Es bilingüe mediante el namespace `phase0Control` de next-intl. Como los datos van en el bundle, todos ven las mismas tareas; solo difieren estados y notas locales. Pantallas relacionadas: ninguna funcionalmente — es una superficie interna independiente.

Fuente: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts.
Verifica: navega a /phase0, cambia el estado de una tarea y recarga — el cambio persiste vía localStorage.', '/phase0 — generated from: src/app/[locale]/(app)/phase0/page.tsx, src/components/phase0/phase0-control-client.tsx, src/data/phase0-tasks.ts, src/hooks/use-phase0-progress.ts'),
    ('screen-product-intelligence', 'Product Intelligence (Product Brain Control Center) screen', 'An internal-only governance cockpit at `/product-intelligence`, not a customer feature. Access is enforced server-side by a strict email allowlist (`isProductBrainAllowedEmail`, "TASK 10A"): unauthorized users receive a 404 so the route''s existence is never revealed, and no Product Brain data is loaded or serialized to the client. For allowed users, the server loads the curated Product Brain markdown corpus via `getAllProductBrainDocs()` (with `?doc=` selecting the initial document, falling back to a default) and the structured registry `PRODUCT_BRAIN_ITEMS`. The client `ProductBrainControlCenter` is a tabbed cockpit: Dashboard (summary), Decisions (product decisions), Regressions, UX Contracts, Modules, ADRs/CAPs, Test Map (test-protection status), Guardrails (AI development guardrails), and Documents (the embedded canonical markdown). It offers full-text search and a module filter, item detail panels with GitHub source links into `docs/product-brain`, and two server actions: `askIsabellaAboutItemAction` (bridge into the Isabella assistant) and `exportProductBrainAction`. Data comes from repo documents and the registry, not tenant Supabase tables. Related screens: the admin Living Graph Observability panel (same allowlist gate) and Isabella surfaces that consume the same Product Brain corpus.

Source: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain*/**.
Verify: with an allowlisted email, open /product-intelligence; non-allowlisted accounts get a 404.', 'Pantalla Inteligencia de Producto (Product Brain Control Center)', 'Una cabina de gobernanza interna en `/product-intelligence`; no es una funcionalidad para clientes. El acceso se aplica del lado del servidor con una lista blanca estricta de correos (`isProductBrainAllowedEmail`, "TASK 10A"): los usuarios no autorizados reciben un 404 para no revelar que la ruta existe, y ningún dato del Product Brain se carga ni se serializa al cliente. Para usuarios permitidos, el servidor carga el corpus markdown curado del Product Brain con `getAllProductBrainDocs()` (el parámetro `?doc=` elige el documento inicial, con respaldo a uno por defecto) y el registro estructurado `PRODUCT_BRAIN_ITEMS`. El cliente `ProductBrainControlCenter` es una cabina con pestañas: Panel (resumen), Decisiones de producto, Regresiones, Contratos UX, Módulos, ADRs/CAPs, Mapa de tests, Guardrails de desarrollo con IA y Documentos (el markdown canónico embebido). Ofrece búsqueda de texto, filtro por módulo, paneles de detalle con enlaces al código fuente en GitHub (`docs/product-brain`) y dos server actions: `askIsabellaAboutItemAction` (puente hacia Isabella) y `exportProductBrainAction`. Los datos provienen de documentos del repositorio y del registro, no de tablas de Supabase del tenant. Pantallas relacionadas: el panel de Observabilidad del Living Graph (misma lista blanca) y las superficies de Isabella que consumen el mismo corpus.

Fuente: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain*/**.
Verifica: con un correo en la lista blanca, abre /product-intelligence; las cuentas no permitidas reciben un 404.', '/product-intelligence — generated from: src/app/[locale]/(app)/product-intelligence/page.tsx, src/components/product-brain/control-center.tsx, src/lib/product-brain/loader.ts, src/lib/product-brain/access.server.ts, src/lib/product-brain-center/registry.ts'),
    ('screen-project-audit', 'Project Audit Log screen', 'A read-only governance trail of who changed what in the project. The page is fully server-rendered with no client component and no write actions. It verifies the project belongs to the user''s organization, then loads the 50 most recent rows from the audit_logs table for that org and project (columns: actor, action, entity type, entity id, metadata, timestamp), plus display names from the profiles table for each actor. Each entry renders as a card with a colored action badge (green create, blue update, red delete — unknown actions fall back to the update style), the localized entity-type label, an optional title from the metadata, "by <actor>" (or an unknown-actor label) and a localized date-time. Two metadata enrichments are shown: for traceability_links entries, pills with the link type and the source → target entity types; and a purple "AI" pill when metadata.source is "ai_extraction", marking records produced by AI extraction flows. An empty state with a shield icon appears when no records exist, and a breadcrumb returns to the project overview. Entries are produced by the logAudit helper called from server actions across modules (charter, decisions, documents, memory, links). Related screens: any screen whose actions write audit entries.
Source: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx.
Verify: open a project and go to /projects/[projectId]/audit.', 'Pantalla Bitácora de Auditoría del Proyecto', 'Rastro de gobernanza de solo lectura sobre quién cambió qué en el proyecto. La página se renderiza por completo en el servidor, sin componente de cliente ni acciones de escritura. Verifica que el proyecto pertenezca a la organización del usuario y carga las 50 filas más recientes de la tabla audit_logs para esa organización y proyecto (actor, acción, tipo de entidad, id, metadatos, fecha), más los nombres visibles desde la tabla profiles para cada actor. Cada entrada es una tarjeta con insignia de acción coloreada (verde crear, azul actualizar, rojo eliminar — acciones desconocidas usan el estilo de actualizar), la etiqueta localizada del tipo de entidad, un título opcional tomado de los metadatos, "por <actor>" (o etiqueta de actor desconocido) y fecha-hora localizada. Se muestran dos enriquecimientos: para entradas de traceability_links, píldoras con el tipo de enlace y los tipos origen → destino; y una píldora morada "AI" cuando metadata.source es "ai_extraction", que marca registros creados por flujos de extracción con IA. Hay estado vacío con icono de escudo cuando no hay registros, y una miga de pan regresa al resumen del proyecto. Las entradas las produce el helper logAudit invocado desde acciones de servidor de otros módulos (charter, decisiones, documentos, memoria, enlaces). Relacionadas: cualquier pantalla cuyas acciones escriban auditoría.
Fuente: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx.
Verifica: abre un proyecto y ve a /projects/[projectId]/audit.', '/projects/[projectId]/audit — generated from: src/app/[locale]/(app)/projects/[projectId]/audit/page.tsx, src/lib/audit/index.ts'),
    ('screen-project-budget', 'Financial Control (Project Budget)', 'The Budget route is now the project''s financial control center, reached from the "Financial Control" tab in the project navigation (Planning group). It stacks up to three flag-gated layers per pilot project (FINANCIAL_PILOT_PROJECT_IDS plus FINANCIAL_FOUNDATION/WRITERS/PROJECTIONS/UI_ENABLED). Financial Setup (writers flag) is the PMO cost-plan form: estimate title, purpose, currency, AACE class, base/as-of dates, and cost-plan lines with type, resource or rate card, quantity, rate, cadence (week/biweek/month/one-time), periods and hours per period, totaled by calculateFinancialSetupLine. Buttons: Save draft, Submit for review, Approve & activate (independent approver), Manage rates (goes to /team), and an SAP/software starter template. Saving writes financial_estimate_versions, financial_boe_versions, financial_baseline_versions (original_budget and current_baseline) and financial_baseline_lines, and upserts rates into resources; submit/approve run capability-guarded transitions (financial.prepare / financial.approve from project_team_members) through executeFinancialTransition, with audit logging. The Financial Cockpit (ui flag) reads the financial_project_cockpit projection: baseline vs original budget, funding, commitments, actual cost plus accrual, settled payments, EAC with P50/P80, CPI/SPI, pending approvals, a quality badge and exception warnings, with links to the Living Graph and Reports. Below, the classic editable estimate still lists material_requirements grouped by metadata.category with inline quantity/unit-cost editing (updateBudgetLineAction) and print-to-PDF. Related screens: Team & Roles, Reports, Living Graph, Drawing Intelligence takeoff.
Source: budget/page.tsx, financial-setup.tsx, setup-actions.ts, financial-cockpit.tsx, budget-client.tsx, actions.ts; src/lib/financial/{flags,setup-model,setup-read-model.server,read-model.server,workflow.server}.ts; project-tabs-config.ts.
Verify: open a pilot project and go to Financial Control, or visit /projects/[projectId]/budget.', 'Control Financiero (Presupuesto del Proyecto)', 'La ruta Budget es ahora el centro de control financiero del proyecto, accesible desde la pestaña "Control Financiero" de la navegación del proyecto (grupo Planificación). Apila hasta tres capas activadas por banderas por proyecto piloto (FINANCIAL_PILOT_PROJECT_IDS más FINANCIAL_FOUNDATION/WRITERS/PROJECTIONS/UI_ENABLED). La Configuración Financiera (bandera writers) es el formulario PMO del plan de costos: título del estimado, propósito, moneda, clase AACE, fechas base y de corte, y líneas con tipo, recurso o tarifa, cantidad, tarifa, cadencia (semanal/quincenal/mensual/único), periodos y horas por periodo, totalizadas por calculateFinancialSetupLine. Botones: Guardar borrador, Enviar a revisión, Aprobar y activar (aprobador independiente), Gestionar tarifas (va a /team) y una plantilla inicial SAP/software. Guardar escribe financial_estimate_versions, financial_boe_versions, financial_baseline_versions (original_budget y current_baseline) y financial_baseline_lines, y actualiza tarifas en resources; enviar y aprobar ejecutan transiciones protegidas por capacidades (financial.prepare / financial.approve desde project_team_members) mediante executeFinancialTransition, con registro de auditoría. El Cockpit Financiero (bandera ui) lee la proyección financial_project_cockpit: baseline frente a presupuesto original, financiamiento, compromisos, costo real más devengos, pagos liquidados, EAC con P50/P80, CPI/SPI, aprobaciones pendientes, insignia de calidad y avisos de excepciones, con enlaces al Living Graph y a Reportes. Debajo, el estimado editable clásico sigue listando material_requirements agrupados por metadata.category, con edición en línea de cantidad y costo unitario (updateBudgetLineAction) e impresión a PDF. Pantallas relacionadas: Equipo y Roles, Reportes, Living Graph, takeoff de Drawing Intelligence.
Fuente: budget/page.tsx, financial-setup.tsx, setup-actions.ts, financial-cockpit.tsx, budget-client.tsx, actions.ts; src/lib/financial/{flags,setup-model,setup-read-model.server,read-model.server,workflow.server}.ts; project-tabs-config.ts.
Verifica: abre un proyecto piloto y entra a Control Financiero, o visita /projects/[projectId]/budget.', '/projects/[projectId]/budget — generated from: src/app/[locale]/(app)/projects/[projectId]/budget/page.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/budget-client.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/actions.ts, src/app/[locale]/(app)/projects/[projectId]/budget/financial-setup.tsx, src/app/[locale]/(app)/projects/[projectId]/budget/setup-actions.ts, src/app/[locale]/(app)/projects/[projectId]/budget/financial-cockpit.tsx, src/lib/financial/flags.ts, src/lib/financial/setup-model.ts, src/lib/financial/setup-read-model.server.ts, src/lib/financial/read-model.server.ts, src/lib/financial/workflow.server.ts, src/components/layout/project-tabs-config.ts'),
    ('screen-project-charter-print', 'Project Charter Print / PDF screen', 'A printable document version of the Project Charter, reached from the "Print / PDF" button on the Charter screen. The server page loads the project, the charter (project_charters) and its child tables — project_charter_roles, project_governance_rules, project_approval_matrix and project_signoffs — all read-only; this screen writes nothing. The client renders a formal document inside #charter-report-print (print CSS in globals.css isolates it): full logo header, a deterministic folio code CHR-<shortId>-V<version> derived from the charter id and version so the on-page folio matches the PDF filename (Pops360-Charter-CHR-…), the status badge, all charter text sections from CHARTER_SECTIONS, the governance tables, and signature blocks for Project Sponsor, Project Manager and Steering Committee (names auto-matched from defined charter roles) plus a client signature line — it is intended for client approval and signature. A non-printing toolbar offers "Back to Charter" and a "Download PDF" button that calls printWithFilename to open the browser print dialog with the canonical filename. Related screens: the editable Charter (/charter) and the stakeholder summary (/charter/summary).
Source: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, charter-print-client.tsx.
Verify: open /projects/[projectId]/charter and click "Print / PDF".', 'Pantalla Charter imprimible / PDF', 'Versión de documento imprimible del Charter del Proyecto, a la que se llega con el botón "Imprimir / PDF" de la pantalla de Charter. La página de servidor carga el proyecto, el charter (project_charters) y sus tablas hijas — project_charter_roles, project_governance_rules, project_approval_matrix y project_signoffs — todo en modo lectura; esta pantalla no escribe nada. El cliente muestra un documento formal dentro de #charter-report-print (el CSS de impresión en globals.css lo aísla): encabezado con logotipo, un folio determinista CHR-<shortId>-V<versión> derivado del id y la versión del charter para que el folio en pantalla coincida con el nombre del PDF (Pops360-Charter-CHR-…), la insignia de estado, todas las secciones de texto del charter, las tablas de gobernanza y bloques de firma para Patrocinador, Gerente de Proyecto y Comité Directivo (nombres tomados de los roles definidos) más una línea de firma del cliente — está pensado para aprobación y firma del cliente. Una barra no imprimible ofrece "Volver al Charter" y "Descargar PDF", que llama a printWithFilename para abrir el diálogo de impresión con el nombre canónico. Pantallas relacionadas: el Charter editable (/charter) y el resumen para stakeholders (/charter/summary).
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, charter-print-client.tsx.
Verifica: abre /projects/[projectId]/charter y pulsa "Imprimir / PDF".', '/projects/[projectId]/charter/print — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/print/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/print/charter-print-client.tsx, src/lib/print-document.ts'),
    ('screen-project-charter-summary', 'Charter Stakeholder Summary screen', 'A read-only "Project Foundation & Status Context" view for stakeholders, reached from the "Stakeholder view" button on the Charter screen. It is a fully server-rendered summary — no editing. The header shows the project name, the charter status badge (from CHARTER_STATUS_META) and the version, with a note that this view helps stakeholders understand current status relative to the approved charter. Below, a StakeholderSummaryButton (from charter-extra.tsx) generates an AI stakeholder summary on demand via the stakeholderSummaryAction server action. Six field cards show project purpose (project_goal or executive_summary), major deliverables, in scope, success criteria, governance model and reporting cadence, each showing "To be defined." when empty. Then a "Key milestones" card lists up to 8 milestones with target dates, and two cards list up to 6 open risks (status open/identified/mitigating) and up to 6 pending decisions (status proposed). Data read: project_charters, milestones, risks and decisions tables, scoped to the org and project. A back link returns to the Charter. Related screens: Charter editor (/charter) and printable charter (/charter/print).
Source: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, ../charter-extra.tsx.
Verify: open /projects/[projectId]/charter and click "Stakeholder view".', 'Pantalla Resumen del Charter para Stakeholders', 'Vista de solo lectura "Fundación y Contexto de Estado del Proyecto" para interesados, a la que se llega con el botón "Vista stakeholders" del Charter. Se renderiza por completo en el servidor — sin edición. El encabezado muestra el nombre del proyecto, la insignia de estado del charter (CHARTER_STATUS_META) y la versión, con una nota que indica que esta vista ayuda a entender el estado actual respecto al charter aprobado. Debajo, el botón StakeholderSummaryButton (de charter-extra.tsx) genera bajo demanda un resumen con IA mediante la acción de servidor stakeholderSummaryAction. Seis tarjetas muestran propósito del proyecto (project_goal o executive_summary), entregables principales, dentro del alcance, criterios de éxito, modelo de gobernanza y cadencia de reportes; cada una muestra "Por definir." si está vacía. Luego, una tarjeta de "Hitos clave" lista hasta 8 hitos con fecha objetivo, y dos tarjetas listan hasta 6 riesgos abiertos (open/identified/mitigating) y hasta 6 decisiones pendientes (proposed). Datos leídos: tablas project_charters, milestones, risks y decisions, acotadas a la organización y al proyecto. Un enlace regresa al Charter. Relacionadas: editor del Charter (/charter) y charter imprimible (/charter/print).
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, ../charter-extra.tsx.
Verifica: abre /projects/[projectId]/charter y pulsa "Vista stakeholders".', '/projects/[projectId]/charter/summary — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/summary/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-extra.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/actions.ts'),
    ('screen-project-charter', 'Project Charter & Governance screen', 'The living Project Charter: the governance foundation of a project. The page auto-creates a charter row if none exists (covers older projects) and loads it with versions, roles, governance rules, approval matrix, sign-offs, project team members and the unified People Directory for person suggestions. The editor shows a section navigation (charter text sections from CHARTER_SECTIONS) plus governance tabs: Roles, Approval Matrix, Governance Rules, Sign-Off and an AI tools tab (gap analysis, scope-creep detection, charter Q&A, stakeholder summary, generate full governance set). Header actions: "Generate with AI" fills only empty fields; "Submit for approval" is gated (100% required fields, at least one role, one approval rule and one sign-off); reviewers can Approve (locks the charter, snapshots a version into project_charter_versions and pushes sections into Project Memory) or Reject (status revision_required with notes). Editing an approved charter opens a new revision and bumps the version. A completion bar shows missing fields and governance blockers. Data: project_charters, project_charter_versions, project_charter_roles, project_governance_rules, project_approval_matrix, project_signoffs, project_team_members; transitions write audit_logs. Links go to the Stakeholder view (/charter/summary), Print/PDF (/charter/print) and the Delivery framework (/delivery?setup=true). The ?onboard=true param shows an onboarding banner.
Source: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, charter-client.tsx, charter-extra.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/charter.', 'Pantalla Charter y Gobernanza del Proyecto', 'El Charter vivo del proyecto: su base de gobernanza. La página crea automáticamente el charter si no existe (cubre proyectos antiguos) y lo carga con versiones, roles, reglas de gobernanza, matriz de aprobación, firmas, miembros del equipo y el Directorio de Personas unificado para sugerir personas. El editor muestra navegación por secciones de texto (CHARTER_SECTIONS) más pestañas de gobernanza: Roles, Matriz de Aprobación, Reglas de Gobernanza, Firmas (Sign-Off) y una pestaña de herramientas de IA (análisis de brechas, detección de scope creep, preguntas al charter, resumen para stakeholders, generación del modelo de gobernanza completo). Acciones del encabezado: "Generar con IA" rellena solo campos vacíos; "Enviar a aprobación" tiene compuerta de preparación (100% de campos requeridos, al menos un rol, una regla de aprobación y una firma); el revisor puede Aprobar (bloquea el charter, guarda una versión en project_charter_versions y sincroniza secciones a la Memoria del Proyecto) o Rechazar (estado revision_required con notas). Editar un charter aprobado abre una nueva revisión y sube la versión. Una barra de completitud señala campos faltantes y bloqueos de gobernanza. Datos: project_charters, project_charter_versions, project_charter_roles, project_governance_rules, project_approval_matrix, project_signoffs, project_team_members; las transiciones registran audit_logs. Enlaces a la vista de stakeholders (/charter/summary), Imprimir/PDF (/charter/print) y el Marco de ejecución (/delivery?setup=true). El parámetro ?onboard=true muestra un banner de bienvenida.
Fuente: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, charter-client.tsx, charter-extra.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/charter.', '/projects/[projectId]/charter — generated from: src/app/[locale]/(app)/projects/[projectId]/charter/page.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-client.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/charter-extra.tsx, src/app/[locale]/(app)/projects/[projectId]/charter/actions.ts, src/lib/charter/service.ts, src/lib/charter/fields.ts'),
    ('screen-project-closeout', 'Project Closeout Report screen', 'The end-of-project report with live metrics and an AI-generated executive narrative. The server page computes metrics, readiness, milestone durations and the project archive via lib/rhythm/closeout, and reads the latest "closing" meeting from the meetings table: a completed one carries the generated narrative inside ai_summary.closeout; the latest of any status drives a guided step rail (UX-010: schedule closing meeting → complete it → generate summary → review → export). Sections: executive summary (with a notice when not yet generated — it requires a completed "Closing Project" meeting in the Rhythm Center), Results KPIs (task completion, schedule and budget variance, risks resolved), key accomplishments, milestone durations table, detail cards for schedule, budget, risks/RFIs/submittals and governance/engagement, lessons learned (what went well / challenges), open items, next steps and the project archive. Actions: generateCloseoutNarrativeAction stores the AI narrative on the closing meeting (viewers are blocked), "Download PDF" prints via printWithFilename and then markCloseoutExportedAction stamps exportedAt so the workflow completes, and resolveRiskAction (REG-017) resolves open risks inline. A footer notes that untracked metrics (customer satisfaction, ROI, revenue) are omitted. Related screens: Rhythm Center meetings, Budget.
Source: src/app/[locale]/(app)/projects/[projectId]/closeout/page.tsx, closeout-client.tsx, actions.ts, src/lib/rhythm/closeout.ts.
Verify: open a project and go to /projects/[projectId]/closeout.', 'Pantalla Reporte de Cierre del Proyecto', 'El reporte de fin de proyecto con métricas en vivo y una narrativa ejecutiva generada con IA. La página de servidor calcula métricas, preparación, duración de hitos y el archivo del proyecto mediante lib/rhythm/closeout, y lee la reunión de tipo "closing" más reciente en la tabla meetings: la completada lleva la narrativa generada en ai_summary.closeout; la más reciente de cualquier estado alimenta un riel de pasos guiado (UX-010: programar reunión de cierre → completarla → generar resumen → revisar → exportar). Secciones: resumen ejecutivo (con aviso cuando aún no se genera — requiere completar la reunión "Cierre del Proyecto" en el Rhythm Center), KPIs de resultados (tareas completadas, variación de cronograma y presupuesto, riesgos resueltos), logros clave, tabla de duración de hitos, tarjetas de detalle de cronograma, presupuesto, riesgos/RFIs/submittals y gobernanza/participación, lecciones aprendidas (qué salió bien / retos), asuntos abiertos, próximos pasos y archivo del proyecto. Acciones: generateCloseoutNarrativeAction guarda la narrativa de IA en la reunión de cierre (los viewers no pueden), "Descargar PDF" imprime con printWithFilename y luego markCloseoutExportedAction registra exportedAt para completar el flujo, y resolveRiskAction (REG-017) resuelve riesgos abiertos en línea. Un pie aclara que métricas no rastreadas (satisfacción del cliente, ROI, ingresos) se omiten. Relacionadas: reuniones del Rhythm Center, Presupuesto.
Fuente: src/app/[locale]/(app)/projects/[projectId]/closeout/page.tsx, closeout-client.tsx, actions.ts, src/lib/rhythm/closeout.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/closeout.', '/projects/[projectId]/closeout — generated from: src/app/[locale]/(app)/projects/[projectId]/closeout/page.tsx, src/app/[locale]/(app)/projects/[projectId]/closeout/closeout-client.tsx, src/app/[locale]/(app)/projects/[projectId]/closeout/actions.ts, src/lib/rhythm/closeout.ts'),
    ('screen-project-communications', 'Project Communications screen', 'The Communications screen (/projects/[projectId]/communications) is the project''s communication log. The server page fans out three parallel queries: the project (organization-scoped), all non-deleted `communication_items` ordered by `item_date` descending, and the project''s `stakeholders` for linking. `CommunicationsListClient` shows a header with a "Create" button, a filter bar (source type, status draft/logged, a "requires follow-up" toggle, and date range) and cards for each entry. Source types confirmed in code: email, meeting, phone, teams, slack, in_person, document, manual_note and other; each card shows a source-type badge, status badge and a follow-up indicator when `requires_follow_up` is set. `CreateCommunicationDialog` and `EditCommunicationDialog` feed the server actions in `actions.ts` — createCommunicationAction, updateCommunicationAction and archiveCommunicationAction (soft delete with confirmation). Writes go to `communication_items` with per-locale i18n fields, then fire-and-forget generate a semantic-search embedding (`generateAndStoreEmbedding`) and emit a Living Graph `communication_flow` event via `emitAndAutoLink`. Communications can also be linked from the meeting detail screen through `traceability_links`. Related screens: Meetings, Meeting detail, Stakeholders, Documents.
Source: communications/page.tsx, communications-list-client.tsx, communications/actions.ts.
Verify: open a project and go to Communications (/projects/[projectId]/communications).', 'Pantalla Comunicaciones del proyecto', 'La pantalla de Comunicaciones (/projects/[projectId]/communications) es la bitácora de comunicaciones del proyecto. La página de servidor lanza tres consultas en paralelo: el proyecto (limitado a la organización), los `communication_items` no eliminados ordenados por `item_date` descendente y los `stakeholders` del proyecto para vincular. `CommunicationsListClient` muestra un encabezado con botón "Crear", una barra de filtros (tipo de fuente, estado borrador/registrado, un interruptor de "requiere seguimiento" y rango de fechas) y tarjetas por cada entrada. Tipos de fuente confirmados en el código: correo, reunión, teléfono, Teams, Slack, presencial, documento, nota manual y otro; cada tarjeta muestra insignias de tipo y estado y un indicador cuando `requires_follow_up` está activo. Los diálogos de creación y edición usan las acciones de servidor createCommunicationAction, updateCommunicationAction y archiveCommunicationAction (borrado lógico con confirmación). Las escrituras van a `communication_items` con campos i18n por idioma y, de forma asíncrona, generan un embedding para búsqueda semántica y emiten un evento `communication_flow` del Living Graph. Las comunicaciones también se vinculan desde el detalle de reunión mediante `traceability_links`. Relacionadas: Reuniones, Detalle de reunión, Stakeholders, Documentos.
Fuente: communications/page.tsx, communications-list-client.tsx, communications/actions.ts.
Verifica: abre un proyecto y entra a Comunicaciones (/projects/[projectId]/communications).', '/projects/[projectId]/communications — generated from: src/app/[locale]/(app)/projects/[projectId]/communications/page.tsx, src/app/[locale]/(app)/projects/[projectId]/communications/communications-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/communications/actions.ts, src/components/communications/index.ts, src/components/communications/communication-card.tsx, src/components/communications/communication-filters.tsx, src/components/communications/create-communication-dialog.tsx, src/components/communications/edit-communication-dialog.tsx, src/components/communications/source-type-badge.tsx, src/components/communications/status-badge.tsx'),
    ('screen-project-decision-detail', 'Project Decision Detail screen', 'Full view of a single decision, reached by clicking a decision card in the project decision log. The server page loads the decision (decisions table), project stakeholders, and the decision''s traceability links in both directions from traceability_links (source_type or target_type = "decision"). Linked-record titles are resolved by batch queries against decisions, meetings, communication_items and documents; the page also loads full catalogs of those four entity types to feed the "Add link" dialog. The detail client displays date, decision maker, source (meeting, communication, document, manual, other), evidence, description, rationale, linked stakeholders and a Linked Records section where the user can add or remove typed links (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) with optional context notes. Edit and archive reuse the list-screen actions: updateDecisionAction rewrites the i18n fields, logs to audit_logs and regenerates the vector embedding; archiveDecisionAction soft-deletes after confirmation. A back link returns to the decisions list. Related screens: decisions list (/decisions), Project Memory (memory items can link to decisions) and the project audit log.
Source: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, decision-detail-client.tsx, ../actions.ts.
Verify: open /projects/[projectId]/decisions and click any decision.', 'Pantalla Detalle de Decisión', 'Vista completa de una decisión, a la que se llega al hacer clic en una tarjeta de la bitácora de decisiones. La página de servidor carga la decisión (tabla decisions), los stakeholders del proyecto y los enlaces de trazabilidad de la decisión en ambas direcciones desde traceability_links (source_type o target_type = "decision"). Los títulos de los registros vinculados se resuelven con consultas por lote a decisions, meetings, communication_items y documents; la página también carga catálogos completos de esos cuatro tipos para el diálogo "Agregar enlace". El cliente muestra fecha, decisor, origen (reunión, comunicación, documento, manual, otro), evidencia, descripción, justificación, stakeholders vinculados y una sección de Registros Vinculados donde se pueden agregar o quitar enlaces tipificados (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) con notas de contexto opcionales. Editar y archivar reutilizan las acciones de la lista: updateDecisionAction reescribe los campos i18n, registra en audit_logs y regenera el embedding vectorial; archiveDecisionAction hace borrado suave tras confirmación. Un enlace regresa a la lista. Relacionadas: lista de decisiones (/decisions), Memoria del Proyecto (los elementos de memoria pueden vincularse a decisiones) y la bitácora de auditoría.
Fuente: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, decision-detail-client.tsx, ../actions.ts.
Verifica: abre /projects/[projectId]/decisions y haz clic en cualquier decisión.', '/projects/[projectId]/decisions/[decisionId] — generated from: src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/[decisionId]/decision-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/actions.ts, src/components/links/linked-records.tsx'),
    ('screen-project-decisions', 'Project Decisions screen', 'The project decision log. The server page loads all non-deleted decisions for the project ordered by decision date (newest first) plus the project stakeholders for the linked-stakeholders multi-select. The client shows a filterable card list: filters by status (proposed, accepted, rejected, deferred, revoked), impact area (scope, schedule, budget, risk, quality, communication, document, other) and date range, applied client-side. A create button opens CreateDecisionDialog with title, description, rationale, decision maker, decision date, source (meeting, communication, document, manual, other, with optional source record), impact area, evidence URL, status and linked stakeholders. Server actions: createDecisionAction and updateDecisionAction write to the decisions table with per-locale i18n fields (title_i18n, description_i18n, rationale_i18n), log to audit_logs, generate a vector embedding (generateAndStoreEmbedding) for semantic search, and can create a traceability link to the source record; archiveDecisionAction soft-deletes with confirmation. Each card links to the decision detail screen. An empty state prompts creating the first decision. Related screens: decision detail (/decisions/[decisionId]), Project Memory (decisions tab) and the Charter summary (pending decisions).
Source: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, decision-list-client.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/decisions.', 'Pantalla Decisiones del Proyecto', 'La bitácora de decisiones del proyecto. La página de servidor carga todas las decisiones no eliminadas ordenadas por fecha de decisión (más recientes primero) y los stakeholders del proyecto para el selector múltiple de vinculación. El cliente muestra una lista de tarjetas con filtros por estado (propuesta, aceptada, rechazada, diferida, revocada), área de impacto (alcance, cronograma, presupuesto, riesgo, calidad, comunicación, documento, otro) y rango de fechas, aplicados en el cliente. El botón de crear abre CreateDecisionDialog con título, descripción, justificación, decisor, fecha, origen (reunión, comunicación, documento, manual, otro, con registro origen opcional), área de impacto, URL de evidencia, estado y stakeholders vinculados. Acciones de servidor: createDecisionAction y updateDecisionAction escriben en la tabla decisions con campos i18n por idioma (title_i18n, description_i18n, rationale_i18n), registran en audit_logs, generan un embedding vectorial (generateAndStoreEmbedding) para búsqueda semántica y pueden crear un enlace de trazabilidad al registro origen; archiveDecisionAction hace borrado suave con confirmación. Cada tarjeta enlaza al detalle. Hay estado vacío que invita a crear la primera decisión. Relacionadas: detalle de decisión (/decisions/[decisionId]), Memoria del Proyecto (pestaña de decisiones) y resumen del Charter (decisiones pendientes).
Fuente: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, decision-list-client.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/decisions.', '/projects/[projectId]/decisions — generated from: src/app/[locale]/(app)/projects/[projectId]/decisions/page.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/decision-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/decisions/actions.ts, src/components/decisions/create-decision-dialog.tsx, src/components/decisions/decision-filters.tsx'),
    ('screen-project-delivery', 'Adaptive Delivery Framework screen', 'Configures and runs how the project executes. If no framework is active (or with ?setup=true, linked from the Charter''s "Delivery framework" button), a setup Wizard asks a diagnostic (project type, uncertainty, governance, documentation, change control, stakeholder feedback, vendor dependency, cadence); recommendFrameworkAction suggests a delivery method with confidence and board columns, which the user can override, then saveFrameworkAction persists it and activateFrameworkAction turns on execution. Once configured, four tabs appear: Overview (framework attribute cards, execution board columns with WIP limits from project_board_columns colored by live roadmap_tasks status counts, suggested meeting rhythm with scheduleFrameworkMeetingsAction creating weekly meetings in the Rhythm Center, recent project_framework_events), Backlog (project_backlog_items: add/edit, reorder, assign milestones, promote items to Workboard tasks, AI generate and prioritize backlog), Cycles (project_execution_cycles and project_cycle_items: create cycles, add backlog items, promote a whole cycle, AI cycle lessons) and AI & Health (open project_scope_creep_alerts with resolve/dismiss or convert to change request, scope check, framework health, stakeholder summary). The page also reads charter goal/objectives for context. Data written spans the framework, backlog, cycle, alert and meeting tables. Related screens: Charter, Workboard, Rhythm Center.
Source: src/app/[locale]/(app)/projects/[projectId]/delivery/page.tsx, delivery-client.tsx, delivery-tabs.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/delivery (or /delivery?setup=true).', 'Pantalla Marco de Ejecución Adaptativo (Delivery)', 'Configura y opera cómo se ejecuta el proyecto. Si no hay marco activo (o con ?setup=true, enlazado desde el botón "Marco de ejecución" del Charter), un Asistente hace un diagnóstico (tipo de proyecto, incertidumbre, gobernanza, documentación, control de cambios, feedback de stakeholders, dependencia de proveedores, cadencia); recommendFrameworkAction sugiere un método de entrega con confianza y columnas de tablero, que el usuario puede cambiar; saveFrameworkAction lo guarda y activateFrameworkAction activa la ejecución. Ya configurado, hay cuatro pestañas: Resumen (tarjetas de atributos del marco, columnas del tablero con límites de WIP desde project_board_columns coloreadas con conteos en vivo de roadmap_tasks, ritmo de reuniones sugerido con scheduleFrameworkMeetingsAction que crea reuniones semanales en el Rhythm Center, eventos recientes de project_framework_events), Backlog (project_backlog_items: crear/editar, reordenar, asignar hitos, promover ítems a tareas del Workboard, generar y priorizar backlog con IA), Ciclos (project_execution_cycles y project_cycle_items: crear ciclos, agregar ítems, promover un ciclo completo, lecciones de ciclo con IA) e IA y Salud (alertas abiertas de project_scope_creep_alerts con resolver/descartar o convertir a solicitud de cambio, chequeo de alcance, salud del marco, resumen para stakeholders). La página también lee objetivo y objetivos del charter como contexto. Se escribe en las tablas de marco, backlog, ciclos, alertas y reuniones. Relacionadas: Charter, Workboard, Rhythm Center.
Fuente: src/app/[locale]/(app)/projects/[projectId]/delivery/page.tsx, delivery-client.tsx, delivery-tabs.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/delivery (o /delivery?setup=true).', '/projects/[projectId]/delivery — generated from: src/app/[locale]/(app)/projects/[projectId]/delivery/page.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/delivery-client.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/delivery-tabs.tsx, src/app/[locale]/(app)/projects/[projectId]/delivery/actions.ts, src/lib/delivery/service.ts'),
    ('screen-project-document-detail', 'Project Document Detail screen', 'Detail view of a single project document, reached by clicking an item in the documents list. The server page loads the document row (documents table) scoped to the organization; for documents with storage_type "upload" it generates a one-hour signed URL from the Supabase Storage "documents" bucket so the file can be opened securely. The detail client shows the document title, status badge (draft, review, approved, archived), owner (or "no owner"), storage type (upload or external URL), document type (evidence, contract, specification, report, presentation, other), description, version, and either an "Open file" button (using the signed URL) or an "Open link" button for external URLs. Edit opens the edit dialog, which can replace the uploaded file or switch storage mode, and saves through updateDocumentAction (zod validation, i18n fields, audit_logs entry, path revalidation). Archive soft-deletes via archiveDocumentAction after a confirmation prompt. A back link returns to the documents list. This screen writes nothing on load; all mutations go through the shared server actions. Related screens: documents list (/documents), Project Memory (documents tab), decision detail (documents appear as linkable records).
Source: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, document-detail-client.tsx, ../actions.ts.
Verify: open /projects/[projectId]/documents and click any document.', 'Pantalla Detalle de Documento', 'Vista de detalle de un documento del proyecto, a la que se llega al hacer clic en un elemento de la lista de documentos. La página de servidor carga la fila del documento (tabla documents) acotada a la organización; para documentos con storage_type "upload" genera una URL firmada de una hora desde el bucket "documents" de Supabase Storage para abrir el archivo de forma segura. El cliente muestra título, insignia de estado (borrador, revisión, aprobado, archivado), responsable (o "sin responsable"), tipo de almacenamiento (carga o URL externa), tipo de documento (evidencia, contrato, especificación, reporte, presentación, otro), descripción, versión, y un botón "Abrir archivo" (con la URL firmada) o "Abrir enlace" para URLs externas. Editar abre el diálogo de edición, que puede reemplazar el archivo subido o cambiar el modo de almacenamiento, y guarda mediante updateDocumentAction (validación zod, campos i18n, registro en audit_logs, revalidación de ruta). Archivar hace borrado suave con archiveDocumentAction tras confirmación. Un enlace regresa a la lista. La pantalla no escribe nada al cargar; todas las mutaciones pasan por las acciones de servidor compartidas. Relacionadas: lista de documentos (/documents), Memoria del Proyecto (pestaña documentos) y detalle de decisión.
Fuente: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, document-detail-client.tsx, ../actions.ts.
Verifica: abre /projects/[projectId]/documents y haz clic en cualquier documento.', '/projects/[projectId]/documents/[documentId] — generated from: src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/[documentId]/document-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/actions.ts'),
    ('screen-project-documents', 'Project Documents screen', 'The project document repository. The server page loads all non-deleted rows from the documents table for the project, newest first. The client renders a filterable list with client-side filters for status (draft, review, approved, archived), document type (evidence, contract, specification, report, presentation, other) and storage type (upload vs external_url). The create button opens CreateDocumentDialog, which supports two storage modes: uploading a file to the Supabase Storage "documents" bucket (the dialog uploads directly from the browser, then the server action stores the file path, name, size and mime type) or registering an external URL. createDocumentAction and updateDocumentAction validate with zod (a file is required for uploads, a URL for external links), write i18n title/description to the documents table, log to audit_logs and revalidate the project layout; archiveDocumentAction soft-deletes with confirmation. Each item links to the document detail screen. Empty state prompts adding the first document. Related screens: document detail (/documents/[documentId]), Project Memory (documents tab and linking), and decisions (documents can be a decision source or linked record).
Source: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, document-list-client.tsx, actions.ts.
Verify: open a project and go to /projects/[projectId]/documents.', 'Pantalla Documentos del Proyecto', 'El repositorio documental del proyecto. La página de servidor carga todas las filas no eliminadas de la tabla documents para el proyecto, de más reciente a más antigua. El cliente muestra una lista con filtros en el cliente por estado (borrador, revisión, aprobado, archivado), tipo de documento (evidencia, contrato, especificación, reporte, presentación, otro) y tipo de almacenamiento (carga de archivo o URL externa). El botón de crear abre CreateDocumentDialog, que admite dos modos: subir un archivo al bucket "documents" de Supabase Storage (el diálogo sube directamente desde el navegador y la acción de servidor guarda ruta, nombre, tamaño y tipo mime) o registrar una URL externa. createDocumentAction y updateDocumentAction validan con zod (archivo obligatorio para cargas, URL para enlaces externos), escriben título y descripción i18n en la tabla documents, registran en audit_logs y revalidan el layout del proyecto; archiveDocumentAction hace borrado suave con confirmación. Cada elemento enlaza al detalle del documento. Hay estado vacío que invita a agregar el primer documento. Relacionadas: detalle de documento (/documents/[documentId]), Memoria del Proyecto (pestaña de documentos y vinculación) y decisiones (un documento puede ser origen o registro vinculado).
Fuente: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, document-list-client.tsx, actions.ts.
Verifica: abre un proyecto y ve a /projects/[projectId]/documents.', '/projects/[projectId]/documents — generated from: src/app/[locale]/(app)/projects/[projectId]/documents/page.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/document-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/documents/actions.ts, src/components/documents/create-document-dialog.tsx'),
    ('screen-project-drawing-intelligence', 'Drawing Intelligence (BIM) screen', 'The project Drawing Intelligence screen ("BIM — Building Information Module") turns construction drawings into structured project knowledge with evidence traceability, at /projects/[projectId]/drawing-intelligence. The server page (force-dynamic) loads `drawing_files`, `drawing_processing_jobs`, `drawing_extractions`, `drawing_insights`, `drawing_versions`, `drawing_evidence` and `roadmap_tasks` (for linking), degrading gracefully if tables are missing, and checks whether the Autodesk APS connector is configured (server-side env check only). The client has thirteen tabs: Upload (functional manual upload zone via `registerDrawingFileAction`; Autodesk connector card; Procore and Google Drive cards are explicitly "coming soon"), Library (files table with status/processing badges and buttons to process (`processDrawingFileAction`), retry (`retryDrawingProcessingJobAction`), archive (`archiveDrawingFileAction`) and open a detail panel (`getDrawingDetailAction`)), Extractions, then insight tabs filtered by type — Risks, RFIs, Submittals (incl. inspections), Schedule and Cost impacts, and Actions — where insights can change status (`updateDrawingInsightStatusAction`) and be linked to tasks (`linkDrawingInsightToTaskAction`). Takeoff generates quantity estimates (`generateTakeoffEstimateAction`); Versions shows `drawing_versions` changes; Evidence and Logs render placeholder "coming soon" panels when data is absent. Processing progress is polled via `getDrawingProcessingProgressAction`. Related screens: screen-execution-map (linked tasks), global drawing-intelligence page.
Source: drawing-intelligence/page.tsx, drawing-intelligence-client.tsx, actions.ts.
Verify: open a project > Drawing Intelligence / BIM (/projects/[projectId]/drawing-intelligence).', 'Pantalla Inteligencia de Planos (BIM)', 'La pantalla de Inteligencia de Planos del proyecto ("BIM — Módulo de Información de Construcción") convierte planos de construcción en conocimiento estructurado con trazabilidad de evidencia, en /projects/[projectId]/drawing-intelligence. La página de servidor (force-dynamic) carga `drawing_files`, `drawing_processing_jobs`, `drawing_extractions`, `drawing_insights`, `drawing_versions`, `drawing_evidence` y `roadmap_tasks` (para vincular), degradando con gracia si faltan tablas, y verifica si el conector Autodesk APS está configurado (solo variables de entorno del servidor). El cliente tiene trece pestañas: Carga (zona de carga manual funcional mediante `registerDrawingFileAction`; tarjeta del conector Autodesk; Procore y Google Drive aparecen explícitamente como "próximamente"), Biblioteca (tabla de archivos con insignias de estado y procesamiento, y botones para procesar (`processDrawingFileAction`), reintentar (`retryDrawingProcessingJobAction`), archivar (`archiveDrawingFileAction`) y abrir un panel de detalle (`getDrawingDetailAction`)), Extracciones, y luego pestañas de insights filtradas por tipo — Riesgos, RFIs, Submittals (incluye inspecciones), impactos de Cronograma y Costo, y Acciones — donde los insights pueden cambiar de estado (`updateDrawingInsightStatusAction`) y vincularse a tareas (`linkDrawingInsightToTaskAction`). Takeoff genera estimaciones de cantidades (`generateTakeoffEstimateAction`); Versiones muestra los cambios de `drawing_versions`; Evidencia y Registros muestran paneles de "próximamente" cuando no hay datos. El progreso de procesamiento se sondea con `getDrawingProcessingProgressAction`. Pantallas relacionadas: screen-execution-map (tareas vinculadas) y la página global de drawing-intelligence.
Fuente: drawing-intelligence/page.tsx, drawing-intelligence-client.tsx, actions.ts.
Verifica: abre un proyecto > Inteligencia de Planos / BIM (/projects/[projectId]/drawing-intelligence).', '/projects/[projectId]/drawing-intelligence — generated from: src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/page.tsx, src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/drawing-intelligence-client.tsx, src/app/[locale]/(app)/projects/[projectId]/drawing-intelligence/actions.ts, src/components/drawing-intelligence/drawing-upload-zone.tsx'),
    ('screen-project-github-integration', 'GitHub Integration Settings screen', 'Where a software project connects a GitHub repository, reached from Project Settings → GitHub Intelligence (card visible only for software projects with the feature flag on). The page is guarded by assertGitHubIntelligenceAvailable: non-software projects see an explicit "software projects only" message; disabled/forbidden cases 404. It advertises two guarantees as badges — Read-only and Software projects only — and states the integration reads repository metadata and receives webhooks but never writes to GitHub. If repositories are connected (getConnectionStatus), they are listed with last-sync time/status and, for managers/admins (guard.canManage), Refresh (manualSyncAction) and Disconnect (disconnectRepositoryAction) buttons, plus an "Open dashboard" link to /projects/[projectId]/github. Otherwise a connect section shows: with a configured GitHub App, StartInstallButton (startInstallationAction → GitHub App install flow); without one, a dev-mode ConnectSampleButton (devConnectSampleRepositoryAction) that loads synthetic sample data. After the install callback (?installation_id, validated against the github_installations table), a RepoPicker lists installable repositories (listInstallationRepositories) and saves the choice via selectRepositoriesAction. A diagnostics section shows whether the GitHub App env vars, webhook secret and GITHUB_INTELLIGENCE_ENABLED flag are configured. Related screens: GitHub Intelligence dashboard, Project Settings.
Source: settings/integrations/github/page.tsx, github/actions.ts, components/github-intelligence/*.
Verify: software project → Settings → GitHub Intelligence card.', 'Pantalla Configuración de la integración con GitHub', 'Donde un proyecto de software conecta un repositorio de GitHub; se llega desde Configuración del Proyecto → GitHub Intelligence (tarjeta visible solo en proyectos de software con el flag activo). La página está protegida por assertGitHubIntelligenceAvailable: los proyectos que no son de software ven un mensaje explícito ("solo proyectos de software"); los casos deshabilitados o sin permiso devuelven 404. Muestra dos garantías como insignias — Solo lectura y Solo proyectos de software — y aclara que la integración lee metadatos y recibe webhooks pero nunca escribe en GitHub. Si hay repositorios conectados (getConnectionStatus), se listan con fecha y estado de la última sincronización y, para gestores/administradores, botones Actualizar (manualSyncAction) y Desconectar (disconnectRepositoryAction), más un enlace "Abrir dashboard" hacia /projects/[projectId]/github. Si no, aparece la sección de conexión: con una GitHub App configurada, el botón de instalación (startInstallationAction); sin ella, un botón de modo desarrollo (devConnectSampleRepositoryAction) que carga datos sintéticos de muestra. Tras el callback de instalación (?installation_id, validado contra la tabla github_installations), un selector de repositorios (listInstallationRepositories) guarda la elección con selectRepositoriesAction. Una sección de diagnóstico indica si están configuradas las variables de la GitHub App, el webhook secret y el flag GITHUB_INTELLIGENCE_ENABLED. Relacionadas: dashboard de GitHub Intelligence, Configuración del Proyecto.
Fuente: settings/integrations/github/page.tsx, github/actions.ts, components/github-intelligence/*.
Verifica: proyecto de software → Settings → tarjeta GitHub Intelligence.', '/projects/[projectId]/settings/integrations/github — generated from: src/app/[locale]/(app)/projects/[projectId]/settings/integrations/github/page.tsx, src/app/[locale]/(app)/projects/[projectId]/github/actions.ts, src/components/github-intelligence/github-action-buttons.tsx, src/components/github-intelligence/repo-picker.tsx, src/lib/github-intelligence/software-project-guard.ts, src/lib/github-intelligence/config.ts, src/lib/github-intelligence/read-model.ts, src/lib/github-intelligence/installation.ts'),
    ('screen-project-github', 'GitHub Intelligence dashboard screen', 'The read-only execution-evidence dashboard for a connected repository, at /projects/[projectId]/github (linked from the integration settings). Guarded by assertGitHubIntelligenceAvailable: non-software projects get an explicit unavailable state; disabled/forbidden access 404s. With no repository connected it shows an empty state pointing managers to the sample-data button and the integration settings. Once connected, loadDashboardData (lib/github-intelligence/read-model) builds everything server-side. Filters are URL query params: a date window (7/14/30 days or all history, default all) and a repository switcher when several repos are connected. Five metric cards summarize commits, active branches, open PRs (with merged count), workflow/CI health and the Release Readiness score with a good/watch/at_risk/blocked band (lib/github-intelligence/readiness). The main visualization is the GitHubLivingGraph (branches/commits/PRs over time). Below sit three panels: an Activity Summary (commits, merged PRs, failed workflows, releases), an "Isabella Insight" card — a deterministic summary/risk/recommendation, labeled in code as a deterministic placeholder, with a note that readiness never replaces human approval — and a Release Path checklist (branch active, PR open, CI passing, staging deploy, production ready) derived from the metrics. The Refresh button calls manualSyncAction; the integration never writes to GitHub. Related screens: GitHub integration settings, Project Settings.
Source: github/page.tsx, github/actions.ts, lib/github-intelligence/read-model.ts.
Verify: software project with a connected repo → GitHub tab, or via Settings → GitHub Intelligence → Open dashboard.', 'Pantalla Dashboard de GitHub Intelligence', 'El dashboard de evidencia de ejecución, de solo lectura, para un repositorio conectado, en /projects/[projectId]/github (enlazado desde la configuración de la integración). Protegido por assertGitHubIntelligenceAvailable: los proyectos que no son de software ven un estado de no disponible explícito; los accesos deshabilitados o sin permiso devuelven 404. Sin repositorio conectado muestra un estado vacío que dirige a los gestores al botón de datos de muestra y a la configuración. Conectado, loadDashboardData (lib/github-intelligence/read-model) construye todo en el servidor. Los filtros son parámetros de URL: ventana de tiempo (7/14/30 días o todo el historial, por defecto todo) y selector de repositorio si hay varios. Cinco tarjetas resumen commits, ramas activas, PRs abiertos (con fusionados), salud de CI/workflows y el puntaje de Release Readiness con banda good/watch/at_risk/blocked (lib/github-intelligence/readiness). La visualización principal es el GitHubLivingGraph (ramas/commits/PRs en el tiempo). Debajo hay tres paneles: Resumen de actividad (commits, PRs fusionados, workflows fallidos, releases), la "Perspectiva de Isabella" — un resumen/riesgo/recomendación determinista, marcado en el código como placeholder determinista, con la nota de que la readiness no reemplaza la aprobación humana — y la Ruta al release (rama activa, PR abierto, CI en verde, deploy a staging, listo para producción) derivada de las métricas. El botón Actualizar llama a manualSyncAction; la integración nunca escribe en GitHub. Relacionadas: configuración de la integración con GitHub, Configuración del Proyecto.
Fuente: github/page.tsx, github/actions.ts, lib/github-intelligence/read-model.ts.
Verifica: proyecto de software con repositorio conectado → pestaña GitHub, o Settings → GitHub Intelligence → Abrir dashboard.', '/projects/[projectId]/github — generated from: src/app/[locale]/(app)/projects/[projectId]/github/page.tsx, src/app/[locale]/(app)/projects/[projectId]/github/actions.ts, src/components/github-intelligence/github-living-graph.tsx, src/lib/github-intelligence/read-model.ts, src/lib/github-intelligence/readiness.ts, src/lib/github-intelligence/summary.ts, src/lib/github-intelligence/software-project-guard.ts'),
    ('screen-project-meeting-detail', 'Project Meeting Detail screen', 'Reached by clicking a meeting card on the Meetings list (/projects/[projectId]/meetings/[meetingId]), this screen shows one record from the `meetings` table: date, duration, location, attendees, agenda, summary, notes, linked stakeholders, and status badge, with Edit and Archive actions. The server page also builds a bidirectional traceability panel: it reads `traceability_links` where the meeting is source or target, resolves titles from `decisions`, `meetings`, `communication_items` and `documents`, and passes entity catalogs so the "Add link" dialog can connect the meeting to decisions, other meetings, communications or documents with a link type (related_to, caused_by, depends_on, supersedes, derived_from, contradicts) and context notes. Two AI features stand out: "Extract decisions" (ai-extract-actions.ts) and "Extract action items" (ai-action-extract-actions.ts) run the meeting''s notes/agenda through `runAi`, return suggestions with confidence and source excerpts, and let the user edit, approve or reject each one; approving inserts into `decisions` or `action_items` and creates a `traceability_link` back to the meeting, with audit logging via `logAudit`. Related screens: Meetings list, Decisions, Communications, Documents.
Source: meetings/[meetingId]/page.tsx, meeting-detail-client.tsx, ai-extract-actions.ts, ai-action-extract-actions.ts.
Verify: open a project, go to Meetings and click any meeting.', 'Pantalla Detalle de reunión', 'Se llega al pulsar una tarjeta en la lista de Reuniones (/projects/[projectId]/meetings/[meetingId]). Muestra un registro de la tabla `meetings`: fecha, duración, lugar, asistentes, agenda, resumen, notas, stakeholders vinculados y estado, con acciones de Editar y Archivar. La página de servidor construye además un panel de trazabilidad bidireccional: lee `traceability_links` donde la reunión es origen o destino, resuelve títulos desde `decisions`, `meetings`, `communication_items` y `documents`, y entrega catálogos para que el diálogo "Agregar vínculo" conecte la reunión con decisiones, otras reuniones, comunicaciones o documentos, con tipo de vínculo (relacionado, causado por, depende de, reemplaza, derivado de, contradice) y notas de contexto. Destacan dos funciones de IA: "Extraer decisiones" y "Extraer acciones" procesan las notas y la agenda con `runAi`, devuelven sugerencias con nivel de confianza y extracto fuente, y permiten editarlas, aprobarlas o rechazarlas; al aprobar se insertan en `decisions` o `action_items` y se crea un `traceability_link` hacia la reunión, con registro de auditoría (`logAudit`). Relacionadas: lista de Reuniones, Decisiones, Comunicaciones, Documentos.
Fuente: meetings/[meetingId]/page.tsx, meeting-detail-client.tsx, ai-extract-actions.ts, ai-action-extract-actions.ts.
Verifica: abre un proyecto, entra a Reuniones y pulsa cualquier reunión.', '/projects/[projectId]/meetings/[meetingId] — generated from: src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/meeting-detail-client.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/ai-extract-actions.ts, src/app/[locale]/(app)/projects/[projectId]/meetings/[meetingId]/ai-action-extract-actions.ts'),
    ('screen-project-meetings', 'Project Meetings screen', 'The Meetings screen is the classic meeting log of a project, reached from the project workspace at /projects/[projectId]/meetings. The server page verifies the project belongs to the user''s organization, then loads all non-deleted rows from the `meetings` table (newest `meeting_date` first) plus the project''s `stakeholders` for a multi-select. The `MeetingListClient` renders a header with a "Create" button, a filter bar (status: scheduled / in_progress / completed / cancelled, plus date-from and date-to), and meeting cards that link to the meeting detail screen. `CreateMeetingDialog` and `EditMeetingDialog` capture title, agenda, notes, summary, date, duration, location, attendees (free text), status and linked stakeholders. Server actions in `actions.ts` (createMeetingAction, updateMeetingAction, archiveMeetingAction) validate with Zod, write to `meetings` using per-locale i18n JSON fields and `linked_stakeholder_ids`, then fire-and-forget generate a semantic-search embedding and emit a Living Graph `communication_flow` event via `emitAndAutoLink`. Archiving is a soft delete with confirmation. Note the Product Brain designates Rhythm Center (/rhythm) as the canonical meeting module; this screen is the simpler standalone log. Related screens: meeting detail, Rhythm Center, Communications.
Source: meetings/page.tsx, meeting-list-client.tsx, meetings/actions.ts.
Verify: open a project and go to Meetings (/projects/[projectId]/meetings).', 'Pantalla Reuniones del proyecto', 'La pantalla de Reuniones es la bitácora clásica de reuniones de un proyecto, accesible desde el espacio del proyecto en /projects/[projectId]/meetings. La página de servidor verifica que el proyecto pertenezca a la organización del usuario y carga las filas no eliminadas de la tabla `meetings` (ordenadas por `meeting_date` descendente) junto con los `stakeholders` del proyecto para un selector múltiple. El componente `MeetingListClient` muestra un encabezado con botón "Crear", una barra de filtros (estado: programada / en curso / completada / cancelada, más fecha desde y hasta) y tarjetas de reunión que enlazan al detalle. Los diálogos de creación y edición capturan título, agenda, notas, resumen, fecha, duración, lugar, asistentes (texto libre), estado y stakeholders vinculados. Las acciones de servidor (createMeetingAction, updateMeetingAction, archiveMeetingAction) validan con Zod, escriben en `meetings` con campos i18n por idioma y `linked_stakeholder_ids`, y de forma asíncrona generan un embedding para búsqueda semántica y emiten un evento `communication_flow` del Living Graph. Archivar es un borrado lógico con confirmación. El Product Brain designa al Rhythm Center (/rhythm) como módulo canónico de reuniones; esta pantalla es la bitácora simple. Relacionadas: detalle de reunión, Rhythm Center, Comunicaciones.
Fuente: meetings/page.tsx, meeting-list-client.tsx, meetings/actions.ts.
Verifica: abre un proyecto y entra a Reuniones (/projects/[projectId]/meetings).', '/projects/[projectId]/meetings — generated from: src/app/[locale]/(app)/projects/[projectId]/meetings/page.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/meeting-list-client.tsx, src/app/[locale]/(app)/projects/[projectId]/meetings/actions.ts, src/components/meetings/index.ts, src/components/meetings/create-meeting-dialog.tsx, src/components/meetings/edit-meeting-dialog.tsx, src/components/meetings/meeting-card.tsx, src/components/meetings/meeting-filters.tsx, src/components/meetings/meeting-status-badge.tsx'),
    ('screen-project-memory', 'Project Memory screen', 'Per-project institutional memory (CAP-006/CAP-008): every captured note, email, meeting note or evidence lives here, classified, vectorized and linkable to project entities. The server page loads up to 500 project_memory_items with their traceability_links (source_type "memory"), plus linkable entities (roadmap_tasks, milestones, risks, stakeholders, decisions, documents, communication_items, meetings) and counts for the secondary tabs. Tabs: Memory (timeline of items), Overview (unified activity feed), Communications, Meetings, Decisions, Documents (each with counts and "Manage" links to their module) and Search. The "ProjectOps Scribe" button opens the capture modal: write, paste or Dictate (browser Web Speech API); analyzeScribeAction extracts structured actions, decisions, follow-ups and risks with verbatim source excerpts; the user reviews and only approved items are saved by saveScribeEntryAction into project_memory_items (+ project_scribe_items), creating the approved entities linked back via traceability_links. Item actions: create, update, archive (also de-indexes), reclassify with AI, reindex, link/unlink entities — creation triggers fire-and-forget AI classification and pgvector indexing (ai_status / index_status), all audit-logged. searchMemoryAction runs semantic plus keyword search scoped to the org and project. The ?item= param deep-links to an item.
Source: src/app/[locale]/(app)/projects/[projectId]/memory/page.tsx, memory-client.tsx, actions.ts, scribe-actions.ts, search-action.ts.
Verify: open a project and go to /projects/[projectId]/memory; click "ProjectOps Scribe".', 'Pantalla Memoria del Proyecto', 'Memoria institucional por proyecto (CAP-006/CAP-008): cada nota, correo, minuta o evidencia capturada vive aquí, clasificada, vectorizada y vinculable a entidades del proyecto. La página de servidor carga hasta 500 project_memory_items con sus traceability_links (source_type "memory"), más entidades vinculables (roadmap_tasks, milestones, risks, stakeholders, decisions, documents, communication_items, meetings) y conteos para las pestañas secundarias. Pestañas: Memoria (línea de tiempo), Resumen (actividad unificada), Comunicaciones, Reuniones, Decisiones, Documentos (con conteos y enlaces "Gestionar" a cada módulo) y Buscar. El botón "ProjectOps Scribe" abre el modal de captura: escribir, pegar o Dictar (Web Speech API del navegador); analyzeScribeAction extrae acciones, decisiones, seguimientos y riesgos estructurados con extractos textuales de origen; el usuario revisa y solo los elementos aprobados se guardan con saveScribeEntryAction en project_memory_items (+ project_scribe_items), creando las entidades aprobadas enlazadas vía traceability_links. Acciones sobre elementos: crear, actualizar, archivar (también desindexar), reclasificar con IA, reindexar, vincular/desvincular — la creación dispara clasificación de IA e indexado pgvector en segundo plano (ai_status / index_status), todo con registro en audit_logs. searchMemoryAction ejecuta búsqueda semántica y por palabras clave acotada a la organización y al proyecto. El parámetro ?item= abre un elemento directo.
Fuente: src/app/[locale]/(app)/projects/[projectId]/memory/page.tsx, memory-client.tsx, actions.ts, scribe-actions.ts, search-action.ts.
Verifica: abre un proyecto, ve a /projects/[projectId]/memory y pulsa "ProjectOps Scribe".', '/projects/[projectId]/memory — generated from: src/app/[locale]/(app)/projects/[projectId]/memory/page.tsx, src/app/[locale]/(app)/projects/[projectId]/memory/memory-client.tsx, src/app/[locale]/(app)/projects/[projectId]/memory/actions.ts, src/app/[locale]/(app)/projects/[projectId]/memory/scribe-actions.ts, src/app/[locale]/(app)/projects/[projectId]/memory/search-action.ts, src/components/memory/scribe-modal.tsx'),
    ('screen-project-overview', 'Project Overview (Command Center) screen', 'This is the project home, reached by opening any project from the projects list. The header shows title, status, type and dates, with Edit and Archive actions. Below it sit three navigation strips: Charter & Governance (status, completion %, locked/approved state, links to /charter), Delivery Framework (method, active cycle, open scope-creep alert count, links to /delivery) and Team & Roles (member count, missing critical roles, completeness score, links to /team). An AI Communication Summary section generates an on-demand summary of communications and decisions. The main ProjectDashboard renders stat cards (communications, meetings, decisions, documents, action items, links), recent-item lists, unresolved action items, traceability health with missing-link entities, a roadmap snapshot (current/next milestone, blocked and upcoming tasks) and a deterministic Status card built client-side with buildProjectBriefing — the same engine Isabella''s briefing uses (REG-013/REG-015), where waiting on a dependency is never counted as blocked. "Resolve now" on a blocker only navigates to the task in the Workboard editor; it never mutates. Reads Supabase tables: projects, project_charters, project_delivery_frameworks, project_scope_creep_alerts, project_execution_cycles, communication_items, meetings, decisions, documents, action_items, milestones, roadmap_tasks, task_dependencies, risks, traceability_links. Related screens: Workboard, Status, Charter, Delivery, Team, Closeout, Export.
Source: src/app/[locale]/(app)/projects/[projectId]/page.tsx, dashboard-client.tsx, src/lib/project-briefing/briefing-engine.ts.
Verify: open a project from the projects list; the Command Center is the landing tab.', 'Pantalla Resumen del Proyecto (Command Center)', 'Es la página de inicio del proyecto; se llega al abrir cualquier proyecto desde la lista. El encabezado muestra título, estado, tipo y fechas, con acciones de editar y archivar. Debajo hay tres franjas de navegación: Charter y Gobernanza (estado, % de completitud, si está aprobado/bloqueado; enlaza a /charter), Marco de Ejecución (método, ciclo activo, alertas de scope creep abiertas; enlaza a /delivery) y Equipo y Roles (miembros, roles críticos faltantes, puntaje; enlaza a /team). La sección de Resumen de Comunicaciones con IA genera bajo demanda un resumen de comunicaciones y decisiones. El panel principal muestra tarjetas de conteo (comunicaciones, reuniones, decisiones, documentos, acciones, enlaces), listas de elementos recientes, acciones sin resolver, salud de trazabilidad con entidades sin enlaces, un snapshot del roadmap (hito actual/siguiente, tareas bloqueadas y próximas) y una tarjeta de Estado determinista calculada con buildProjectBriefing, el mismo motor del briefing de Isabella (REG-013/REG-015): esperar una dependencia nunca cuenta como bloqueado. "Resolver ahora" solo navega a la tarea en el Workboard; nunca modifica datos. Lee las tablas projects, project_charters, project_delivery_frameworks, project_scope_creep_alerts, project_execution_cycles, communication_items, meetings, decisions, documents, action_items, milestones, roadmap_tasks, task_dependencies, risks y traceability_links. Pantallas relacionadas: Workboard, Estado, Charter, Delivery, Equipo, Cierre, Exportación.
Fuente: src/app/[locale]/(app)/projects/[projectId]/page.tsx, dashboard-client.tsx, src/lib/project-briefing/briefing-engine.ts.
Verifica: abre un proyecto desde la lista de proyectos; el Command Center es la pestaña inicial.', '/projects/[projectId] — generated from: src/app/[locale]/(app)/projects/[projectId]/page.tsx, src/app/[locale]/(app)/projects/[projectId]/dashboard-client.tsx, src/app/[locale]/(app)/projects/[projectId]/project-header-client.tsx, src/app/[locale]/(app)/projects/[projectId]/project-detail-client.tsx, src/lib/project-briefing/briefing-engine.ts, src/lib/roadmap/progress.ts'),
    ('screen-project-rhythm-center', 'Project Rhythm Center screen', 'The Rhythm Center (/projects/[projectId]/rhythm) is the canonical calendar-plus-meetings module — not to be confused with /rythm (no "h"), the retired Rythm Meeting Intelligence route that now merely redirects here (REG-011). The server page loads `project_events`, `meetings` linked by event_id, `meeting_attendees`, meeting-linked `decisions` and `action_items`, `project_memory_items` (source_system "rhythm_center", used to badge memory-synced meetings) and `stakeholders`. `RhythmClient` offers List and Calendar views, filters (event type, status, date range), an "Upcoming events" panel and a "New meeting" dialog that creates a meeting from templates (kickoff, status_update, stakeholder_review, project_review, closing, other), auto-generating agenda, objective and expected outcome. Clicking an event opens the `MeetingDrawer`: AI Summary, agenda sections with per-section notes, general notes, decisions, action items and attendees, plus "Generate summary" (runAi) and "Complete & save to memory", which calls `completeMeeting` in `lib/rhythm/service` to write `project_memory_items`. Server actions cover event CRUD, `createMeetingFromTemplateAction` (inserts event + meeting + attendees), attendee CRUD, adding decisions/action items, and audit logging. Related screens: Meetings, Workboard, Decisions, Project Memory, Closeout.
Source: rhythm/page.tsx, rhythm-client.tsx, meeting-drawer.tsx, rhythm/actions.ts, lib/rhythm/service.ts.
Verify: open a project and go to Rhythm Center (/projects/[projectId]/rhythm).', 'Pantalla Centro de Ritmo del proyecto', 'El Centro de Ritmo (/projects/[projectId]/rhythm) es el módulo canónico de calendario y reuniones — no confundir con /rythm (sin "h"), la ruta retirada de Rythm Meeting Intelligence que hoy solo redirige aquí (REG-011). La página de servidor carga `project_events`, `meetings` enlazadas por event_id, `meeting_attendees`, `decisions` y `action_items` ligados a reuniones, `project_memory_items` (source_system "rhythm_center", para marcar reuniones sincronizadas con memoria) y `stakeholders`. `RhythmClient` ofrece vistas Lista y Calendario, filtros (tipo de evento, estado, rango de fechas), un panel de "Próximos eventos" y un diálogo "Nueva reunión" que crea reuniones desde plantillas (arranque, actualización de estado, revisión con stakeholders, revisión de proyecto, cierre, otra), generando automáticamente agenda, objetivo y resultado esperado. Al pulsar un evento se abre el `MeetingDrawer`: Resumen IA, secciones de agenda con notas por sección, notas generales, decisiones, acciones y asistentes, más "Generar resumen" (runAi) y "Completar y guardar en memoria", que llama a `completeMeeting` en `lib/rhythm/service` para escribir en `project_memory_items`. Las acciones de servidor cubren CRUD de eventos, `createMeetingFromTemplateAction` (inserta evento + reunión + asistentes), CRUD de asistentes, alta de decisiones/acciones y auditoría. Relacionadas: Reuniones, Workboard, Decisiones, Memoria del Proyecto, Closeout.
Fuente: rhythm/page.tsx, rhythm-client.tsx, meeting-drawer.tsx, rhythm/actions.ts, lib/rhythm/service.ts.
Verifica: abre un proyecto y entra al Centro de Ritmo (/projects/[projectId]/rhythm).', '/projects/[projectId]/rhythm — generated from: src/app/[locale]/(app)/projects/[projectId]/rhythm/page.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/rhythm-client.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/meeting-drawer.tsx, src/app/[locale]/(app)/projects/[projectId]/rhythm/actions.ts, src/lib/rhythm/templates.ts, src/lib/rhythm/service.ts'),
    ('screen-project-roadmap', 'Project Roadmap screen (redirect to Execution Map)', 'The /roadmap route no longer renders its own UI: its page.tsx is a server redirect that immediately sends the user to /projects/[projectId]/execution-map, preserving the locale (localizedHref + redirect). So anyone navigating to the Roadmap URL lands on the Execution Map, which loads projects, milestones, roadmap_tasks and task_dependencies, computes per-milestone task counts, overall roadmap progress (computeRoadmapProgress) and a recommended next step (computeNextStep), and renders the ExecutionMapClient with milestone/task views. Even though the roadmap page itself is only a redirect, the roadmap folder remains important: roadmap/actions.ts hosts the canonical server actions for the whole task/milestone domain — createTaskAction, updateTaskAction, updateTaskStatusAction (with dependency_not_met enforcement and blocker_reason notes), reorderTasksAction, createMilestoneAction, updateMilestoneAction, reorderMilestoneAction, archiveTaskAction, archiveMilestoneAction, recordPromptSentAction and getTaskAuditTrailAction — which the Workboard and Execution Map clients import. All of these write to the milestones and roadmap_tasks tables scoped by organization and project. Related screens: Workboard (same task data as a Kanban board), Execution Map (the actual destination), Task Execution Map, Command Center roadmap snapshot.
Source: roadmap/page.tsx (redirect), roadmap/actions.ts, execution-map/page.tsx.
Verify: open a project and visit /projects/{id}/roadmap; the browser redirects to /projects/{id}/execution-map.', 'Pantalla Roadmap del proyecto (redirección al Execution Map)', 'La ruta /roadmap ya no tiene interfaz propia: su page.tsx es una redirección de servidor que envía de inmediato a /projects/[projectId]/execution-map, conservando el idioma (localizedHref + redirect). Quien navegue a la URL del Roadmap aterriza en el Execution Map, que carga projects, milestones, roadmap_tasks y task_dependencies, calcula conteos de tareas por hito, el progreso general del roadmap (computeRoadmapProgress) y un siguiente paso recomendado (computeNextStep), y muestra el ExecutionMapClient con vistas de hitos y tareas. Aunque la página en sí solo redirige, la carpeta roadmap sigue siendo clave: roadmap/actions.ts contiene las acciones de servidor canónicas del dominio de tareas e hitos — createTaskAction, updateTaskAction, updateTaskStatusAction (con validación dependency_not_met y notas blocker_reason), reorderTasksAction, createMilestoneAction, updateMilestoneAction, reorderMilestoneAction, archiveTaskAction, archiveMilestoneAction, recordPromptSentAction y getTaskAuditTrailAction — que importan el Workboard y el Execution Map. Todas escriben en las tablas milestones y roadmap_tasks con alcance de organización y proyecto. Pantallas relacionadas: Workboard (los mismos datos como tablero Kanban), Execution Map (el destino real), Mapa de Ejecución de tarea y el snapshot del roadmap en el Command Center.
Fuente: roadmap/page.tsx (redirección), roadmap/actions.ts, execution-map/page.tsx.
Verifica: abre un proyecto y visita /projects/{id}/roadmap; el navegador redirige a /projects/{id}/execution-map.', '/projects/[projectId]/roadmap — generated from: src/app/[locale]/(app)/projects/[projectId]/roadmap/page.tsx, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts, src/app/[locale]/(app)/projects/[projectId]/execution-map/page.tsx'),
    ('screen-project-rythm-intelligence', 'Rythm Meeting Intelligence screen (redirect alias)', 'Important naming note: /rythm (no "h") is NOT the Rhythm Center. It was the standalone "Rythm Meeting Intelligence" module — audio-focused meetings (in-person recording, video call, uploaded audio) with a transcription pipeline (draft, recording, audio_uploaded, ready_for_transcription, transcribing, transcribed, summary_ready, failed), backed by `lib/rythm/meeting-service` and tables like `project_rythm_audio_files`, transcripts and processing jobs. Per REG-011 in the regression log, the standalone dashboard queried a `project_rythm_meetings` table that never reached production and crashed, so today `rythm/page.tsx` contains no UI at all: it is a backward-compatible alias that immediately calls `redirect()` to the canonical Rhythm Center at /projects/[projectId]/rhythm, preserving old bookmarks and deep links. The dormant `rythm/actions.ts` (createMeetingAction, registerAudioAction, status updates, processing jobs) and `lib/rythm` remain in the repo but are not reachable from this route. Anyone asking about "Rythm" screens should be pointed to the Rhythm Center, where the meeting capability now lives. Related screens: Rhythm Center (/rhythm), Meetings.
Source: rythm/page.tsx, rythm/actions.ts, docs/product-brain/04-module-map.md.
Verify: navigating to /projects/[projectId]/rythm redirects to /projects/[projectId]/rhythm.', 'Pantalla Rythm Meeting Intelligence (alias de redirección)', 'Nota importante de nombres: /rythm (sin "h") NO es el Centro de Ritmo. Era el módulo independiente "Rythm Meeting Intelligence" — reuniones centradas en audio (grabación presencial, videollamada, audio subido) con un pipeline de transcripción (draft, recording, audio_uploaded, ready_for_transcription, transcribing, transcribed, summary_ready, failed), apoyado en `lib/rythm/meeting-service` y tablas como `project_rythm_audio_files`, transcripciones y trabajos de procesamiento. Según REG-011 del registro de regresiones, el tablero independiente consultaba una tabla `project_rythm_meetings` que nunca llegó a producción y fallaba; por eso hoy `rythm/page.tsx` no contiene interfaz alguna: es un alias retrocompatible que llama de inmediato a `redirect()` hacia el Centro de Ritmo canónico en /projects/[projectId]/rhythm, preservando marcadores y enlaces antiguos. El archivo `rythm/actions.ts` (crear reunión, registrar audio, estados, trabajos de procesamiento) y `lib/rythm` siguen en el repositorio pero inactivos desde esta ruta. Ante preguntas sobre "Rythm", dirige al usuario al Centro de Ritmo, donde vive hoy la capacidad de reuniones. Relacionadas: Centro de Ritmo (/rhythm), Reuniones.
Fuente: rythm/page.tsx, rythm/actions.ts, docs/product-brain/04-module-map.md.
Verifica: al navegar a /projects/[projectId]/rythm se redirige a /projects/[projectId]/rhythm.', '/projects/[projectId]/rythm — generated from: src/app/[locale]/(app)/projects/[projectId]/rythm/page.tsx, src/app/[locale]/(app)/projects/[projectId]/rythm/actions.ts, src/lib/rythm/index.ts, src/lib/rythm/meeting-service.ts, src/lib/rythm/recording-service.ts, src/lib/rythm/storage-service.ts, src/lib/rythm/types.ts, docs/product-brain/04-module-map.md'),
    ('screen-project-rythm-meeting-detail', 'Rythm meeting detail screen (redirect alias)', 'This route (/projects/[projectId]/rythm/[meetingId], "rythm" without the "h") was the detail view of a single meeting in the retired Rythm Meeting Intelligence module — the audio/transcription-oriented meeting surface, distinct from the Rhythm Center calendar module at /rhythm. According to the REG-011 consolidation documented in the file itself and in the product brain''s regression log, this detail surface queried the `project_rythm_meetings` table, which never reached production, so the page crashed with a server error. The current `rythm/[meetingId]/page.tsx` therefore renders nothing: it is a pure backward-compatibility alias whose only code is a `redirect()` to the canonical Rhythm Center at /projects/[projectId]/rhythm (the meetingId segment is accepted but not used to deep-link into a specific event). Old bookmarks and deep links to individual Rythm meetings land safely on the Rhythm Center list, where the user can locate the meeting and open its drawer. There is no data read or write on this route beyond resolving locale and projectId for the redirect. Related screens: Rhythm Center (/rhythm), Rythm alias index (/rythm), Meetings and meeting detail under /meetings.
Source: rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md.
Verify: opening /projects/[projectId]/rythm/<any-id> redirects to /projects/[projectId]/rhythm.', 'Pantalla Detalle de reunión Rythm (alias de redirección)', 'Esta ruta (/projects/[projectId]/rythm/[meetingId], "rythm" sin "h") era la vista de detalle de una reunión del módulo retirado Rythm Meeting Intelligence — la superficie de reuniones orientada a audio y transcripción, distinta del módulo de calendario Centro de Ritmo en /rhythm. Según la consolidación REG-011 documentada en el propio archivo y en el registro de regresiones del product brain, esta vista consultaba la tabla `project_rythm_meetings`, que nunca llegó a producción, por lo que la página fallaba con un error de servidor. El `rythm/[meetingId]/page.tsx` actual no muestra nada: es un alias de retrocompatibilidad cuyo único código es un `redirect()` al Centro de Ritmo canónico en /projects/[projectId]/rhythm (el segmento meetingId se acepta pero no se usa para abrir un evento específico). Los marcadores y enlaces antiguos a reuniones Rythm aterrizan sin errores en la lista del Centro de Ritmo, donde el usuario puede ubicar la reunión y abrir su panel. Esta ruta no lee ni escribe datos más allá de resolver locale y projectId para redirigir. Relacionadas: Centro de Ritmo (/rhythm), alias índice de Rythm (/rythm), Reuniones y detalle bajo /meetings.
Fuente: rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md.
Verifica: abrir /projects/[projectId]/rythm/<cualquier-id> redirige a /projects/[projectId]/rhythm.', '/projects/[projectId]/rythm/[meetingId] — generated from: src/app/[locale]/(app)/projects/[projectId]/rythm/[meetingId]/page.tsx, docs/product-brain/04-module-map.md'),
    ('screen-project-search', 'Project Search screen', 'Cross-entity search inside one project, reached from the project''s Search tab. The server page only verifies the project belongs to the user''s organization and passes translations; all searching happens in the SearchClient through the searchProjectAction server action. A single query box with type filter chips (All, Communication, Meeting, Decision, Document, Task, Memory) runs two strategies in one action: keyword matching (case-insensitive substring with generated snippets) over communication_items, meetings, decisions, documents, roadmap_tasks and project_memory_items, and semantic search — the query is embedded with OpenAI text-embedding-3-small and matched via the match_documents Supabase RPC (pgvector). Semantic hits are listed first sorted by similarity and tagged with a "semantic match" badge; keyword duplicates are removed, then keyword results follow sorted by date. Semantic search is non-fatal: if no API key or the RPC fails, keyword results still return. Each result links to the entity''s detail screen. A second server action, backfillEmbeddingsAction, generates missing embeddings in idempotent batches of 50 for roadmap_tasks, communication_items, meetings, decisions and documents — the only write this screen can trigger (embedding columns). Related screens: Communications, Meetings, Decisions, Documents, Workboard, Project Memory.
Source: search/page.tsx, search/search-client.tsx, search/search-action.ts, search/embedding-backfill-action.ts.
Verify: open a project → Search tab, type a keyword and toggle the type filters.', 'Pantalla Búsqueda del proyecto', 'Búsqueda entre entidades dentro de un proyecto; se llega desde la pestaña Search del proyecto. La página de servidor solo verifica que el proyecto pertenezca a la organización y pasa traducciones; la búsqueda ocurre en el SearchClient mediante la acción de servidor searchProjectAction. Un cuadro de consulta con filtros por tipo (Todos, Comunicación, Reunión, Decisión, Documento, Tarea, Memoria) ejecuta dos estrategias en una sola acción: coincidencia por palabra clave (subcadena sin distinguir mayúsculas, con fragmentos generados) sobre communication_items, meetings, decisions, documents, roadmap_tasks y project_memory_items, y búsqueda semántica: la consulta se convierte en embedding con OpenAI text-embedding-3-small y se compara con la RPC match_documents de Supabase (pgvector). Los resultados semánticos van primero, ordenados por similitud y con la insignia de coincidencia semántica; se eliminan duplicados y luego siguen los resultados por palabra clave ordenados por fecha. La búsqueda semántica no es bloqueante: si falta la clave de API o falla la RPC, se devuelven igual los resultados por palabra clave. Cada resultado enlaza al detalle de su entidad. Una segunda acción, backfillEmbeddingsAction, genera embeddings faltantes en lotes idempotentes de 50; es la única escritura posible desde esta pantalla (columnas de embedding). Relacionadas: Comunicaciones, Reuniones, Decisiones, Documentos, Workboard, Memoria del proyecto.
Fuente: search/page.tsx, search/search-client.tsx, search/search-action.ts, search/embedding-backfill-action.ts.
Verifica: abre un proyecto → pestaña Search, escribe una palabra y prueba los filtros por tipo.', '/projects/[projectId]/search — generated from: src/app/[locale]/(app)/projects/[projectId]/search/page.tsx, src/app/[locale]/(app)/projects/[projectId]/search/search-client.tsx, src/app/[locale]/(app)/projects/[projectId]/search/search-action.ts, src/app/[locale]/(app)/projects/[projectId]/search/embedding-backfill-action.ts'),
    ('screen-project-settings', 'Project Settings screen', 'A lightweight settings hub for one project, reached from the project''s Settings tab. It is mostly navigation, not forms: the page itself performs no writes. After validating the project against the user''s organization, it renders a header ("Manage stakeholders, audit trail, and project metadata") and a grid of quick-link cards: Stakeholders (with a live count from the stakeholders table, linking to /projects/[projectId]/stakeholders), Audit Log (linking to /projects/[projectId]/audit), and Import / Merge Project Data, which links to the org-level import wizard preloaded for this project (/import?projectId=...) to upload Excel, CSV or PDF files and merge them into the project. A fourth card, GitHub Intelligence, appears only when the GITHUB_INTELLIGENCE_ENABLED feature flag is on and the project''s project_type is software_development; it links to /projects/[projectId]/settings/integrations/github and is hidden entirely otherwise. Below, a Project Metadata section shows read-only cards for start date, target end date and creation date, formatted per locale. Data read: projects (id, slug, title, status, dates, project_type) and a stakeholders count. Related screens: Stakeholders, Audit trail, Project Import, GitHub integration settings, Project Overview (where project editing/archiving lives).
Source: settings/page.tsx, lib/env.ts.
Verify: open a project → Settings tab; the GitHub card only shows for software projects with the flag enabled.', 'Pantalla Configuración del Proyecto', 'Un centro ligero de configuración por proyecto; se llega desde la pestaña Settings del proyecto. Es sobre todo navegación, no formularios: la página en sí no escribe nada. Tras validar que el proyecto pertenece a la organización, muestra un encabezado ("Gestiona stakeholders, auditoría y metadatos del proyecto") y una cuadrícula de tarjetas de acceso rápido: Stakeholders (con conteo en vivo de la tabla stakeholders, enlaza a /projects/[projectId]/stakeholders), Registro de Auditoría (enlaza a /projects/[projectId]/audit) e Importar / Fusionar datos, que lleva al asistente de importación de la organización precargado para este proyecto (/import?projectId=...) para subir Excel, CSV o PDF y fusionarlos. Una cuarta tarjeta, GitHub Intelligence, aparece solo cuando el feature flag GITHUB_INTELLIGENCE_ENABLED está activo y el project_type del proyecto es software_development; enlaza a /projects/[projectId]/settings/integrations/github y en cualquier otro caso queda oculta. Debajo, la sección Metadatos del Proyecto muestra tarjetas de solo lectura con fecha de inicio, fecha objetivo y fecha de creación, formateadas según el idioma. Lee: projects (id, slug, título, estado, fechas, tipo) y el conteo de stakeholders. Relacionadas: Stakeholders, Auditoría, Importación de proyectos, Configuración de GitHub, Resumen del proyecto (donde se edita/archiva el proyecto).
Fuente: settings/page.tsx, lib/env.ts.
Verifica: abre un proyecto → pestaña Settings; la tarjeta de GitHub solo aparece en proyectos de software con el flag activo.', '/projects/[projectId]/settings — generated from: src/app/[locale]/(app)/projects/[projectId]/settings/page.tsx, src/lib/env.ts'),
    ('screen-project-stakeholders', 'Project Stakeholders screen', 'The project stakeholder registry. The server page fans out two queries in parallel: the project row (org-scoped) and all non-deleted rows from the stakeholders table ordered by name, then hands them to StakeholderListClient with localized labels. The client shows a title, description and a create button that opens CreateStakeholderDialog; created stakeholders are appended to the list. Stakeholders render in a responsive card grid (2 columns on small screens, 3 on large): each StakeholderCard shows the person with influence and interest badges mapped to high/medium/low localized labels (InfluenceBadge component), plus edit and archive actions — archiving asks for confirmation and soft-deletes the row. When the list is empty, a dashed empty state invites creating the first stakeholder. Data: reads and writes only the stakeholders table (per-project, org-scoped, soft delete via deleted_at). Stakeholders registered here feed other governance screens: the decisions module links decisions to stakeholders, the Charter suggests them in governance roles through the unified People Directory, and Project Memory offers them as linkable entities.
Source: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, stakeholder-list-client.tsx, src/components/stakeholders/*.
Verify: open a project and go to /projects/[projectId]/stakeholders.', 'Pantalla Stakeholders del Proyecto', 'El registro de interesados del proyecto. La página de servidor lanza dos consultas en paralelo: la fila del proyecto (acotada a la organización) y todas las filas no eliminadas de la tabla stakeholders ordenadas por nombre, y las entrega a StakeholderListClient con etiquetas localizadas. El cliente muestra título, descripción y un botón de crear que abre CreateStakeholderDialog; los creados se agregan a la lista. Los stakeholders se muestran en una cuadrícula de tarjetas adaptable (2 columnas en pantallas pequeñas, 3 en grandes): cada StakeholderCard muestra a la persona con insignias de influencia e interés mapeadas a etiquetas alta/media/baja (componente InfluenceBadge), más acciones de editar y archivar — archivar pide confirmación y hace borrado suave. Con la lista vacía, un estado vacío con borde punteado invita a crear el primer stakeholder. Datos: lee y escribe solo la tabla stakeholders (por proyecto, acotada a la organización, borrado suave con deleted_at). Los stakeholders registrados aquí alimentan otras pantallas de gobernanza: el módulo de decisiones vincula decisiones a stakeholders, el Charter los sugiere en roles de gobernanza mediante el Directorio de Personas unificado, y la Memoria del Proyecto los ofrece como entidades vinculables.
Fuente: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, stakeholder-list-client.tsx, src/components/stakeholders/*.
Verifica: abre un proyecto y ve a /projects/[projectId]/stakeholders.', '/projects/[projectId]/stakeholders — generated from: src/app/[locale]/(app)/projects/[projectId]/stakeholders/page.tsx, src/app/[locale]/(app)/projects/[projectId]/stakeholders/stakeholder-list-client.tsx, src/components/stakeholders/stakeholder-card.tsx, src/components/stakeholders/create-stakeholder-dialog.tsx'),
    ('screen-project-status', 'Project Status Report screen', 'A plain-language, printable status report, reached from the project''s Status tab (also surfaced from the Command Center''s Reports & Executive Outputs area). The server page loads milestones, roadmap_tasks, task_dependencies, material_requirements, a budget_items count, profiles and resources (for owner names) plus the project_charters goal/status for context, and feeds everything into the deterministic buildStatusReport engine (lib/execution/status-report.ts) — no AI is involved. The report shows a headline sentence, a completion progress ring and stat buckets (done/started/blocked/not started), the milestone phases with completed/in-progress/upcoming states, blocked tasks surfaced first with their blocker_reason, attention items (blocked work, missing info), materials status and a daily plan with action types unblock/do_now/start/assign. Crucially, tasks whose only issue is an unfinished predecessor are counted as "waiting", never as blocked — blocked means an explicit impediment, matching the product''s independent status dimensions. The only button is "Download PDF", which uses the browser print dialog via printWithFilename with a generated document name; print CSS isolates the #status-report-print block. The screen is read-only and writes nothing. Related screens: Command Center (Status card uses the briefing engine), Closeout Report, Workboard.
Source: status/page.tsx, status/status-report-client.tsx, lib/execution/status-report.ts.
Verify: open a project → Status tab; click Download PDF to print.', 'Pantalla Reporte de Estado del proyecto', 'Un reporte de estado en lenguaje claro e imprimible; se llega desde la pestaña Status del proyecto (también enlazado desde el área de reportes del Command Center). El servidor carga milestones, roadmap_tasks, task_dependencies, material_requirements, el conteo de budget_items, profiles y resources (nombres de responsables) y el objetivo/estado del charter (project_charters) como contexto, y lo pasa al motor determinista buildStatusReport (lib/execution/status-report.ts); no interviene IA. El reporte muestra una frase titular, un anillo de progreso y contadores (hechas/iniciadas/bloqueadas/sin iniciar), las fases por hito con estados completado/en curso/próximo, las tareas bloqueadas primero con su blocker_reason, elementos de atención (trabajo bloqueado, información faltante), estado de materiales y un plan diario con acciones de tipo desbloquear/hacer ahora/iniciar/asignar. Un punto clave: las tareas cuyo único problema es una predecesora sin terminar se cuentan como "en espera", nunca como bloqueadas; bloqueado implica un impedimento explícito, acorde con las dimensiones de estado independientes del producto. El único botón es "Descargar PDF", que usa el diálogo de impresión del navegador (printWithFilename) con un nombre de documento generado. La pantalla es de solo lectura y no escribe nada. Relacionadas: Command Center, Reporte de Cierre, Workboard.
Fuente: status/page.tsx, status/status-report-client.tsx, lib/execution/status-report.ts.
Verifica: abre un proyecto → pestaña Status; usa Descargar PDF para imprimir.', '/projects/[projectId]/status — generated from: src/app/[locale]/(app)/projects/[projectId]/status/page.tsx, src/app/[locale]/(app)/projects/[projectId]/status/status-report-client.tsx, src/lib/execution/status-report.ts'),
    ('screen-project-task-detail', 'Task Execution Map (task detail) screen', 'The drill-down for one task, reached from a Workboard card''s "Execution Map" link (there is a "Back to Workboard" breadcrumb). While the Living Graph is the project-level visualization, this page is the task-level execution mind map: a parent task node (title, status, progress, owner, critical flag, estimate vs actual hours), its subtasks, and its external predecessor tasks drawn as dotted dependency nodes. Progress is calculated from subtasks; the parent can be overridden. The server page loads the roadmap_tasks row, task_subtasks, task_dependencies (predecessors), owner names from profiles (admin client, names only) and the assignable-owner list via getAssignableProjectOwners (org workspace users plus project team members, always including current owners). The ExecutionMapClient offers a canvas view and a table view, a subtask detail panel and a subtask form dialog. Writes go through lib/subtasks/actions.ts: createSubtaskAction, updateSubtaskAction, completeSubtaskAction, blockSubtaskAction/unblockSubtaskAction (blocking is an explicit action with a reason — separate from dependency waiting), reassignSubtaskAction, deleteSubtaskAction, overrideParentProgressAction, clearParentProgressOverrideAction and closeParentTaskWithIncompleteAction. Managing requires org owner/admin (canManage); uploads are blocked for viewers. Related screens: Workboard, Execution Map, Living Graph.
Source: tasks/[taskId]/page.tsx, components/task-execution-map/execution-map-client.tsx, lib/subtasks/actions.ts.
Verify: Workboard → open a task card''s Execution Map link.', 'Pantalla Mapa de Ejecución de tarea (detalle de tarea)', 'El detalle de una tarea; se llega desde el enlace "Execution Map" de una tarjeta del Workboard (hay una miga "Volver al Workboard"). Mientras el Living Graph es la visualización a nivel de proyecto, esta página es el mapa mental de ejecución de UNA tarea: un nodo padre (título, estado, progreso, responsable, marca de ruta crítica, horas estimadas y reales), sus subtareas y sus tareas predecesoras externas dibujadas como nodos punteados de dependencia. El progreso se calcula a partir de las subtareas; el del padre puede sobrescribirse. El servidor carga la fila de roadmap_tasks, task_subtasks, task_dependencies (predecesoras), nombres de responsables desde profiles (cliente admin, solo nombres) y la lista de responsables asignables con getAssignableProjectOwners (usuarios del workspace más miembros del equipo del proyecto, incluyendo siempre a los responsables actuales). El ExecutionMapClient ofrece vista de lienzo y de tabla, panel de detalle y formulario de subtareas. Las escrituras pasan por lib/subtasks/actions.ts: crear, actualizar, completar, bloquear/desbloquear (bloquear es una acción explícita con motivo, distinta de esperar una dependencia), reasignar y eliminar subtareas, además de sobrescribir o limpiar el progreso del padre y cerrar el padre con subtareas incompletas. Gestionar requiere rol owner/admin; los viewers no pueden subir archivos. Relacionadas: Workboard, Execution Map, Living Graph.
Fuente: tasks/[taskId]/page.tsx, components/task-execution-map/execution-map-client.tsx, lib/subtasks/actions.ts.
Verifica: Workboard → enlace Execution Map de una tarjeta de tarea.', '/projects/[projectId]/tasks/[taskId] — generated from: src/app/[locale]/(app)/projects/[projectId]/tasks/[taskId]/page.tsx, src/components/task-execution-map/execution-map-client.tsx, src/lib/subtasks/actions.ts, src/lib/subtasks/map-model.ts, src/lib/people/service.ts'),
    ('screen-project-team', 'Project Team & Roles screen', 'The Team screen (/projects/[projectId]/team) is the Project Team & Roles Center. The server page uses `lib/team-roles/service` to load the project team (`project_team_members`), RACI assignments (`project_raci_assignments`), stakeholder access (`stakeholder_access`), the company directory and company teams (`organization_team_members` + `profiles`), external contacts and the project''s `milestones`, then computes a team-completeness score (has PM, has approver, missing critical roles). `TeamClient` has three tabs: "Members & roles" (with a List view and a drag-and-drop Role Assignment Board built on dnd-kit, where people from the directory are dragged into role buckets with optimistic UI and undo), "RACI", and "Stakeholder access". Server actions cover adding individual members or whole company teams, editing/removing/restoring members, assigning and moving people between roles (with server-side dedup), AI role recommendations (recommendRolesAction / addRecommendedRolesAction), RACI add/delete plus generateRaciDraftAction, and granting or revoking read-only stakeholder viewers. A code comment confirms project teams are operational: adding people here never creates a paid seat. Related screens: Stakeholders, Organization members/teams, Charter governance roles, Resources.
Source: team/page.tsx, team-client.tsx, role-board.tsx, team/actions.ts, lib/team-roles/service.ts.
Verify: open a project and go to Team (/projects/[projectId]/team).', 'Pantalla Equipo y roles del proyecto', 'La pantalla de Equipo (/projects/[projectId]/team) es el Centro de Equipo y Roles del proyecto. La página de servidor usa `lib/team-roles/service` para cargar el equipo (`project_team_members`), asignaciones RACI (`project_raci_assignments`), accesos de stakeholders (`stakeholder_access`), el directorio y los equipos de la empresa (`organization_team_members` + `profiles`), contactos externos y los `milestones` del proyecto, y calcula un puntaje de completitud del equipo (hay PM, hay aprobador, roles críticos faltantes). `TeamClient` tiene tres pestañas: "Miembros y roles" (con vista Lista y un tablero de asignación de roles con arrastrar y soltar basado en dnd-kit, donde se arrastran personas del directorio a cubetas de rol, con interfaz optimista y deshacer), "RACI" y "Acceso de stakeholders". Las acciones de servidor permiten agregar miembros individuales o equipos completos de la empresa, editar/quitar/restaurar miembros, asignar y mover personas entre roles (con deduplicación en servidor), recomendaciones de roles con IA (recommendRolesAction / addRecommendedRolesAction), alta/baja de RACI más generateRaciDraftAction, y otorgar o revocar visores de solo lectura para stakeholders. Un comentario del código confirma que agregar personas aquí nunca crea un asiento de pago. Relacionadas: Stakeholders, Miembros/equipos de la organización, roles de gobernanza del Charter, Recursos.
Fuente: team/page.tsx, team-client.tsx, role-board.tsx, team/actions.ts, lib/team-roles/service.ts.
Verifica: abre un proyecto y entra a Equipo (/projects/[projectId]/team).', '/projects/[projectId]/team — generated from: src/app/[locale]/(app)/projects/[projectId]/team/page.tsx, src/app/[locale]/(app)/projects/[projectId]/team/team-client.tsx, src/app/[locale]/(app)/projects/[projectId]/team/role-board.tsx, src/app/[locale]/(app)/projects/[projectId]/team/actions.ts, src/lib/team-roles/service.ts, src/lib/team-roles/board-model.ts, src/lib/team-roles/config.ts'),
    ('screen-project-workboard', 'Project Workboard screen', 'The Kanban execution board, reached from the project''s Workboard tab. Columns map to TaskStatus values (not_started, prompt_ready, sent_to_ai, in_progress, implemented, tested, done, blocked, deferred); labels adapt to the project''s delivery framework via workboardColumnLabels, but the board always operates on the same statuses. Tasks are topologically sorted so predecessors appear before successors, grouped by milestone. Cards show owner (assigned_to person or assigned_resource_id crew/vendor, names resolved via the admin client from profiles, project_team_members and resources), priority (p1–p3), dependencies and subtask badges (blocked/overdue/done, aggregated from task_subtasks) plus a link to the Task Execution Map. Drag-and-drop calls updateTaskStatusAction and reorderTasksAction in roadmap/actions.ts; a move can fail with dependency_not_met if a predecessor is not done, and a status-change dialog captures an optional note (a blocked move stores blocker_reason — Blocked is an explicit impediment, distinct from waiting on a dependency). Other actions: create/edit tasks via TaskFormDialog (createTaskAction/updateTaskAction), delete (archiveTaskAction), filter by sprint or milestone, column visibility, collapse and resize. Reads projects, milestones, roadmap_tasks, task_dependencies, task_subtasks and project_delivery_frameworks; writes roadmap_tasks through the roadmap server actions. Related screens: Task Execution Map, Execution Map (roadmap), Command Center.
Source: workboard/page.tsx, workboard/workboard-client.tsx, roadmap/actions.ts.
Verify: open a project, then the Workboard tab.', 'Pantalla Workboard del proyecto', 'El tablero Kanban de ejecución; se llega desde la pestaña Workboard del proyecto. Las columnas corresponden a los estados de tarea (not_started, prompt_ready, sent_to_ai, in_progress, implemented, tested, done, blocked, deferred); las etiquetas se adaptan al marco de ejecución del proyecto con workboardColumnLabels, pero el tablero siempre opera sobre los mismos estados. Las tareas se ordenan topológicamente (predecesoras antes que sucesoras) y se agrupan por hito. Las tarjetas muestran responsable (persona en assigned_to o recurso/equipo en assigned_resource_id, con nombres resueltos desde profiles, project_team_members y resources), prioridad (p1–p3), dependencias e insignias de subtareas (bloqueadas/vencidas/hechas desde task_subtasks), con enlace al Mapa de Ejecución de la tarea. Arrastrar y soltar llama a updateTaskStatusAction y reorderTasksAction en roadmap/actions.ts; el movimiento puede fallar con dependency_not_met si una predecesora no está terminada, y un diálogo permite añadir una nota (al bloquear se guarda blocker_reason: bloqueado es un impedimento explícito, distinto de esperar una dependencia). También permite crear/editar tareas (TaskFormDialog), eliminarlas (archiveTaskAction), filtrar por sprint o hito y ajustar columnas. Lee projects, milestones, roadmap_tasks, task_dependencies, task_subtasks y project_delivery_frameworks; escribe en roadmap_tasks mediante las acciones de servidor de roadmap. Relacionadas: Mapa de Ejecución de tarea, Execution Map, Command Center.
Fuente: workboard/page.tsx, workboard/workboard-client.tsx, roadmap/actions.ts.
Verifica: abre un proyecto y entra a la pestaña Workboard.', '/projects/[projectId]/workboard — generated from: src/app/[locale]/(app)/projects/[projectId]/workboard/page.tsx, src/app/[locale]/(app)/projects/[projectId]/workboard/workboard-client.tsx, src/app/[locale]/(app)/projects/[projectId]/roadmap/actions.ts, src/lib/roadmap/topological-sort.ts, src/lib/subtasks/map-model.ts, src/lib/delivery/config.ts'),
    ('screen-projects-list', 'Projects list screen', 'The organization''s project portfolio at `/projects`, reached from the main navigation or from Command Center links. The server page queries the Supabase `projects` table scoped to the current organization (soft-deleted rows excluded, newest first) and resolves localized status labels via next-intl. The client component (`ProjectListClient`) renders a "create" button that opens the `CreateProjectDialog`, and either an empty state (folder icon plus localized empty copy) or a responsive grid of `ProjectCard`s, each linking to that project''s Command Center at `/projects/{id}`. Project creation goes through the `createProject` server action in `actions.ts`: input is validated with Zod (name, description, status, and a project type such as software_development or several construction types), the project row is inserted, a type-specific template can be instantiated (`getTemplateForType` / `instantiateTemplate` from `lib/execution`), a Project Charter is created via `createCharterForProject` (`lib/charter/service`), and the operation is recorded with `logAudit`. Data read: `projects`. Data written: `projects` plus template- and charter-related tables on creation. Related screens: the per-project Command Center and all project tabs, the Import screen (alternative way to create a project from a file), and the Home dashboard.

Source: src/app/[locale]/(app)/projects/page.tsx, project-list-client.tsx, actions.ts.
Verify: open Projects from the app navigation; create a project with the button and see its card appear.', 'Pantalla Lista de proyectos', 'El portafolio de proyectos de la organización en `/projects`, accesible desde la navegación principal o desde enlaces del Command Center. La página de servidor consulta la tabla `projects` de Supabase limitada a la organización actual (excluyendo eliminados, más recientes primero) y resuelve etiquetas de estado localizadas con next-intl. El componente cliente (`ProjectListClient`) muestra un botón de crear que abre el `CreateProjectDialog`, y un estado vacío (icono de carpeta con textos localizados) o una cuadrícula de tarjetas `ProjectCard`, cada una enlazando al Command Center de ese proyecto en `/projects/{id}`. La creación pasa por la server action `createProject` en `actions.ts`: valida con Zod (nombre, descripción, estado y tipo de proyecto, como desarrollo de software o varios tipos de construcción), inserta la fila del proyecto, puede instanciar una plantilla según el tipo (`getTemplateForType` / `instantiateTemplate` de `lib/execution`), crea un Charter mediante `createCharterForProject` (`lib/charter/service`) y registra la operación con `logAudit`. Lee: `projects`. Escribe: `projects` y tablas de plantilla y charter al crear. Pantallas relacionadas: el Command Center por proyecto y sus pestañas, la pantalla de Importación y el dashboard de inicio.

Fuente: src/app/[locale]/(app)/projects/page.tsx, project-list-client.tsx, actions.ts.
Verifica: abre Proyectos desde la navegación; crea un proyecto con el botón y observa su tarjeta aparecer.', '/projects — generated from: src/app/[locale]/(app)/projects/page.tsx, src/app/[locale]/(app)/projects/project-list-client.tsx, src/app/[locale]/(app)/projects/actions.ts, src/components/projects/project-card.tsx, src/components/projects/create-project-dialog.tsx'),
    ('screen-reports', 'Reports screen', 'The org-level "Reports & Intelligence" studio at `/reports` (subtitle: create executive dashboards, analyze performance, build custom reports from trusted datasets). The server page preloads saved reports and the project list via `listSavedReportsAction` and `listProjectsForReportsAction`, honoring `?report=` and `?project=` URL params for deep links. The client is a tabbed studio: Overview, Report Library (prebuilt reports from `lib/reports/report-library`), Report Builder, Saved Reports, Data Explorer, and KPI Dictionary (`lib/reports/kpi-dictionary`). The builder works only against curated datasets from the semantic layer (`lib/reports/registry`) — never raw SQL — with columns, filters, grouping, sorting, visualization type, calculated fields (formula expressions, plus an AI helper via `suggestCalculatedFieldAction`), and a scope selector for all projects or a single project. Running a report calls `runReportAction` (logs to `report_runs`, results capped at 5,000 rows); CSV export goes through `exportReportCsvAction` (logged in `report_exports`); saving, listing, duplicating, and deleting use the `saved_reports` table with private/project/organization visibility. Related screens: Home dashboard (links here from Budget & Forecast Signals) and project-level Status Report.

Source: src/app/[locale]/(app)/reports/page.tsx, reports-client.tsx, actions.ts, src/lib/reports/*.
Verify: open Reports from the app navigation, pick a dataset in Report Builder, select columns, and press Run.', 'Pantalla Reportes', 'El estudio organizacional "Reportes e Inteligencia" en `/reports` (subtítulo: crear dashboards ejecutivos, analizar desempeño y construir reportes a la medida desde datasets confiables). La página de servidor precarga reportes guardados y la lista de proyectos con `listSavedReportsAction` y `listProjectsForReportsAction`, respetando los parámetros `?report=` y `?project=` para enlaces directos. El cliente es un estudio con pestañas: Resumen, Biblioteca de Reportes (prediseñados en `lib/reports/report-library`), Constructor de Reportes, Reportes Guardados, Explorador de Datos y Diccionario de KPIs (`lib/reports/kpi-dictionary`). El constructor trabaja solo contra datasets curados de la capa semántica (`lib/reports/registry`) — nunca SQL crudo — con columnas, filtros, agrupación, orden, tipo de visualización, campos calculados (fórmulas, con ayudante de IA vía `suggestCalculatedFieldAction`) y un selector de alcance: todos los proyectos o uno solo. Ejecutar llama `runReportAction` (registra en `report_runs`, máximo 5.000 filas mostradas); la exportación CSV usa `exportReportCsvAction` (registrada en `report_exports`); guardar, listar, duplicar y eliminar usan la tabla `saved_reports` con visibilidad privada/proyecto/organización. Pantallas relacionadas: dashboard de inicio y el Reporte de Estado por proyecto.

Fuente: src/app/[locale]/(app)/reports/page.tsx, reports-client.tsx, actions.ts, src/lib/reports/*.
Verifica: abre Reportes desde la navegación, elige un dataset en el Constructor, selecciona columnas y pulsa Ejecutar.', '/reports — generated from: src/app/[locale]/(app)/reports/page.tsx, src/app/[locale]/(app)/reports/reports-client.tsx, src/app/[locale]/(app)/reports/actions.ts, src/lib/reports/registry.ts'),
    ('screen-resource-capacity', 'Resource Capacity Intelligence screen', 'The Resource Capacity Intelligence screen (pillar P3, CAP-009) answers "do we have enough real workforce capacity to deliver the plan?", at /projects/[projectId]/resource-capacity. The server page runs the deterministic engine `computeResourceCapacity` (lib/capacity/service, 4-week window), which reads `project_resource_allocations` plus team/task/milestone data, and adapts its wording per project type (construction types say "Crew", software says "Resource"). Eight overview cards show Health Index (with band color), effective vs nominal capacity, assigned workload with average utilization, remaining hours, overallocated hours, workforce availability with overhead percent, bottleneck count and at-risk milestones. The Capacity Editor lets PM/PMO users add team members as allocations and edit weekly capacity hours, availability percent, overhead percent and role inline, via `getCapacityEditorDataAction`, `saveAllocationAction` and `removeAllocationAction` (writes to `project_resource_allocations`). A PMO Summary section (deterministic `buildCapacitySummary` from lib/capacity/insight) presents a headline, bullets, bottlenecks, missing-data warnings and recommended actions explicitly labeled as requiring your approval. Below are the per-resource capacity table (effective, assigned, remaining, utilization, overhead, status), a milestone capacity-risk section and a health-deductions breakdown explaining every point subtracted. The same engine feeds the Living Graph''s workforceCapacity overlay. Related: screen-living-graph, screen-labor-capacity.
Source: resource-capacity/page.tsx, capacity-editor.tsx, actions.ts, lib/capacity/service.ts.
Verify: open a project > Resource Capacity (/projects/[projectId]/resource-capacity).', 'Pantalla Inteligencia de Capacidad de Recursos', 'La pantalla Inteligencia de Capacidad de Recursos (pilar P3, CAP-009) responde "¿tenemos capacidad real de fuerza de trabajo para entregar el plan?", en /projects/[projectId]/resource-capacity. La página de servidor ejecuta el motor determinista `computeResourceCapacity` (lib/capacity/service, ventana de 4 semanas), que lee `project_resource_allocations` junto con datos de equipo, tareas e hitos, y adapta la terminología por tipo de proyecto (en construcción dice "Cuadrilla"; en software, "Recurso"). Ocho tarjetas muestran el Índice de Salud (con color por banda), capacidad efectiva vs nominal, carga asignada con utilización promedio, horas restantes, horas sobreasignadas, disponibilidad con porcentaje de overhead, cuellos de botella e hitos en riesgo. El Editor de Capacidad permite a PM/PMO agregar miembros del equipo como asignaciones y editar en línea horas semanales, porcentaje de disponibilidad, porcentaje de overhead y rol, mediante `getCapacityEditorDataAction`, `saveAllocationAction` y `removeAllocationAction` (escribe en `project_resource_allocations`). La sección Resumen PMO (determinista, `buildCapacitySummary` de lib/capacity/insight) presenta un titular, viñetas, cuellos de botella, advertencias de datos faltantes y acciones recomendadas marcadas explícitamente como sujetas a tu aprobación. Debajo están la tabla de capacidad por recurso (efectiva, asignada, restante, utilización, overhead, estado), la sección de riesgo de capacidad por hito y el desglose de deducciones del índice de salud, que explica cada punto restado. El mismo motor alimenta la superposición de capacidad de fuerza de trabajo del Grafo Vivo. Relacionadas: screen-living-graph, screen-labor-capacity.
Fuente: resource-capacity/page.tsx, capacity-editor.tsx, actions.ts, lib/capacity/service.ts.
Verifica: abre un proyecto > Capacidad de Recursos (/projects/[projectId]/resource-capacity).', '/projects/[projectId]/resource-capacity — generated from: src/app/[locale]/(app)/projects/[projectId]/resource-capacity/page.tsx, src/app/[locale]/(app)/projects/[projectId]/resource-capacity/capacity-editor.tsx, src/app/[locale]/(app)/projects/[projectId]/resource-capacity/actions.ts, src/lib/capacity/service.ts, src/lib/capacity/insight.ts'),
    ('screen-settings', 'Settings screen', 'The personal preferences screen at `/settings` inside the authenticated app. It is intentionally small: after confirming the session with `getOrgContext()`, the server page renders two cards with inline bilingual copy. The Appearance card hosts `ThemeControl`, letting the user pick Light, Dark, or System theme. The Language card hosts `LanguageControl`, which offers English and Español buttons; selecting one writes the `NEXT_LOCALE` cookie (one-year max-age) and hard-navigates to the equivalent localized URL via `buildLocaleSwitchPath`, so the whole app reloads in the chosen language — the page copy states this explicitly. No Supabase tables are read or written on this screen and there are no server actions; preferences are client-side (cookie/theme). Organization-level configuration is not here: billing, plans, members, teams, and external contacts live under `/organization/*`, and per-project settings live in each project''s own Settings tab. Related screens: Team (workspace users), the Organization pages, and every localized screen affected by the language switch.

Source: src/app/[locale]/(app)/settings/page.tsx, src/components/settings/theme-control.tsx, src/components/settings/language-control.tsx.
Verify: open Settings from the app navigation, toggle the theme, then switch the language and watch the app reload in the other locale.', 'Pantalla Configuración', 'La pantalla de preferencias personales en `/settings`, dentro de la aplicación autenticada. Es intencionalmente pequeña: tras confirmar la sesión con `getOrgContext()`, la página de servidor muestra dos tarjetas con textos bilingües en línea. La tarjeta Apariencia contiene `ThemeControl`, que permite elegir tema Claro, Oscuro o Sistema. La tarjeta Idioma contiene `LanguageControl`, con botones English y Español; al elegir uno se escribe la cookie `NEXT_LOCALE` (vigencia de un año) y se navega de forma dura a la URL localizada equivalente mediante `buildLocaleSwitchPath`, de modo que toda la aplicación se recarga en el idioma elegido — el propio texto de la página lo indica. En esta pantalla no se leen ni escriben tablas de Supabase y no hay server actions; las preferencias son del lado del cliente (cookie/tema). La configuración organizacional no está aquí: facturación, planes, miembros, equipos y contactos externos viven bajo `/organization/*`, y los ajustes por proyecto en la pestaña Settings de cada proyecto. Pantallas relacionadas: Equipo, las páginas de Organización y todas las pantallas afectadas por el cambio de idioma.

Fuente: src/app/[locale]/(app)/settings/page.tsx, src/components/settings/theme-control.tsx, src/components/settings/language-control.tsx.
Verifica: abre Configuración desde la navegación, cambia el tema y luego el idioma; la aplicación se recarga en el otro idioma.', '/settings — generated from: src/app/[locale]/(app)/settings/page.tsx, src/components/settings/theme-control.tsx, src/components/settings/language-control.tsx'),
    ('screen-signup', 'Signup screen', 'The account-creation screen at `/signup` in the `(auth)` route group, reached from the landing page or from the login screen''s "no account" link. The server page renders a localized title/subtitle (namespace `auth.signup`) above the client `SignupForm`. The form collects display name, email, password, and password confirmation (minimum 6 characters). Before calling the server, the client validates that both passwords match and meet the length rule; it then calls the `signupAction` server action, which runs `supabase.auth.signUp` with the display name stored as `display_name` in user metadata and an `emailRedirectTo` pointing at `{origin}/auth/callback`. The origin prefers the configured `NEXT_PUBLIC_SITE_URL` over the request origin so confirmation emails always link to the canonical domain rather than a preview URL. On success the form replaces itself with a confirmation state — an envelope icon, a "check your email" message including the address, and a link back to login. Errors are mapped to localized messages (email already taken, weak password, mismatch, unexpected). No tables are written directly; account creation goes through Supabase Auth. Related screens: Login (`/login`) and the auth callback route that completes email confirmation.

Source: src/app/[locale]/(auth)/signup/page.tsx, src/components/auth/signup-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verify: open /signup, register with a new email, and confirm the "check your email" success state appears.', 'Pantalla Registro', 'La pantalla de creación de cuenta en `/signup`, dentro del grupo `(auth)`; se llega desde la landing o desde el enlace "sin cuenta" del login. La página de servidor muestra título y subtítulo localizados (namespace `auth.signup`) sobre el componente cliente `SignupForm`. El formulario pide nombre para mostrar, correo, contraseña y confirmación (mínimo 6 caracteres). Antes de llamar al servidor, el cliente valida que las contraseñas coincidan y cumplan la longitud; luego llama la server action `signupAction`, que ejecuta `supabase.auth.signUp` guardando el nombre como `display_name` en los metadatos del usuario y con `emailRedirectTo` apuntando a `{origen}/auth/callback`. El origen prefiere `NEXT_PUBLIC_SITE_URL` sobre el origen de la petición, para que los correos de confirmación enlacen al dominio canónico y no a una URL de preview. Si tiene éxito, el formulario se reemplaza por un estado de confirmación — icono de sobre, mensaje "revisa tu correo" con la dirección y enlace de vuelta al login. Los errores se traducen (correo ya registrado, contraseña débil, no coinciden, inesperado). No se escriben tablas directamente; todo pasa por Supabase Auth. Pantallas relacionadas: Login (`/login`) y la ruta de callback de autenticación.

Fuente: src/app/[locale]/(auth)/signup/page.tsx, src/components/auth/signup-form.tsx, src/app/[locale]/(auth)/actions.ts.
Verifica: abre /signup, regístrate con un correo nuevo y confirma que aparece el estado de éxito "revisa tu correo".', '/signup — generated from: src/app/[locale]/(auth)/signup/page.tsx, src/components/auth/signup-form.tsx, src/app/[locale]/(auth)/actions.ts'),
    ('screen-team', 'Team screen', 'The org-wide people directory at `/team`. The server page uses the Supabase admin client to load: `organization_members` for the current org (excluding removed members), their `profiles` fetched by user id (deliberately not filtered by the profile''s home org), best-effort emails from the auth admin API, org `resources` of types person/crew/team/role/vendor/subcontractor, and `projects` for name resolution. The `TeamClient` then shows two populations: workspace users (name, email, role, seat type, workspace role, status, department, job title — with a "you" marker) and project people/crews/vendors. For managers (`canManage`) it exposes real management actions: add, edit, merge duplicate resources, archive, and invite a resource as a workspace user (`createTeamResourceAction`, `updateTeamResourceAction`, `mergeTeamResourcesAction`, `archiveTeamResourceAction`, `inviteResourceAsUserAction` in the local `actions.ts`), plus workspace-user administration reused from `/organization/members/actions.ts`: create a member with a generated temporary password, update role/seat, reset password, remove, or permanently delete a user. A client-side helper generates readable temporary passwords; users created this way are pushed through the forced change-password flow. Data written: `resources`, `organization_members`, and Supabase Auth users. Related screens: Organization members, Change password, and per-project Team/RACI.

Source: src/app/[locale]/(app)/team/page.tsx, team-client.tsx, actions.ts, ../organization/members/actions.ts.
Verify: open Team from the app navigation; workspace users and project resources are listed, with management buttons if you are a manager.', 'Pantalla Equipo', 'El directorio de personas de toda la organización en `/team`. La página de servidor usa el cliente admin de Supabase para cargar: `organization_members` de la organización actual (excluyendo removidos), sus `profiles` buscados por id de usuario (a propósito sin filtrar por la organización personal del perfil), correos de mejor esfuerzo desde la API admin de auth, `resources` de tipos persona/cuadrilla/equipo/rol/proveedor/subcontratista y `projects` para resolver nombres. El `TeamClient` muestra dos poblaciones: usuarios del workspace (nombre, correo, rol, tipo de asiento, rol de workspace, estado, departamento, puesto — con marcador "tú") y personas/cuadrillas/proveedores de proyecto. Para gestores (`canManage`) expone acciones reales: agregar, editar, fusionar duplicados, archivar e invitar un recurso como usuario (`createTeamResourceAction`, `updateTeamResourceAction`, `mergeTeamResourcesAction`, `archiveTeamResourceAction`, `inviteResourceAsUserAction`), más administración de usuarios reutilizada de `/organization/members/actions.ts`: crear miembro con contraseña temporal generada, actualizar rol/asiento, restablecer contraseña, remover o eliminar permanentemente. Los usuarios creados así pasan por el flujo forzado de cambio de contraseña. Escribe: `resources`, `organization_members` y usuarios de Supabase Auth. Pantallas relacionadas: Miembros de la organización, Cambio de contraseña y Equipo/RACI por proyecto.

Fuente: src/app/[locale]/(app)/team/page.tsx, team-client.tsx, actions.ts, ../organization/members/actions.ts.
Verifica: abre Equipo desde la navegación; se listan usuarios del workspace y recursos de proyecto, con botones de gestión si eres gestor.', '/team — generated from: src/app/[locale]/(app)/team/page.tsx, src/app/[locale]/(app)/team/team-client.tsx, src/app/[locale]/(app)/team/actions.ts, src/app/[locale]/(app)/organization/members/actions.ts')
),
ins_pkg AS (
  INSERT INTO public.knowledge_packages (organization_id, slug, domain, status, default_language)
  SELECT NULL, d.slug, 'app_screens', 'published', 'en' FROM data d
  ON CONFLICT DO NOTHING
  RETURNING id, slug
),
allpkg AS (
  -- freshly inserted packages (invisible to a same-statement table scan) ∪ pre-existing ones
  SELECT id, slug FROM ins_pkg
  UNION
  SELECT p.id, p.slug
  FROM public.knowledge_packages p
  JOIN data d ON d.slug = p.slug
  WHERE p.organization_id IS NULL AND p.deleted_at IS NULL
),
changed AS (
  SELECT a.id AS package_id, d.*
  FROM allpkg a
  JOIN data d ON d.slug = a.slug
  WHERE NOT EXISTS (
    SELECT 1
    FROM public.knowledge_package_versions v
    JOIN public.knowledge_localizations le ON le.version_id = v.id AND le.language = 'en'
    JOIN public.knowledge_localizations ls ON ls.version_id = v.id AND ls.language = 'es'
    WHERE v.package_id = a.id AND v.is_current
      AND le.title = d.title_en AND le.body = d.body_en
      AND ls.title = d.title_es AND ls.body = d.body_es
  )
),
demoted AS (
  UPDATE public.knowledge_package_versions v
  SET is_current = false
  WHERE v.is_current AND v.package_id IN (SELECT package_id FROM changed)
  RETURNING v.id
),
retired_chunks AS (
  UPDATE public.knowledge_chunks c
  SET deleted_at = now()
  WHERE c.deleted_at IS NULL AND c.version_id IN (SELECT id FROM demoted)
  RETURNING c.id
),
ver AS (
  INSERT INTO public.knowledge_package_versions
    (package_id, organization_id, version_no, is_current, confidence_tier, source_refs)
  SELECT
    c.package_id, NULL,
    coalesce((SELECT max(vv.version_no) FROM public.knowledge_package_versions vv
              WHERE vv.package_id = c.package_id), 0) + 1,
    true, 'learned_pattern', jsonb_build_array(c.source_ref)
  FROM changed c
  -- force 'demoted'/'retired_chunks' to run first (one-current partial index)
  WHERE (SELECT count(*) FROM demoted) >= 0
    AND (SELECT count(*) FROM retired_chunks) >= 0
  RETURNING id, package_id
),
loc AS (
  INSERT INTO public.knowledge_localizations
    (version_id, package_id, organization_id, language, title, body)
  SELECT v.id, v.package_id, NULL, x.language, x.title, x.body
  FROM ver v
  JOIN changed c ON c.package_id = v.package_id
  CROSS JOIN LATERAL (VALUES
    ('en', c.title_en, c.body_en),
    ('es', c.title_es, c.body_es)
  ) AS x(language, title, body)
  RETURNING id, version_id, package_id, organization_id, language, body
)
INSERT INTO public.knowledge_chunks
  (localization_id, version_id, package_id, organization_id, language,
   ordinal, body, content_hash, index_status)
SELECT
  l.id, l.version_id, l.package_id, l.organization_id, l.language,
  0, l.body, encode(digest(l.body, 'sha256'), 'hex'), 'pending'
FROM loc l;
